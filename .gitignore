# 派生构建产物(inject 生成,不入库)
.overlay-injected.json
vite.config.overlay.ts
*.log
node_modules
node_modules/
# inject 在 overlay 目录内生成的上游派生文件/目录
packages/
tests/
# macOS 系统文件
.DS_Store
# 基础运行时:远端 vendor 模式的上游检出缓存(脚本生成,不入库)
.runtime-cache/
