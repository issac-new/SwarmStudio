# 驾驶舱实现差异审计（当前代码 vs 高保真原型 + spec）

**日期**：2026-06-22
**对照基准**：高保真原型 `.superpowers/brainstorm/12551-1782031991/content/cockpit-final-polished.html` + spec `2026-06-22-swarm-studio-cockpit-design.md`
**当前实现**：分支 `feat/cockpit-p1`，HEAD `be8832de`，13 个 cockpit 组件

**最近核对**：2026-06-22（批次 1 代码质量债清理时逐项核查组件代码）。15 项中 ✅已完成 8 项、🔶部分 4 项、⬜真欠 3 项。详见各表"核查状态"列与文末"校正后真欠清单"。

## 审计结论

**结构骨架对，视觉/布局/联动大面积走样。** P1–P6 以"功能拆分"推进，每个组件实现了交互逻辑，但未对照原型做视觉还原，导致能跑但不像设计稿。

---

## 差异清单（按严重度）

### 🔴 严重缺失（整个区域/功能没有）

| # | 设计要求 | 当前状态 | 修复 | 核查状态（2026-06-22） |
|---|---------|---------|------|------|
| 1 | **顶栏**：品牌+日程+时钟+搜索+在线状态+通知+用户菜单 | 完全没有顶栏；CockpitView 从注意力条开始 | 新增 `CockpitTopBar.vue`（品牌/日程按钮+弹窗/实时时钟/搜索/在线状态组/通知/用户菜单+弹窗） | ✅已完成（CockpitTopBar.vue 已实现并在 CockpitView 使用） |
| 2 | **A2UI 表单的 diff 代码块**（add/del 行高亮） | Workspace 只有 modifiedFiles 标签，无 diff 块 | Workspace 加 `.diff` 块（基于 workItem.modifiedFiles 渲染示例 diff 行） | ✅已完成（CockpitWorkspace.vue 含 diff-ln--add/del 渲染） |
| 3 | **A2UI 表单的评估打分**（spec §5.6 提到的"评估"维度） | 只有决定/风险标签/意见，无打分 | 加评分字段（store WorkItem 加 score，Workspace 加打分行） | ✅已完成（WorkItem.score 已加；Workspace 含星级打分行） |
| 4 | **中栏协作图的"空间-组织"双视图**（空间列=workspace 在场者 + 组织列=编排链） | 只有应用级节点+连线，无空间/组织分区 | CollabMap 应用级加左右两栏（空间在场者 + 组织编排链） | ⬜真欠（CollabMap 仍仅应用级单视图，无空间/组织双栏） |

### 🟡 视觉走样（功能在但样式和原型差远）

| # | 设计要求 | 当前状态 | 修复 | 核查状态（2026-06-22） |
|---|---------|---------|------|------|
| 5 | **注意力条**：左侧数字徽章(黑底白字圆) + "需要你" + 严重度色条 + 横向滚动项 + 右侧历史按钮(左分隔线) | 有但简陋：无数字徽章样式、历史按钮无视觉分隔 | 重构 Attention 样式对照原型（数字徽章 + 分隔布局） | ✅已完成（CockpitAttention 含数字徽章/严重度色条/历史按钮分隔线） |
| 6 | **右栏"重心"感**：加宽、大字号(标题15px)、字段间距16px、处理区 max-width | 右栏是默认窄样式，无重心感 | 调 CockpitWorkspace 字号/间距/内边距，加大处理区 | 🔶部分（Workspace 已有 max-width:640px；字号/间距对照原型待细化） |
| 7 | **三列折叠竖条**：贴边 + 竖排标题 + hover 变深 | 折叠在，竖条样式简陋（只有竖排字） | 补竖条背景/hover/icon 对照原型 | 🔶部分（cockpit-rail 类已含竖排标题+hover 变色；缺背景/icon 对照原型） |
| 8 | **协作图画布**：网格点背景 + 节点拖拽手感 + 焦点节点视觉 | 网格背景在，节点样式简陋、缺焦点标记强调 | 补节点 focus 高亮、画布提示文字 | 🔶部分（网格点背景+节点拖拽在；焦点节点标记对照原型待细化） |

### 🟠 联动不对（设计要求的交互链断了）

| # | 设计要求 | 当前状态 | 修复 | 核查状态（2026-06-22） |
|---|---------|---------|------|------|
| 9 | **点协作图拓扑节点 → 中栏时序流切换为该事项** | 拓扑节点点击只 toggleGraphNode(选中)，不切时序流 | GraphNode 点击 → 同时调用一个 `selectTimelineNodeByGraph(node.taskId)` 或切换时序源 | ⬜真欠（focusOnGraphNodeForTimeline 仅设时序源标签，未真正切换时序流过滤源） |
| 10 | **点时序节点 → 右栏 A2UI 切换为该节点处理内容** | 时序节点点击只 selectTimelineNode，右栏不变 | 时序节点点击 → 触发右栏切 work 模式 + 更新 Workspace 标题/待办为该节点 | ✅已完成（focusOnTimelineNode 已接通 selectTimelineNode + setWorkspaceMode('work')；审计原判误） |
| 11 | **注意力条点击 → 左栏选中任务 + 右栏切工作项 + 更新待办描述** | Attention 点击只 selectTask，右栏不联动更新 | Attention 点击 → setWorkspaceMode('work') + 更新 banner 文案 | ✅已完成（CockpitAttention.handleClick → focusOnTaskFromAttention，含 setWorkspaceMode + banner 文案） |
| 12 | **归档只读态**：表单控件半透明禁用 + 隐藏提交按钮 + 只留"发起新协作" | 只有右上角 readonly 徽章，表单仍可编辑 | archivedMode 下 Workspace 加 `is-readonly` 类（pointer-events:none + opacity）+ 隐藏 foot 按钮 + 显示"发起新协作" | 🔶部分（archivedMode + Workspace is-readonly 类 + 隐藏 foot 按钮 + 显示"发起新协作"已实现；表单控件 pointer-events/opacity 细节对照原型待核） |

### 🟢 小差异

| # | 设计要求 | 当前状态 | 修复 | 核查状态（2026-06-22） |
|---|---------|---------|------|------|
| 13 | Kanban 任务卡的**优先级标记** P0-P3 字符 | 有 pri 标记，但样式可强化（P0 加粗已做） | 微调 | ✅已完成 |
| 14 | 文件树**修改标记 M** + 加粗 | 已实现 | 无需改 | ✅已完成 |
| 15 | 终端区深色（spec §5.5 例外） | 已实现 | 无需改 | ✅已完成 |

---

## 修复优先级（2026-06-22 批次 1 校正后）

**校正后真欠 6 项**（原 15 项经核查：8 项已完成、4 项部分、3 项真欠）：

1. **联动断（P0）**：#9 图→时序切源（focusOnGraphNodeForTimeline 未真正切换时序流过滤源）
2. **部分→细化（P1）**：#6 右栏重心字号/间距、#7 折叠竖条 hover/icon、#8 协作图焦点节点标记、#12 归档只读表单禁用细节
3. **功能缺（P2）**：#4 空间-组织双视图（CollabMap 加左右两栏）

**已完成不需再做**：#1 #2 #3 #5 #10 #11 #13 #14 #15（共 8 项）。

## 历史修复优先级（2026-06-22 首版，已被上方校正版取代）

<details>
<summary>展开首版优先级（归档）</summary>

1. **P0（最影响"像不像设计"）**：#1 顶栏、#5 注意力条样式、#6 右栏重心、#2 diff 块
2. **P1（联动断裂）**：#9 #10 #11 #12 四个联动
3. **P2（增强）**：#3 打分、#4 空间-组织视图、#7 #8 样式细化

</details>

## 执行方式

按优先级分 3 批修复，每批跑测试 + 类型检查 + 对照原型目检。先做 P0（视觉骨架还原），再做 P1（联动），最后 P2（增强）。
