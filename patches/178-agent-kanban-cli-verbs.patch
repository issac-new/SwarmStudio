diff --git a/hermes_cli/kanban.py b/hermes_cli/kanban.py
index 052c2ff..5774118 100644
--- a/hermes_cli/kanban.py
+++ b/hermes_cli/kanban.py
@@ -222,6 +222,7 @@ _DELEGATED_CHILD_DENIED_ACTIONS: frozenset[str] = frozenset({
 _DELEGATED_CHILD_DENIED_BOARD_ACTIONS: frozenset[str] = frozenset({
     "create", "new", "rm", "remove", "delete", "switch", "use", "rename",
     "set-default-workdir", "import",
+    "set-project",  # HERMES_CUSTOM[v020]
 })
 
 
@@ -369,6 +370,7 @@ def _cmd_create(args: argparse.Namespace) -> int:
             max_runtime_seconds=max_runtime, skills=getattr(args, "skills", None) or None,
             max_retries=max_retries, model_override=getattr(args, "model_override", None),
             provider_override=getattr(args, "provider_override", None),
+            reasoning_effort=getattr(args, "reasoning_effort", None),  # HERMES_CUSTOM[v020]
             goal_mode=bool(getattr(args, "goal_mode", False)),
             goal_max_turns=getattr(args, "goal_max_turns", None),
             completion_contract=getattr(args, "completion_contract", None),
@@ -596,6 +598,137 @@ def _cmd_set_model(args: argparse.Namespace) -> int:
     return 0
 
 
+# HERMES_CUSTOM[v020] BEGIN: set-reasoning + estimate handlers
+def _cmd_set_reasoning(args: argparse.Namespace) -> int:
+    effort = args.effort
+    if effort is not None and effort.lower() in {"clear", "-", "null", ""}:
+        effort = None
+    try:
+        with kbc.connect_closing() as conn:
+            ok = kb.set_reasoning_effort(conn, args.task_id, effort)
+    except (ValueError, RuntimeError) as exc:
+        return _err(f"kanban: {exc}", 2)
+    if not ok:
+        return _err(f"no such task: {args.task_id}", 1)
+    if effort:
+        print(f"Set reasoning effort on {args.task_id}: {effort} "
+              "(applies on next dispatch)")
+    else:
+        print(f"Cleared reasoning effort on {args.task_id} "
+              "(worker uses its profile default)")
+    return 0
+
+
+_ESTIMATE_SYSTEM_PROMPT = (
+    "You estimate how much work an autonomous coding agent will spend on a "
+    "kanban task. Given the task title and description, respond with STRICT "
+    "JSON only (no prose, no code fence):\n"
+    '{"est_tokens": <integer total tokens across the whole run>, '
+    '"complexity": "S"|"M"|"L", '
+    '"rationale": "<one short sentence>"}\n'
+    "Base the token figure on a realistic multi-turn agent run (reading files, "
+    "tool calls, edits, retries) — not a single reply. S≈small/localized, "
+    "M≈multi-file, L≈broad or ambiguous. Be honest that this is a rough guess."
+)
+
+
+def _run_estimate(title: str, body: Optional[str]) -> dict:
+    """Mirror of plugins/kanban/dashboard/plugin_api.py::_run_estimate.
+
+    Kept self-contained (no plugin import) to avoid pulling FastAPI into the
+    CLI. Never raises — a bad config / parse / API error becomes
+    ``{"ok": False, "reason": ...}`` so callers can render it inline.
+    """
+    if not (title or "").strip():
+        return {"ok": False, "reason": "a title is required to estimate"}
+    try:
+        from agent.auxiliary_client import call_llm
+    except Exception:
+        return {"ok": False, "reason": "auxiliary client unavailable"}
+
+    def _cap(s, n):
+        s = (s or "").strip()
+        return s if len(s) <= n else s[:n] + "…"
+
+    user_msg = (
+        f"Title: {_cap(title, 400)}\n\n"
+        f"Description:\n{_cap(body, 4000) or '(none)'}"
+    )
+    try:
+        resp = call_llm(
+            task="kanban_estimator",
+            messages=[
+                {"role": "system", "content": _ESTIMATE_SYSTEM_PROMPT},
+                {"role": "user", "content": user_msg},
+            ],
+            temperature=0.0,
+            max_tokens=300,
+            timeout=60,
+        )
+    except Exception as exc:
+        return {"ok": False, "reason": f"LLM error: {type(exc).__name__}"}
+    try:
+        raw = (resp.choices[0].message.content or "").strip()
+        model = getattr(resp, "model", None)
+    except Exception:
+        raw, model = "", None
+    parsed = None
+    try:
+        import re as _re
+        blob = raw
+        if not blob.lstrip().startswith("{"):
+            m = _re.search(r"\{.*\}", blob, _re.DOTALL)
+            blob = m.group(0) if m else blob
+        obj = json.loads(blob)
+        if isinstance(obj, dict):
+            parsed = obj
+    except Exception:
+        parsed = None
+    if not parsed:
+        return {"ok": False, "reason": "could not parse an estimate from the model"}
+    try:
+        est_tokens = int(parsed.get("est_tokens") or 0)
+    except (TypeError, ValueError):
+        est_tokens = 0
+    complexity = str(parsed.get("complexity") or "").strip().upper()
+    if complexity not in {"S", "M", "L"}:
+        complexity = None
+    rationale = str(parsed.get("rationale") or "").strip() or None
+    return {
+        "ok": True,
+        "est_tokens": est_tokens,
+        "complexity": complexity,
+        "rationale": rationale,
+        "model": model,
+    }
+
+
+def _cmd_estimate(args: argparse.Namespace) -> int:
+    title, body = args.title, args.body
+    if args.task_id:
+        with kbc.connect_closing() as conn:
+            task = kb.get_task(conn, args.task_id)
+        if task is None:
+            return _err(f"no such task: {args.task_id}", 1)
+        title, body = task.title, task.body
+    result = _run_estimate(title or "", body)
+    if getattr(args, "json", False):
+        _print_json(result)
+    elif result.get("ok"):
+        cx = result.get("complexity") or "?"
+        line = f"~{result.get('est_tokens', 0)} tokens · {cx}"
+        if result.get("rationale"):
+            line += f" — {result['rationale']}"
+        print(line)
+    else:
+        print(f"estimate failed: {result.get('reason')}", file=sys.stderr)
+    # Always exit 0 once the estimate ran: a non-OK outcome is carried in the
+    # payload (mirrors the REST endpoint, where ok:false is not an HTTP error)
+    # so CLI callers can render it inline instead of treating it as a failure.
+    return 0
+# HERMES_CUSTOM[v020] END
+
+
 def _cmd_reclaim(args: argparse.Namespace) -> int:
     with kbc.connect_closing() as conn:
         ok = kb.reclaim_task(conn, args.task_id, reason=getattr(args, "reason", None))
@@ -1232,6 +1365,7 @@ _HANDLERS = {
     "init": _cmd_init, "create": _cmd_create, "swarm": _cmd_swarm,
     "list": _cmd_list, "ls": _cmd_list, "show": _cmd_show,
     "assign": _cmd_assign, "set-model": _cmd_set_model,
+    "set-reasoning": _cmd_set_reasoning, "estimate": _cmd_estimate,  # HERMES_CUSTOM[v020]
     "reclaim": _cmd_reclaim, "reassign": _cmd_reassign,
     "diagnostics": _cmd_diagnostics, "diag": _cmd_diagnostics,
     "link": _cmd_link, "unlink": _cmd_unlink, "claim": _cmd_claim,
diff --git a/hermes_cli/kanban_boards.py b/hermes_cli/kanban_boards.py
index 0c6db18..f56274d 100644
--- a/hermes_cli/kanban_boards.py
+++ b/hermes_cli/kanban_boards.py
@@ -76,9 +76,23 @@ def _cmd_boards_create(args: argparse.Namespace) -> int:
     if rc:
         return rc
     already = kb.board_exists(normed) and normed != kb.DEFAULT_BOARD
+    # HERMES_CUSTOM[v020] BEGIN: resolve --project scope
+    project_id = None
+    project_path = None
+    project_ref = getattr(args, "project", None)
+    if project_ref:
+        from hermes_cli import projects_db as pdb
+        with pdb.connect_closing() as pconn:
+            proj = pdb.get_project(pconn, project_ref.strip())
+        if proj is None:
+            return _err(f"kanban boards create: project {project_ref!r} does not exist", 2)
+        project_id = proj.id
+        project_path = proj.primary_path or None
+    # HERMES_CUSTOM[v020] END
     meta = kb.create_board(
         normed, name=args.name, description=args.description, icon=args.icon, color=args.color,
-        default_workdir=args.default_workdir,
+        default_workdir=args.default_workdir or project_path,  # HERMES_CUSTOM[v020]
+        project_id=project_id,  # HERMES_CUSTOM[v020]
     )
     print(f"Board {meta['slug']!r} {'already exists' if already else 'created'}.\n"
           f"  Display name: {meta.get('name', '')}\n"
@@ -201,6 +215,35 @@ def _cmd_boards_import(args: argparse.Namespace) -> int:
     return 0
 
 
+# HERMES_CUSTOM[v020] BEGIN: boards set-project handler
+def _cmd_boards_set_project(args: argparse.Namespace) -> int:
+    normed, rc = _board_slug_arg(args, "set-project", must_exist=True)
+    if rc:
+        return rc
+    ref = args.project
+    if ref is not None and ref.lower() in {"none", "-", "null", ""}:
+        ref = None
+    if ref is None:
+        kb.write_board_metadata(normed, project_id="")
+        print(f"Board {normed!r} project scope cleared.")
+        return 0
+    from hermes_cli import projects_db as pdb
+    with pdb.connect_closing() as pconn:
+        proj = pdb.get_project(pconn, ref.strip())
+    if proj is None:
+        return _err(f"kanban boards set-project: project {ref!r} does not exist", 2)
+    meta = kb.write_board_metadata(
+        normed,
+        project_id=proj.id,
+        default_workdir=proj.primary_path or None,
+    )
+    print(f"Board {normed!r} scoped to project {proj.name!r} ({proj.slug}).")
+    if meta.get("default_workdir"):
+        print(f"  default_workdir: {meta['default_workdir']}")
+    return 0
+# HERMES_CUSTOM[v020] END
+
+
 _BOARD_HANDLERS = {
     "list": _cmd_boards_list, "ls": _cmd_boards_list,
     "create": _cmd_boards_create, "new": _cmd_boards_create,
@@ -209,6 +252,7 @@ _BOARD_HANDLERS = {
     "show": _cmd_boards_show, "current": _cmd_boards_show,
     "rename": _cmd_boards_rename,
     "set-default-workdir": _cmd_boards_set_default_workdir,
+    "set-project": _cmd_boards_set_project,  # HERMES_CUSTOM[v020]
     "export": _cmd_boards_export,
     "import": _cmd_boards_import,
 }
diff --git a/hermes_cli/kanban_output.py b/hermes_cli/kanban_output.py
index c5abcce..27de649 100644
--- a/hermes_cli/kanban_output.py
+++ b/hermes_cli/kanban_output.py
@@ -20,6 +20,7 @@ _TASK_DICT_FIELDS = (
     "workspace_kind", "workspace_path", "branch_name", "project_id",
     "created_by", "created_at", "started_at", "completed_at", "result",
     "skills", "max_retries", "model_override", "provider_override",
+    "reasoning_effort",  # HERMES_CUSTOM[v020]
     "session_id", "workflow_template_id", "current_step_key", "completion_contract", "last_failure_error",
 )
 _SHOW_RUN_FIELDS = (
diff --git a/hermes_cli/kanban_parser.py b/hermes_cli/kanban_parser.py
index 6d0b413..5e15264 100644
--- a/hermes_cli/kanban_parser.py
+++ b/hermes_cli/kanban_parser.py
@@ -95,6 +95,9 @@ _BOARD_SPECS = [
         _arg("--color", help="Optional hex color (e.g. '#8b5cf6') for the dashboard"),
         _arg("--switch", action="store_true", help="Switch to the new board after creating it"),
         _arg("--default-workdir", help="Default workspace path for tasks created on this board"),
+        _arg("--project",  # HERMES_CUSTOM[v020]
+             help="Scope the board to a project (id or slug): default_workdir "
+                  "mirrors the project's primary folder and new tasks inherit the project"),
     ], aliases=["new"], help="Create a new board"),
     _cmd("rm", [
         _SLUG,
@@ -110,6 +113,11 @@ _BOARD_SPECS = [
         _SLUG,
         _arg("path", nargs="?", help="Absolute path to use as default workdir. Omit to clear."),
     ], help="Set the default workspace path for tasks on a board"),
+    _cmd("set-project", [  # HERMES_CUSTOM[v020]
+        _SLUG,
+        _arg("project", nargs="?",
+             help="Project id or slug. Omit or 'none' to clear."),
+    ], help="Scope a board to a project (or clear the scope)"),
     _cmd("export", [
         _arg("slug", nargs="?", help="Board to export (default: the current board)"),
         _arg("-o", "--output", help="Archive path (default: ./<slug>.tar.gz)"),
@@ -186,6 +194,10 @@ _SPECS = [
         _arg("--provider", dest="provider_override",
              help="Provider the --model belongs to (passed as --provider <name> to "
                   "the worker). Requires --model."),
+        _arg("--reasoning", default=None, dest="reasoning_effort", metavar="LEVEL",  # HERMES_CUSTOM[v020]
+             help="Pin the worker's thinking depth for this task "
+                  "(minimal/low/medium/high/xhigh/max/ultra, or 'none' for thinking "
+                  "off). Omit to use the assignee profile's agent.reasoning_effort."),
         _arg("--completion-contract", metavar="CONTRACT",
              help="local-only (default), OWNER/REPO for publication, or exact GitHub PR URL; required CI gates done."),
         _arg("--goal", action="store_true", dest="goal_mode",
@@ -240,6 +252,18 @@ _SPECS = [
              help="Provider the model belongs to (worker is spawned with "
                   "--provider <name>). Cleared together with the model."),
     ], help="Set or clear a task's model/provider override (takes effect on the next dispatch)"),
+    _cmd("set-reasoning", [  # HERMES_CUSTOM[v020]
+        _TASK_ID,
+        _arg("effort", nargs="?",
+             help="minimal/low/medium/high/xhigh/max/ultra, 'none' to pin thinking "
+                  "off, or 'clear' to inherit the profile default"),
+    ], help="Set or clear a task's reasoning effort override (takes effect on the next dispatch)"),
+    _cmd("estimate", [  # HERMES_CUSTOM[v020]
+        _arg("task_id", nargs="?", help="Existing task id; omit to estimate raw --title/--body"),
+        _arg("--title"),
+        _arg("--body"),
+        _json_flag(),
+    ], help="Rough token + complexity estimate via the auxiliary model"),
     _cmd("reclaim", [_TASK_ID, _RECLAIM_REASON], help="Release an active worker claim on a running task"),
     _cmd("reassign", [
         _TASK_ID,
