diff --git a/packages/client/src/components/hermes/files/FileTree.vue b/packages/client/src/components/hermes/files/FileTree.vue
index e128dd5..67abad5 100644
--- a/packages/client/src/components/hermes/files/FileTree.vue
+++ b/packages/client/src/components/hermes/files/FileTree.vue
@@ -67,6 +67,11 @@ watch([effectiveProfile, () => filesStore.currentWorkspaceSessionId, () => props
   const nextTreeData = await loadChildren('')
   if (seq === rootLoadSeq) treeData.value = nextTreeData
 }, { immediate: true })
+
+// 当 workspaceRoot 变化时（切换到不同任务），重新加载根级目录树
+watch(() => filesStore.workspaceRoot, async () => {
+  treeData.value = await loadChildren('')
+})
 </script>
 
 <template>
@@ -87,7 +92,11 @@ watch([effectiveProfile, () => filesStore.currentWorkspaceSessionId, () => props
       expand-on-click
       block-line
       @update:selected-keys="handleSelect"
-    />
+    >
+      <template #empty>
+        <span class="file-tree__empty">{{ t('files.noSubdirs', '无子目录') }}</span>
+      </template>
+    </NTree>
   </div>
 </template>
 
@@ -122,4 +131,12 @@ watch([effectiveProfile, () => filesStore.currentWorkspaceSessionId, () => props
   white-space: nowrap;
   vertical-align: bottom;
 }
+
+.file-tree__empty {
+  display: block;
+  padding: 16px 12px;
+  color: var(--text-muted, #999);
+  font-size: 12px;
+  text-align: center;
+}
 </style>
