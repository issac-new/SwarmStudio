diff --git a/packages/client/src/components/hermes/files/FileTree.vue b/packages/client/src/components/hermes/files/FileTree.vue
index 902ece7b..bb169856 100644
--- a/packages/client/src/components/hermes/files/FileTree.vue
+++ b/packages/client/src/components/hermes/files/FileTree.vue
@@ -19,7 +19,7 @@ const selectedKeys = ref<string[]>([])
 
 async function loadChildren(path: string): Promise<TreeOption[]> {
   try {
-    const result = await filesApi.listFiles(path, effectiveProfile.value)
+    const result = await filesApi.listFiles(path, filesStore.workspaceRoot, effectiveProfile.value)
     return result.entries
       .filter(e => e.isDir)
       .sort((a, b) => a.name.localeCompare(b.name))
@@ -58,6 +58,11 @@ watch(effectiveProfile, async () => {
   selectedKeys.value = []
   treeData.value = await loadChildren('')
 }, { immediate: true })
+
+// 当 workspaceRoot 变化时（切换到不同任务），重新加载根级目录树
+watch(() => filesStore.workspaceRoot, async () => {
+  treeData.value = await loadChildren('')
+})
 </script>
 
 <template>
@@ -77,7 +82,11 @@ watch(effectiveProfile, async () => {
       expand-on-click
       block-line
       @update:selected-keys="handleSelect"
-    />
+    >
+      <template #empty>
+        <span class="file-tree__empty">{{ t('files.noSubdirs', '无子目录') }}</span>
+      </template>
+    </NTree>
   </div>
 </template>
 
@@ -112,4 +121,12 @@ watch(effectiveProfile, async () => {
   white-space: nowrap;
   vertical-align: bottom;
 }
+
+.file-tree__empty {
+  display: block;
+  padding: 16px 12px;
+  color: var(--text-muted, #999);
+  font-size: 12px;
+  text-align: center;
+}
 </style>
