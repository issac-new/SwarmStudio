diff --git a/packages/server/src/modules/studio/repositories/session-store.ts b/packages/server/src/modules/studio/repositories/session-store.ts
index b1e45d2..8c444d0 100644
--- a/packages/server/src/modules/studio/repositories/session-store.ts
+++ b/packages/server/src/modules/studio/repositories/session-store.ts
@@ -587,6 +587,20 @@ export function countSessions(
   return Number(row.total)
 }
 
+// HERMES_CUSTOM[ChatRunAutoJoin] BEGIN: list session ids owned by a user, for socket auto-join
+// Enables cockpit unread notifications: the chat-run socket auto-joins all of a user's
+// session rooms on connect so it receives run.completed / peer_user_message events for
+// sessions the user is not currently viewing. Unread is in-memory on the client.
+export function listSessionIdsByUserId(userId: string, limit = 2000): string[] {
+  if (!isSqliteAvailable()) return []
+  const db = getDb()!
+  const rows = db.prepare(
+    `SELECT id FROM ${SESSIONS_TABLE} WHERE user_id = ? ORDER BY last_active DESC LIMIT ?`,
+  ).all(String(userId), limit) as Record<string, unknown>[]
+  return rows.map(row => String(row.id || '')).filter(Boolean)
+}
+// HERMES_CUSTOM[ChatRunAutoJoin] END
+
 function escapeSessionSearchLike(value: string): string {
   return value.replace(/[\\%_]/g, '\\$&')
 }
diff --git a/packages/server/src/modules/studio/sockets/chat-run.ts b/packages/server/src/modules/studio/sockets/chat-run.ts
index bc7c1b9..d027fa7 100644
--- a/packages/server/src/modules/studio/sockets/chat-run.ts
+++ b/packages/server/src/modules/studio/sockets/chat-run.ts
@@ -15,7 +15,7 @@ import type { Server, Socket } from 'socket.io'
 import { randomUUID } from 'crypto'
 import { logger } from '../public/logging'
 import { getSystemPrompt } from '../public/runs/prompt'
-import { clearSessionMessages, deleteSession, getSession, getSessionMetadata, listSessions, updateMessageDisplayContent } from '../repositories/session-store'
+import { clearSessionMessages, deleteSession, getSession, getSessionMetadata, listSessions, updateMessageDisplayContent, listSessionIdsByUserId } from '../repositories/session-store'
 import { listWorkspaceRunChangesForAssistantMessages } from '../repositories/workspace-run-changes-store'
 import { getSessionCategory } from '../repositories/session-category-store'
 import { getActiveProfileName, getProfileDir, listProfileNamesFromDisk } from '../public/profile-config'
