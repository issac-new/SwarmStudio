diff --git a/hermes_cli/kanban_db.py b/hermes_cli/kanban_db.py
index 0000000..0000000 100644
--- a/hermes_cli/kanban_db.py
+++ b/hermes_cli/kanban_db.py
@@ -992,7 +992,7 @@ def _init_schema(conn: sqlite3.Connection) -> None:
     consecutive_failures INTEGER NOT NULL DEFAULT 0,
     worker_pid           INTEGER,
     last_failure_error   TEXT,
-    workspace_kind       TEXT NOT NULL DEFAULT 'scratch',
+    workspace_kind       TEXT NOT NULL DEFAULT 'dir',
     workspace_path       TEXT,
     branch_name          TEXT,
     claim_lock           TEXT,
@@ -2066,7 +2066,7 @@ def create_task(
     body: Optional[str] = None,
     assignee: Optional[str] = None,
     created_by: Optional[str] = None,
-    workspace_kind: str = "scratch",
+    workspace_kind: str = "dir",
     workspace_path: Optional[str] = None,
     branch_name: Optional[str] = None,
     tenant: Optional[str] = None,
diff --git a/hermes_cli/kanban_swarm.py b/hermes_cli/kanban_swarm.py
index 0000000..0000000 100644
--- a/hermes_cli/kanban_swarm.py
+++ b/hermes_cli/kanban_swarm.py
@@ -86,7 +86,7 @@ def kanban_swarm(
     synthesizer_title: str = "Synthesize swarm outputs",
     tenant: Optional[str] = None,
     created_by: str = "swarm-orchestrator",
-    workspace_kind: str = "scratch",
+    workspace_kind: str = "dir",
     workspace_path: Optional[str] = None,
     priority: int = 0,
     idempotency_key: Optional[str] = None,
diff --git a/plugins/kanban/dashboard/plugin_api.py b/plugins/kanban/dashboard/plugin_api.py
index 0000000..0000000 100644
--- a/plugins/kanban/dashboard/plugin_api.py
+++ b/plugins/kanban/dashboard/plugin_api.py
@@ -583,7 +583,7 @@ class CreateTaskPayload(BaseModel):
     tenant: Optional[str] = None
     priority: int = 0
-    workspace_kind: str = "scratch"
+    workspace_kind: str = "dir"
     workspace_path: Optional[str] = None
     parents: list[str] = Field(default_factory=list)