diff --git a/packages/desktop/src/main/index.ts b/packages/desktop/src/main/index.ts
index 548099e2..ab9ac217 100644
--- a/packages/desktop/src/main/index.ts
+++ b/packages/desktop/src/main/index.ts
@@ -876,7 +876,13 @@ function runDesktopApp() {
       quitApp()
       return
     }
-    showMainWindow()
+    // overlay[fix-second-instance]: a second SwarmStudio.exe launch must NOT
+    // steal foreground focus. On Windows the NSIS auto-updater, tray "open
+    // app", scheduled tasks, and the CLI shim (`start "" "%APP%"`) all fire
+    // second-instance events every few seconds. Restoring/showing the window
+    // on each one is what made SwarmStudio keep popping to the foreground
+    // every 3-4s even when minimized. Do nothing here; users reach the app
+    // via the tray icon click instead.
   })
 
   app.whenReady().then(async () => {
