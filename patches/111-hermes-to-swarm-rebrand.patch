diff --git a/package.json b/package.json
index 7a1cfdb2..1d17ac5b 100644
--- a/package.json
+++ b/package.json
@@ -76,9 +76,12 @@
     "@vue-flow/minimap": "^1.5.4",
     "@vue-flow/node-resizer": "^1.5.1",
     "adm-zip": "^0.5.17",
+    "echarts": "^6.1.0",
     "eventsource": "^4.1.0",
     "js-tiktoken": "^1.0.21",
     "katex": "^0.17.0",
+    "lunar-typescript": "^1.8.6",
+    "matrix-js-sdk": "^41.8.0-rc.0",
     "node-edge-tts": "^1.2.10",
     "node-pty": "^1.1.0",
     "socket.io": "^4.8.3",
diff --git a/packages/client/index.html b/packages/client/index.html
index 98c3768a..d8fdae9b 100644
--- a/packages/client/index.html
+++ b/packages/client/index.html
@@ -1,124 +1,9 @@
-<!doctype html>
-<html lang="zh-CN">
-
-<head>
-  <meta charset="UTF-8" />
-  <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
-  <link rel="apple-touch-icon" href="/logo.png" />
-  <link rel="manifest" href="/manifest.webmanifest" />
-  <meta name="theme-color" content="#f7f7f4" />
-  <meta name="apple-mobile-web-app-capable" content="yes" />
-  <meta name="apple-mobile-web-app-title" content="Hermes Studio" />
-  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-  <title>Hermes Studio</title>
-  <style>
-    html,
-    body,
-    #app {
-      min-height: 100%;
-    }
-
-    body {
-      margin: 0;
-      background: #f7f7f4;
-    }
-
-    html.dark body {
-      background: #1a1a1a;
-    }
-
-    .boot-fallback {
-      min-height: 100vh;
-      display: grid;
-      place-items: center;
-      color: #222222;
-      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
-    }
-
-    html.dark .boot-fallback {
-      color: #f0f0f0;
-    }
-
-    .boot-fallback__logo-wrap {
-      position: relative;
-      display: block;
-      width: 72px;
-      height: 72px;
-      border-radius: 18px;
-      overflow: hidden;
-    }
-
-    .boot-fallback__logo-wrap::after {
-      content: "";
-      position: absolute;
-      inset: 0;
-      background: linear-gradient(105deg, transparent 30%, rgba(255, 255, 255, 0.85) 48%, transparent 66%);
-      transform: translateX(-140%);
-      mix-blend-mode: screen;
-      pointer-events: none;
-      -webkit-mask: url("/logo.png") center / contain no-repeat;
-      mask: url("/logo.png") center / contain no-repeat;
-      animation: boot-logo-shimmer 1.8s cubic-bezier(0.45, 0, 0.25, 1) infinite;
-    }
-
-    .boot-fallback__logo-wrap img {
-      width: 100%;
-      height: 100%;
-      object-fit: contain;
-      border-radius: 18px;
-    }
-
-    @keyframes boot-logo-shimmer {
-      0% {
-        transform: translateX(-140%);
-      }
-
-      52%,
-      100% {
-        transform: translateX(140%);
-      }
-    }
-  </style>
-
-  <!-- Prevent FOUC by applying theme classes immediately -->
-  <script>
-    (function() {
-      try {
-        const brightness = localStorage.getItem('hermes_brightness') || 'system';
-        const style = localStorage.getItem('hermes_style') || 'ink';
-
-        // Resolve dark mode
-        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
-        const isDark = brightness === 'dark' || (brightness === 'system' && prefersDark);
-
-        // Resolve comic style
-        const isComic = style === 'comic';
-
-        // Apply classes immediately
-        if (isDark) {
-          document.documentElement.classList.add('dark');
-        }
-        if (isComic) {
-          document.documentElement.classList.add('comic');
-        }
-      } catch (e) {
-        console.warn('Failed to apply theme:', e);
-      }
-    })();
-  </script>
-</head>
-
-<body>
-  <div id="app">
-    <div class="boot-fallback">
-      <div class="boot-fallback__brand">
-        <span class="boot-fallback__logo-wrap">
-          <img src="/logo.png" alt="" />
-        </span>
-      </div>
-    </div>
-  </div>
-  <script type="module" src="/src/main.ts"></script>
-</body>
-
-</html>
+<!doctype html><html lang="zh-CN"><head>
+<meta charset="UTF-8" /><link rel="manifest" href="/manifest.webmanifest" />
+<meta name="theme-color" content="#f7f7f4" /><meta name="apple-mobile-web-app-capable" content="yes" />
+<meta name="apple-mobile-web-app-title" content="Swarm Studio" />
+<meta name="viewport" content="width=device-width, initial-scale=1.0" />
+<title>Swarm Studio</title>
+<style>html,body,#app{min-height:100%}body{margin:0;background:#f7f7f4}html.dark body{background:#1a1a1a}.boot-fallback{min-height:100vh;display:grid;place-items:center;color:#222;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Helvetica,Arial,sans-serif}html.dark .boot-fallback{color:#f0f0f0}</style>
+<script>(function(){try{var b=localStorage.getItem('hermes_brightness')||'auto';var s=localStorage.getItem('hermes_style')||'ink';var d;if(b==='auto'){var n=new Date();var doy=Math.floor((n.getTime()-new Date(n.getFullYear(),0,0).getTime())/864e5);var se=-Math.cos(2*Math.PI*(doy-172)/365);var sm=300+se*85;var em=1140-se*85;var bm=((n.getUTCHours()+8)*60+n.getUTCMinutes())%1440;d=bm>=em||bm<sm}else{d=b==='dark'||(b==='system'&&window.matchMedia('(prefers-color-scheme:dark)').matches)}if(d)document.documentElement.classList.add('dark');if(s==='comic')document.documentElement.classList.add('comic')}catch(e){console.warn('Failed to apply theme:',e)}})();</script>
+</head><body><div id="app"><div class="boot-fallback"></div></div><script type="module" src="/src/main.ts"></script></body></html>
diff --git a/packages/client/public/coding-agents/hermes.png b/packages/client/public/coding-agents/hermes.png
deleted file mode 100644
index fa4eada3..00000000
Binary files a/packages/client/public/coding-agents/hermes.png and /dev/null differ
diff --git a/packages/client/public/favicon.ico b/packages/client/public/favicon.ico
deleted file mode 100644
index dbc3f567..00000000
Binary files a/packages/client/public/favicon.ico and /dev/null differ
diff --git a/packages/client/public/logo-original.png b/packages/client/public/logo-original.png
deleted file mode 100644
index 451200cb..00000000
Binary files a/packages/client/public/logo-original.png and /dev/null differ
diff --git a/packages/client/public/logo.png b/packages/client/public/logo.png
deleted file mode 100644
index 6a5b1f0b..00000000
Binary files a/packages/client/public/logo.png and /dev/null differ
diff --git a/packages/client/public/manifest.webmanifest b/packages/client/public/manifest.webmanifest
index df086e94..3ed94d56 100644
--- a/packages/client/public/manifest.webmanifest
+++ b/packages/client/public/manifest.webmanifest
@@ -1,18 +1,9 @@
 {
-  "name": "Hermes Studio",
-  "short_name": "Hermes",
-  "description": "Self-hosted AI chat dashboard for Hermes Agent",
+  "name": "Swarm Studio",
+  "short_name": "Swarm",
   "start_url": "/#/hermes/chat",
   "scope": "/",
   "display": "standalone",
   "background_color": "#f7f7f4",
-  "theme_color": "#f7f7f4",
-  "icons": [
-    {
-      "src": "/logo.png",
-      "sizes": "1254x1254",
-      "type": "image/png",
-      "purpose": "any maskable"
-    }
-  ]
+  "theme_color": "#f7f7f4"
 }
diff --git a/packages/client/src/App.vue b/packages/client/src/App.vue
index dc8105f2..b2e8f0b8 100644
--- a/packages/client/src/App.vue
+++ b/packages/client/src/App.vue
@@ -9,6 +9,7 @@ import AppSidebar from '@/components/layout/AppSidebar.vue'
 import DesktopTitleBar from '@/components/layout/DesktopTitleBar.vue'
 import { useKeyboard } from '@/composables/useKeyboard'
 import { useAppStore } from '@/stores/hermes/app'
+import { useAuthStore } from '@/stores/hermes/auth'
 import SessionSearchModal from '@/components/hermes/chat/SessionSearchModal.vue'
 import AuthEventListener from '@/components/auth/AuthEventListener.vue'
 import DefaultCredentialPrompt from '@/components/auth/DefaultCredentialPrompt.vue'
@@ -16,17 +17,19 @@ import DefaultCredentialPrompt from '@/components/auth/DefaultCredentialPrompt.v
 const { isDark, isComic } = useTheme()
 const { t } = useI18n()
 const appStore = useAppStore()
+const authStore = useAuthStore()
 const route = useRoute()
 
 const themeOverrides = computed(() => getThemeOverrides(isDark.value, isComic.value))
 const naiveTheme = computed(() => isDark.value ? darkTheme : null)
 
 const isLoginPage = computed(() => route.name === 'login')
-const usesPageSidebar = computed(() =>
-  ['hermes.chat', 'hermes.session', 'hermes.history', 'hermes.historySession', 'hermes.globalAgent', 'hermes.globalAgentSession', 'hermes.groupChat', 'hermes.groupChatRoom', 'hermes.workflow'].includes(route.name as string),
-)
-const showAppSidebar = computed(() => !isLoginPage.value && !usesPageSidebar.value)
-const showMobileMenuButton = computed(() => !isLoginPage.value && (showAppSidebar.value || usesPageSidebar.value))
+// Chat-related routes are now children of hermes.cockpit and render inside
+// the cockpit layout. AppSidebar stays visible for all authenticated routes.
+const usesPageSidebar = computed(() => false)
+const isFullscreen = computed(() => route.meta?.fullscreen === true)
+const showAppSidebar = computed(() => !isLoginPage.value && !isFullscreen.value)
+const showMobileMenuButton = computed(() => !isLoginPage.value && !isFullscreen.value && showAppSidebar.value)
 
 const nodeVersionLow = computed(() => {
   const v = appStore.nodeVersion
@@ -55,6 +58,8 @@ watch(isLoginPage, (loginPage) => {
     appStore.stopHealthPolling()
     return
   }
+  // Restore user session on page refresh (authStore.user is lost on reload)
+  authStore.fetchUser()
   appStore.loadModels()
   appStore.startHealthPolling()
 }, {
@@ -80,9 +85,9 @@ useKeyboard()
               {{ t('sidebar.nodeVersionWarning', { version: appStore.nodeVersion }) }}
             </div>
             <div class="app-layout" :class="{ 'no-sidebar': isLoginPage || !showAppSidebar }">
-              <button v-if="showMobileMenuButton" class="hamburger-btn" @click="handleMobileMenuClick">
-                <img src="/logo.png" alt="Menu" style="width: 24px; height: 24px;" />
-              </button>
+                <button v-if="showMobileMenuButton" class="hamburger-btn" @click="handleMobileMenuClick">
+                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
+                </button>
               <div v-if="!isLoginPage && showAppSidebar && appStore.sidebarOpen" class="mobile-backdrop" @click="appStore.closeSidebar" />
               <AppSidebar v-if="!isLoginPage && showAppSidebar" />
               <main class="app-main">
diff --git a/packages/client/src/api/auth.ts b/packages/client/src/api/auth.ts
index 6213ed03..29984d3f 100644
--- a/packages/client/src/api/auth.ts
+++ b/packages/client/src/api/auth.ts
@@ -27,6 +27,29 @@ export async function loginWithPassword(username: string, password: string): Pro
   return data.token
 }
 
+// ─── Matrix login via matrix-js-sdk ──────────────────────────
+
+export interface MatrixLoginResult {
+  accessToken: string
+  userId: string
+  deviceId: string
+  homeserverUrl: string
+}
+
+export async function matrixSdkLogin(homeserverUrl: string, username: string, password: string): Promise<MatrixLoginResult> {
+  const { createClient } = await import('matrix-js-sdk')
+  const baseUrl = homeserverUrl.replace(/\/+$/, '')
+  const client = createClient({ baseUrl })
+
+  const loginRes = await client.loginWithPassword(username, password)
+  return {
+    accessToken: loginRes.access_token,
+    userId: loginRes.user_id,
+    deviceId: loginRes.device_id || '',
+    homeserverUrl: baseUrl,
+  }
+}
+
 export interface CurrentUser {
   id: number
   username: string
@@ -37,6 +60,12 @@ export interface CurrentUser {
   last_login_at: number | null
   avatar?: string
   requiresCredentialChange?: boolean
+  // Matrix fields
+  matrix_user_id?: string
+  matrix_display_name?: string
+  matrix_avatar_url?: string
+  matrix_homeserver_url?: string
+  auth_source?: 'local' | 'matrix'
 }
 
 export interface UserAvatar {
@@ -124,6 +153,90 @@ export interface ManagedUsersResponse {
   profiles: string[]
 }
 
+export async function hermesMatrixLogin(
+  matrixAccessToken: string,
+  matrixUserId: string,
+  deviceId: string,
+  homeserverUrl: string
+): Promise<{ token: string; user: CurrentUser }> {
+  const res = await fetch('/api/auth/matrix-login', {
+    method: 'POST',
+    headers: { 'Content-Type': 'application/json' },
+    body: JSON.stringify({ matrixAccessToken, matrixUserId, deviceId, homeserverUrl }),
+  })
+  if (!res.ok) {
+    const data = await res.json().catch(() => ({}))
+    throw new Error(data.error || 'Matrix login failed')
+  }
+  return res.json()
+}
+
+export interface MatrixUser {
+  matrix_user_id: string
+  display_name: string
+  avatar_url: string | null
+  role: UserRole
+  status: UserStatus
+  profiles: string[]
+  last_login_at: number | null
+  is_admin: boolean
+  source: 'matrix' | 'hermes'
+}
+
+export interface MatrixUserListResponse {
+  users: MatrixUser[]
+  total: number
+}
+
+export async function fetchMatrixUsers(page = 0, pageSize = 100): Promise<MatrixUserListResponse> {
+  return request<MatrixUserListResponse>(`/api/auth/matrix-users?page=${page}&pageSize=${pageSize}`)
+}
+
+export async function createMatrixUser(input: {
+  userId: string
+  password: string
+  displayName?: string
+  role: UserRole
+  profiles: string[]
+}): Promise<{ user: CurrentUser }> {
+  return request<{ user: CurrentUser }>('/api/auth/matrix-users', {
+    method: 'POST',
+    body: JSON.stringify(input),
+  })
+}
+
+export async function updateMatrixUser(matrixUserId: string, input: {
+  displayName?: string
+  role?: UserRole
+  status?: UserStatus
+  profiles?: string[]
+}): Promise<{ user: CurrentUser }> {
+  return request<{ user: CurrentUser }>(`/api/auth/matrix-users/${encodeURIComponent(matrixUserId)}`, {
+    method: 'PUT',
+    body: JSON.stringify(input),
+  })
+}
+
+export async function deleteMatrixUser(matrixUserId: string): Promise<{ success: boolean }> {
+  return request<{ success: boolean }>(`/api/auth/matrix-users/${encodeURIComponent(matrixUserId)}`, {
+    method: 'DELETE',
+  })
+}
+
+export async function deactivateMatrixUser(matrixUserId: string, active: boolean): Promise<{ success: boolean }> {
+  return request<{ success: boolean }>(`/api/auth/matrix-users/${encodeURIComponent(matrixUserId)}/deactivate`, {
+    method: 'POST',
+    body: JSON.stringify({ active }),
+  })
+}
+
+export async function resetMatrixUserPassword(matrixUserId: string, password: string): Promise<{ success: boolean }> {
+  return request<{ success: boolean }>(`/api/auth/matrix-users/${encodeURIComponent(matrixUserId)}/reset-password`, {
+    method: 'POST',
+    body: JSON.stringify({ password }),
+  })
+}
+
 export async function fetchManagedUsers(): Promise<ManagedUsersResponse> {
   return request<ManagedUsersResponse>('/api/auth/users')
 }
diff --git a/packages/client/src/api/client.ts b/packages/client/src/api/client.ts
index 0e5664e8..3f3656ed 100644
--- a/packages/client/src/api/client.ts
+++ b/packages/client/src/api/client.ts
@@ -108,11 +108,54 @@ function isProfileWideSessionCollection(pathname: string): boolean {
     pathname === '/api/hermes/sessions/conversations'
 }
 
-function emitAuthNotice(kind: 'expired' | 'forbidden') {
+export function emitAuthNotice(kind: 'expired' | 'forbidden') {
   if (typeof window === 'undefined') return
   window.dispatchEvent(new CustomEvent('hermes-auth-notice', { detail: { kind } }))
 }
 
+/**
+ * Authoritative auth-state recovery: clear the stored token, notify listeners
+ * (AuthEventListener shows a toast), and redirect to login. Shared by the REST
+ * 401 handler below and by callers that cannot read HTTP status directly
+ * (e.g. the kanban WebSocket, whose `onclose` only surfaces code 1006).
+ */
+export function triggerAuthRecheck() {
+  clearAuthSessionState()
+  emitAuthNotice('expired')
+  if (router.currentRoute.value.name !== 'login') {
+    router.replace({ name: 'login' })
+  }
+}
+
+/**
+ * Lightweight REST probe used to authoritatively decide whether a persistent
+ * WebSocket failure is an auth problem. Returns `false` only when REST returns
+ * a confirmed 401 (auth expired — in which case request() has already cleared
+ * the token and redirected to login). Returns `true` when the token is valid
+ * (REST 200) OR when the probe itself failed inconclusively (network error,
+ * 5xx) — callers should keep retrying the WebSocket in that case rather than
+ * force a logout they cannot justify.
+ *
+ * Use this when a caller observes repeated failures but cannot read HTTP
+ * status directly (e.g. the kanban WebSocket, whose `onclose` only surfaces
+ * close code 1006).
+ */
+export async function probeAuthViaRest(): Promise<boolean> {
+  try {
+    // getCapabilities is a cheap, always-available local BFF endpoint.
+    await request('/api/hermes/kanban/capabilities')
+    return true
+  } catch (err) {
+    // Only a 401 counts as an auth verdict. The request() handler throws
+    // 'Unauthorized' for 401 (and has already cleared the token + redirected
+    // to login). Network errors, 5xx, etc. mean the probe itself failed and
+    // say nothing about auth — treat as "inconclusive, not an auth failure"
+    // so callers keep retrying the WebSocket instead of forcing logout.
+    const msg = err instanceof Error ? err.message : String(err)
+    return msg !== 'Unauthorized'
+  }
+}
+
 function messageFromErrorValue(value: unknown): string {
   if (typeof value === 'string') return value
   if (value == null) return ''
@@ -175,11 +218,7 @@ export async function request<T>(path: string, options: RequestInit = {}): Promi
     !path.startsWith('/v1/')
 
   if (res.status === 401 && isLocalBff) {
-    clearAuthSessionState()
-    emitAuthNotice('expired')
-    if (router.currentRoute.value.name !== 'login') {
-      router.replace({ name: 'login' })
-    }
+    triggerAuthRecheck()
     throw new Error('Unauthorized')
   }
 
@@ -187,11 +226,7 @@ export async function request<T>(path: string, options: RequestInit = {}): Promi
     const text = await res.text().catch(() => '')
     if (res.status === 403 && isLocalBff) {
       if (text.includes('User is disabled or does not exist')) {
-        clearAuthSessionState()
-        emitAuthNotice('expired')
-        if (router.currentRoute.value.name !== 'login') {
-          router.replace({ name: 'login' })
-        }
+        triggerAuthRecheck()
       } else {
         emitAuthNotice('forbidden')
       }
@@ -205,3 +240,50 @@ export async function request<T>(path: string, options: RequestInit = {}): Promi
 export function getBaseUrlValue(): string {
   return getBaseUrl()
 }
+
+export function isMatrixBypassMode(): boolean {
+  return false
+}
+
+// ─── Matrix credential helpers ────────────────────────────────
+
+const MATRIX_KEYS = {
+  accessToken: 'matrix_access_token',
+  userId: 'matrix_user_id',
+  deviceId: 'matrix_device_id',
+  homeserverUrl: 'matrix_homeserver_url',
+} as const
+
+export interface MatrixCredentials {
+  accessToken: string
+  userId: string
+  deviceId: string
+  homeserverUrl: string
+}
+
+export function storeMatrixCredentials(creds: MatrixCredentials): void {
+  localStorage.setItem(MATRIX_KEYS.accessToken, creds.accessToken)
+  localStorage.setItem(MATRIX_KEYS.userId, creds.userId)
+  localStorage.setItem(MATRIX_KEYS.deviceId, creds.deviceId)
+  localStorage.setItem(MATRIX_KEYS.homeserverUrl, creds.homeserverUrl)
+}
+
+export function getMatrixCredentials(): MatrixCredentials | null {
+  const accessToken = localStorage.getItem(MATRIX_KEYS.accessToken)
+  const userId = localStorage.getItem(MATRIX_KEYS.userId)
+  const deviceId = localStorage.getItem(MATRIX_KEYS.deviceId)
+  const homeserverUrl = localStorage.getItem(MATRIX_KEYS.homeserverUrl)
+  if (!accessToken || !userId || !homeserverUrl) return null
+  return { accessToken, userId, deviceId: deviceId || '', homeserverUrl }
+}
+
+export function clearMatrixCredentials(): void {
+  localStorage.removeItem(MATRIX_KEYS.accessToken)
+  localStorage.removeItem(MATRIX_KEYS.userId)
+  localStorage.removeItem(MATRIX_KEYS.deviceId)
+  localStorage.removeItem(MATRIX_KEYS.homeserverUrl)
+}
+
+export function hasMatrixCredentials(): boolean {
+  return !!getMatrixCredentials()
+}
diff --git a/packages/client/src/api/hermes/files.ts b/packages/client/src/api/hermes/files.ts
index 18117f2d..30aaa16e 100644
--- a/packages/client/src/api/hermes/files.ts
+++ b/packages/client/src/api/hermes/files.ts
@@ -19,57 +19,67 @@ export interface FileStat {
   permissions?: string
 }
 
-export async function listFiles(path: string = ''): Promise<{ entries: FileEntry[]; path: string; absolutePath?: string }> {
+export async function listFiles(path: string = '', root?: string): Promise<{ entries: FileEntry[]; path: string; absolutePath?: string }> {
   const params = new URLSearchParams()
   if (path) params.set('path', path)
+  if (root) params.set('root', root)
   const query = params.toString()
   return request<{ entries: FileEntry[]; path: string }>(`/api/hermes/files/list${query ? `?${query}` : ''}`)
 }
 
-export async function statFile(path: string): Promise<FileStat> {
-  return request<FileStat>(`/api/hermes/files/stat?path=${encodeURIComponent(path)}`)
+export async function statFile(path: string, root?: string): Promise<FileStat> {
+  const params = new URLSearchParams({ path })
+  if (root) params.set('root', root)
+  return request<FileStat>(`/api/hermes/files/stat?${params.toString()}`)
 }
 
-export async function readFile(path: string): Promise<{ content: string; path: string; size: number }> {
-  return request<{ content: string; path: string; size: number }>(`/api/hermes/files/read?path=${encodeURIComponent(path)}`)
+export async function readFile(path: string, root?: string): Promise<{ content: string; path: string; size: number }> {
+  const params = new URLSearchParams({ path })
+  if (root) params.set('root', root)
+  return request<{ content: string; path: string; size: number }>(`/api/hermes/files/read?${params.toString()}`)
 }
 
-export async function writeFile(path: string, content: string): Promise<void> {
-  await request<{ ok: boolean }>('/api/hermes/files/write', {
+export async function writeFile(path: string, content: string, root?: string): Promise<void> {
+  const query = root ? `?root=${encodeURIComponent(root)}` : ''
+  await request<{ ok: boolean }>(`/api/hermes/files/write${query}`, {
     method: 'PUT',
     body: JSON.stringify({ path, content }),
   })
 }
 
-export async function deleteFile(path: string, recursive: boolean = false): Promise<void> {
-  await request<{ ok: boolean }>('/api/hermes/files/delete', {
+export async function deleteFile(path: string, recursive: boolean = false, root?: string): Promise<void> {
+  const query = root ? `?root=${encodeURIComponent(root)}` : ''
+  await request<{ ok: boolean }>(`/api/hermes/files/delete${query}`, {
     method: 'DELETE',
     body: JSON.stringify({ path, recursive }),
   })
 }
 
-export async function renameFile(oldPath: string, newPath: string): Promise<void> {
-  await request<{ ok: boolean }>('/api/hermes/files/rename', {
+export async function renameFile(oldPath: string, newPath: string, root?: string): Promise<void> {
+  const query = root ? `?root=${encodeURIComponent(root)}` : ''
+  await request<{ ok: boolean }>(`/api/hermes/files/rename${query}`, {
     method: 'POST',
     body: JSON.stringify({ oldPath, newPath }),
   })
 }
 
-export async function mkDir(path: string): Promise<void> {
-  await request<{ ok: boolean }>('/api/hermes/files/mkdir', {
+export async function mkDir(path: string, root?: string): Promise<void> {
+  const query = root ? `?root=${encodeURIComponent(root)}` : ''
+  await request<{ ok: boolean }>(`/api/hermes/files/mkdir${query}`, {
     method: 'POST',
     body: JSON.stringify({ path }),
   })
 }
 
-export async function copyFile(srcPath: string, destPath: string): Promise<void> {
-  await request<{ ok: boolean }>('/api/hermes/files/copy', {
+export async function copyFile(srcPath: string, destPath: string, root?: string): Promise<void> {
+  const query = root ? `?root=${encodeURIComponent(root)}` : ''
+  await request<{ ok: boolean }>(`/api/hermes/files/copy${query}`, {
     method: 'POST',
     body: JSON.stringify({ srcPath, destPath }),
   })
 }
 
-export async function uploadFiles(targetDir: string, files: File[]): Promise<{ name: string; path: string }[]> {
+export async function uploadFiles(targetDir: string, files: File[], root?: string): Promise<{ name: string; path: string }[]> {
   const base = getBaseUrlValue()
   const formData = new FormData()
   for (const file of files) {
@@ -77,6 +87,7 @@ export async function uploadFiles(targetDir: string, files: File[]): Promise<{ n
   }
   const params = new URLSearchParams()
   if (targetDir) params.set('path', targetDir)
+  if (root) params.set('root', root)
   const query = params.toString()
   const url = `${base}/api/hermes/files/upload${query ? `?${query}` : ''}`
 
@@ -117,10 +128,11 @@ export async function uploadRuntimeFiles(files: File[]): Promise<{ name: string;
   return data.files
 }
 
-export function getFileDownloadUrl(relativePath: string, fileName?: string): string {
+export function getFileDownloadUrl(relativePath: string, fileName?: string, root?: string): string {
   const base = getBaseUrlValue()
   const params = new URLSearchParams({ path: relativePath })
   if (fileName) params.set('name', fileName)
+  if (root) params.set('root', root)
   const profileName = getActiveProfileName()
   if (profileName) params.set('profile', profileName)
   const token = getApiKey()
diff --git a/packages/client/src/api/hermes/kanban.ts b/packages/client/src/api/hermes/kanban.ts
index 3912ea2a..6a7df80d 100644
--- a/packages/client/src/api/hermes/kanban.ts
+++ b/packages/client/src/api/hermes/kanban.ts
@@ -18,8 +18,10 @@ export interface KanbanTask {
   workspace_kind: string
   workspace_path: string | null
   tenant: string | null
+  project_id: string | null
   result: string | null
   skills: string[] | null
+  latest_summary?: string | null
 }
 
 export interface KanbanRun {
@@ -139,21 +141,73 @@ export interface KanbanCapabilities {
   capabilities?: KanbanCapabilityStatus[]
 }
 
-export interface KanbanTaskLog {
+export interface KanbanAttachment {
+  id: number
   task_id: string
-  path: string | null
-  exists: boolean
-  size_bytes: number
-  content: string
-  truncated: boolean
+  filename: string
+  content_type: string
+  size: number
+  uploaded_by: string
+  stored_path: string
+  created_at: number
+}
+
+export interface KanbanDiagnosticAction {
+  kind: string
+  label: string
+  suggested?: boolean
+  payload?: Record<string, unknown>
+}
+
+export interface KanbanDiagnosticItem {
+  kind: string
+  title: string
+  detail: string
+  severity: 'warning' | 'error' | 'critical'
+  count: number
+  last_seen_at: number
+  data: Record<string, unknown>
+  actions: KanbanDiagnosticAction[]
+}
+
+export interface KanbanDiagnostic {
+  task_id: string
+  task_title: string
+  task_status: string
+  task_assignee: string | null
+  diagnostics: KanbanDiagnosticItem[]
+}
+
+export interface KanbanOrchestrationProfile {
+  name: string
+  description: string
+}
+
+export interface KanbanOrchestration {
+  orchestrator_profile?: string
+  default_assignee?: string
+  auto_decompose: boolean
+  profiles: KanbanOrchestrationProfile[]
+}
+
+export interface KanbanOrchestrationPatch {
+  orchestrator_profile?: string
+  default_assignee?: string
+  auto_decompose?: boolean
+}
+
+export interface KanbanProfileDescriptionPatch {
+  description: string
 }
 
+
 export interface KanbanCreateRequest {
   title: string
   body?: string
   assignee?: string
   priority?: number
   tenant?: string
+  project?: string
   workspace?: string
   branch?: string
   triage?: boolean
@@ -184,6 +238,14 @@ export interface KanbanTaskLogOptions extends KanbanBoardOptions {
   tail?: number
 }
 
+export interface KanbanTaskLog {
+  exists: boolean
+  content: string
+  size_bytes: number
+  truncated: boolean
+  path: string
+}
+
 export interface KanbanDiagnosticsOptions extends KanbanBoardOptions {
   task?: string
   severity?: 'warning' | 'error' | 'critical'
@@ -400,14 +462,6 @@ export async function getTaskLog(taskId: string, opts?: KanbanTaskLogOptions): P
   return request<KanbanTaskLog>(appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/log`, params))
 }
 
-export async function getDiagnostics(opts?: KanbanDiagnosticsOptions): Promise<unknown[]> {
-  const params = boardParams(opts?.board)
-  if (opts?.task) params.set('task', opts.task)
-  if (opts?.severity) params.set('severity', opts.severity)
-  const res = await request<{ diagnostics: unknown[] }>(appendQuery('/api/hermes/kanban/diagnostics', params))
-  return res.diagnostics
-}
-
 export async function reclaimTask(taskId: string, opts?: KanbanReclaimOptions): Promise<{ ok: boolean; output?: string }> {
   return request<{ ok: boolean; output?: string }>(appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/reclaim`, boardParams(opts?.board)), {
     method: 'POST',
@@ -448,3 +502,182 @@ export async function getAssignees(opts?: KanbanBoardOptions): Promise<KanbanAss
   const res = await request<{ assignees: KanbanAssignee[] }>(appendQuery('/api/hermes/kanban/assignees', boardParams(opts?.board)))
   return res.assignees
 }
+
+export async function deleteTask(taskId: string, opts?: KanbanBoardOptions): Promise<{ ok: boolean }> {
+  return request<{ ok: boolean }>(appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}`, boardParams(opts?.board)), {
+    method: 'DELETE',
+  })
+}
+
+export interface KanbanTaskPatch {
+  status?: KanbanTaskStatus
+  assignee?: string | null
+  priority?: number
+  title?: string
+  body?: string
+  result?: string
+  block_reason?: string
+  summary?: string
+}
+
+export async function patchTask(taskId: string, patch: KanbanTaskPatch, opts?: KanbanBoardOptions): Promise<KanbanTask> {
+  const res = await request<{ task: KanbanTask }>(appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}`, boardParams(opts?.board)), {
+    method: 'PATCH',
+    body: JSON.stringify(patch),
+  })
+  return res.task
+}
+
+export async function decomposeTask(taskId: string, opts?: KanbanBoardOptions & { author?: string }): Promise<unknown> {
+  const res = await request<{ result: unknown }>(appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/decompose`, boardParams(opts?.board)), {
+    method: 'POST',
+    body: JSON.stringify({ author: opts?.author }),
+  })
+  return res.result
+}
+
+// ─── Attachments ───────────────────────────────────────────────
+
+export async function listAttachments(taskId: string, opts?: KanbanBoardOptions): Promise<KanbanAttachment[]> {
+  const res = await request<{ attachments: KanbanAttachment[] }>(
+    appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/attachments`, boardParams(opts?.board)),
+  )
+  return res.attachments
+}
+
+export async function uploadAttachment(
+  taskId: string,
+  file: File,
+  opts?: KanbanBoardOptions & { uploadedBy?: string },
+): Promise<KanbanAttachment> {
+  const params = boardParams(opts?.board)
+  if (opts?.uploadedBy) params.set('uploaded_by', opts.uploadedBy)
+  const formData = new FormData()
+  formData.append('file', file)
+  const res = await request<{ attachment: KanbanAttachment }>(
+    appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/attachments`, params),
+    {
+      method: 'POST',
+      body: formData,
+    },
+  )
+  return res.attachment
+}
+
+export async function downloadAttachment(attachmentId: number, opts?: KanbanBoardOptions): Promise<Blob> {
+  const base = getBaseUrlValue()
+  const params = boardParams(opts?.board)
+  const url = `${base}${appendQuery(`/api/hermes/kanban/attachments/${attachmentId}`, params)}`
+  const headers: Record<string, string> = {}
+  const apiKey = getApiKey()
+  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`
+  const res = await fetch(url, { headers })
+  if (!res.ok) throw new Error(`Download failed: ${res.status}`)
+  return res.blob()
+}
+
+export async function deleteAttachment(attachmentId: number, opts?: KanbanBoardOptions): Promise<{ ok: boolean }> {
+  return request<{ ok: boolean }>(
+    appendQuery(`/api/hermes/kanban/attachments/${attachmentId}`, boardParams(opts?.board)),
+    {
+      method: 'DELETE',
+    },
+  )
+}
+
+// ─── Diagnostics ───────────────────────────────────────────────
+
+export async function getDiagnostics(opts?: KanbanDiagnosticsOptions): Promise<KanbanDiagnostic[]> {
+  const params = boardParams(opts?.board)
+  if (opts?.task) params.set('task', opts.task)
+  if (opts?.severity) params.set('severity', opts.severity)
+  const res = await request<{ diagnostics: KanbanDiagnostic[]; count: number }>(
+    appendQuery('/api/hermes/kanban/diagnostics', params),
+  )
+  return res.diagnostics
+}
+
+// ─── Orchestration ─────────────────────────────────────────────
+
+export async function getOrchestration(opts?: KanbanBoardOptions): Promise<KanbanOrchestration> {
+  return request<KanbanOrchestration>(appendQuery('/api/hermes/kanban/orchestration', boardParams(opts?.board)))
+}
+
+export async function saveOrchestration(
+  patch: KanbanOrchestrationPatch,
+  opts?: KanbanBoardOptions,
+): Promise<KanbanOrchestration> {
+  return request<KanbanOrchestration>(appendQuery('/api/hermes/kanban/orchestration', boardParams(opts?.board)), {
+    method: 'PUT',
+    body: JSON.stringify(patch),
+  })
+}
+
+export async function saveProfileDescription(
+  name: string,
+  patch: KanbanProfileDescriptionPatch,
+  opts?: KanbanBoardOptions,
+): Promise<{ ok: boolean }> {
+  return request<{ ok: boolean }>(
+    appendQuery(`/api/hermes/kanban/profiles/${encodeURIComponent(name)}`, boardParams(opts?.board)),
+    {
+      method: 'PATCH',
+      body: JSON.stringify(patch),
+    },
+  )
+}
+
+export async function autoGenerateProfileDescription(
+  name: string,
+  opts?: KanbanBoardOptions & { overwrite?: boolean },
+): Promise<{ ok: boolean; description?: string }> {
+  const params = boardParams(opts?.board)
+  if (opts?.overwrite) params.set('overwrite', 'true')
+  return request<{ ok: boolean; description?: string }>(
+    appendQuery(`/api/hermes/kanban/profiles/${encodeURIComponent(name)}/describe-auto`, params),
+    {
+      method: 'POST',
+    },
+  )
+}
+
+// ─── Home channel notification subscriptions ──────────────────────
+
+export interface HomeChannel {
+  platform: string
+  chat_id: string
+  thread_id: string
+  name: string
+  subscribed: boolean
+}
+
+export async function getHomeChannels(taskId: string | null, opts?: KanbanBoardOptions): Promise<HomeChannel[]> {
+  const params = boardParams(opts?.board)
+  if (taskId) params.set('task', taskId)
+  const res = await request<{ home_channels: HomeChannel[] }>(
+    appendQuery('/api/hermes/kanban/home-channels', params),
+  )
+  return res.home_channels
+}
+
+export async function subscribeHomeChannel(
+  taskId: string,
+  platform: string,
+  opts?: KanbanBoardOptions,
+): Promise<{ ok: boolean }> {
+  return request<{ ok: boolean }>(
+    appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/home-subscribe/${encodeURIComponent(platform)}`, boardParams(opts?.board)),
+    { method: 'POST' },
+  )
+}
+
+export async function unsubscribeHomeChannel(
+  taskId: string,
+  platform: string,
+  opts?: KanbanBoardOptions,
+): Promise<{ ok: boolean }> {
+  return request<{ ok: boolean }>(
+    appendQuery(`/api/hermes/kanban/${encodeURIComponent(taskId)}/home-subscribe/${encodeURIComponent(platform)}`, boardParams(opts?.board)),
+    { method: 'DELETE' },
+  )
+}
diff --git a/packages/client/src/api/hermes/profiles.ts b/packages/client/src/api/hermes/profiles.ts
index 4c47d7c0..140e032f 100644
--- a/packages/client/src/api/hermes/profiles.ts
+++ b/packages/client/src/api/hermes/profiles.ts
@@ -61,7 +61,7 @@ export interface ProfileRuntimeStatusesResponse {
 
 export async function fetchProfiles(): Promise<HermesProfile[]> {
   const res = await request<{ profiles: HermesProfile[] }>('/api/hermes/profiles')
-  return res.profiles
+  return res?.profiles ?? []
 }
 
 export async function fetchProfileDetail(name: string): Promise<HermesProfileDetail> {
diff --git a/packages/client/src/api/hermes/sessions.ts b/packages/client/src/api/hermes/sessions.ts
index 5a9d2398..92ff15ad 100644
--- a/packages/client/src/api/hermes/sessions.ts
+++ b/packages/client/src/api/hermes/sessions.ts
@@ -96,7 +96,7 @@ export async function fetchSessions(source?: string, limit?: number, profile?: s
   if (profile) params.set('profile', profile)
   const query = params.toString()
   const res = await request<{ sessions: SessionSummary[] }>(`/api/hermes/sessions${query ? `?${query}` : ''}`)
-  return res.sessions
+  return res?.sessions ?? []
 }
 
 /**
@@ -109,7 +109,7 @@ export async function fetchHermesSessions(source?: string, limit?: number, profi
   if (profile) params.set('profile', profile)
   const query = params.toString()
   const res = await request<{ sessions: SessionSummary[] }>(`/api/hermes/sessions/hermes${query ? `?${query}` : ''}`)
-  return res.sessions
+  return res?.sessions ?? []
 }
 
 export async function searchSessions(q: string, source?: string, limit?: number, profile?: string): Promise<SessionSearchResult[]> {
diff --git a/packages/client/src/components/hermes/chat/ChatPanel.vue b/packages/client/src/components/hermes/chat/ChatPanel.vue
index 492b2c2a..2f2bdb04 100644
--- a/packages/client/src/components/hermes/chat/ChatPanel.vue
+++ b/packages/client/src/components/hermes/chat/ChatPanel.vue
@@ -34,17 +34,17 @@ import OutlinePanel from "./OutlinePanel.vue";
 import FilesPanel from "./FilesPanel.vue";
 import TerminalPanel from "./TerminalPanel.vue";
 import PageSidebarNav from "@/components/layout/PageSidebarNav.vue";
-import SettingsCircuitBadge from "@/components/layout/SettingsCircuitBadge.vue";
-import { isStoredSuperAdmin } from "@/api/client";
+import { useAuthStore } from "@/stores/hermes/auth";
 
 const chatStore = useChatStore();
 const appStore = useAppStore();
 const profilesStore = useProfilesStore();
 const sessionBrowserPrefsStore = useSessionBrowserPrefsStore();
+const authStore = useAuthStore();
 const router = useRouter();
 const message = useMessage();
 const { t } = useI18n();
-const isSuperAdmin = computed(() => isStoredSuperAdmin());
+const isSuperAdmin = computed(() => authStore.isSuperAdmin);
 
 const showOutline = ref(false);
 const messageListRef = ref<InstanceType<typeof MessageList> | null>(null);
@@ -762,10 +762,6 @@ const contextMenuOptions = computed(() => {
   return options
 });
 
-function openSettingsPage() {
-  router.push({ name: "hermes.settings" });
-}
-
 function handleContextMenu(e: MouseEvent, sessionId: string) {
   e.preventDefault();
   contextSessionId.value = sessionId;
@@ -1266,23 +1262,14 @@ async function handleSessionModelCustomSubmit() {
         />
       </div>
       <div v-if="showSessions" class="page-sidebar-bottom">
-        <button class="page-sidebar-menu-btn" type="button" @click="openSettingsPage">
-          <svg
-            width="16"
-            height="16"
-            viewBox="0 0 24 24"
-            fill="none"
-            stroke="currentColor"
-            stroke-width="1.8"
-            stroke-linecap="round"
-            stroke-linejoin="round"
-          >
-            <circle cx="12" cy="12" r="3" />
-            <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
+        <button class="page-sidebar-menu-btn" type="button" @click="authStore.logout()">
+          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
+            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
+            <polyline points="16 17 21 12 16 7" />
+            <line x1="21" y1="12" x2="9" y2="12" />
           </svg>
-          <span>{{ t("sidebar.settings") }}</span>
+          <span>{{ t('sidebar.logout') }}</span>
         </button>
-        <SettingsCircuitBadge />
       </div>
     </aside>
 
diff --git a/packages/client/src/components/hermes/chat/HistoryMessageList.vue b/packages/client/src/components/hermes/chat/HistoryMessageList.vue
index 196bf82b..35475951 100644
--- a/packages/client/src/components/hermes/chat/HistoryMessageList.vue
+++ b/packages/client/src/components/hermes/chat/HistoryMessageList.vue
@@ -191,9 +191,8 @@ defineExpose({
       @top-reach="handleTopReach"
     >
       <template #empty>
-        <div class="empty-state">
-          <img :src="'/coding-agents/hermes.png'" alt="Hermes" class="empty-logo" />
-          <p>{{ t("chat.emptyState") }}</p>
+	        <div class="empty-state">
+	          <p>{{ t("chat.emptyState") }}</p>
         </div>
       </template>
       <template #before>
@@ -256,12 +255,6 @@ defineExpose({
   color: $text-muted;
   gap: 12px;
 
-  .empty-logo {
-    width: 48px;
-    height: 48px;
-    opacity: 0.25;
-  }
-
   p {
     font-size: 14px;
   }
diff --git a/packages/client/src/components/hermes/chat/MessageItem.vue b/packages/client/src/components/hermes/chat/MessageItem.vue
index cbb1d4a1..34d2fbd2 100644
--- a/packages/client/src/components/hermes/chat/MessageItem.vue
+++ b/packages/client/src/components/hermes/chat/MessageItem.vue
@@ -37,6 +37,7 @@ const toast = useMessage();
 
 const isSystem = computed(() => props.message.role === "system");
 const isAgentError = computed(() => props.message.role === "assistant" && props.message.systemType === "error");
+const isGatewayNotice = computed(() => props.message.systemType === "gateway");
 
 const effectiveHeadingIdPrefix = computed(() => props.headingIdPrefix || `msg-${props.message.id}`);
 const isCommandMessage = computed(() => props.message.role === "command" || props.message.systemType === "command");
@@ -102,7 +103,7 @@ function parseContentBlocks(content: string): Array<ContentBlock | Record<string
   try {
     return parse(trimmed)
   } catch {
-    // Hermes Agent stored some multimodal user messages via Python str(list),
+    // Swarm Agent stored some multimodal user messages via Python str(list),
     // e.g. [{'type': 'text'}, {'type': 'image_url', ...}]. Convert that
     // legacy repr into JSON for display only.
     if (!trimmed.startsWith("[{'") && !trimmed.startsWith('[{"')) return null
@@ -850,6 +851,7 @@ onBeforeUnmount(() => {
             :class="{
               system: isSystem,
               'agent-error': isAgentError,
+              gateway: isGatewayNotice,
               command: isCommandMessage,
               'command-error': isCommandError,
               'speech-playing': isPlayingThisMessage && !isPausedThisMessage,
@@ -934,7 +936,7 @@ onBeforeUnmount(() => {
               </div>
             </div>
             <MarkdownRenderer
-              v-if="parsedThinking.body && message.role === 'assistant'"
+              v-if="parsedThinking.body && message.role === 'assistant' && !isGatewayNotice"
               :content="parsedThinking.body"
               :heading-id-prefix="effectiveHeadingIdPrefix"
             />
@@ -987,9 +989,9 @@ onBeforeUnmount(() => {
               :heading-id-prefix="effectiveHeadingIdPrefix"
             />
 
-            <!-- Render system message content -->
+            <!-- Render system / gateway-notice content (compact, no slash icon) -->
             <MarkdownRenderer
-              v-if="message.role === 'system' && message.content && !isCommandMessage"
+              v-if="(message.role === 'system' || isGatewayNotice) && message.content && !isCommandMessage"
               :content="message.content"
             />
             <div v-if="isStatusCommand" class="command-result command-status">
@@ -1139,6 +1141,10 @@ onBeforeUnmount(() => {
     align-items: flex-start;
   }
 
+  &.gateway {
+    align-items: flex-start;
+  }
+
   &.highlight {
     .message-bubble {
       box-shadow: 0 0 0 1px rgba(var(--accent-primary-rgb), 0.45);
@@ -1203,6 +1209,15 @@ onBeforeUnmount(() => {
     padding: 8px 10px;
   }
 
+  &.gateway {
+    border-left: none;
+    border: 1px solid rgba(var(--accent-primary-rgb), 0.12);
+    background-color: rgba(var(--accent-primary-rgb), 0.04);
+    color: $text-secondary;
+    max-width: min(100%, 960px);
+    padding: 8px 10px;
+  }
+
   &.command-error {
     border-color: rgba(var(--warning-rgb), 0.28);
     background-color: rgba(var(--warning-rgb), 0.06);
diff --git a/packages/client/src/components/hermes/chat/MessageList.vue b/packages/client/src/components/hermes/chat/MessageList.vue
index 5c9199d5..f40627f3 100644
--- a/packages/client/src/components/hermes/chat/MessageList.vue
+++ b/packages/client/src/components/hermes/chat/MessageList.vue
@@ -23,6 +23,7 @@ import MessageItem from "./MessageItem.vue";
 import { LIVE_CHAT_MAX_LOADED_MESSAGES, useChatStore, type Message } from "@/stores/hermes/chat";
 import thinkingImage from "@/assets/thinking.gif";
 import { useToolTraceVisibility } from "@/composables/useToolTraceVisibility";
+import GatewayNoticeBanner from "@/custom/chat/components/GatewayNoticeBanner.vue";
 
 const chatStore = useChatStore();
 const { t } = useI18n();
@@ -111,7 +112,7 @@ const emptyState = computed(() => {
     };
   }
   return {
-    logo: "/coding-agents/hermes.png",
+    logo: "",
     alt: "Hermes",
     text: t("chat.emptyState"),
   };
@@ -124,11 +125,12 @@ const displayMessages = computed(() => {
       return toolTraceVisible.value && !!m.toolName && !(chatStore.isRunActive && currentToolIds.has(m.id));
     }
     if (
-      m.role === "assistant" &&
-      m.isStreaming &&
-      !m.content?.trim() &&
-      !!m.reasoning?.trim() &&
-      currentToolCalls.value.length === 0
+      m.systemType === "gateway" ||
+      (m.role === "assistant" &&
+       m.isStreaming &&
+       !m.content?.trim() &&
+       !!m.reasoning?.trim() &&
+       currentToolCalls.value.length === 0)
     ) {
       return false;
     }
@@ -469,6 +471,7 @@ defineExpose({
 
 <template>
   <div class="message-list-shell">
+    <GatewayNoticeBanner :session-id="chatStore.activeSessionId" :messages="chatStore.messages" />
     <VirtualMessageList
       :key="chatStore.activeSessionId || 'chat-empty'"
       ref="listRef"
@@ -478,9 +481,8 @@ defineExpose({
       @scroll="handleListScroll"
       @top-reach="handleTopReach"
     >
-      <template #empty>
-        <div class="empty-state">
-          <img :src="emptyState.logo" :alt="emptyState.alt" class="empty-logo" />
+	      <template #empty>
+	        <div class="empty-state">
           <p>{{ emptyState.text }}</p>
         </div>
       </template>
@@ -1263,12 +1265,6 @@ defineExpose({
   color: $text-muted;
   gap: 12px;
 
-  .empty-logo {
-    width: 48px;
-    height: 48px;
-    opacity: 0.25;
-  }
-
   p {
     font-size: 14px;
   }
diff --git a/packages/client/src/components/hermes/chat/SessionListItem.vue b/packages/client/src/components/hermes/chat/SessionListItem.vue
index 6158c729..57d0143a 100644
--- a/packages/client/src/components/hermes/chat/SessionListItem.vue
+++ b/packages/client/src/components/hermes/chat/SessionListItem.vue
@@ -50,7 +50,7 @@ const sessionAgentLogo = computed(() => {
     }
     return { label: 'Claude Code', src: '/coding-agents/claude-code.svg' }
   }
-  return { label: 'Hermes', src: '/coding-agents/hermes.png' }
+  return { label: 'Hermes', src: '' }
 })
 
 let longPressTimer: ReturnType<typeof setTimeout> | null = null
diff --git a/packages/client/src/components/hermes/files/FileTree.vue b/packages/client/src/components/hermes/files/FileTree.vue
index a392a840..6977673f 100644
--- a/packages/client/src/components/hermes/files/FileTree.vue
+++ b/packages/client/src/components/hermes/files/FileTree.vue
@@ -1,5 +1,5 @@
 <script setup lang="ts">
-import { ref, onMounted, h } from 'vue'
+import { ref, onMounted, watch, h } from 'vue'
 import { NTree } from 'naive-ui'
 import { useI18n } from 'vue-i18n'
 import { useFilesStore } from '@/stores/hermes/files'
@@ -14,7 +14,7 @@ const selectedKeys = ref<string[]>([])
 
 async function loadChildren(path: string): Promise<TreeOption[]> {
   try {
-    const result = await filesApi.listFiles(path)
+    const result = await filesApi.listFiles(path, filesStore.workspaceRoot)
     return result.entries
       .filter(e => e.isDir)
       .sort((a, b) => a.name.localeCompare(b.name))
@@ -52,6 +52,11 @@ function renderLabel({ option }: { option: TreeOption }) {
 onMounted(async () => {
   treeData.value = await loadChildren('')
 })
+
+// 当 workspaceRoot 变化时（切换到不同任务），重新加载根级目录树
+watch(() => filesStore.workspaceRoot, async () => {
+  treeData.value = await loadChildren('')
+})
 </script>
 
 <template>
diff --git a/packages/client/src/components/hermes/group-chat/GroupChatPanel.vue b/packages/client/src/components/hermes/group-chat/GroupChatPanel.vue
index cc7ceed9..ce47e867 100644
--- a/packages/client/src/components/hermes/group-chat/GroupChatPanel.vue
+++ b/packages/client/src/components/hermes/group-chat/GroupChatPanel.vue
@@ -11,6 +11,7 @@ import GroupChatInput from './GroupChatInput.vue'
 import ProfileAvatar from '@/components/hermes/profiles/ProfileAvatar.vue'
 import PageSidebarNav from '@/components/layout/PageSidebarNav.vue'
 import SettingsCircuitBadge from '@/components/layout/SettingsCircuitBadge.vue'
+import { useAuthStore } from '@/stores/hermes/auth'
 import { copyToClipboard } from '@/utils/clipboard'
 import type { Attachment } from '@/stores/hermes/chat'
 import type { RoomAgent } from '@/api/hermes/group-chat'
@@ -20,6 +21,7 @@ const router = useRouter()
 const message = useMessage()
 const store = useGroupChatStore()
 const profilesStore = useProfilesStore()
+const authStore = useAuthStore()
 
 const showSidebar = ref(window.innerWidth > 768)
 const showCreateModal = ref(false)
@@ -436,14 +438,14 @@ async function handleApproval(choice: 'once' | 'session' | 'always' | 'deny') {
                 </div>
             </div>
             <div class="page-sidebar-bottom">
-                <button class="page-sidebar-menu-btn" type="button" @click="openSettingsPage">
+                <button class="page-sidebar-menu-btn" type="button" @click="authStore.logout()">
                     <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
-                        <circle cx="12" cy="12" r="3" />
-                        <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
+                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
+                        <polyline points="16 17 21 12 16 7" />
+                        <line x1="21" y1="12" x2="9" y2="12" />
                     </svg>
-                    <span>{{ t('sidebar.settings') }}</span>
+                    <span>{{ t('sidebar.logout') }}</span>
                 </button>
-                <SettingsCircuitBadge />
             </div>
         </div>
 
diff --git a/packages/client/src/components/hermes/group-chat/GroupMessageItem.vue b/packages/client/src/components/hermes/group-chat/GroupMessageItem.vue
index 30497293..8359a9bd 100644
--- a/packages/client/src/components/hermes/group-chat/GroupMessageItem.vue
+++ b/packages/client/src/components/hermes/group-chat/GroupMessageItem.vue
@@ -13,6 +13,7 @@ import {
     renderHighlightedCodeBlock,
 } from '../chat/highlight'
 import { parseThinking, countThinkingChars } from '@/utils/thinking-parser'
+import { isGatewayNotice as isGatewayNoticeText } from '@/custom/chat/gateway-notice'
 import { useGlobalSpeech } from '@/composables/useSpeech'
 import { useVoiceSettings } from '@/composables/useVoiceSettings'
 import { speedToEdgeRate, hzToEdgePitch } from '@/utils/ttsHelpers'
@@ -51,6 +52,8 @@ const isAgentError = computed(() => {
     return /^Error:\s*/i.test(props.message.content || '')
 })
 
+const isGatewayNotice = computed(() => isGatewayNoticeText(props.message.content))
+
 const isSelf = computed(() => {
     return !!props.currentUserId && props.message.senderId === props.currentUserId
 })
@@ -541,6 +544,7 @@ onBeforeUnmount(() => {
                 :class="{
                     'agent-content': isAgent,
                     'agent-error': isAgentError,
+                    'gateway-notice': isGatewayNotice,
                     'speech-playing': isPlayingThisMessage && !isPausedThisMessage,
                 }"
             >
@@ -663,6 +667,19 @@ onBeforeUnmount(() => {
         }
     }
 
+    &.agent .msg-content.gateway-notice {
+        color: $text-secondary;
+        background-color: rgba(128, 128, 128, 0.06);
+        border: 1px solid rgba(128, 128, 128, 0.2);
+        font-size: 0.85em;
+
+        :deep(.markdown-body),
+        :deep(.markdown-body p) {
+            color: $text-secondary;
+            margin: 0;
+        }
+    }
+
     &.self .msg-content {
         background-color: rgba(var(--accent-primary-rgb), 0.1);
     }
diff --git a/packages/client/src/components/hermes/group-chat/GroupMessageList.vue b/packages/client/src/components/hermes/group-chat/GroupMessageList.vue
index b4f7c6ae..cf4944eb 100644
--- a/packages/client/src/components/hermes/group-chat/GroupMessageList.vue
+++ b/packages/client/src/components/hermes/group-chat/GroupMessageList.vue
@@ -5,13 +5,17 @@ import { useGroupChatStore } from '@/stores/hermes/group-chat'
 import { useToolTraceVisibility } from '@/composables/useToolTraceVisibility'
 import GroupMessageItem from './GroupMessageItem.vue'
 import VirtualMessageList from '../chat/VirtualMessageList.vue'
+import GatewayNoticeBanner from '@/custom/chat/components/GatewayNoticeBanner.vue'
 
 const store = useGroupChatStore()
 const { t } = useI18n()
 const { toolTraceVisible } = useToolTraceVisibility()
 const listRef = ref<InstanceType<typeof VirtualMessageList> | null>(null)
 const showScrollBottomButton = ref(false)
-const displayMessages = computed(() => store.sortedMessages.filter(msg => msg.role !== 'tool' || toolTraceVisible.value || msg.toolStatus === 'running'))
+const displayMessages = computed(() => store.sortedMessages.filter(msg =>
+  msg.systemType !== 'gateway' &&
+  (msg.role !== 'tool' || toolTraceVisible.value || msg.toolStatus === 'running')
+))
 const listPadding = computed(() => store.activePendingApproval ? '16px 20px 260px' : '16px 20px')
 let pendingInitialBottomRoomId: string | null = store.currentRoomId
 
@@ -86,6 +90,7 @@ defineExpose({ scrollToBottom })
 
 <template>
     <div class="group-message-list-shell">
+        <GatewayNoticeBanner :session-id="store.currentRoomId" :messages="store.sortedMessages" />
         <VirtualMessageList
             class="group-message-list"
             ref="listRef"
@@ -97,10 +102,9 @@ defineExpose({ scrollToBottom })
             @scroll="handleListScroll"
             @top-reach="handleTopReach"
         >
-            <template #empty>
-                <div class="empty-state">
-                <img :src="'/coding-agents/hermes.png'" alt="Hermes" class="empty-logo" />
-                <p>{{ t("chat.emptyState") }}</p>
+	            <template #empty>
+	                <div class="empty-state">
+	                <p>{{ t("chat.emptyState") }}</p>
             </div>
             </template>
             <template #before>
@@ -205,16 +209,10 @@ defineExpose({ scrollToBottom })
     flex-direction: column;
     align-items: center;
     justify-content: center;
-    gap: 12px;
-    color: $text-muted;
+	    gap: 12px;
+	    color: $text-muted;
 
-    .empty-logo {
-        width: 48px;
-        height: 48px;
-        opacity: 0.25;
-    }
-
-    p {
+	    p {
         font-size: 14px;
     }
 }
diff --git a/packages/client/src/components/hermes/settings/AccountSettings.vue b/packages/client/src/components/hermes/settings/AccountSettings.vue
index 0922e9b7..b24a0abe 100644
--- a/packages/client/src/components/hermes/settings/AccountSettings.vue
+++ b/packages/client/src/components/hermes/settings/AccountSettings.vue
@@ -1,14 +1,18 @@
 <script setup lang="ts">
-import { ref, onMounted } from "vue";
-import { NButton, NInput, NModal, NForm, NFormItem, NPopconfirm, useMessage } from "naive-ui";
+import { ref, computed, onMounted } from "vue";
+import { NButton, NInput, NModal, NForm, NFormItem, NPopconfirm, NTag, useMessage } from "naive-ui";
 import { useI18n } from "vue-i18n";
-import { changePassword, changeUsername, fetchCurrentUser, fetchLockedIps, unlockSpecificIp, unlockAllIps, fetchMyAvatar, updateMyAvatar, resetMyAvatar } from "@/api/auth";
+import { useAuthStore } from "@/stores/hermes/auth";
+import { changePassword, changeUsername, fetchCurrentUser, fetchLockedIps, unlockSpecificIp, unlockAllIps, fetchMyAvatar, updateMyAvatar, resetMyAvatar, resetMatrixUserPassword } from "@/api/auth";
 import type { LockedIp, UserAvatar } from "@/api/auth";
 import ProfileAvatar from "@/components/hermes/profiles/ProfileAvatar.vue";
 import multiavatar from "@multiavatar/multiavatar";
 
 const { t } = useI18n();
 const message = useMessage();
+const authStore = useAuthStore();
+const user = computed(() => authStore.user);
+const isMatrixUser = computed(() => user.value?.authSource === 'matrix');
 
 const username = ref<string | null>(null);
 const loading = ref(false);
@@ -125,6 +129,11 @@ const showChangeUsernameModal = ref(false);
 const currentPasswordForName = ref("");
 const newUsernameVal = ref("");
 
+// Matrix password reset state
+const showMatrixPasswordModal = ref(false);
+const matrixNewPassword = ref("");
+const matrixPasswordConfirm = ref("");
+
 onMounted(async () => {
   try {
     const user = await fetchCurrentUser();
@@ -193,6 +202,37 @@ function openChangeUsernameModal() {
   showChangeUsernameModal.value = true;
 }
 
+// Matrix password handlers
+function openMatrixPasswordModal() {
+  matrixNewPassword.value = "";
+  matrixPasswordConfirm.value = "";
+  showMatrixPasswordModal.value = true;
+}
+
+async function handleMatrixPasswordChange() {
+  if (matrixNewPassword.value !== matrixPasswordConfirm.value) {
+    message.error(t("login.passwordMismatch"));
+    return;
+  }
+  if (matrixNewPassword.value.length < 6) {
+    message.error(t("login.passwordTooShort"));
+    return;
+  }
+  loading.value = true;
+  try {
+    if (!user.value?.matrixUserId) throw new Error('No Matrix user');
+    await resetMatrixUserPassword(user.value.matrixUserId, matrixNewPassword.value);
+    showMatrixPasswordModal.value = false;
+    matrixNewPassword.value = "";
+    matrixPasswordConfirm.value = "";
+    message.success(t("settings.matrixIdentity.passwordChanged"));
+  } catch (err: any) {
+    message.error(err.message || t("common.saveFailed"));
+  } finally {
+    loading.value = false;
+  }
+}
+
 // Locked IPs management
 const lockedIps = ref<LockedIp[]>([]);
 const loadingLocks = ref(false);
@@ -272,9 +312,59 @@ onMounted(() => { loadLockedIps(); });
       </div>
     </div>
 
+    <!-- Matrix Identity Section -->
+    <div v-if="isMatrixUser" class="matrix-identity-section">
+      <h3 class="section-title">{{ t('settings.matrixIdentity.title') }}</h3>
+      <div class="identity-card">
+        <div class="identity-avatar">
+          <ProfileAvatar
+            :name="user?.displayName || 'default'"
+            :avatar="user?.avatarUrl ? { type: 'image', dataUrl: user.avatarUrl } : null"
+            :size="64"
+          />
+        </div>
+        <div class="identity-info">
+          <div class="identity-row">
+            <span class="identity-label">{{ t('settings.matrixIdentity.userId') }}</span>
+            <span class="identity-value">{{ user?.matrixUserId }}</span>
+          </div>
+          <div class="identity-row">
+            <span class="identity-label">{{ t('settings.matrixIdentity.displayName') }}</span>
+            <span class="identity-value">{{ user?.displayName }}</span>
+          </div>
+          <div class="identity-row">
+            <span class="identity-label">{{ t('settings.matrixIdentity.homeserver') }}</span>
+            <span class="identity-value">{{ user?.homeserverUrl }}</span>
+          </div>
+          <div class="identity-row">
+            <span class="identity-label">{{ t('settings.matrixIdentity.role') }}</span>
+            <span class="identity-value">
+              <NTag :type="user?.role === 'super_admin' ? 'warning' : 'default'" size="small">
+                {{ user?.role === 'super_admin' ? t('users.roles.superAdmin') : t('users.roles.admin') }}
+              </NTag>
+            </span>
+          </div>
+          <div class="identity-row">
+            <span class="identity-label">{{ t('settings.matrixIdentity.authSource') }}</span>
+            <span class="identity-value">
+              <NTag type="info" size="small">Matrix</NTag>
+            </span>
+          </div>
+        </div>
+      </div>
+    </div>
+
     <div class="configured-section">
-      <div class="action-row">
-        <span class="action-label">{{ t("login.passwordLoginConfigured", { username }) }}</span>
+      <!-- For Matrix users: show Matrix password change -->
+      <div v-if="isMatrixUser" class="action-row">
+        <span class="action-label">{{ t("settings.matrixIdentity.passwordManagedByMatrix") }}</span>
+        <div class="action-buttons">
+          <NButton @click="openMatrixPasswordModal">{{ t("settings.matrixIdentity.changeMatrixPassword") }}</NButton>
+        </div>
+      </div>
+      <!-- For local users: keep existing password/username change -->
+      <div v-else class="action-row">
+        <span class="action-label">{{ t("login.passwordLoginConfigured", { username: user?.username }) }}</span>
         <div class="action-buttons">
           <NButton @click="openChangePasswordModal">{{ t("login.changePassword") }}</NButton>
           <NButton @click="openChangeUsernameModal">{{ t("login.changeUsername") }}</NButton>
@@ -344,6 +434,22 @@ onMounted(() => { loadLockedIps(); });
         <NButton type="primary" :loading="loading" @click="handleChangeUsername">{{ t("common.save") }}</NButton>
       </template>
     </NModal>
+
+    <!-- Matrix password reset modal -->
+    <NModal v-model:show="showMatrixPasswordModal" preset="dialog" :title="t('settings.matrixIdentity.changeMatrixPassword')">
+      <NForm label-placement="top">
+        <NFormItem :label="t('login.newPassword')">
+          <NInput v-model:value="matrixNewPassword" type="password" show-password-on="click" :placeholder="t('login.newPassword')" />
+        </NFormItem>
+        <NFormItem :label="t('login.confirmPassword')">
+          <NInput v-model:value="matrixPasswordConfirm" type="password" show-password-on="click" :placeholder="t('login.confirmPassword')" @keyup.enter="handleMatrixPasswordChange" />
+        </NFormItem>
+      </NForm>
+      <template #action>
+        <NButton @click="showMatrixPasswordModal = false">{{ t("common.cancel") }}</NButton>
+        <NButton type="primary" :loading="loading" @click="handleMatrixPasswordChange">{{ t("common.save") }}</NButton>
+      </template>
+    </NModal>
   </div>
 </template>
 
@@ -464,4 +570,49 @@ onMounted(() => { loadLockedIps(); });
   color: $text-muted;
   margin: 0 0 12px;
 }
+
+.matrix-identity-section {
+  margin-bottom: 32px;
+  padding-bottom: 20px;
+  border-bottom: 1px solid $border-color;
+}
+
+.identity-card {
+  display: flex;
+  align-items: flex-start;
+  gap: 20px;
+  padding: 16px;
+  border: 1px solid $border-color;
+  border-radius: $radius-sm;
+  background: $bg-input;
+}
+
+.identity-avatar {
+  flex-shrink: 0;
+}
+
+.identity-info {
+  flex: 1;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.identity-row {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+}
+
+.identity-label {
+  font-size: 13px;
+  color: $text-muted;
+  min-width: 100px;
+}
+
+.identity-value {
+  font-size: 14px;
+  color: $text-primary;
+  font-family: $font-code;
+}
 </style>
diff --git a/packages/client/src/components/hermes/settings/DisplaySettings.vue b/packages/client/src/components/hermes/settings/DisplaySettings.vue
index f8df2347..6fd5b831 100644
--- a/packages/client/src/components/hermes/settings/DisplaySettings.vue
+++ b/packages/client/src/components/hermes/settings/DisplaySettings.vue
@@ -14,6 +14,7 @@ const { brightness, setBrightness } = useTheme()
 const themeOptions = [
   { label: t('settings.display.themeLight'), value: 'light' },
   { label: t('settings.display.themeDark'), value: 'dark' },
+  { label: t('settings.display.themeAuto'), value: 'auto' },
   { label: t('settings.display.themeSystem'), value: 'system' },
 ]
 
@@ -51,7 +52,7 @@ async function handleNotifyOnCompleteChange(value: boolean) {
     void showCompletionNotification({
       title: 'Hermes',
       body: t('settings.display.notifyOnCompleteTest'),
-      icon: '/coding-agents/hermes.png',
+      icon: '',
       tag: `hermes-complete-test-${Date.now()}`,
     })
   }
@@ -66,7 +67,7 @@ async function testCompletionNotification() {
   const shown = await showCompletionNotification({
     title: 'Hermes',
     body: t('settings.display.notifyOnCompleteTest'),
-    icon: '/coding-agents/hermes.png',
+    icon: '',
     tag: `hermes-complete-test-${Date.now()}`,
   })
   if (!shown) {
diff --git a/packages/client/src/components/hermes/settings/UserManagementSettings.vue b/packages/client/src/components/hermes/settings/UserManagementSettings.vue
index 33adeab8..44270b53 100644
--- a/packages/client/src/components/hermes/settings/UserManagementSettings.vue
+++ b/packages/client/src/components/hermes/settings/UserManagementSettings.vue
@@ -3,11 +3,13 @@ import { computed, h, onMounted, reactive, ref } from 'vue'
 import { NButton, NDataTable, NForm, NFormItem, NInput, NModal, NPopconfirm, NSelect, NSpace, NTag, useMessage, type DataTableColumns } from 'naive-ui'
 import { useI18n } from 'vue-i18n'
 import {
-  createManagedUser,
-  deleteManagedUser,
-  fetchManagedUsers,
-  updateManagedUser,
-  type ManagedUser,
+  createMatrixUser,
+  deleteMatrixUser,
+  deactivateMatrixUser,
+  fetchMatrixUsers,
+  updateMatrixUser,
+  resetMatrixUserPassword,
+  type MatrixUser,
   type UserRole,
   type UserStatus,
 } from '@/api/auth'
@@ -17,14 +19,17 @@ const message = useMessage()
 
 const loading = ref(false)
 const saving = ref(false)
-const users = ref<ManagedUser[]>([])
-const profiles = ref<string[]>([])
+const users = ref<MatrixUser[]>([])
 const showModal = ref(false)
-const editingUser = ref<ManagedUser | null>(null)
+const editingUser = ref<MatrixUser | null>(null)
+const showResetPasswordModal = ref(false)
+const resetPasswordTarget = ref<MatrixUser | null>(null)
+const newPassword = ref('')
 
 const form = reactive({
-  username: '',
+  userId: '',
   password: '',
+  displayName: '',
   role: 'admin' as UserRole,
   status: 'active' as UserStatus,
   profiles: [] as string[],
@@ -40,12 +45,13 @@ const statusOptions = computed(() => [
   { label: t('users.status.disabled'), value: 'disabled' },
 ])
 
-const profileOptions = computed(() => profiles.value.map(profile => ({ label: profile, value: profile })))
+const profileOptions = computed(() => []) // Profiles fetched from API
 
 function resetForm() {
   editingUser.value = null
-  form.username = ''
+  form.userId = ''
   form.password = ''
+  form.displayName = ''
   form.role = 'admin'
   form.status = 'active'
   form.profiles = []
@@ -54,9 +60,8 @@ function resetForm() {
 async function loadUsers() {
   loading.value = true
   try {
-    const res = await fetchManagedUsers()
+    const res = await fetchMatrixUsers()
     users.value = res.users
-    profiles.value = res.profiles
   } catch (err: any) {
     message.error(err.message || t('users.loadFailed'))
   } finally {
@@ -69,9 +74,10 @@ function openCreate() {
   showModal.value = true
 }
 
-function openEdit(user: ManagedUser) {
+function openEdit(user: MatrixUser) {
   editingUser.value = user
-  form.username = user.username
+  form.userId = user.matrix_user_id
+  form.displayName = user.display_name
   form.password = ''
   form.role = user.role
   form.status = user.status
@@ -80,36 +86,57 @@ function openEdit(user: ManagedUser) {
 }
 
 async function submit() {
-  if (form.username.trim().length < 2) {
-    message.error(t('login.usernameTooShort'))
-    return
-  }
-  if (!editingUser.value && form.password.length < 6) {
-    message.error(t('login.passwordTooShort'))
-    return
-  }
-  if (form.password && form.password.length < 6) {
-    message.error(t('login.passwordTooShort'))
-    return
+  if (!editingUser.value) {
+    // Create new user
+    if (!form.userId.trim() || !form.password || form.password.length < 6) {
+      message.error(t('login.passwordTooShort'))
+      return
+    }
+    saving.value = true
+    try {
+      await createMatrixUser({
+        userId: form.userId.trim(),
+        password: form.password,
+        displayName: form.displayName,
+        role: form.role,
+        profiles: form.role === 'super_admin' ? [] : form.profiles,
+      })
+      await loadUsers()
+      showModal.value = false
+      resetForm()
+      message.success(t('common.saved'))
+    } catch (err: any) {
+      message.error(err.message || t('common.saveFailed'))
+    } finally {
+      saving.value = false
+    }
+  } else {
+    // Update existing user
+    saving.value = true
+    try {
+      await updateMatrixUser(editingUser.value.matrix_user_id, {
+        displayName: form.displayName,
+        role: form.role,
+        status: form.status,
+        profiles: form.role === 'super_admin' ? [] : form.profiles,
+      })
+      await loadUsers()
+      showModal.value = false
+      resetForm()
+      message.success(t('common.saved'))
+    } catch (err: any) {
+      message.error(err.message || t('common.saveFailed'))
+    } finally {
+      saving.value = false
+    }
   }
+}
 
+async function setStatus(user: MatrixUser, status: UserStatus) {
   saving.value = true
   try {
-    const payload = {
-      username: form.username.trim(),
-      password: form.password || undefined,
-      role: form.role,
-      status: form.status,
-      profiles: form.role === 'super_admin' ? [] : form.profiles,
-      defaultProfile: form.profiles[0] || null,
-    }
-    const res = editingUser.value
-      ? await updateManagedUser(editingUser.value.id, payload)
-      : await createManagedUser({ ...payload, password: form.password })
-    users.value = res.users
-    profiles.value = res.profiles
-    showModal.value = false
-    resetForm()
+    await deactivateMatrixUser(user.matrix_user_id, status === 'active')
+    await loadUsers()
     message.success(t('common.saved'))
   } catch (err: any) {
     message.error(err.message || t('common.saveFailed'))
@@ -118,29 +145,34 @@ async function submit() {
   }
 }
 
-async function setStatus(user: ManagedUser, status: UserStatus) {
+async function removeUser(user: MatrixUser) {
   saving.value = true
   try {
-    const res = await updateManagedUser(user.id, { status })
-    users.value = res.users
-    profiles.value = res.profiles
+    await deleteMatrixUser(user.matrix_user_id)
+    await loadUsers()
     message.success(t('common.saved'))
   } catch (err: any) {
-    message.error(err.message || t('common.saveFailed'))
+    message.error(err.message || t('common.deleteFailed'))
   } finally {
     saving.value = false
   }
 }
 
-async function removeUser(user: ManagedUser) {
+async function handleResetPassword() {
+  if (!newPassword.value || newPassword.value.length < 6) {
+    message.error(t('login.passwordTooShort'))
+    return
+  }
+  if (!resetPasswordTarget.value) return
   saving.value = true
   try {
-    const res = await deleteManagedUser(user.id)
-    users.value = res.users
-    profiles.value = res.profiles
-    message.success(t('common.saved'))
+    await resetMatrixUserPassword(resetPasswordTarget.value.matrix_user_id, newPassword.value)
+    showResetPasswordModal.value = false
+    newPassword.value = ''
+    resetPasswordTarget.value = null
+    message.success(t('settings.matrixIdentity.passwordChanged'))
   } catch (err: any) {
-    message.error(err.message || t('common.deleteFailed'))
+    message.error(err.message || t('common.saveFailed'))
   } finally {
     saving.value = false
   }
@@ -151,10 +183,16 @@ function formatTime(value: number | null): string {
   return new Date(value).toLocaleString()
 }
 
-const columns = computed<DataTableColumns<ManagedUser>>(() => [
+const columns = computed<DataTableColumns<MatrixUser>>(() => [
   {
-    title: t('users.username'),
-    key: 'username',
+    title: t('users.matrixUserId'),
+    key: 'matrix_user_id',
+    minWidth: 180,
+    render: (row) => h('span', { class: 'mono' }, row.matrix_user_id),
+  },
+  {
+    title: t('users.displayName'),
+    key: 'display_name',
     minWidth: 140,
   },
   {
@@ -194,10 +232,11 @@ const columns = computed<DataTableColumns<ManagedUser>>(() => [
   {
     title: t('common.edit'),
     key: 'actions',
-    width: 280,
+    width: 340,
     render: (row) => h(NSpace, { size: 8 }, {
       default: () => [
         h(NButton, { size: 'small', onClick: () => openEdit(row) }, { default: () => t('common.edit') }),
+        h(NButton, { size: 'small', onClick: () => { resetPasswordTarget.value = row; showResetPasswordModal.value = true; } }, { default: () => t('users.resetPassword') }),
         h(NButton, {
           size: 'small',
           type: row.status === 'active' ? 'warning' : 'primary',
@@ -238,12 +277,15 @@ onMounted(loadUsers)
 
     <NModal v-model:show="showModal" preset="dialog" :title="editingUser ? t('users.edit') : t('users.create')">
       <NForm label-placement="top">
-        <NFormItem :label="t('users.username')">
-          <NInput v-model:value="form.username" :placeholder="t('login.usernamePlaceholder')" />
+        <NFormItem :label="t('users.matrixUserId')">
+          <NInput v-model:value="form.userId" :disabled="!!editingUser" :placeholder="t('users.matrixUserIdPlaceholder')" />
         </NFormItem>
-        <NFormItem :label="editingUser ? t('users.newPasswordOptional') : t('login.newPassword')">
+        <NFormItem v-if="!editingUser" :label="t('login.newPassword')">
           <NInput v-model:value="form.password" type="password" show-password-on="click" :placeholder="t('login.passwordPlaceholder')" />
         </NFormItem>
+        <NFormItem :label="t('users.displayName')">
+          <NInput v-model:value="form.displayName" :placeholder="t('users.displayNamePlaceholder')" />
+        </NFormItem>
         <NFormItem :label="t('users.role')">
           <NSelect v-model:value="form.role" :options="roleOptions" />
         </NFormItem>
@@ -265,6 +307,19 @@ onMounted(loadUsers)
         <NButton type="primary" :loading="saving" @click="submit">{{ t('common.save') }}</NButton>
       </template>
     </NModal>
+
+    <!-- Reset Password Modal -->
+    <NModal v-model:show="showResetPasswordModal" preset="dialog" :title="t('users.resetPassword')">
+      <NForm label-placement="top">
+        <NFormItem :label="t('login.newPassword')">
+          <NInput v-model:value="newPassword" type="password" show-password-on="click" :placeholder="t('login.passwordPlaceholder')" />
+        </NFormItem>
+      </NForm>
+      <template #action>
+        <NButton @click="showResetPasswordModal = false">{{ t('common.cancel') }}</NButton>
+        <NButton type="primary" :loading="saving" @click="handleResetPassword">{{ t('common.save') }}</NButton>
+      </template>
+    </NModal>
   </div>
 </template>
 
@@ -299,4 +354,9 @@ onMounted(loadUsers)
 :deep(.muted) {
   color: $text-muted;
 }
+
+:deep(.mono) {
+  font-family: $font-code;
+  font-size: 13px;
+}
 </style>
diff --git a/packages/client/src/components/layout/AppSidebar.vue b/packages/client/src/components/layout/AppSidebar.vue
index e429a02b..d17923c7 100644
--- a/packages/client/src/components/layout/AppSidebar.vue
+++ b/packages/client/src/components/layout/AppSidebar.vue
@@ -12,17 +12,19 @@ import LanguageSwitch from "@/components/layout/LanguageSwitch.vue";
 import ThemeSwitch from "@/components/layout/ThemeSwitch.vue";
 import VersionManagementModal from "@/components/layout/VersionManagementModal.vue";
 import { changelog } from "@/data/changelog";
-import { getStoredUsername, isStoredSuperAdmin } from "@/api/client";
+import { useAuthStore } from "@/stores/hermes/auth";
+import { getStoredUsername } from "@/api/client";
 
 const { t } = useI18n();
 const message = useMessage();
 const route = useRoute();
 const router = useRouter();
 const appStore = useAppStore();
+const authStore = useAuthStore();
 const selectedKey = computed(() => {
   return route.name as string;
 });
-const isSuperAdmin = computed(() => isStoredSuperAdmin());
+const isSuperAdmin = computed(() => authStore.isSuperAdmin);
 const currentUsername = computed(() => getStoredUsername());
 const isVersionPreview = import.meta.env.VITE_HERMES_PREVIEW === '1';
 const isDesktopShell = computed(() =>
@@ -119,6 +121,13 @@ function openVersionManagement() {
             </svg>
             <span>{{ t("sidebar.kanban") }}</span>
           </RouteLinkItem>
+          <RouteLinkItem class="nav-item" :to="{ name: 'hermes.cockpit' }" :active="selectedKey === 'hermes.cockpit'">
+            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
+              <circle cx="12" cy="12" r="3" />
+              <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
+            </svg>
+            <span>{{ t("sidebar.cockpit") }}</span>
+          </RouteLinkItem>
           <RouteLinkItem class="nav-item" :to="{ name: 'hermes.channels' }" :active="selectedKey === 'hermes.channels'">
             <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
               <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
@@ -315,40 +324,10 @@ function openVersionManagement() {
         </div>
         <LanguageSwitch />
       </div>
-      <div class="version-info">
-        <div class="version-links">
-          <a class="sidebar-footer-link" href="https://github.com/EKKOLearnAI/hermes-studio" target="_blank" rel="noopener noreferrer" title="GitHub">
-            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
-          </a>
-          <a class="sidebar-footer-link" href="https://hermes-studio.ai/" target="_blank" rel="noopener noreferrer" title="Website">
-            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
-          </a>
-        </div>
-        <span
-          class="version-text"
-          role="button"
-          tabindex="0"
-          @click="openChangelog"
-          @keydown.enter="openChangelog"
-          @keydown.space.prevent="openChangelog"
-        >
-          Studio v{{ appStore.serverVersion || "0.1.0" }}
-        </span>
-        <ThemeSwitch />
-      </div>
-      <NButton v-if="isDesktopShell" type="primary" size="tiny" block class="update-btn" @click="openVersionManagement">
-        {{ t('sidebar.versionManagement') }}
-      </NButton>
-      <NButton v-if="appStore.clientOutdated" type="warning" size="tiny" block class="update-btn" @click="handleReloadClient">
-        {{ t('sidebar.reloadClientVersion', { version: appStore.serverVersion }) }}
-      </NButton>
-      <NButton v-if="appStore.updateAvailable" type="primary" size="tiny" block class="update-btn" :loading="appStore.updating" @click="handleUpdate">
-        {{ appStore.updating ? t('sidebar.updating') : t('sidebar.updateVersion', { version: appStore.latestVersion }) }}
-      </NButton>
     </div>
 
-    <div class="sidebar-top-actions">
-      <RouteLinkItem class="nav-item sidebar-return-tab" :to="{ name: 'hermes.chat' }" :title="t('sidebar.backToChat')">
+      <div class="sidebar-top-actions">
+      <RouteLinkItem class="nav-item sidebar-return-tab" :to="{ name: 'hermes.cockpit' }" :title="t('sidebar.backToChat')">
         <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
           <polyline points="15 18 9 12 15 6" />
           <line x1="9" y1="12" x2="21" y2="12" />
@@ -586,35 +565,6 @@ function openVersionManagement() {
   white-space: nowrap;
 }
 
-.version-info {
-  padding: 2px 0 8px 12px;
-  font-size: 11px;
-  color: $text-muted;
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: 6px;
-  overflow: hidden;
-}
-
-.version-links {
-  display: flex;
-  align-items: center;
-  flex-shrink: 0;
-  gap: 6px;
-}
-
-.sidebar-footer-link {
-  color: $text-muted;
-  display: flex;
-  align-items: center;
-  transition: color $transition-fast;
-
-  &:hover {
-    color: $text-primary;
-  }
-}
-
 .version-text {
   flex: 0 0 auto;
   overflow: visible;
@@ -627,15 +577,6 @@ function openVersionManagement() {
   }
 }
 
-.version-info :deep(.theme-switch-container) {
-  flex-shrink: 0;
-}
-
-.update-btn {
-  margin: 4px 0 0;
-  border-radius: $radius-sm;
-}
-
 .changelog-list {
   max-height: min(70vh, 640px);
   overflow-y: auto;
@@ -773,9 +714,7 @@ function openVersionManagement() {
     padding-top: 8px;
   }
 
-  .status-row,
-  .version-info,
-  .update-btn {
+  .status-row {
     display: none;
   }
 
diff --git a/packages/client/src/components/layout/DesktopTitleBar.vue b/packages/client/src/components/layout/DesktopTitleBar.vue
index 55357b5d..0dc58354 100644
--- a/packages/client/src/components/layout/DesktopTitleBar.vue
+++ b/packages/client/src/components/layout/DesktopTitleBar.vue
@@ -48,9 +48,8 @@ onMounted(() => {
 <template>
   <header v-if="showTitleBar" class="desktop-titlebar" :class="{ mac: isMac }" @dblclick="controlWindow('toggle-maximize')">
     <div class="desktop-titlebar__drag">
-      <div class="desktop-titlebar__brand">
-        <img class="desktop-titlebar__logo" src="/logo.png" alt="" draggable="false">
-        <span class="desktop-titlebar__title">Hermes Studio</span>
+        <div class="desktop-titlebar__brand">
+          <span class="desktop-titlebar__title">Swarm Studio</span>
       </div>
     </div>
     <div v-if="showWindowButtons" class="desktop-titlebar__controls" @dblclick.stop>
diff --git a/packages/client/src/components/layout/PageSidebarNav.vue b/packages/client/src/components/layout/PageSidebarNav.vue
index 58751c5d..265bb1d7 100644
--- a/packages/client/src/components/layout/PageSidebarNav.vue
+++ b/packages/client/src/components/layout/PageSidebarNav.vue
@@ -5,7 +5,7 @@ import { useI18n } from 'vue-i18n'
 import { useRouter } from 'vue-router'
 import { useSessionSearch } from '@/composables/useSessionSearch'
 
-type ActiveSection = 'chat' | 'history' | 'group' | 'global' | 'workflow'
+type ActiveSection = 'chat' | 'history' | 'group' | 'global' | 'workflow' | 'matrix' | 'swarm-kanban'
 
 const props = defineProps<{
   active: ActiveSection
@@ -50,9 +50,14 @@ function openWorkflow() {
   void router.push({ name: 'hermes.workflow' })
 }
 
-function openApiRelay() {
-  if (typeof window === 'undefined') return
-  window.open('https://apikey.fun/register?aff=LIBAPI', '_blank', 'noopener,noreferrer')
+function openMatrixChat() {
+  if (props.active === 'matrix') return
+  void router.push({ name: 'hermes.matrixChat' })
+}
+
+function openSwarmKanban() {
+  if (props.active === 'swarm-kanban') return
+  void router.push({ name: 'hermes.swarmKanban' })
 }
 </script>
 
@@ -124,24 +129,27 @@ function openApiRelay() {
           <path d="M12 7v5l3 2" />
         </svg>
         <span>{{ historyButtonLabel }}</span>
-      </button>
-      <button class="page-sidebar-tab" type="button" @click="openApiRelay">
-        <svg
-          width="15"
-          height="15"
-          viewBox="0 0 24 24"
-          fill="none"
-          stroke="currentColor"
-          stroke-width="1.8"
-        >
-          <path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6" />
-          <polyline points="15 3 21 3 21 9" />
-          <line x1="10" y1="14" x2="21" y2="3" />
-        </svg>
-        <span>{{ t('sidebar.apiRelay') }}</span>
-      </button>
-    </div>
-    <div v-if="showModeSwitch" class="conversation-switch conversation-switch--three" role="tablist" aria-label="Conversation type">
+	      </button>
+	    </div>
+    <div v-if="showModeSwitch" class="conversation-switch conversation-switch--four" role="tablist" aria-label="Conversation type">
+      <NTooltip trigger="hover" placement="top">
+        <template #trigger>
+          <button
+            class="conversation-switch-tab"
+            :class="{ active: active === 'matrix' }"
+            type="button"
+            role="tab"
+            :aria-label="t('sidebar.matrixChat')"
+            :aria-selected="active === 'matrix'"
+            @click="openMatrixChat"
+          >
+            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
+              <path d="M21 15a2 2 0 0 1-2 2H8l-5 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
+            </svg>
+          </button>
+        </template>
+        {{ t('sidebar.matrixChat') }}
+      </NTooltip>
       <NTooltip trigger="hover" placement="top">
         <template #trigger>
           <button
@@ -274,6 +282,10 @@ function openApiRelay() {
   grid-template-columns: repeat(3, minmax(0, 1fr));
 }
 
+.conversation-switch--four {
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+}
+
 .conversation-switch-tab {
   width: 100%;
   min-width: 0;
diff --git a/packages/client/src/composables/useTheme.ts b/packages/client/src/composables/useTheme.ts
index 6b953454..a5b18d4f 100644
--- a/packages/client/src/composables/useTheme.ts
+++ b/packages/client/src/composables/useTheme.ts
@@ -1,13 +1,13 @@
 import { ref, watch, computed } from 'vue'
 
-export type BrightnessMode = 'light' | 'dark' | 'system'
+export type BrightnessMode = 'light' | 'dark' | 'system' | 'auto'
 export type ThemeStyle = 'ink' | 'comic'
 
 const BRIGHTNESS_KEY = 'hermes_brightness'
 const STYLE_KEY = 'hermes_style'
 
 const brightness = ref<BrightnessMode>(
-  (localStorage.getItem(BRIGHTNESS_KEY) as BrightnessMode) || 'system',
+  (localStorage.getItem(BRIGHTNESS_KEY) as BrightnessMode) || 'auto',
 )
 
 const style = ref<ThemeStyle>(
@@ -17,10 +17,87 @@ const style = ref<ThemeStyle>(
 const isDark = ref(false)
 const isComic = ref(false)
 
+// ── Beijing sunrise/sunset estimation ──
+// Approximate Beijing sunrise/sunset based on day-of-year.
+// Beijing: lat ~40°N, lon ~116°E, UTC+8.
+// Uses simple sinusoidal approximation (± seasonal variation).
+function beijingSunriseSunset(): { sunrise: number; sunset: number } {
+  const now = new Date()
+  const dayOfYear = Math.floor(
+    (now.getTime() - new Date(now.getFullYear(), 0, 0).getTime()) / 86400000,
+  )
+  // Day 172 (summer solstice): sunrise ~4:45, sunset ~19:45
+  // Day 355 (winter solstice): sunrise ~7:35, sunset ~16:50
+  // Seasonal offset uses cosine curve; max variation ~2.8h
+  const seasonal = -Math.cos((2 * Math.PI * (dayOfYear - 172)) / 365)
+  const sunriseMinutes = 5 * 60 + seasonal * 85   // range 04:20–07:20
+  const sunsetMinutes = 19 * 60 - seasonal * 85   // range 17:05–19:45
+
+  return {
+    sunrise: sunriseMinutes,
+    sunset: sunsetMinutes,
+  }
+}
+
+function msUntil(targetMinutes: number): number {
+  const now = new Date()
+  // Use Beijing time (UTC+8)
+  const utcH = now.getUTCHours()
+  const utcM = now.getUTCMinutes()
+  const nowBeijingMinutes = ((utcH + 8) * 60 + utcM) % 1440
+
+  let diff = targetMinutes - nowBeijingMinutes
+  if (diff <= 0) diff += 1440 // next occurrence
+  // Convert minutes diff to ms from now
+  const nowMs = utcH * 3600_000 + utcM * 60_000 + now.getUTCSeconds() * 1000 + now.getUTCMilliseconds()
+  const targetMs = diff * 60_000 - (nowMs % 3600_000)
+  return targetMs > 0 ? targetMs : diff * 60_000
+}
+
+let autoTimer: ReturnType<typeof setTimeout> | undefined
+
+function scheduleAutoSwitch() {
+  if (autoTimer) clearTimeout(autoTimer)
+  const { sunrise, sunset } = beijingSunriseSunset()
+  const now = new Date()
+  const utcH = now.getUTCHours()
+  const utcM = now.getUTCMinutes()
+  const currentMinutes = ((utcH + 8) * 60 + utcM) % 1440
+
+  // Determine next switch
+  const nextIsDark = currentMinutes >= sunset || currentMinutes < sunrise
+  const nextTarget = nextIsDark ? sunrise : sunset
+
+  autoTimer = setTimeout(() => {
+    applyAutoMode()
+    scheduleAutoSwitch() // reschedule for next day
+  }, msUntil(nextTarget))
+}
+
+function applyAutoMode() {
+  const { sunrise, sunset } = beijingSunriseSunset()
+  const now = new Date()
+  const utcH = now.getUTCHours()
+  const utcM = now.getUTCMinutes()
+  const currentMinutes = ((utcH + 8) * 60 + utcM) % 1440
+
+  const dark = currentMinutes >= sunset || currentMinutes < sunrise
+  isDark.value = dark
+  document.documentElement.classList.toggle('dark', dark)
+}
+
 function resolveDark(b: BrightnessMode): boolean {
   if (b === 'system') {
     return window.matchMedia('(prefers-color-scheme: dark)').matches
   }
+  if (b === 'auto') {
+    const { sunrise, sunset } = beijingSunriseSunset()
+    const now = new Date()
+    const utcH = now.getUTCHours()
+    const utcM = now.getUTCMinutes()
+    const currentMinutes = ((utcH + 8) * 60 + utcM) % 1440
+    return currentMinutes >= sunset || currentMinutes < sunrise
+  }
   return b === 'dark'
 }
 
@@ -30,6 +107,13 @@ function applyClasses() {
   isComic.value = style.value === 'comic'
   document.documentElement.classList.toggle('dark', dark)
   document.documentElement.classList.toggle('comic', isComic.value)
+
+  // Manage auto-timer
+  if (brightness.value === 'auto') {
+    scheduleAutoSwitch()
+  } else {
+    if (autoTimer) { clearTimeout(autoTimer); autoTimer = undefined }
+  }
 }
 
 // Initial
diff --git a/packages/client/src/i18n/locales/de.ts b/packages/client/src/i18n/locales/de.ts
index a2b71031..3297c15e 100644
--- a/packages/client/src/i18n/locales/de.ts
+++ b/packages/client/src/i18n/locales/de.ts
@@ -1,7 +1,7 @@
 export default {
   // Login
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Geben Sie Benutzername und Passwort ein, um fortzufahren.',
     placeholder: 'Zugangs-Token',
     submit: 'Anmelden',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: 'Remote-Versionen konnten nicht geladen werden',
     downloadTasks: 'Download-Aufgaben',
     downloadStarted: 'Download gestartet',
-    activateSuccess: 'Versionsauswahl gespeichert. Starten Sie Hermes Studio neu, um sie zu verwenden.',
+    activateSuccess: 'Versionsauswahl gespeichert. Starten Sie Swarm Studio neu, um sie zu verwenden.',
     jobStatus: {
       queued: 'In Warteschlange',
       running: 'Läuft',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: 'Bitte geben Sie eine gültige Kontextlänge ein',
     contextEditSuccess: 'Kontextlänge aktualisiert',
     contextEditFailed: 'Aktualisierung fehlgeschlagen',
-    emptyState: 'Starten Sie eine Konversation mit Hermes Agent',
+    emptyState: 'Starten Sie eine Konversation mit Swarm Agent',
     emptyStateAgent: 'Starten Sie eine Konversation mit {agent}',
     viewOlderInHistory: 'Fruehere Nachrichten im Verlauf ansehen',
     scrollToBottom: 'Nach unten scrollen',
@@ -751,10 +751,10 @@ jobTriggered: 'Job ausgelost',
     reloadHint: 'Aktive Sitzungen: /reload-skills ausführen; neue Sitzungen laden automatisch.',
     writeApprovalButton: 'Ausstehend {count}',
     writeApprovalTitle: 'Ausstehende Schreibfreigaben',
-    writeApprovalDescription: 'Freigeben oder ablehnen, was Hermes Agent fuer Speicher und Skills vorgemerkt hat.',
+    writeApprovalDescription: 'Freigeben oder ablehnen, was Swarm Agent fuer Speicher und Skills vorgemerkt hat.',
     writeApprovalRefresh: 'Aktualisieren',
     writeApprovalEmpty: 'Keine ausstehenden Schreibvorgaenge.',
-    writeApprovalUnsupported: 'Diese Hermes-Agent-Version unterstuetzt keine Schreibfreigaben. Aktualisiere Hermes Agent, um diese Funktion zu nutzen.',
+    writeApprovalUnsupported: 'Diese Hermes-Agent-Version unterstuetzt keine Schreibfreigaben. Aktualisiere Swarm Agent, um diese Funktion zu nutzen.',
     writeApprovalMemory: 'Speicher',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Pruefen',
@@ -1185,7 +1185,7 @@ jobTriggered: 'Job ausgelost',
       enabled: 'Gateways automatisch starten',
       enabledHint: 'Wenn deaktiviert, startet die Web UI beim Start kein Profile-Gateway automatisch.',
       management: 'Vereinheitlichtes Gateway',
-      managementHint: 'Erfordert Hermes Agent 0.17.0 oder neuer. Wenn aktiviert, schreibt die Web UI multiplex_profiles, damit das default gateway alle Profile bedient; andere Profile konnen keine separaten Feishu-, Weixin-, QQBot- oder ahnlichen Messaging-Einstiegspunkte konfigurieren. Beim Deaktivieren wird diese Einstellung entfernt.',
+      managementHint: 'Erfordert Swarm Agent 0.17.0 oder neuer. Wenn aktiviert, schreibt die Web UI multiplex_profiles, damit das default gateway alle Profile bedient; andere Profile konnen keine separaten Feishu-, Weixin-, QQBot- oder ahnlichen Messaging-Einstiegspunkte konfigurieren. Beim Deaktivieren wird diese Einstellung entfernt.',
       managementAuto: 'Automatisch',
       managementPerProfile: 'Ein Gateway pro Profil',
       managementUnified: 'Vereinheitlichtes default gateway',
@@ -1243,7 +1243,7 @@ jobTriggered: 'Job ausgelost',
       skillsWriteApproval: 'Skill-Schreibungen pruefen',
       skillsWriteApprovalHint: 'Hermes-Agent-Skill-Aenderungen vor dem Speichern zur Freigabe vormerken.',
       writeApprovalTitle: 'Ausstehende Schreibfreigaben',
-      writeApprovalDescription: 'Freigeben oder ablehnen, was Hermes Agent fuer Speicher und Skills vorgemerkt hat.',
+      writeApprovalDescription: 'Freigeben oder ablehnen, was Swarm Agent fuer Speicher und Skills vorgemerkt hat.',
       writeApprovalRefresh: 'Aktualisieren',
       writeApprovalEmpty: 'Keine ausstehenden Schreibvorgaenge.',
       writeApprovalMemory: 'Speicher',
@@ -1852,11 +1852,11 @@ jobTriggered: 'Job ausgelost',
     new_0_6_19_4: 'TTS Einstellungen werden jetzt pro profile gespeichert; Speed, Pitch und Voice Tests lesen dieselbe Konfigurationsquelle',
     new_0_6_19_5: 'Coding-Agent Modellwechsel beeinflussen bestehende Sessions nicht mehr, und custom provider keys werden korrekt weitergegeben',
     new_0_6_19_6: 'Skills runtime Filter, Cron-Job Sortierung, begrenzte Skill-Detail-Hoehe, Windows Updater Recovery, Backup Fallbacks und komprimierte Logo Assets runden das Update ab',
-    new_0_6_18_2: 'Hermes Studio MCP ist in fokussierte api-, devices- und use-Toolsets aufgeteilt, mit neuen Session-, Modell-, Chat-Run- und Ops-Aktionen',
+    new_0_6_18_2: 'Swarm Studio MCP ist in fokussierte api-, devices- und use-Toolsets aufgeteilt, mit neuen Session-, Modell-, Chat-Run- und Ops-Aktionen',
     new_0_6_18_3: 'Chat- und Gruppenchat-Historien sind zuverlaessiger durch getrennte UI-Historie, sichere Null-Message-Behandlung, erhaltene Assistant-Antworten nach run failures und abgestimmtes Reasoning-Replay',
     new_0_6_18_4: 'MCU-Device-Feedback ist klarer: Tool Calls bleiben vor Sprache sichtbar, Audio markiert Aufgaben nicht mehr zu frueh als fertig, Active-Device-Login ist stabiler und lange ONLINE/IP-Texte scrollen',
     new_0_6_18_5: 'Voice- und Message-Workflows erhalten eigene HTTP TTS/STT-Endpunkte, Audio-Transcoding, Fork-Message-Aktionen, persistente Fork-Marker und klarere Upload-Fehler',
-    new_0_6_18_6: 'Desktop- und Runtime-Setup sind sicherer mit Unicode-sicherer Windows-PATH-Registrierung, Hermes Agent 0.17.0 Runtime-Publishing, mobiler PWA-Metadata und mehreren Profile/Auth-Recovery-Fixes',
+    new_0_6_18_6: 'Desktop- und Runtime-Setup sind sicherer mit Unicode-sicherer Windows-PATH-Registrierung, Swarm Agent 0.17.0 Runtime-Publishing, mobiler PWA-Metadata und mehreren Profile/Auth-Recovery-Fixes',
     new_0_6_18_7: 'Feinschliff und Detailverbesserungen in der gesamten App',
     new_0_6_17_1: 'Probleme beim Starten von Codex- und Claude-Code-Unterhaltungen wurden behoben',
     new_0_6_16_1: 'Provider-Einstellungen speichern jetzt eigene API-Modes, decken LongCat korrekt ab, unterstuetzen das Loeschen von v12-Providern und halten Modellkataloge mit Auth- und API-Key-Bildprovidern synchron',
diff --git a/packages/client/src/i18n/locales/en.ts b/packages/client/src/i18n/locales/en.ts
index fdc761f3..50f91204 100644
--- a/packages/client/src/i18n/locales/en.ts
+++ b/packages/client/src/i18n/locales/en.ts
@@ -1,7 +1,7 @@
 export default {
   // Login
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Enter your username and password to continue.',
     placeholder: 'Access token',
     submit: 'Login',
@@ -103,6 +103,9 @@ export default {
     collapse: 'Collapse',
     start: 'Start',
     stop: 'Stop',
+    search: 'Search',
+    refresh: 'Refresh',
+    open: 'Open',
   },
 
   // MCP Management
@@ -186,6 +189,9 @@ export default {
     history: 'History',
     jobs: 'Jobs',
     kanban: 'Kanban',
+    swarmKanban: 'Swarm kanban',
+    matrixChat: 'Matrix Chat',
+    cockpit: 'Cockpit',
     workflow: 'Workflow',
     models: 'Models',
     profiles: 'Profiles',
@@ -261,7 +267,7 @@ export default {
     remoteLoadFailed: 'Failed to load remote versions',
     downloadTasks: 'Download tasks',
     downloadStarted: 'Download started',
-    activateSuccess: 'Version selection saved. Restart Hermes Studio to use it.',
+    activateSuccess: 'Version selection saved. Restart Swarm Studio to use it.',
     jobStatus: {
       queued: 'Queued',
       running: 'Running',
@@ -413,7 +419,7 @@ export default {
     contextEditInvalid: 'Please enter a valid context length',
     contextEditSuccess: 'Context length updated',
     contextEditFailed: 'Update failed',
-    emptyState: 'Start a conversation with Hermes Agent',
+    emptyState: 'Start a conversation with Swarm Agent',
     emptyStateAgent: 'Start a conversation with {agent}',
     viewOlderInHistory: 'View earlier messages in History',
     scrollToBottom: 'Scroll to bottom',
@@ -727,10 +733,23 @@ export default {
   // Kanban
   kanban: {
     title: 'Kanban Board',
+    diagnostics: 'Diagnostics',
     createTask: 'New Task',
     noTasks: 'No tasks',
     allStatuses: 'All Statuses',
     allAssignees: 'All Assignees',
+    allTenants: 'All Tenants',
+    helpTooltip: 'Kanban board for managing tasks across tenants',
+    searchPlaceholder: 'Search tasks...',
+    tenant: 'Tenant',
+    assignee: 'Assignee',
+    showArchived: 'Show archived',
+    lanesByProfile: 'Lanes by profile',
+    clearFilters: 'Clear filters',
+    unassigned: 'Unassigned',
+    untitled: 'Untitled',
+    taskNeedsAttention: 'Task needs attention',
+    tasksNeedAttention: '{count} tasks need attention',
     board: {
       create: 'New Board',
       archive: 'Archive Board',
@@ -740,6 +759,15 @@ export default {
       created: 'Board created',
       archived: 'Board archived',
       archiveConfirm: 'Archive the current board?',
+      slug: 'Board slug',
+      slugInvalid: 'Invalid slug — use lowercase letters, numbers, and hyphens only',
+      name: 'Board name',
+      description: 'Board description',
+      icon: 'Icon',
+      iconPlaceholder: 'Emoji or icon name',
+      color: 'Color',
+      colorPlaceholder: '#hex or color name',
+      switchToNew: 'Switch to new board after creation',
     },
     columns: {
       triage: 'Triage',
@@ -752,6 +780,16 @@ export default {
       done: 'Done',
       archived: 'Archived',
     },
+    columnSubtitles: {
+      triage: 'Needs triage',
+      todo: 'Not started',
+      scheduled: 'Waiting to start',
+      ready: 'Ready to run',
+      running: 'In progress',
+      blocked: 'Blocked',
+      done: 'Completed',
+      archived: 'Archived',
+    },
     form: {
       title: 'Title',
       titlePlaceholder: 'Task title',
@@ -836,6 +874,8 @@ export default {
       specify: 'Specify',
       loadLog: 'Load Log',
       loadDiagnostics: 'Load Diagnostics',
+      nudgeDispatcher: 'Nudge dispatcher',
+      archive: 'Archive',
     },
     message: {
       taskCreated: 'Task created',
@@ -849,7 +889,116 @@ export default {
       taskReassigned: 'Task reassigned',
       taskSpecified: 'Task specified',
       loadFailed: 'Failed to load task',
-    },
+      bulkFailed: 'Bulk operation failed',
+      taskUpdated: 'Task updated',
+      updateFailed: 'Failed to update task',
+      linkAdded: 'Task link added',
+      linkFailed: 'Failed to update task link',
+      linkRemoved: 'Task link removed',
+      commentFailed: 'Failed to add comment',
+      logFailed: 'Failed to load worker log',
+    },
+    trash: {
+      dropHere: 'Drop here to archive',
+    },
+    reclaim: {
+      confirm: 'Reclaim this task? The current assignee will be notified.',
+      success: 'Task reclaimed',
+      failed: 'Failed to reclaim task',
+      button: 'Reclaim',
+    },
+    recovery: {
+      hint: 'Recovery reason (optional)',
+    },
+    reassign: {
+      success: 'Task reassigned',
+      failed: 'Failed to reassign task',
+      button: 'Reassign',
+      placeholder: 'Select a profile...',
+      confirm: 'Reassign this task?',
+    },
+    orchestration: {
+      defaultOption: 'Default',
+      saved: 'Orchestration settings saved',
+      saveFailed: 'Failed to save orchestration settings',
+      descriptionSaved: 'Description saved',
+      autoGenerated: 'Auto-generated',
+      autoGenerateFailed: 'Failed to auto-generate',
+      autoTitle: 'Auto Decompose',
+      manualTitle: 'Manual',
+      mode: 'Mode',
+      auto: 'Auto',
+      manual: 'Manual',
+      expandTitle: 'Expand orchestration settings',
+      settings: 'Settings',
+      reload: 'Reload',
+      orchestratorProfile: 'Orchestrator profile',
+      resolved: 'Resolved',
+      default: 'Default',
+      defaultAssignee: 'Default assignee',
+      orchestrationMode: 'Orchestration mode',
+      autoDecompose: 'Auto decompose',
+      autoHint: 'Automatically decompose tasks into subtasks',
+      manualHint: 'Manually create and assign subtasks',
+      loading: 'Loading...',
+      profileDescriptions: 'Profile descriptions',
+      profileDescriptionsHint: 'Describe each orchestrator profile',
+      noProfiles: 'No profiles available',
+      defaultTag: 'Default',
+      descriptionPlaceholder: 'Orchestration description...',
+      saveDescriptionTitle: 'Save description',
+      saving: 'Saving...',
+      save: 'Save',
+      autoGenerateTitle: 'Auto-generate',
+      generating: 'Generating...',
+    },
+    completionSummaryRequired: 'Completion summary is required before marking a task as done.',
+    confirmDone: 'Mark this task as done? This will complete the task.',
+    confirmArchive: 'Archive this task? Archived tasks are hidden from the board.',
+    confirmBlocked: 'Mark this task as blocked? Blocked tasks are paused until unblocked.',
+    restoreDrawer: 'Restore drawer',
+    maximizeDrawer: 'Maximize drawer',
+    clickToEdit: 'Click to edit',
+    clickToEditAssignee: 'Click to edit assignee',
+    project: 'Project',
+    workspace: 'Workspace',
+    skills: 'Skills',
+    goalMode: 'Goal mode',
+    createdBy: 'Created by',
+    specify: 'Specify',
+    decompose: 'Decompose',
+    recovery: 'Recovery',
+    notifyHomeChannels: 'Notify home channels',
+    sendingUpdates: 'Sending updates...',
+    clickToStop: 'Click to stop',
+    sendNotifications: 'Send notifications',
+    description: 'Description',
+    noDescription: 'No description',
+    dependencies: 'Dependencies',
+    parents: 'Parent tasks',
+    none: 'None',
+    parent: 'Parent',
+    children: 'Child tasks',
+    child: 'Child',
+    result: 'Result',
+    events: 'Events',
+    phantomIds: 'Phantom IDs',
+    workerLog: 'Worker log',
+    loadingLog: 'Loading log...',
+    noWorkerLog: 'No worker log available',
+    logTruncated: 'Log truncated — showing last {count} lines',
+    runHistory: 'Run history',
+    showAllAttempts: 'Show all attempts',
+    ended: 'Ended',
+    active: 'Active',
+    noProfile: 'No profile',
+    comments: 'Comments',
+    noComments: 'No comments',
+    addComment: 'Add comment',
+    comment: 'Comment',
+    completionSummary: 'Completion summary',
+    completionSummaryHint: 'Summarize what was accomplished',
+    completionSummaryPlaceholder: 'Write a completion summary...',
     stats: {
       total: 'Total',
       tasks: 'Tasks',
@@ -1002,10 +1151,10 @@ export default {
     reloadHint: 'Active sessions: run /reload-skills; new sessions pick it up automatically.',
     writeApprovalButton: 'Pending {count}',
     writeApprovalTitle: 'Pending write approvals',
-    writeApprovalDescription: 'Approve or reject staged Hermes Agent memory and skill writes.',
+    writeApprovalDescription: 'Approve or reject staged Swarm Agent memory and skill writes.',
     writeApprovalRefresh: 'Refresh',
     writeApprovalEmpty: 'No pending writes.',
-    writeApprovalUnsupported: 'This Hermes Agent version does not support write approvals. Upgrade Hermes Agent to use this feature.',
+    writeApprovalUnsupported: 'This Swarm Agent version does not support write approvals. Upgrade Swarm Agent to use this feature.',
     writeApprovalMemory: 'Memory',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Review',
@@ -1428,9 +1577,10 @@ export default {
       busyInputMode: 'Busy Input Mode',
       busyInputModeHint: 'Allow input while AI is processing',
       theme: 'Theme',
-      themeHint: 'Choose light, dark, or follow system preference',
+      themeHint: 'Choose light, dark, auto, or follow system preference',
       themeLight: 'Light',
       themeDark: 'Dark',
+      themeAuto: 'Auto (Beijing)',
       themeSystem: 'System',
     },
     agent: {
@@ -1452,7 +1602,7 @@ export default {
       enabled: 'Auto-start gateways',
       enabledHint: 'When disabled, startup will not automatically start any profile gateway.',
       management: 'Unified gateway',
-      managementHint: 'Requires Hermes Agent 0.17.0 or later. When enabled, Web UI writes multiplex_profiles so the default gateway serves every profile; other profiles cannot configure separate Feishu, Weixin, QQBot, or similar messaging entrypoints. Turning it off removes that config.',
+      managementHint: 'Requires Swarm Agent 0.17.0 or later. When enabled, Web UI writes multiplex_profiles so the default gateway serves every profile; other profiles cannot configure separate Feishu, Weixin, QQBot, or similar messaging entrypoints. Turning it off removes that config.',
       managementAuto: 'Auto',
       managementPerProfile: 'One gateway per profile',
       managementUnified: 'Unified default gateway',
@@ -1506,11 +1656,11 @@ export default {
       requireAuth: 'Session Authorization',
       requireAuthHint: 'Require authorization for session operations',
       memoryWriteApproval: 'Review memory writes',
-      memoryWriteApprovalHint: 'Stage Hermes Agent memory writes for approval before they are saved.',
+      memoryWriteApprovalHint: 'Stage Swarm Agent memory writes for approval before they are saved.',
       skillsWriteApproval: 'Review skill writes',
-      skillsWriteApprovalHint: 'Stage Hermes Agent skill changes for approval before they are saved.',
+      skillsWriteApprovalHint: 'Stage Swarm Agent skill changes for approval before they are saved.',
       writeApprovalTitle: 'Pending write approvals',
-      writeApprovalDescription: 'Approve or reject staged Hermes Agent memory and skill writes.',
+      writeApprovalDescription: 'Approve or reject staged Swarm Agent memory and skill writes.',
       writeApprovalRefresh: 'Refresh',
       writeApprovalEmpty: 'No pending writes.',
       writeApprovalMemory: 'Memory',
@@ -1838,6 +1988,17 @@ export default {
       mimoStylePromptHint: 'Optional — describe the speaking style in natural language',
       mimoStylePromptPlaceholder: 'e.g., Bright and bouncy tone, fast pace',
     },
+    matrixIdentity: {
+      title: 'Matrix Identity',
+      userId: 'User ID',
+      displayName: 'Display Name',
+      homeserver: 'Homeserver',
+      role: 'Role',
+      authSource: 'Auth Source',
+      passwordManagedByMatrix: 'Password is managed by your Matrix homeserver',
+      changeMatrixPassword: 'Change Matrix Password',
+      passwordChanged: 'Matrix password changed successfully',
+    },
   },
   githubPreview: {
     title: "Version Preview",
@@ -2232,11 +2393,11 @@ export default {
     new_0_6_19_4: 'TTS settings are now saved by profile, and speed, pitch, and voice tests read from the same configuration source',
     new_0_6_19_5: 'Coding Agent model switches no longer affect existing sessions, and custom provider keys are forwarded correctly',
     new_0_6_19_6: 'Skills runtime filters, cron job sorting, skill-detail height limits, Windows updater recovery, backup fallbacks, and compressed logo assets round out this update',
-    new_0_6_18_2: 'Hermes Studio MCP is split into focused api, devices, and use toolsets with new session, model, chat-run, and ops actions',
+    new_0_6_18_2: 'Swarm Studio MCP is split into focused api, devices, and use toolsets with new session, model, chat-run, and ops actions',
     new_0_6_18_3: 'Chat and group-chat history are more reliable with separate UI history, safer null-message handling, preserved assistant replies after run failures, and aligned reasoning replay',
     new_0_6_18_4: 'MCU device feedback is clearer: tool calls stay visible over speech, audio no longer marks tasks done too early, active-device login is steadier, and long online/IP status text scrolls',
     new_0_6_18_5: 'Voice and message workflows gained custom HTTP TTS/STT endpoint support, audio transcoding, fork message actions, persistent fork markers, and clearer upload errors',
-    new_0_6_18_6: 'Desktop and runtime setup are safer with Unicode-safe Windows PATH registration, Hermes Agent 0.17.0 runtime publishing support, mobile PWA metadata, and several profile/auth recovery fixes',
+    new_0_6_18_6: 'Desktop and runtime setup are safer with Unicode-safe Windows PATH registration, Swarm Agent 0.17.0 runtime publishing support, mobile PWA metadata, and several profile/auth recovery fixes',
     new_0_6_18_7: 'Polish and detail improvements across the app',
     new_0_6_17_1: 'Fixed Codex and Claude Code conversation startup issues',
     new_0_6_16_1: 'Provider settings now preserve custom API modes, cover LongCat correctly, support deleting v12 provider entries, and keep model catalogs in sync with auth and API-key image providers',
@@ -2263,7 +2424,247 @@ export default {
     new_0_6_13_2: 'File handling is safer with literal-dot path validation and malformed skill-import filename rejection',
     new_0_6_13_3: 'Chat is more stable on mobile and in browsers without speech synthesis support',
     new_0_6_13_4: 'Agent failures now preserve the bridge error tail, and Claude Code root-permission launches are more reliable',
-    new_0_6_13_5: 'Desktop updates are more reliable: Windows upgrades close stale Hermes Studio processes, Cloudflare is checked first, and GitHub remains the fallback feed',
+    new_0_6_13_5: 'Desktop updates are more reliable: Windows upgrades close stale Swarm Studio processes, Cloudflare is checked first, and GitHub remains the fallback feed',
     new_0_6_13_6: 'Release automation now keeps Web UI and Docker releases out of GitHub Latest while full desktop releases are manually promoted to Latest',
   },
+
+  matrix: {
+    options: 'Options',
+    threadStartDisabled: 'Cannot start a thread from a message with an existing relation',
+    undecryptable: 'Unable to decrypt message',
+    historyLoadError: 'Server error, unable to load earlier history',
+  },
+
+  cockpit: {
+    modeWorkspace: 'Workspace',
+    editTitlePlaceholder: 'Title',
+    actions: 'Actions',
+    specify: 'Specify',
+    decompose: 'Decompose',
+    moveToTriage: 'Move to triage',
+    moveToReady: 'Move to ready',
+    moveToBlocked: 'Mark blocked',
+    moveToDone: 'Mark done',
+    moveToArchived: 'Archive',
+    unblock: 'Unblock',
+    reclaim: 'Reclaim',
+    reassign: 'Reassign',
+    reassignProfile: 'Reassign profile',
+    recovery: 'Recovery',
+    diagnostics: 'Diagnostics',
+    notifyHomeChannels: 'Notify home channels',
+    subscribe: 'Subscribe',
+    unsubscribe: 'Unsubscribe',
+    comments: 'Comments',
+    addCommentPlaceholder: 'Add a comment…',
+    confirmDone: 'Mark this task as done? This will complete the task in the kanban board.',
+    confirmArchive: 'Archive this task? Archived tasks are hidden from the board but can be restored.',
+    confirmBlocked: 'Mark this task as blocked? Blocked tasks are paused until unblocked.',
+    completionSummaryRequired: 'Completion summary is required before marking a task as done.',
+    evaluation: 'Evaluation',
+    saveDraft: 'Save draft',
+    parentTasks: 'Parent tasks',
+    childTasks: 'Child tasks',
+    parentIdPlaceholder: 'Parent task id',
+    childIdPlaceholder: 'Child task id',
+    pendingLinksHint: 'pending link changes',
+    assignee: 'Assignee',
+    selectAssignee: 'Select assignee…',
+    description: 'Description',
+    descriptionPlaceholder: 'Task description…',
+    add: 'Add',
+    attachments: 'Attachments',
+    noAttachments: 'No attachments',
+    uploadFile: 'Upload file',
+    none: 'None',
+    historySearchPlaceholder: 'Search history…',
+    historyToday: 'Today',
+    historyWeek: 'This Week',
+    historyMonth: 'This Month',
+    historyCategory: 'Category',
+    historyCatEvent: 'Events',
+    historyCatComment: 'Comments',
+    historyActive: 'Active',
+    historyDone: 'Done',
+    notifyTitle: 'Notifications',
+    notifyUnread: 'unread',
+    notifySource: 'Source',
+    notifyAll: 'All',
+    notifyMatrix: 'Matrix',
+    notifyChat: 'Chat',
+    notifyGroup: 'Group',
+    notifyEmpty: 'No unread messages',
+    notifyClickHint: 'Click an item to open the chat',
+    notifyReadAll: 'Mark all as read',
+    attention: 'Attention',
+    backToWork: 'Back to Work',
+    openFull: 'Open Full Page',
+    saySomething: 'Say something...',
+    send: 'Send',
+    noTaskSelected: 'No task selected',
+    collabWith: 'Collab with',
+    addCollab: 'Chat & Collab',
+    addCollabTitle: 'Add Collaborator',
+    collaborationMap: 'Collaboration Map',
+    handleLater: 'Handle Later',
+    taskFiles: 'Task Files',
+    currentTaskWorkspace: 'Current task workspace',
+    filterFiles: 'Filter files...',
+    history: 'History',
+    historyTitle: 'History',
+    historyTime: 'Time',
+    historyAction: 'Action',
+    historyStatus: 'Status',
+    historyArchived: 'Archived',
+    restore: 'Restore',
+    maximize: 'Maximize',
+    modeWork: 'Work',
+    modeTerm: 'Terminal',
+    schedule: 'Schedule',
+    today: 'Today',
+    scheduleEvents: 'Schedule Events',
+    scheduleEmpty: 'No events for this date',
+    scheduleAddTodo: 'Add Todo',
+    scheduleTodoPlaceholder: 'What needs to be done?',
+    templateManager: 'Template Manager',
+    templateName: 'Template name...',
+    saveAsTemplate: 'Save as Template',
+    applyTemplate: 'Apply',
+    deleteTemplate: 'Delete',
+    noTemplates: 'No templates saved',
+    termExit: 'Exit Terminal',
+    timeline: 'Timeline',
+    pending: 'Pending',
+    done: 'Done',
+    olderHistory: '{n} older entries',
+    search: 'Search',
+    priority: 'Priority',
+    yourDecision: 'Your Decision',
+    recommend: 'Recommended',
+    riskTags: 'Risk Tags',
+    agentPrefilled: 'Agent pre-filled',
+    reviewOpinion: 'Review Opinion',
+    submit: 'Submit',
+    newCollabFromArchive: 'New Collab from Template',
+    readOnly: 'Read Only',
+  },
+
+  matrixChat: {
+    actionDelete: 'Delete',
+    actionEdit: 'Edit',
+    actionReply: 'Reply',
+    addReaction: 'Add Reaction',
+    addTopic: 'Add Topic',
+    allThreads: 'All Threads',
+    backToThreads: 'Back to Threads',
+    bold: 'Bold',
+    cancel: 'Cancel',
+    code: 'Code',
+    codeBlock: 'Code Block',
+    comingSoon: 'Coming Soon',
+    confirmDelete: 'Confirm Delete',
+    confirmDeleteAction: 'Are you sure you want to delete this message?',
+    copyLink: 'Copy Link',
+    copyText: 'Copy Text',
+    createRoom: 'Create Room',
+    delete: 'Delete',
+    deleteReasonPlaceholder: 'Reason (optional)',
+    edit: 'Edit',
+    editMessagePlaceholder: 'Edit message...',
+    edited: 'edited',
+    editingMessage: 'Editing message',
+    emoji: 'Emoji',
+    failed: 'Failed',
+    favorite: 'Favorite',
+    forward: 'Forward',
+    forwardMessage: 'Forward Message',
+    forwardingMessage: 'Forwarding message...',
+    historyLoadError: 'Server error, unable to load earlier history',
+    ignoreUser: 'Ignore User',
+    inviteFailed: 'Invite failed',
+    inviteSending: 'Sending invite...',
+    inviteToRoom: 'Invite to Room',
+    inviteUser: 'Invite User',
+    italic: 'Italic',
+    joinRoom: 'Join Room',
+    kickUser: 'Kick User',
+    leaveRoom: 'Leave Room',
+    linkCopied: 'Link copied',
+    loading: 'Loading...',
+    loadingThreads: 'Loading threads...',
+    markAllThreadsRead: 'Mark all threads as read',
+    memberAdmin: 'Admin',
+    memberDefault: 'Member',
+    memberInvited: 'Invited',
+    memberMod: 'Moderator',
+    memberSearch: 'Search members',
+    messageSendFailed: 'Failed to send message',
+    messageSending: 'Sending...',
+    myThreads: 'My Threads',
+    newMessages: 'New messages',
+    noMembersFound: 'No members found',
+    noResults: 'No results',
+    noRoomSelected: 'No room selected',
+    noRooms: 'No rooms',
+    noThreadsDesc: 'No threads in this room yet',
+    noThreadsTitle: 'No Threads',
+    noUsersFound: 'No users found',
+    notAuthenticated: 'Not authenticated',
+    offline: 'Offline',
+    online: 'Online',
+    options: 'Options',
+    paginateLoading: 'Loading more...',
+    people: 'People',
+    publicRoom: 'Public Room',
+    replyInThread: 'Reply in Thread',
+    replyMessagePlaceholder: 'Reply...',
+    roomEncryption: 'Encrypted room',
+    roomIdOrAlias: 'Room ID or alias',
+    roomInfo: 'Room Info',
+    roomMembers: 'Room Members',
+    roomName: 'Room Name',
+    roomPublic: 'Public',
+    save: 'Save',
+    search: 'Search',
+    searchHint: 'Search rooms and people',
+    searchRooms: 'Search Rooms',
+    searchUsers: 'Search Users',
+    searchUsersHint: 'Search by username or display name',
+    send: 'Send',
+    sendMessage: 'Send Message',
+    sent: 'Sent',
+    showAll: 'Show All',
+    showLess: 'Show Less',
+    startChat: 'Start Chat',
+    stateBanned: 'banned',
+    stateChangedName: 'changed display name',
+    stateInvited: 'was invited',
+    stateJoined: 'joined',
+    stateKicked: 'was kicked',
+    stateLeft: 'left',
+    stateLoadingOlder: 'Loading older messages...',
+    stateNoMoreHistory: 'No more history',
+    stateReason: 'Reason',
+    stateRejectedInvite: 'rejected the invitation',
+    stateRoomCreated: 'Room created',
+    stateUnbanned: 'was unbanned',
+    stateWithdrewInvite: 'withdrew invitation',
+    suggestions: 'Suggestions',
+    syncConnecting: 'Connecting...',
+    syncError: 'Connection error',
+    thread: 'Thread',
+    threadStartDisabled: 'Cannot start a thread from a message with an existing relation',
+    threads: 'Threads',
+    threadsMenu: 'Threads',
+    typingMany: '{n} people are typing...',
+    typingOne: '{user} is typing...',
+    typingTwo: '{user1} and {user2} are typing...',
+    unavailable: 'Unavailable',
+    undecryptable: 'Unable to decrypt message',
+    unencrypted: 'Unencrypted',
+    unignoreUser: 'Unignore User',
+    uploadFile: 'Upload File',
+    videoCall: 'Video Call',
+    voiceCall: 'Voice Call',
+  },
 }
diff --git a/packages/client/src/i18n/locales/es.ts b/packages/client/src/i18n/locales/es.ts
index 85c47f18..a3384253 100644
--- a/packages/client/src/i18n/locales/es.ts
+++ b/packages/client/src/i18n/locales/es.ts
@@ -1,7 +1,7 @@
 export default {
   // Login
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Introduce tu nombre de usuario y contrasena para continuar.',
     placeholder: 'Token de acceso',
     submit: 'Iniciar sesion',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: 'No se pudieron cargar las versiones remotas',
     downloadTasks: 'Tareas de descarga',
     downloadStarted: 'Descarga iniciada',
-    activateSuccess: 'Selección de versión guardada. Reinicia Hermes Studio para usarla.',
+    activateSuccess: 'Selección de versión guardada. Reinicia Swarm Studio para usarla.',
     jobStatus: {
       queued: 'En cola',
       running: 'En ejecución',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: 'Por favor ingresa una longitud de contexto válida',
     contextEditSuccess: 'Longitud del contexto actualizada',
     contextEditFailed: 'Error en la actualización',
-    emptyState: 'Inicia una conversacion con Hermes Agent',
+    emptyState: 'Inicia una conversacion con Swarm Agent',
     emptyStateAgent: 'Inicia una conversacion con {agent}',
     viewOlderInHistory: 'Ver mensajes anteriores en el historial',
     scrollToBottom: 'Ir al final',
@@ -751,10 +751,10 @@ jobTriggered: 'Job ejecutado',
     reloadHint: 'Sesiones activas: ejecuta /reload-skills; las nuevas la cargan automáticamente.',
     writeApprovalButton: 'Pendientes {count}',
     writeApprovalTitle: 'Escrituras pendientes de revision',
-    writeApprovalDescription: 'Aprueba o rechaza escrituras de memoria y skills preparadas por Hermes Agent.',
+    writeApprovalDescription: 'Aprueba o rechaza escrituras de memoria y skills preparadas por Swarm Agent.',
     writeApprovalRefresh: 'Actualizar',
     writeApprovalEmpty: 'No hay escrituras pendientes.',
-    writeApprovalUnsupported: 'Esta version de Hermes Agent no admite revision de escrituras. Actualiza Hermes Agent para usar esta funcion.',
+    writeApprovalUnsupported: 'Esta version de Swarm Agent no admite revision de escrituras. Actualiza Swarm Agent para usar esta funcion.',
     writeApprovalMemory: 'Memoria',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Revisar',
@@ -1185,7 +1185,7 @@ jobTriggered: 'Job ejecutado',
       enabled: 'Iniciar gateways automáticamente',
       enabledHint: 'Si está desactivado, el arranque no iniciará automáticamente ningún gateway de perfil.',
       management: 'Gateway unificado',
-      managementHint: 'Requiere Hermes Agent 0.17.0 o posterior. Al activarlo, Web UI escribe multiplex_profiles para que el gateway default sirva todos los perfiles; otros perfiles no pueden configurar entradas de mensajeria separadas como Feishu, Weixin o QQBot. Al desactivarlo elimina esa configuración.',
+      managementHint: 'Requiere Swarm Agent 0.17.0 o posterior. Al activarlo, Web UI escribe multiplex_profiles para que el gateway default sirva todos los perfiles; otros perfiles no pueden configurar entradas de mensajeria separadas como Feishu, Weixin o QQBot. Al desactivarlo elimina esa configuración.',
       managementAuto: 'Automático',
       managementPerProfile: 'Un gateway por perfil',
       managementUnified: 'Gateway default unificado',
@@ -1239,11 +1239,11 @@ jobTriggered: 'Job ejecutado',
       requireAuth: 'Autorizacion de sesion',
       requireAuthHint: 'Requerir autorizacion para operaciones de sesion',
       memoryWriteApproval: 'Revisar escrituras de memoria',
-      memoryWriteApprovalHint: 'Preparar las escrituras de memoria de Hermes Agent para aprobacion antes de guardarlas.',
+      memoryWriteApprovalHint: 'Preparar las escrituras de memoria de Swarm Agent para aprobacion antes de guardarlas.',
       skillsWriteApproval: 'Revisar escrituras de skills',
-      skillsWriteApprovalHint: 'Preparar los cambios de skills de Hermes Agent para aprobacion antes de guardarlos.',
+      skillsWriteApprovalHint: 'Preparar los cambios de skills de Swarm Agent para aprobacion antes de guardarlos.',
       writeApprovalTitle: 'Escrituras pendientes de revision',
-      writeApprovalDescription: 'Aprueba o rechaza escrituras de memoria y skills preparadas por Hermes Agent.',
+      writeApprovalDescription: 'Aprueba o rechaza escrituras de memoria y skills preparadas por Swarm Agent.',
       writeApprovalRefresh: 'Actualizar',
       writeApprovalEmpty: 'No hay escrituras pendientes.',
       writeApprovalMemory: 'Memoria',
@@ -1852,11 +1852,11 @@ jobTriggered: 'Job ejecutado',
     new_0_6_19_4: 'Los ajustes TTS ahora se guardan por profile, y velocidad, tono y pruebas de voz leen la misma fuente de configuracion',
     new_0_6_19_5: 'Los cambios de modelo de Coding Agent ya no afectan sesiones existentes y las custom provider keys se envian correctamente',
     new_0_6_19_6: 'Filtros de skills runtime, orden de cron jobs, limite de altura en detalles de skill, recuperacion del updater de Windows, fallbacks de backup y logos comprimidos completan la version',
-    new_0_6_18_2: 'Hermes Studio MCP se divide en toolsets api, devices y use, con nuevas acciones de sesion, modelos, chat-run y ops',
+    new_0_6_18_2: 'Swarm Studio MCP se divide en toolsets api, devices y use, con nuevas acciones de sesion, modelos, chat-run y ops',
     new_0_6_18_3: 'El historial de chat y grupos es mas fiable con historial de UI separado, manejo seguro de mensajes nulos, respuestas del assistant preservadas tras run failures y replay de reasoning alineado',
     new_0_6_18_4: 'El feedback de MCU es mas claro: las tool calls tienen prioridad sobre speech, el audio ya no marca tareas como terminadas demasiado pronto, el login del dispositivo activo es mas estable y ONLINE/IP largo se desplaza',
     new_0_6_18_5: 'Los flujos de voz y mensajes agregan endpoints HTTP TTS/STT personalizados, transcodificacion de audio, acciones de fork de mensajes, marcadores de fork persistentes y errores de upload mas claros',
-    new_0_6_18_6: 'La configuracion de desktop y runtime es mas segura con registro Unicode-safe del PATH en Windows, soporte para publicar runtime Hermes Agent 0.17.0, metadata PWA movil y varias correcciones de recuperacion de profile/auth',
+    new_0_6_18_6: 'La configuracion de desktop y runtime es mas segura con registro Unicode-safe del PATH en Windows, soporte para publicar runtime Swarm Agent 0.17.0, metadata PWA movil y varias correcciones de recuperacion de profile/auth',
     new_0_6_18_7: 'Optimizaciones y mejoras de detalles en toda la app',
     new_0_6_17_1: 'Se corrigieron los problemas de inicio de conversaciones en Codex y Claude Code',
     new_0_6_16_1: 'Los ajustes de Provider ahora conservan modos API personalizados, cubren LongCat correctamente, permiten eliminar Providers v12 y sincronizan catalogos de modelos con Providers de autenticacion e imagen con API key',
@@ -1883,7 +1883,7 @@ jobTriggered: 'Job ejecutado',
     new_0_6_13_2: 'El manejo de archivos es mas seguro, con validacion de puntos literales en rutas y rechazo de nombres de archivo de importacion de skills mal codificados',
     new_0_6_13_3: 'El chat es mas estable en moviles y en navegadores sin soporte de speech synthesis',
     new_0_6_13_4: 'Los fallos de Agent ahora conservan el final del error del bridge, y los lanzamientos de Claude Code con permiso root son mas fiables',
-    new_0_6_13_5: 'Las actualizaciones de escritorio son mas fiables: las actualizaciones de Windows cierran procesos Hermes Studio residuales, revisan Cloudflare primero y mantienen GitHub como feed de respaldo',
+    new_0_6_13_5: 'Las actualizaciones de escritorio son mas fiables: las actualizaciones de Windows cierran procesos Swarm Studio residuales, revisan Cloudflare primero y mantienen GitHub como feed de respaldo',
     new_0_6_13_6: 'La automatizacion de releases mantiene Web UI y Docker fuera de GitHub Latest, mientras que las releases completas de escritorio se promocionan manualmente a Latest tras una compilacion correcta',
   },
 
diff --git a/packages/client/src/i18n/locales/fr.ts b/packages/client/src/i18n/locales/fr.ts
index 2ff0c2c4..b23dc991 100644
--- a/packages/client/src/i18n/locales/fr.ts
+++ b/packages/client/src/i18n/locales/fr.ts
@@ -1,7 +1,7 @@
 export default {
   // Login
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Entrez votre nom d\'utilisateur et votre mot de passe pour continuer.',
     placeholder: 'Jeton d\'acces',
     submit: 'Connexion',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: 'Impossible de charger les versions distantes',
     downloadTasks: 'Tâches de téléchargement',
     downloadStarted: 'Téléchargement démarré',
-    activateSuccess: 'Sélection de version enregistrée. Redémarrez Hermes Studio pour l’utiliser.',
+    activateSuccess: 'Sélection de version enregistrée. Redémarrez Swarm Studio pour l’utiliser.',
     jobStatus: {
       queued: 'En attente',
       running: 'En cours',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: 'Veuillez entrer une longueur de contexte valide',
     contextEditSuccess: 'Longueur du contexte mise à jour',
     contextEditFailed: 'Échec de la mise à jour',
-    emptyState: 'Demarrer une conversation avec Hermes Agent',
+    emptyState: 'Demarrer une conversation avec Swarm Agent',
     emptyStateAgent: 'Demarrer une conversation avec {agent}',
     viewOlderInHistory: 'Voir les messages precedents dans l’historique',
     scrollToBottom: 'Aller en bas',
@@ -751,10 +751,10 @@ jobTriggered: 'Job declenche',
     reloadHint: 'Sessions actives : exécutez /reload-skills ; les nouvelles le chargent automatiquement.',
     writeApprovalButton: 'En attente {count}',
     writeApprovalTitle: 'Ecritures en attente de validation',
-    writeApprovalDescription: 'Approuvez ou refusez les ecritures de memoire et de skills preparees par Hermes Agent.',
+    writeApprovalDescription: 'Approuvez ou refusez les ecritures de memoire et de skills preparees par Swarm Agent.',
     writeApprovalRefresh: 'Actualiser',
     writeApprovalEmpty: 'Aucune ecriture en attente.',
-    writeApprovalUnsupported: 'Cette version de Hermes Agent ne prend pas en charge la validation des ecritures. Mettez Hermes Agent a jour pour utiliser cette fonction.',
+    writeApprovalUnsupported: 'Cette version de Swarm Agent ne prend pas en charge la validation des ecritures. Mettez Swarm Agent a jour pour utiliser cette fonction.',
     writeApprovalMemory: 'Memoire',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Verifier',
@@ -1185,7 +1185,7 @@ jobTriggered: 'Job declenche',
       enabled: 'Démarrer automatiquement les gateways',
       enabledHint: 'Si désactivé, aucun gateway de profil ne sera démarré automatiquement au lancement.',
       management: 'Gateway unifié',
-      managementHint: 'Nécessite Hermes Agent 0.17.0 ou ultérieur. Si activé, la Web UI écrit multiplex_profiles afin que le gateway default serve tous les profils; les autres profils ne peuvent pas configurer des points d entree de messagerie distincts comme Feishu, Weixin ou QQBot. Désactiver retire cette configuration.',
+      managementHint: 'Nécessite Swarm Agent 0.17.0 ou ultérieur. Si activé, la Web UI écrit multiplex_profiles afin que le gateway default serve tous les profils; les autres profils ne peuvent pas configurer des points d entree de messagerie distincts comme Feishu, Weixin ou QQBot. Désactiver retire cette configuration.',
       managementAuto: 'Automatique',
       managementPerProfile: 'Un gateway par profil',
       managementUnified: 'Gateway default unifié',
@@ -1239,11 +1239,11 @@ jobTriggered: 'Job declenche',
       requireAuth: 'Autorisation de session',
       requireAuthHint: 'Exiger une autorisation pour les operations de session',
       memoryWriteApproval: 'Verifier les ecritures de memoire',
-      memoryWriteApprovalHint: 'Mettre les ecritures de memoire de Hermes Agent en attente de validation avant de les enregistrer.',
+      memoryWriteApprovalHint: 'Mettre les ecritures de memoire de Swarm Agent en attente de validation avant de les enregistrer.',
       skillsWriteApproval: 'Verifier les ecritures de skills',
-      skillsWriteApprovalHint: 'Mettre les modifications de skills de Hermes Agent en attente de validation avant de les enregistrer.',
+      skillsWriteApprovalHint: 'Mettre les modifications de skills de Swarm Agent en attente de validation avant de les enregistrer.',
       writeApprovalTitle: 'Ecritures en attente de validation',
-      writeApprovalDescription: 'Approuvez ou refusez les ecritures de memoire et de skills preparees par Hermes Agent.',
+      writeApprovalDescription: 'Approuvez ou refusez les ecritures de memoire et de skills preparees par Swarm Agent.',
       writeApprovalRefresh: 'Actualiser',
       writeApprovalEmpty: 'Aucune ecriture en attente.',
       writeApprovalMemory: 'Memoire',
@@ -1852,11 +1852,11 @@ jobTriggered: 'Job declenche',
     new_0_6_19_4: 'Les reglages TTS sont maintenant enregistres par profile, et la vitesse, le pitch et les tests vocaux lisent la meme source de configuration',
     new_0_6_19_5: 'Les changements de modele Coding Agent n affectent plus les sessions existantes, et les custom provider keys sont transmises correctement',
     new_0_6_19_6: 'Filtres skills runtime, tri des cron jobs, limite de hauteur des details skill, recuperation updater Windows, fallbacks de backup et logos compresses completent cette mise a jour',
-    new_0_6_18_2: 'Hermes Studio MCP est separe en toolsets api, devices et use, avec de nouvelles actions session, model, chat-run et ops',
+    new_0_6_18_2: 'Swarm Studio MCP est separe en toolsets api, devices et use, avec de nouvelles actions session, model, chat-run et ops',
     new_0_6_18_3: 'Les historiques de chat et de groupe sont plus fiables avec historique UI separe, gestion sure des messages nuls, reponses assistant preservees apres run failures et replay reasoning aligne',
     new_0_6_18_4: 'Le retour MCU est plus clair: les tool calls restent prioritaires sur la voix, l audio ne marque plus les taches terminees trop tot, le login active-device est plus stable et les longs textes ONLINE/IP defilent',
     new_0_6_18_5: 'Les workflows voix et messages ajoutent des endpoints HTTP TTS/STT personnalises, le transcodage audio, les actions fork message, les marqueurs fork persistants et des erreurs d upload plus claires',
-    new_0_6_18_6: 'La configuration desktop et runtime est plus sure avec un enregistrement PATH Windows compatible Unicode, le support de publication runtime Hermes Agent 0.17.0, les metadonnees PWA mobile et plusieurs corrections profile/auth',
+    new_0_6_18_6: 'La configuration desktop et runtime est plus sure avec un enregistrement PATH Windows compatible Unicode, le support de publication runtime Swarm Agent 0.17.0, les metadonnees PWA mobile et plusieurs corrections profile/auth',
     new_0_6_18_7: 'Optimisations et ameliorations de details dans toute l application',
     new_0_6_17_1: 'Correction des problemes de demarrage des conversations Codex et Claude Code',
     new_0_6_16_1: 'Les reglages Provider conservent maintenant les API modes personnalises, couvrent LongCat correctement, permettent de supprimer les Providers v12 et synchronisent les catalogues de modeles avec les Providers auth et image a API key',
@@ -1883,7 +1883,7 @@ jobTriggered: 'Job declenche',
     new_0_6_13_2: 'La gestion des fichiers est plus sure, avec validation des points litteraux dans les chemins et rejet des noms de fichiers d import de skill mal encodes',
     new_0_6_13_3: 'Le chat est plus stable sur mobile et dans les navigateurs sans prise en charge de speech synthesis',
     new_0_6_13_4: 'Les echecs d Agent conservent maintenant la fin d erreur du bridge, et les lancements Claude Code avec permission root sont plus fiables',
-    new_0_6_13_5: 'Les mises a jour desktop sont plus fiables : les mises a niveau Windows ferment les anciens processus Hermes Studio, Cloudflare est verifie en premier et GitHub reste le flux de secours',
+    new_0_6_13_5: 'Les mises a jour desktop sont plus fiables : les mises a niveau Windows ferment les anciens processus Swarm Studio, Cloudflare est verifie en premier et GitHub reste le flux de secours',
     new_0_6_13_6: 'L automatisation des releases garde les releases Web UI et Docker hors de GitHub Latest, tandis que les releases desktop completes sont promues manuellement en Latest apres build reussi',
   },
 
diff --git a/packages/client/src/i18n/locales/ja.ts b/packages/client/src/i18n/locales/ja.ts
index ac2d41d8..966b334d 100644
--- a/packages/client/src/i18n/locales/ja.ts
+++ b/packages/client/src/i18n/locales/ja.ts
@@ -1,7 +1,7 @@
 export default {
   // ログイン
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'ユーザー名とパスワードを入力して続行してください。',
     placeholder: 'アクセストークン',
     submit: 'ログイン',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: 'リモートバージョンの読み込みに失敗しました',
     downloadTasks: 'ダウンロードタスク',
     downloadStarted: 'ダウンロードを開始しました',
-    activateSuccess: 'バージョン選択を保存しました。Hermes Studio を再起動すると反映されます。',
+    activateSuccess: 'バージョン選択を保存しました。Swarm Studio を再起動すると反映されます。',
     jobStatus: {
       queued: '待機中',
       running: '実行中',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: '有効なコンテキスト長を入力してください',
     contextEditSuccess: 'コンテキスト長を更新しました',
     contextEditFailed: '更新に失敗しました',
-    emptyState: 'Hermes Agent と会話を開始しましょう',
+    emptyState: 'Swarm Agent と会話を開始しましょう',
     emptyStateAgent: '{agent} と会話を開始しましょう',
     viewOlderInHistory: '以前のメッセージは履歴で表示',
     scrollToBottom: '一番下へ移動',
@@ -751,10 +751,10 @@ export default {
     reloadHint: 'アクティブなセッションは /reload-skills を実行してください。新規セッションは自動で読み込みます。',
     writeApprovalButton: '保留中 {count}',
     writeApprovalTitle: '書き込み承認待ち',
-    writeApprovalDescription: 'Hermes Agent が保留したメモリとスキルの書き込みを承認または拒否します。',
+    writeApprovalDescription: 'Swarm Agent が保留したメモリとスキルの書き込みを承認または拒否します。',
     writeApprovalRefresh: '更新',
     writeApprovalEmpty: '承認待ちの書き込みはありません。',
-    writeApprovalUnsupported: 'この Hermes Agent バージョンは書き込み承認に対応していません。この機能を使うには Hermes Agent を更新してください。',
+    writeApprovalUnsupported: 'この Swarm Agent バージョンは書き込み承認に対応していません。この機能を使うには Swarm Agent を更新してください。',
     writeApprovalMemory: 'メモリ',
     writeApprovalSkills: 'スキル',
     writeApprovalViewDiff: '確認',
@@ -1185,7 +1185,7 @@ export default {
       enabled: 'Gateway を自動起動',
       enabledHint: '無効にすると、起動時に profile gateway を自動起動しません。',
       management: '統一 gateway',
-      managementHint: 'Hermes Agent 0.17.0 以降が必要です。有効にすると Web UI が multiplex_profiles を書き込み、default gateway がすべての profiles を処理します。他の profile では Feishu、Weixin、QQBot などのメッセージング入口を個別設定できません。無効にするとその設定を削除します。',
+      managementHint: 'Swarm Agent 0.17.0 以降が必要です。有効にすると Web UI が multiplex_profiles を書き込み、default gateway がすべての profiles を処理します。他の profile では Feishu、Weixin、QQBot などのメッセージング入口を個別設定できません。無効にするとその設定を削除します。',
       managementAuto: '自動',
       managementPerProfile: 'Profile ごとに 1 gateway',
       managementUnified: '統一 default gateway',
@@ -1239,11 +1239,11 @@ export default {
       requireAuth: 'セッション認可',
       requireAuthHint: 'セッション操作に認可を要求します',
       memoryWriteApproval: 'メモリ書き込みを確認',
-      memoryWriteApprovalHint: 'Hermes Agent のメモリ書き込みを保存前に承認待ちにします。',
+      memoryWriteApprovalHint: 'Swarm Agent のメモリ書き込みを保存前に承認待ちにします。',
       skillsWriteApproval: 'スキル書き込みを確認',
-      skillsWriteApprovalHint: 'Hermes Agent のスキル変更を保存前に承認待ちにします。',
+      skillsWriteApprovalHint: 'Swarm Agent のスキル変更を保存前に承認待ちにします。',
       writeApprovalTitle: '書き込み承認待ち',
-      writeApprovalDescription: 'Hermes Agent が保留したメモリとスキルの書き込みを承認または拒否します。',
+      writeApprovalDescription: 'Swarm Agent が保留したメモリとスキルの書き込みを承認または拒否します。',
       writeApprovalRefresh: '更新',
       writeApprovalEmpty: '承認待ちの書き込みはありません。',
       writeApprovalMemory: 'メモリ',
@@ -1851,11 +1851,11 @@ export default {
     new_0_6_19_4: 'TTS settings は profile ごとに保存され、speed、pitch、voice test は同じ設定ソースを参照します',
     new_0_6_19_5: 'Coding Agent の model switch は既存 session に影響せず、custom provider key も正しく渡されます',
     new_0_6_19_6: 'Skills runtime filter、cron job sort、skill detail height limit、Windows updater recovery、backup fallback、logo asset compression も含まれます',
-    new_0_6_18_2: 'Hermes Studio MCP を api、devices、use の toolset に分割し、session、model、chat-run、ops 操作を追加しました',
+    new_0_6_18_2: 'Swarm Studio MCP を api、devices、use の toolset に分割し、session、model、chat-run、ops 操作を追加しました',
     new_0_6_18_3: 'チャットとグループチャット履歴は、UI 履歴の分離、安全な null message 処理、run failure 後の assistant reply 保持、reasoning replay の整合でより信頼できます',
     new_0_6_18_4: 'MCU デバイス表示が明確になり、tool call が speech より優先表示され、audio が早すぎる DONE を出さず、active device login が安定し、長い ONLINE/IP テキストはスクロールします',
     new_0_6_18_5: '音声とメッセージの workflow にカスタム HTTP TTS/STT endpoint、audio transcoding、fork message action、永続 fork marker、より明確な upload error を追加しました',
-    new_0_6_18_6: 'Desktop と runtime setup は、Unicode-safe な Windows PATH 登録、Hermes Agent 0.17.0 runtime publish 対応、mobile PWA metadata、profile/auth recovery 修正で安全になりました',
+    new_0_6_18_6: 'Desktop と runtime setup は、Unicode-safe な Windows PATH 登録、Swarm Agent 0.17.0 runtime publish 対応、mobile PWA metadata、profile/auth recovery 修正で安全になりました',
     new_0_6_18_7: '各所の細部を改善しました',
     new_0_6_17_1: 'Codex と Claude Code の会話起動問題を修正しました',
     new_0_6_16_1: 'Provider 設定でカスタム API mode を保持し、LongCat プリセットを正しく扱い、v12 Provider の削除と auth/API-key 画像 Provider を含むモデルカタログ同期に対応しました',
@@ -1882,7 +1882,7 @@ export default {
     new_0_6_13_2: 'ファイル処理をより安全にし、リテラルのドットを含むパス検証と、不正なエンコードのスキルインポートファイル名の拒否に対応しました',
     new_0_6_13_3: 'モバイル環境と speech synthesis 非対応ブラウザでのチャットがより安定しました',
     new_0_6_13_4: 'Agent 失敗時に bridge のエラー末尾を保持し、Claude Code の root 権限起動をより安定させました',
-    new_0_6_13_5: 'デスクトップ更新の信頼性を改善しました。Windows アップグレード時に残留 Hermes Studio プロセスを閉じ、Cloudflare を先に確認し、GitHub をフォールバック更新元として保持します',
+    new_0_6_13_5: 'デスクトップ更新の信頼性を改善しました。Windows アップグレード時に残留 Swarm Studio プロセスを閉じ、Cloudflare を先に確認し、GitHub をフォールバック更新元として保持します',
     new_0_6_13_6: 'リリース自動化を調整し、Web UI と Docker の release は GitHub Latest にせず、完全なデスクトップ release だけを手動ビルド成功後に Latest へ昇格します',
   },
 
diff --git a/packages/client/src/i18n/locales/ko.ts b/packages/client/src/i18n/locales/ko.ts
index 2d673ecf..72c8d506 100644
--- a/packages/client/src/i18n/locales/ko.ts
+++ b/packages/client/src/i18n/locales/ko.ts
@@ -1,7 +1,7 @@
 export default {
   // 로그인
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: '계속하려면 사용자 이름과 비밀번호를 입력하세요.',
     placeholder: '액세스 토큰',
     submit: '로그인',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: '원격 버전을 불러오지 못했습니다',
     downloadTasks: '다운로드 작업',
     downloadStarted: '다운로드를 시작했습니다',
-    activateSuccess: '버전 선택이 저장되었습니다. Hermes Studio를 다시 시작하면 적용됩니다.',
+    activateSuccess: '버전 선택이 저장되었습니다. Swarm Studio를 다시 시작하면 적용됩니다.',
     jobStatus: {
       queued: '대기 중',
       running: '실행 중',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: '유효한 컨텍스트 길이를 입력하세요',
     contextEditSuccess: '컨텍스트 길이가 업데이트되었습니다',
     contextEditFailed: '업데이트 실패',
-    emptyState: 'Hermes Agent와 대화를 시작하세요',
+    emptyState: 'Swarm Agent와 대화를 시작하세요',
     emptyStateAgent: '{agent}와 대화를 시작하세요',
     viewOlderInHistory: '이전 메시지는 기록에서 보기',
     scrollToBottom: '맨 아래로 이동',
@@ -751,10 +751,10 @@ export default {
     reloadHint: '활성 세션에서는 /reload-skills 실행. 새 세션은 자동 로드됩니다.',
     writeApprovalButton: '대기 {count}',
     writeApprovalTitle: '쓰기 승인 대기',
-    writeApprovalDescription: 'Hermes Agent가 보류한 메모리와 스킬 쓰기를 승인하거나 거부합니다.',
+    writeApprovalDescription: 'Swarm Agent가 보류한 메모리와 스킬 쓰기를 승인하거나 거부합니다.',
     writeApprovalRefresh: '새로고침',
     writeApprovalEmpty: '승인 대기 중인 쓰기가 없습니다.',
-    writeApprovalUnsupported: '이 Hermes Agent 버전은 쓰기 승인을 지원하지 않습니다. 이 기능을 사용하려면 Hermes Agent를 업그레이드하세요.',
+    writeApprovalUnsupported: '이 Swarm Agent 버전은 쓰기 승인을 지원하지 않습니다. 이 기능을 사용하려면 Swarm Agent를 업그레이드하세요.',
     writeApprovalMemory: '메모리',
     writeApprovalSkills: '스킬',
     writeApprovalViewDiff: '검토',
@@ -1185,7 +1185,7 @@ export default {
       enabled: 'Gateway 자동 시작',
       enabledHint: '끄면 시작 시 어떤 profile gateway도 자동으로 실행하지 않습니다.',
       management: '통합 gateway',
-      managementHint: 'Hermes Agent 0.17.0 이상이 필요합니다. 켜면 Web UI가 multiplex_profiles를 기록해 default gateway가 모든 profiles를 처리합니다. 다른 profile은 Feishu, Weixin, QQBot 같은 메시징 진입점을 별도로 설정할 수 없습니다. 끄면 해당 설정을 제거합니다.',
+      managementHint: 'Swarm Agent 0.17.0 이상이 필요합니다. 켜면 Web UI가 multiplex_profiles를 기록해 default gateway가 모든 profiles를 처리합니다. 다른 profile은 Feishu, Weixin, QQBot 같은 메시징 진입점을 별도로 설정할 수 없습니다. 끄면 해당 설정을 제거합니다.',
       managementAuto: '자동',
       managementPerProfile: 'Profile별 gateway 하나',
       managementUnified: '통합 default gateway',
@@ -1239,11 +1239,11 @@ export default {
       requireAuth: '세션 승인',
       requireAuthHint: '세션 작업에 승인을 요구합니다',
       memoryWriteApproval: '메모리 쓰기 검토',
-      memoryWriteApprovalHint: 'Hermes Agent의 메모리 쓰기를 저장하기 전에 승인 대기 상태로 둡니다.',
+      memoryWriteApprovalHint: 'Swarm Agent의 메모리 쓰기를 저장하기 전에 승인 대기 상태로 둡니다.',
       skillsWriteApproval: '스킬 쓰기 검토',
-      skillsWriteApprovalHint: 'Hermes Agent의 스킬 변경을 저장하기 전에 승인 대기 상태로 둡니다.',
+      skillsWriteApprovalHint: 'Swarm Agent의 스킬 변경을 저장하기 전에 승인 대기 상태로 둡니다.',
       writeApprovalTitle: '쓰기 승인 대기',
-      writeApprovalDescription: 'Hermes Agent가 보류한 메모리와 스킬 쓰기를 승인하거나 거부합니다.',
+      writeApprovalDescription: 'Swarm Agent가 보류한 메모리와 스킬 쓰기를 승인하거나 거부합니다.',
       writeApprovalRefresh: '새로고침',
       writeApprovalEmpty: '승인 대기 중인 쓰기가 없습니다.',
       writeApprovalMemory: '메모리',
@@ -1851,11 +1851,11 @@ export default {
     new_0_6_19_4: 'TTS 설정은 이제 profile별로 저장되며 speed, pitch, voice test가 같은 설정 소스를 읽습니다',
     new_0_6_19_5: 'Coding Agent의 model switch는 기존 session에 영향을 주지 않으며 custom provider key가 올바르게 전달됩니다',
     new_0_6_19_6: 'Skills runtime filter, cron job sort, skill detail height limit, Windows updater recovery, backup fallback, logo asset compression도 포함됩니다',
-    new_0_6_18_2: 'Hermes Studio MCP가 api, devices, use toolset으로 나뉘고 session, model, chat-run, ops 작업이 추가되었습니다',
+    new_0_6_18_2: 'Swarm Studio MCP가 api, devices, use toolset으로 나뉘고 session, model, chat-run, ops 작업이 추가되었습니다',
     new_0_6_18_3: '채팅과 그룹 채팅 기록은 UI 기록 분리, 안전한 null message 처리, run failure 후 assistant reply 보존, 정렬된 reasoning replay로 더 안정적입니다',
     new_0_6_18_4: 'MCU 디바이스 피드백이 더 명확해져 tool call이 speech보다 우선 표시되고, audio가 작업을 너무 일찍 DONE으로 표시하지 않으며, active device login이 안정화되고 긴 ONLINE/IP 텍스트가 스크롤됩니다',
     new_0_6_18_5: '음성 및 메시지 workflow에 custom HTTP TTS/STT endpoint, audio transcoding, fork message action, 지속되는 fork marker, 더 명확한 upload error가 추가되었습니다',
-    new_0_6_18_6: 'Desktop 및 runtime setup은 Unicode-safe Windows PATH 등록, Hermes Agent 0.17.0 runtime publish 지원, mobile PWA metadata, 여러 profile/auth recovery 수정으로 더 안전해졌습니다',
+    new_0_6_18_6: 'Desktop 및 runtime setup은 Unicode-safe Windows PATH 등록, Swarm Agent 0.17.0 runtime publish 지원, mobile PWA metadata, 여러 profile/auth recovery 수정으로 더 안전해졌습니다',
     new_0_6_18_7: '여러 세부 사항을 개선했습니다',
     new_0_6_17_1: 'Codex와 Claude Code 대화 시작 문제를 수정했습니다',
     new_0_6_16_1: 'Provider 설정이 사용자 API mode를 보존하고 LongCat preset을 올바르게 처리하며 v12 Provider 삭제와 auth/API-key 이미지 Provider가 포함된 모델 카탈로그 동기화를 지원합니다',
@@ -1882,7 +1882,7 @@ export default {
     new_0_6_13_2: '파일 처리가 더 안전해져 리터럴 점이 포함된 경로 검증과 잘못 인코딩된 skill import 파일명 거부를 지원합니다',
     new_0_6_13_3: '모바일과 speech synthesis를 지원하지 않는 브라우저에서 채팅이 더 안정적입니다',
     new_0_6_13_4: 'Agent 실패 시 bridge 오류 꼬리 로그를 보존하고 Claude Code root 권한 실행이 더 안정적입니다',
-    new_0_6_13_5: '데스크톱 업데이트 안정성을 개선했습니다. Windows 업그레이드는 남아 있는 Hermes Studio 프로세스를 닫고, Cloudflare를 먼저 확인하며, GitHub 업데이트 피드를 fallback으로 유지합니다',
+    new_0_6_13_5: '데스크톱 업데이트 안정성을 개선했습니다. Windows 업그레이드는 남아 있는 Swarm Studio 프로세스를 닫고, Cloudflare를 먼저 확인하며, GitHub 업데이트 피드를 fallback으로 유지합니다',
     new_0_6_13_6: '릴리스 자동화를 조정해 Web UI와 Docker release는 GitHub Latest로 표시하지 않고, 전체 데스크톱 release만 수동 빌드 성공 후 Latest로 승격합니다',
   },
 
diff --git a/packages/client/src/i18n/locales/pt.ts b/packages/client/src/i18n/locales/pt.ts
index ddc34fb7..825b11b8 100644
--- a/packages/client/src/i18n/locales/pt.ts
+++ b/packages/client/src/i18n/locales/pt.ts
@@ -1,7 +1,7 @@
 export default {
   // Login
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Insira seu nome de usuario e senha para continuar.',
     placeholder: 'Token de acesso',
     submit: 'Entrar',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: 'Falha ao carregar versões remotas',
     downloadTasks: 'Tarefas de download',
     downloadStarted: 'Download iniciado',
-    activateSuccess: 'Seleção de versão salva. Reinicie o Hermes Studio para usá-la.',
+    activateSuccess: 'Seleção de versão salva. Reinicie o Swarm Studio para usá-la.',
     jobStatus: {
       queued: 'Na fila',
       running: 'Em execução',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: 'Por favor, insira um tamanho de contexto válido',
     contextEditSuccess: 'Tamanho do contexto atualizado',
     contextEditFailed: 'Falha na atualização',
-    emptyState: 'Inicie uma conversa com o Hermes Agent',
+    emptyState: 'Inicie uma conversa com o Swarm Agent',
     emptyStateAgent: 'Inicie uma conversa com {agent}',
     viewOlderInHistory: 'Ver mensagens anteriores no historico',
     scrollToBottom: 'Rolar ate o fim',
@@ -751,10 +751,10 @@ jobTriggered: 'Job acionado',
     reloadHint: 'Sessões ativas: execute /reload-skills; novas sessões carregam automaticamente.',
     writeApprovalButton: 'Pendentes {count}',
     writeApprovalTitle: 'Escritas pendentes de revisao',
-    writeApprovalDescription: 'Aprove ou rejeite escritas de memoria e skills preparadas pelo Hermes Agent.',
+    writeApprovalDescription: 'Aprove ou rejeite escritas de memoria e skills preparadas pelo Swarm Agent.',
     writeApprovalRefresh: 'Atualizar',
     writeApprovalEmpty: 'Nenhuma escrita pendente.',
-    writeApprovalUnsupported: 'Esta versao do Hermes Agent nao suporta revisao de escritas. Atualize o Hermes Agent para usar este recurso.',
+    writeApprovalUnsupported: 'Esta versao do Swarm Agent nao suporta revisao de escritas. Atualize o Swarm Agent para usar este recurso.',
     writeApprovalMemory: 'Memoria',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Revisar',
@@ -1185,7 +1185,7 @@ jobTriggered: 'Job acionado',
       enabled: 'Iniciar gateways automaticamente',
       enabledHint: 'Quando desativado, nenhum gateway de perfil será iniciado automaticamente.',
       management: 'Gateway unificado',
-      managementHint: 'Requer Hermes Agent 0.17.0 ou superior. Quando ativado, a Web UI grava multiplex_profiles para o gateway default servir todos os perfis; outros perfis nao podem configurar entradas de mensagens separadas como Feishu, Weixin ou QQBot. Ao desativar, remove essa configuração.',
+      managementHint: 'Requer Swarm Agent 0.17.0 ou superior. Quando ativado, a Web UI grava multiplex_profiles para o gateway default servir todos os perfis; outros perfis nao podem configurar entradas de mensagens separadas como Feishu, Weixin ou QQBot. Ao desativar, remove essa configuração.',
       managementAuto: 'Automático',
       managementPerProfile: 'Um gateway por perfil',
       managementUnified: 'Gateway default unificado',
@@ -1239,11 +1239,11 @@ jobTriggered: 'Job acionado',
       requireAuth: 'Autorizacao da sessao',
       requireAuthHint: 'Exigir autorizacao para operacoes da sessao',
       memoryWriteApproval: 'Revisar escritas de memoria',
-      memoryWriteApprovalHint: 'Colocar escritas de memoria do Hermes Agent em aprovacao antes de salvar.',
+      memoryWriteApprovalHint: 'Colocar escritas de memoria do Swarm Agent em aprovacao antes de salvar.',
       skillsWriteApproval: 'Revisar escritas de skills',
-      skillsWriteApprovalHint: 'Colocar alteracoes de skills do Hermes Agent em aprovacao antes de salvar.',
+      skillsWriteApprovalHint: 'Colocar alteracoes de skills do Swarm Agent em aprovacao antes de salvar.',
       writeApprovalTitle: 'Escritas pendentes de revisao',
-      writeApprovalDescription: 'Aprove ou rejeite escritas de memoria e skills preparadas pelo Hermes Agent.',
+      writeApprovalDescription: 'Aprove ou rejeite escritas de memoria e skills preparadas pelo Swarm Agent.',
       writeApprovalRefresh: 'Atualizar',
       writeApprovalEmpty: 'Nenhuma escrita pendente.',
       writeApprovalMemory: 'Memoria',
@@ -1852,11 +1852,11 @@ jobTriggered: 'Job acionado',
     new_0_6_19_4: 'As configuracoes TTS agora sao salvas por profile, e velocidade, tom e testes de voz leem a mesma fonte de configuracao',
     new_0_6_19_5: 'Trocas de modelo no Coding Agent nao afetam mais sessoes existentes e custom provider keys sao encaminhadas corretamente',
     new_0_6_19_6: 'Filtros de skills runtime, ordenacao de cron jobs, limite de altura de detalhes de skill, recuperacao do updater do Windows, fallbacks de backup e logos comprimidos completam a atualizacao',
-    new_0_6_18_2: 'Hermes Studio MCP foi dividido em toolsets api, devices e use, com novas acoes de session, model, chat-run e ops',
+    new_0_6_18_2: 'Swarm Studio MCP foi dividido em toolsets api, devices e use, com novas acoes de session, model, chat-run e ops',
     new_0_6_18_3: 'O historico de chat e grupos e mais confiavel com historico de UI separado, tratamento seguro de mensagens nulas, respostas do assistant preservadas apos run failures e replay de reasoning alinhado',
     new_0_6_18_4: 'O feedback do MCU ficou mais claro: tool calls ficam acima de speech, audio nao marca tarefas como concluidas cedo demais, active-device login esta mais estavel e textos ONLINE/IP longos rolam',
     new_0_6_18_5: 'Workflows de voz e mensagens ganharam endpoints HTTP TTS/STT customizados, transcodificacao de audio, acoes de fork message, marcadores fork persistentes e erros de upload mais claros',
-    new_0_6_18_6: 'Desktop e runtime setup ficaram mais seguros com registro Unicode-safe do PATH no Windows, suporte a publicar runtime Hermes Agent 0.17.0, metadata PWA movel e varias correcoes de profile/auth recovery',
+    new_0_6_18_6: 'Desktop e runtime setup ficaram mais seguros com registro Unicode-safe do PATH no Windows, suporte a publicar runtime Swarm Agent 0.17.0, metadata PWA movel e varias correcoes de profile/auth recovery',
     new_0_6_18_7: 'Otimizacoes e melhorias de detalhes em todo o app',
     new_0_6_17_1: 'Corrigidos problemas ao iniciar conversas no Codex e no Claude Code',
     new_0_6_16_1: 'As configuracoes de Provider agora preservam API modes personalizados, cobrem LongCat corretamente, permitem excluir Providers v12 e sincronizam catalogos de modelos com Providers de auth e imagem por API key',
@@ -1883,7 +1883,7 @@ jobTriggered: 'Job acionado',
     new_0_6_13_2: 'O tratamento de arquivos esta mais seguro, com validacao de pontos literais em caminhos e rejeicao de nomes de arquivo de importacao de skills mal codificados',
     new_0_6_13_3: 'O chat esta mais estavel em dispositivos moveis e em navegadores sem suporte a speech synthesis',
     new_0_6_13_4: 'Falhas do Agent agora preservam o final do erro do bridge, e inicializacoes do Claude Code com permissao root estao mais confiaveis',
-    new_0_6_13_5: 'As atualizacoes desktop estao mais confiaveis: upgrades no Windows fecham processos Hermes Studio residuais, verificam Cloudflare primeiro e mantem GitHub como feed de fallback',
+    new_0_6_13_5: 'As atualizacoes desktop estao mais confiaveis: upgrades no Windows fecham processos Swarm Studio residuais, verificam Cloudflare primeiro e mantem GitHub como feed de fallback',
     new_0_6_13_6: 'A automacao de releases mantem Web UI e Docker fora do GitHub Latest, enquanto releases desktop completas sao promovidas manualmente para Latest apos build bem-sucedido',
   },
 
diff --git a/packages/client/src/i18n/locales/ru.ts b/packages/client/src/i18n/locales/ru.ts
index e77aed82..2d4db015 100644
--- a/packages/client/src/i18n/locales/ru.ts
+++ b/packages/client/src/i18n/locales/ru.ts
@@ -1,7 +1,7 @@
 export default {
 
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: 'Введите имя пользователя и пароль для продолжения.',
     placeholder: 'Токен доступа',
     submit: 'Войти',
@@ -183,7 +183,7 @@ export default {
     remoteLoadFailed: 'Не удалось загрузить удаленные версии',
     downloadTasks: 'Задачи загрузки',
     downloadStarted: 'Загрузка начата',
-    activateSuccess: 'Выбор версии сохранен. Перезапустите Hermes Studio, чтобы применить его.',
+    activateSuccess: 'Выбор версии сохранен. Перезапустите Swarm Studio, чтобы применить его.',
     jobStatus: {
       queued: 'В очереди',
       running: 'Выполняется',
@@ -340,7 +340,7 @@ export default {
     contextEditInvalid: 'Введите корректный размер контекста',
     contextEditSuccess: 'Размер контекста обновлён',
     contextEditFailed: 'Ошибка обновления',
-    emptyState: 'Начните диалог с Hermes Agent',
+    emptyState: 'Начните диалог с Swarm Agent',
     viewOlderInHistory: 'Посмотреть более ранние сообщения в истории',
     scrollToBottom: 'Прокрутить вниз',
     cliEmptyState: 'Начните CLI-диалог',
@@ -866,10 +866,10 @@ export default {
     },
     writeApprovalButton: 'Ожидает {count}',
     writeApprovalTitle: 'Ожидающие подтверждения записи',
-    writeApprovalDescription: 'Подтвердите или отклоните записи памяти и skills, подготовленные Hermes Agent.',
+    writeApprovalDescription: 'Подтвердите или отклоните записи памяти и skills, подготовленные Swarm Agent.',
     writeApprovalRefresh: 'Обновить',
     writeApprovalEmpty: 'Нет ожидающих записей.',
-    writeApprovalUnsupported: 'Эта версия Hermes Agent не поддерживает подтверждение записей. Обновите Hermes Agent, чтобы использовать эту функцию.',
+    writeApprovalUnsupported: 'Эта версия Swarm Agent не поддерживает подтверждение записей. Обновите Swarm Agent, чтобы использовать эту функцию.',
     writeApprovalMemory: 'Память',
     writeApprovalSkills: 'Skills',
     writeApprovalViewDiff: 'Проверить',
@@ -1303,11 +1303,11 @@ export default {
     },
     gatewayAutoStart: {
       title: 'Автозапуск gateway',
-      description: 'Управляет тем, какие локальные gateway профилей Hermes Web UI запускает при старте. Настройка хранится в конфигурации Web UI, а не в profile config.yaml.',
+      description: 'Управляет тем, какие локальные gateway профилей Swarm Web UI запускает при старте. Настройка хранится в конфигурации Web UI, а не в profile config.yaml.',
       enabled: 'Автоматически запускать gateway',
       enabledHint: 'Если выключено, при старте не будет автоматически запущен ни один gateway профиля.',
       management: 'Единый gateway',
-      managementHint: 'Требуется Hermes Agent 0.17.0 или новее. Если включено, Web UI записывает multiplex_profiles, чтобы default gateway обслуживал все профили; другие профили не могут отдельно настраивать точки входа сообщений, такие как Feishu, Weixin или QQBot. При выключении эта настройка удаляется.',
+      managementHint: 'Требуется Swarm Agent 0.17.0 или новее. Если включено, Web UI записывает multiplex_profiles, чтобы default gateway обслуживал все профили; другие профили не могут отдельно настраивать точки входа сообщений, такие как Feishu, Weixin или QQBot. При выключении эта настройка удаляется.',
       managementAuto: 'Автоматически',
       managementPerProfile: 'Один gateway на профиль',
       managementUnified: 'Единый default gateway',
@@ -1361,11 +1361,11 @@ export default {
       requireAuth: 'Авторизация сеанса',
       requireAuthHint: 'Требовать авторизацию для операций сеанса',
       memoryWriteApproval: 'Проверять записи памяти',
-      memoryWriteApprovalHint: 'Отправлять записи памяти Hermes Agent на подтверждение перед сохранением.',
+      memoryWriteApprovalHint: 'Отправлять записи памяти Swarm Agent на подтверждение перед сохранением.',
       skillsWriteApproval: 'Проверять записи skills',
-      skillsWriteApprovalHint: 'Отправлять изменения skills от Hermes Agent на подтверждение перед сохранением.',
+      skillsWriteApprovalHint: 'Отправлять изменения skills от Swarm Agent на подтверждение перед сохранением.',
       writeApprovalTitle: 'Ожидающие подтверждения записи',
-      writeApprovalDescription: 'Подтвердите или отклоните записи памяти и skills, подготовленные Hermes Agent.',
+      writeApprovalDescription: 'Подтвердите или отклоните записи памяти и skills, подготовленные Swarm Agent.',
       writeApprovalRefresh: 'Обновить',
       writeApprovalEmpty: 'Нет ожидающих записей.',
       writeApprovalMemory: 'Память',
@@ -1981,11 +1981,11 @@ export default {
     new_0_6_19_4: 'TTS настройки теперь сохраняются по profile, а speed, pitch и voice tests читают один источник конфигурации',
     new_0_6_19_5: 'Переключение модели в Coding Agent больше не влияет на существующие sessions, а custom provider keys передаются корректно',
     new_0_6_19_6: 'Skills runtime filters, сортировка cron jobs, ограничение высоты skill details, Windows updater recovery, backup fallbacks и сжатые logo assets завершают обновление',
-    new_0_6_18_2: 'Hermes Studio MCP разделен на toolsets api, devices и use с новыми действиями для session, model, chat-run и ops',
+    new_0_6_18_2: 'Swarm Studio MCP разделен на toolsets api, devices и use с новыми действиями для session, model, chat-run и ops',
     new_0_6_18_3: 'История чатов и групповых чатов надежнее благодаря отдельной UI history, безопасной обработке null messages, сохранению assistant replies после run failures и согласованному reasoning replay',
     new_0_6_18_4: 'Обратная связь MCU стала понятнее: tool calls показываются поверх speech, audio больше не помечает задачи DONE слишком рано, active-device login стабильнее, а длинный ONLINE/IP текст прокручивается',
     new_0_6_18_5: 'Voice и message workflows получили custom HTTP TTS/STT endpoints, audio transcoding, fork message actions, persistent fork markers и более понятные upload errors',
-    new_0_6_18_6: 'Desktop и runtime setup стали безопаснее: Unicode-safe регистрация Windows PATH, поддержка публикации Hermes Agent 0.17.0 runtime, mobile PWA metadata и несколько исправлений profile/auth recovery',
+    new_0_6_18_6: 'Desktop и runtime setup стали безопаснее: Unicode-safe регистрация Windows PATH, поддержка публикации Swarm Agent 0.17.0 runtime, mobile PWA metadata и несколько исправлений profile/auth recovery',
     new_0_6_18_7: 'Улучшения и доработка деталей во всем приложении',
     new_0_6_17_1: 'Исправлены проблемы запуска диалогов Codex и Claude Code',
     new_0_6_16_1: 'Настройки Provider теперь сохраняют пользовательские API mode, корректно покрывают LongCat, поддерживают удаление Provider v12 и синхронизируют каталоги моделей с auth и API-key image Provider',
@@ -2012,7 +2012,7 @@ export default {
     new_0_6_13_2: 'Работа с файлами стала безопаснее: проверка путей допускает буквальные точки, а некорректно закодированные имена файлов импорта skills отклоняются',
     new_0_6_13_3: 'Чат стал стабильнее на мобильных устройствах и в браузерах без поддержки speech synthesis',
     new_0_6_13_4: 'При сбоях Agent теперь сохраняется хвост ошибки bridge, а запуск Claude Code с root-разрешениями стал надежнее',
-    new_0_6_13_5: 'Обновления desktop стали надежнее: обновления Windows закрывают оставшиеся процессы Hermes Studio, сначала проверяют Cloudflare и сохраняют GitHub как резервный feed',
+    new_0_6_13_5: 'Обновления desktop стали надежнее: обновления Windows закрывают оставшиеся процессы Swarm Studio, сначала проверяют Cloudflare и сохраняют GitHub как резервный feed',
     new_0_6_13_6: 'Автоматизация релизов больше не помечает Web UI и Docker releases как GitHub Latest, а полные desktop releases вручную продвигаются в Latest после успешной сборки',
   },
 
diff --git a/packages/client/src/i18n/locales/zh-TW.ts b/packages/client/src/i18n/locales/zh-TW.ts
index 5a7951e7..e1e85eb6 100644
--- a/packages/client/src/i18n/locales/zh-TW.ts
+++ b/packages/client/src/i18n/locales/zh-TW.ts
@@ -1,7 +1,7 @@
 export default {
   // 登入
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: '輸入使用者名稱和密碼以繼續。',
     placeholder: '存取權杖',
     submit: '登入',
@@ -261,7 +261,7 @@ export default {
     remoteLoadFailed: '遠端版本載入失敗',
     downloadTasks: '下載任務',
     downloadStarted: '已開始下載',
-    activateSuccess: '版本選擇已儲存，重啟 Hermes Studio 後生效。',
+    activateSuccess: '版本選擇已儲存，重啟 Swarm Studio 後生效。',
     jobStatus: {
       queued: '排隊中',
       running: '下載中',
@@ -413,7 +413,7 @@ export default {
     contextEditInvalid: '請輸入有效的上下文長度',
     contextEditSuccess: '上下文長度已更新',
     contextEditFailed: '更新失敗',
-    emptyState: '開始與 Hermes Agent 對話',
+    emptyState: '開始與 Swarm Agent 對話',
     emptyStateAgent: '開始與 {agent} 對話',
     viewOlderInHistory: '更早的訊息請到歷史記錄查看',
     scrollToBottom: '捲動到底部',
@@ -1002,10 +1002,10 @@ export default {
     reloadHint: '活躍會話請執行 /reload-skills；新會話會自動載入。',
     writeApprovalButton: '待審核 {count}',
     writeApprovalTitle: '待審核寫入',
-    writeApprovalDescription: '批准或拒絕 Hermes Agent 暫存的記憶和技能寫入。',
+    writeApprovalDescription: '批准或拒絕 Swarm Agent 暫存的記憶和技能寫入。',
     writeApprovalRefresh: '重新整理',
     writeApprovalEmpty: '暫無待審核寫入。',
-    writeApprovalUnsupported: '目前 Hermes Agent 版本不支援寫入審核。請升級 Hermes Agent 後再使用。',
+    writeApprovalUnsupported: '目前 Swarm Agent 版本不支援寫入審核。請升級 Swarm Agent 後再使用。',
     writeApprovalMemory: '記憶',
     writeApprovalSkills: '技能',
     writeApprovalViewDiff: '查看',
@@ -1444,7 +1444,7 @@ export default {
       enabled: '自動啟動 gateway',
       enabledHint: '關閉後，啟動期不會自動拉起任何 profile gateway。',
       management: '統一 gateway',
-      managementHint: '需要 Hermes Agent 0.17.0 及以上版本。開啟後會寫入 multiplex_profiles，讓 default gateway 服務所有 profiles；其他 profile 不能單獨設定飛書、微信、QQBot 等通訊工具入口。關閉後會移除此設定。',
+      managementHint: '需要 Swarm Agent 0.17.0 及以上版本。開啟後會寫入 multiplex_profiles，讓 default gateway 服務所有 profiles；其他 profile 不能單獨設定飛書、微信、QQBot 等通訊工具入口。關閉後會移除此設定。',
       managementAuto: '自動',
       managementPerProfile: '每個 profile 一個 gateway',
       managementUnified: '統一 default gateway',
@@ -1498,11 +1498,11 @@ export default {
       requireAuth: '工作階段授權',
       requireAuthHint: '修改工作階段操作是否授權',
       memoryWriteApproval: '記憶寫入審核',
-      memoryWriteApprovalHint: 'Hermes Agent 寫入 MEMORY.md / USER.md 前先進入待審核佇列。',
+      memoryWriteApprovalHint: 'Swarm Agent 寫入 MEMORY.md / USER.md 前先進入待審核佇列。',
       skillsWriteApproval: '技能寫入審核',
-      skillsWriteApprovalHint: 'Hermes Agent 建立或修改技能前先進入待審核佇列。',
+      skillsWriteApprovalHint: 'Swarm Agent 建立或修改技能前先進入待審核佇列。',
       writeApprovalTitle: '待審核寫入',
-      writeApprovalDescription: '批准或拒絕 Hermes Agent 暫存的記憶和技能寫入。',
+      writeApprovalDescription: '批准或拒絕 Swarm Agent 暫存的記憶和技能寫入。',
       writeApprovalRefresh: '重新整理',
       writeApprovalEmpty: '暫無待審核寫入。',
       writeApprovalMemory: '記憶',
@@ -2237,11 +2237,11 @@ export default {
     new_0_6_19_4: 'TTS 設定改為依 profile 儲存，語速、音調和測試語音會讀取同一套設定來源',
     new_0_6_19_5: 'Coding Agent 的模型切換不會影響既有會話，並修復自訂 provider key 透傳',
     new_0_6_19_6: '補齊 skills runtime 過濾、定時任務排序、技能詳情高度限制、Windows 更新恢復、設定備份兜底和 logo 資源壓縮',
-    new_0_6_18_2: 'Hermes Studio MCP 拆分為 api、devices 和 use 工具集，新增 session、model、chat-run 與 ops 相關操作',
+    new_0_6_18_2: 'Swarm Studio MCP 拆分為 api、devices 和 use 工具集，新增 session、model、chat-run 與 ops 相關操作',
     new_0_6_18_3: '聊天和群聊歷史更可靠：UI 歷史與模型上下文分離，空訊息處理更安全，run failed 不再覆蓋既有助手回覆，reasoning 回放也更一致',
     new_0_6_18_4: 'MCU 裝置回饋更清楚：工具呼叫會優先顯示在語音之上，音訊不會過早標記任務完成，活躍裝置登入更穩定，ONLINE/IP 長文字支援捲動',
     new_0_6_18_5: '語音和訊息工作流新增自訂 HTTP TTS/STT 端點、音訊轉碼、fork 訊息操作、持久化 fork 標記，以及更清楚的上傳錯誤',
-    new_0_6_18_6: '桌面與 runtime 設定更安全：Windows PATH 註冊支援 Unicode，支援發布 Hermes Agent 0.17.0 runtime，並補齊行動端 PWA metadata 和多項 profile/auth 恢復修復',
+    new_0_6_18_6: '桌面與 runtime 設定更安全：Windows PATH 註冊支援 Unicode，支援發布 Swarm Agent 0.17.0 runtime，並補齊行動端 PWA metadata 和多項 profile/auth 恢復修復',
     new_0_6_18_7: '各項細節最佳化',
     new_0_6_17_1: '修復 Codex 和 Claude Code 的對話啟動問題',
     new_0_6_16_1: 'Provider 設定現在會保留自訂 API mode，正確涵蓋 LongCat，支援刪除 v12 Provider，並讓模型目錄同步 auth 和 API-key 圖像 Provider',
@@ -2268,7 +2268,7 @@ export default {
     new_0_6_13_2: '檔案處理更安全：路徑校驗支援字面量點號，並拒絕異常編碼的技能匯入檔名',
     new_0_6_13_3: '聊天在行動端和不支援 speech synthesis 的瀏覽器中更穩定',
     new_0_6_13_4: 'Agent 失敗時會保留 bridge 錯誤尾部資訊，Claude Code root 權限啟動也更可靠',
-    new_0_6_13_5: '桌面更新更可靠：Windows 升級會關閉殘留 Hermes Studio 行程，優先檢查 Cloudflare，並保留 GitHub 更新源兜底',
+    new_0_6_13_5: '桌面更新更可靠：Windows 升級會關閉殘留 Swarm Studio 行程，優先檢查 Cloudflare，並保留 GitHub 更新源兜底',
     new_0_6_13_6: '發佈自動化調整：Web UI 和 Docker release 不再標記為 GitHub Latest，完整桌面 release 手動建置成功後才提升為 Latest',
   },
 }
diff --git a/packages/client/src/i18n/locales/zh.ts b/packages/client/src/i18n/locales/zh.ts
index d10836a8..e28f5f3e 100644
--- a/packages/client/src/i18n/locales/zh.ts
+++ b/packages/client/src/i18n/locales/zh.ts
@@ -1,7 +1,7 @@
 export default {
   // 登录
   login: {
-    title: 'Hermes Web UI',
+    title: 'Swarm Web UI',
     description: '输入用户名和密码以继续。',
     placeholder: '访问令牌',
     submit: '登录',
@@ -103,6 +103,9 @@ export default {
     collapse: '收起',
     start: '启动',
     stop: '停止',
+    search: '搜索',
+    refresh: '刷新',
+    open: '打开',
   },
 
   // 侧边栏
@@ -186,6 +189,9 @@ export default {
     history: '历史',
     jobs: '任务',
     kanban: '看板',
+    swarmKanban: '协作看板',
+    matrixChat: 'Matrix 聊天',
+    cockpit: 'AI协作中心',
     workflow: '工作流',
     models: '模型',
     profiles: '用户',
@@ -261,7 +267,7 @@ export default {
     remoteLoadFailed: '远程版本加载失败',
     downloadTasks: '下载任务',
     downloadStarted: '已开始下载',
-    activateSuccess: '版本选择已保存，重启 Hermes Studio 后生效。',
+    activateSuccess: '版本选择已保存，重启 Swarm Studio 后生效。',
     jobStatus: {
       queued: '排队中',
       running: '下载中',
@@ -413,7 +419,7 @@ export default {
     contextEditInvalid: '请输入有效的上下文长度',
     contextEditSuccess: '上下文长度已更新',
     contextEditFailed: '更新失败',
-    emptyState: '开始与 Hermes Agent 对话',
+    emptyState: '开始与 Swarm Agent 对话',
     emptyStateAgent: '开始与 {agent} 对话',
     viewOlderInHistory: '前面的消息请到历史记录查看',
     scrollToBottom: '滚动到底部',
@@ -727,10 +733,23 @@ export default {
   // 看板
   kanban: {
     title: '看板',
+    diagnostics: '诊断',
     createTask: '新建任务',
     noTasks: '暂无任务',
     allStatuses: '全部状态',
     allAssignees: '全部负责人',
+    allTenants: '所有租户',
+    helpTooltip: '跨租户任务管理看板',
+    searchPlaceholder: '搜索任务...',
+    tenant: '租户',
+    assignee: '负责人',
+    showArchived: '显示已归档',
+    lanesByProfile: '按配置分组',
+    clearFilters: '清除筛选',
+    unassigned: '未分配',
+    untitled: '未命名',
+    taskNeedsAttention: '任务需要关注',
+    tasksNeedAttention: '{count} 个任务需要关注',
     board: {
       create: '新建看板',
       archive: '归档看板',
@@ -740,6 +759,15 @@ export default {
       created: '看板已创建',
       archived: '看板已归档',
       archiveConfirm: '确定归档当前看板？',
+      slug: '看板标识',
+      slugInvalid: '无效标识 — 仅允许小写字母、数字和连字符',
+      name: '看板名称',
+      description: '看板描述',
+      icon: '图标',
+      iconPlaceholder: 'Emoji 或图标名称',
+      color: '颜色',
+      colorPlaceholder: '#hex 或颜色名称',
+      switchToNew: '创建后切换到新看板',
     },
     columns: {
       triage: '待分拣',
@@ -752,6 +780,16 @@ export default {
       done: '已完成',
       archived: '已归档',
     },
+    columnSubtitles: {
+      triage: '待分诊',
+      todo: '未开始',
+      scheduled: '等待开始',
+      ready: '就绪',
+      running: '进行中',
+      blocked: '已阻塞',
+      done: '已完成',
+      archived: '已归档',
+    },
     form: {
       title: '标题',
       titlePlaceholder: '任务标题',
@@ -836,6 +874,8 @@ export default {
       specify: '细化',
       loadLog: '加载日志',
       loadDiagnostics: '加载诊断',
+      nudgeDispatcher: '催促调度器',
+      archive: '归档',
     },
     message: {
       taskCreated: '任务已创建',
@@ -849,7 +889,116 @@ export default {
       taskReassigned: '任务已重新分配',
       taskSpecified: '任务已细化',
       loadFailed: '加载任务失败',
-    },
+      bulkFailed: '批量操作失败',
+      taskUpdated: '任务已更新',
+      updateFailed: '更新任务失败',
+      linkAdded: '任务关联已添加',
+      linkFailed: '更新任务关联失败',
+      linkRemoved: '任务关联已移除',
+      commentFailed: '添加评论失败',
+      logFailed: '加载工作日志失败',
+    },
+    trash: {
+      dropHere: '拖放到此处归档',
+    },
+    reclaim: {
+      confirm: '收回此任务？当前负责人将被通知。',
+      success: '任务已收回',
+      failed: '收回任务失败',
+      button: '收回',
+    },
+    recovery: {
+      hint: '恢复原因（可选）',
+    },
+    reassign: {
+      success: '任务已重新分配',
+      failed: '重新分配任务失败',
+      button: '重新分配',
+      placeholder: '选择配置...',
+      confirm: '重新分配此任务？',
+    },
+    orchestration: {
+      defaultOption: '默认',
+      saved: '编排设置已保存',
+      saveFailed: '保存编排设置失败',
+      descriptionSaved: '描述已保存',
+      autoGenerated: '自动生成',
+      autoGenerateFailed: '自动生成失败',
+      autoTitle: '自动分解',
+      manualTitle: '手动',
+      mode: '模式',
+      auto: '自动',
+      manual: '手动',
+      expandTitle: '展开编排设置',
+      settings: '设置',
+      reload: '重新加载',
+      orchestratorProfile: '编排器配置',
+      resolved: '已解析',
+      default: '默认',
+      defaultAssignee: '默认负责人',
+      orchestrationMode: '编排模式',
+      autoDecompose: '自动分解',
+      autoHint: '自动将任务分解为子任务',
+      manualHint: '手动创建和分配子任务',
+      loading: '加载中...',
+      profileDescriptions: '配置描述',
+      profileDescriptionsHint: '描述每个编排器配置',
+      noProfiles: '无可用配置',
+      defaultTag: '默认',
+      descriptionPlaceholder: '编排描述...',
+      saveDescriptionTitle: '保存描述',
+      saving: '保存中...',
+      save: '保存',
+      autoGenerateTitle: '自动生成',
+      generating: '生成中...',
+    },
+    completionSummaryRequired: '标记任务完成前需要填写完成摘要。',
+    confirmDone: '将此任务标记为完成？这将在看板上完成任务。',
+    confirmArchive: '归档此任务？归档的任务将从看板隐藏，但可以恢复。',
+    confirmBlocked: '将此任务标记为阻塞？阻塞的任务将暂停，直到解除阻塞。',
+    restoreDrawer: '还原抽屉',
+    maximizeDrawer: '最大化抽屉',
+    clickToEdit: '点击编辑',
+    clickToEditAssignee: '点击编辑负责人',
+    project: '项目',
+    workspace: '工作区',
+    skills: '技能',
+    goalMode: '目标模式',
+    createdBy: '创建者',
+    specify: '指定',
+    decompose: '分解',
+    recovery: '恢复',
+    notifyHomeChannels: '通知主频道',
+    sendingUpdates: '正在发送更新...',
+    clickToStop: '点击停止',
+    sendNotifications: '发送通知',
+    description: '描述',
+    noDescription: '暂无描述',
+    dependencies: '依赖关系',
+    parents: '父任务',
+    none: '无',
+    parent: '父任务',
+    children: '子任务',
+    child: '子任务',
+    result: '结果',
+    events: '事件',
+    phantomIds: '幻影 ID',
+    workerLog: '工作日志',
+    loadingLog: '正在加载日志...',
+    noWorkerLog: '无可用工作日志',
+    logTruncated: '日志已截断 — 显示最后 {count} 行',
+    runHistory: '运行历史',
+    showAllAttempts: '显示所有尝试',
+    ended: '已结束',
+    active: '活跃',
+    noProfile: '无配置',
+    comments: '评论',
+    noComments: '暂无评论',
+    addComment: '添加评论',
+    comment: '评论',
+    completionSummary: '完成摘要',
+    completionSummaryHint: '总结完成的工作',
+    completionSummaryPlaceholder: '撰写完成摘要...',
     stats: {
       total: '总计',
       tasks: '任务数',
@@ -1002,10 +1151,10 @@ export default {
     reloadHint: '活跃会话请执行 /reload-skills；新会话会自动加载。',
     writeApprovalButton: '待审核 {count}',
     writeApprovalTitle: '待审核写入',
-    writeApprovalDescription: '批准或拒绝 Hermes Agent 暂存的记忆和技能写入。',
+    writeApprovalDescription: '批准或拒绝 Swarm Agent 暂存的记忆和技能写入。',
     writeApprovalRefresh: '刷新',
     writeApprovalEmpty: '暂无待审核写入。',
-    writeApprovalUnsupported: '当前 Hermes Agent 版本不支持写入审核。请升级 Hermes Agent 后再使用。',
+    writeApprovalUnsupported: '当前 Swarm Agent 版本不支持写入审核。请升级 Swarm Agent 后再使用。',
     writeApprovalMemory: '记忆',
     writeApprovalSkills: '技能',
     writeApprovalViewDiff: '查看',
@@ -1420,9 +1569,10 @@ export default {
       busyInputMode: '忙碌输入模式',
       busyInputModeHint: 'AI 处理中仍可输入',
       theme: '主题',
-      themeHint: '选择浅色、暗色或跟随系统',
+      themeHint: '选择浅色、暗色、自动或跟随系统',
       themeLight: '浅色',
       themeDark: '暗色',
+      themeAuto: '自动（北京时间）',
       themeSystem: '跟随系统',
     },
     agent: {
@@ -1444,7 +1594,7 @@ export default {
       enabled: '自动启动 gateway',
       enabledHint: '关闭后，启动期不会自动拉起任何 profile gateway。',
       management: '统一 gateway',
-      managementHint: '需要 Hermes Agent 0.17.0 及以上版本。开启后会写入 multiplex_profiles，让 default gateway 服务所有 profiles；其他 profile 不能单独配置飞书、微信、QQBot 等通讯工具入口。关闭后会移除该配置。',
+      managementHint: '需要 Swarm Agent 0.17.0 及以上版本。开启后会写入 multiplex_profiles，让 default gateway 服务所有 profiles；其他 profile 不能单独配置飞书、微信、QQBot 等通讯工具入口。关闭后会移除该配置。',
       managementAuto: '自动',
       managementPerProfile: '每个 profile 一个 gateway',
       managementUnified: '统一 default gateway',
@@ -1498,11 +1648,11 @@ export default {
       requireAuth: '会话授权',
       requireAuthHint: '修改会话操作是否授权',
       memoryWriteApproval: '记忆写入审核',
-      memoryWriteApprovalHint: 'Hermes Agent 写入 MEMORY.md / USER.md 前先进入待审核队列。',
+      memoryWriteApprovalHint: 'Swarm Agent 写入 MEMORY.md / USER.md 前先进入待审核队列。',
       skillsWriteApproval: '技能写入审核',
-      skillsWriteApprovalHint: 'Hermes Agent 创建或修改技能前先进入待审核队列。',
+      skillsWriteApprovalHint: 'Swarm Agent 创建或修改技能前先进入待审核队列。',
       writeApprovalTitle: '待审核写入',
-      writeApprovalDescription: '批准或拒绝 Hermes Agent 暂存的记忆和技能写入。',
+      writeApprovalDescription: '批准或拒绝 Swarm Agent 暂存的记忆和技能写入。',
       writeApprovalRefresh: '刷新',
       writeApprovalEmpty: '暂无待审核写入。',
       writeApprovalMemory: '记忆',
@@ -1830,6 +1980,17 @@ export default {
       mimoStylePromptHint: '可选，用自然语言描述语音风格',
       mimoStylePromptPlaceholder: '例如：用轻快上扬的语调，语速稍快',
     },
+    matrixIdentity: {
+      title: 'Matrix 身份',
+      userId: '用户 ID',
+      displayName: '显示名称',
+      homeserver: 'Homeserver',
+      role: '角色',
+      authSource: '认证来源',
+      passwordManagedByMatrix: '密码由 Matrix homeserver 管理',
+      changeMatrixPassword: '修改 Matrix 密码',
+      passwordChanged: 'Matrix 密码已成功修改',
+    },
   },
   githubPreview: {
     title: "版本预览",
@@ -2234,11 +2395,11 @@ export default {
     new_0_6_19_4: 'TTS 配置改为按 profile 保存，音速、音调和测试语音会读取同一套配置来源',
     new_0_6_19_5: 'Coding Agent 的模型切换不会影响既有会话，并修复自定义 provider key 透传',
     new_0_6_19_6: '补齐 skills runtime 过滤、定时任务排序、技能详情高度限制、Windows 更新恢复、配置备份兜底和 logo 资源压缩',
-    new_0_6_18_2: 'Hermes Studio MCP 拆分为 api、devices 和 use 工具集，新增 session、model、chat-run 与 ops 相关操作',
+    new_0_6_18_2: 'Swarm Studio MCP 拆分为 api、devices 和 use 工具集，新增 session、model、chat-run 与 ops 相关操作',
     new_0_6_18_3: '聊天和群聊历史更可靠：UI 历史与模型上下文分离，空消息处理更安全，run failed 不再覆盖已有助手回复，reasoning 回放也更一致',
     new_0_6_18_4: 'MCU 设备反馈更清楚：工具调用会优先显示在语音之上，音频不会过早标记任务完成，活跃设备登录更稳定，ONLINE/IP 长文本支持滚动',
     new_0_6_18_5: '语音和消息工作流新增自定义 HTTP TTS/STT 端点、音频转码、fork 消息操作、持久化 fork 标记，以及更清晰的上传错误',
-    new_0_6_18_6: '桌面与 runtime 设置更安全：Windows PATH 注册支持 Unicode，支持发布 Hermes Agent 0.17.0 runtime，并补齐移动端 PWA 元数据和多项 profile/auth 恢复修复',
+    new_0_6_18_6: '桌面与 runtime 设置更安全：Windows PATH 注册支持 Unicode，支持发布 Swarm Agent 0.17.0 runtime，并补齐移动端 PWA 元数据和多项 profile/auth 恢复修复',
     new_0_6_18_7: '各项细节的优化',
     new_0_6_17_1: '修复 Codex 和 Claude Code 的对话启动问题',
     new_0_6_16_1: 'Provider 设置会正确保存自定义 API mode，补齐 LongCat 预设，支持删除 v12 Provider，并让模型目录同步认证和 API-key 图像 Provider',
@@ -2265,7 +2426,128 @@ export default {
     new_0_6_13_2: '文件处理更安全：路径校验支持字面量点号，并拒绝异常编码的技能导入文件名',
     new_0_6_13_3: '聊天在移动端和不支持 speech synthesis 的浏览器中更稳定',
     new_0_6_13_4: 'Agent 失败时会保留 bridge 错误尾部信息，Claude Code root 权限启动也更可靠',
-    new_0_6_13_5: '桌面更新更可靠：Windows 升级会关闭残留 Hermes Studio 进程，优先检查 Cloudflare，并保留 GitHub 更新源兜底',
+    new_0_6_13_5: '桌面更新更可靠：Windows 升级会关闭残留 Swarm Studio 进程，优先检查 Cloudflare，并保留 GitHub 更新源兜底',
     new_0_6_13_6: '发布自动化调整：Web UI 和 Docker release 不再标记为 GitHub Latest，完整桌面 release 手动构建成功后才提升为 Latest',
   },
+
+  matrix: {
+    options: '选项',
+    threadStartDisabled: '无法基于已有关系（回复/编辑）的消息发起话题',
+    undecryptable: '无法解密此消息',
+    historyLoadError: '服务器错误，无法加载更早的历史',
+  },
+
+  cockpit: {
+    modeWorkspace: '工作区',
+    editTitlePlaceholder: '标题',
+    actions: '操作',
+    specify: '细化',
+    decompose: '分解',
+    moveToTriage: '转入分诊',
+    moveToReady: '转入就绪',
+    moveToBlocked: '标记阻塞',
+    moveToDone: '标记完成',
+    moveToArchived: '归档',
+    unblock: '取消阻塞',
+    reclaim: '回收',
+    reassign: '改派',
+    reassignProfile: '改派 profile',
+    recovery: '恢复',
+    diagnostics: '诊断',
+    notifyHomeChannels: '主页通知频道',
+    subscribe: '订阅',
+    unsubscribe: '退订',
+    comments: '评论',
+    addCommentPlaceholder: '写评论…',
+    confirmDone: '确认将此任务标记为完成？此操作将完成任务在看板中的状态。',
+    confirmArchive: '归档此任务？归档后的任务会从看板中隐藏，但仍可恢复。',
+    confirmBlocked: '标记此任务为阻塞？阻塞的任务将暂停执行。',
+    completionSummaryRequired: '标记完成前必须填写完成摘要。',
+    evaluation: '评估',
+    saveDraft: '保存草稿',
+    parentTasks: '父任务',
+    childTasks: '子任务',
+    parentIdPlaceholder: '父任务 id',
+    childIdPlaceholder: '子任务 id',
+    pendingLinksHint: '个待提交的关联变更',
+    assignee: '负责人',
+    selectAssignee: '选择负责人…',
+    description: '描述',
+    descriptionPlaceholder: '任务描述…',
+    add: '添加',
+    attachments: '附件',
+    noAttachments: '暂无附件',
+    uploadFile: '上传文件',
+    none: '无',
+    historySearchPlaceholder: '搜索历史事件…',
+    historyToday: '今天',
+    historyWeek: '本周',
+    historyMonth: '本月',
+    historyCategory: '类别',
+    historyCatEvent: '事件',
+    historyCatComment: '评论',
+    historyActive: '进行中',
+    historyDone: '已完成',
+    notifyTitle: '通知',
+    notifyUnread: '条未读',
+    notifySource: '来源',
+    notifyAll: '全部',
+    notifyMatrix: 'Matrix',
+    notifyChat: '单聊',
+    notifyGroup: '群聊',
+    notifyEmpty: '没有未读消息',
+    notifyClickHint: '点击条目进入对应聊天',
+    notifyReadAll: '全部标为已读',
+    attention: '注意力',
+    backToWork: '返回工作',
+    openFull: '全屏打开',
+    saySomething: '输入消息...',
+    send: '发送',
+    noTaskSelected: '未选择任务',
+    collabWith: '协作对象',
+    addCollab: '沟通协作',
+    addCollabTitle: '添加协作者',
+    collaborationMap: '协作地图',
+    handleLater: '稍后处理',
+    taskFiles: '任务文件',
+    currentTaskWorkspace: '当前任务工作区',
+    filterFiles: '过滤文件...',
+    history: '历史',
+    historyTitle: '历史记录',
+    historyTime: '时间',
+    historyAction: '操作',
+    historyStatus: '状态',
+    historyArchived: '已归档',
+    restore: '恢复',
+    maximize: '最大化',
+    modeWork: '工作',
+    modeTerm: '终端',
+    schedule: '日程',
+    today: '今天',
+    scheduleEvents: '日程事件',
+    scheduleEmpty: '此日期无事件',
+    scheduleAddTodo: '添加待办',
+    scheduleTodoPlaceholder: '需要做什么？',
+    templateManager: '模板管理',
+    templateName: '模板名称...',
+    saveAsTemplate: '保存为模板',
+    applyTemplate: '应用',
+    deleteTemplate: '删除',
+    noTemplates: '暂无保存的模板',
+    termExit: '退出终端',
+    timeline: '时间线',
+    pending: '待处理',
+    done: '已完成',
+    olderHistory: '{n} 条更早记录',
+    search: '搜索',
+    priority: '优先级',
+    yourDecision: '你的决定',
+    recommend: '推荐',
+    riskTags: '风险标签',
+    agentPrefilled: 'Agent 预填',
+    reviewOpinion: '审核意见',
+    submit: '提交',
+    newCollabFromArchive: '从模板新建协作',
+    readOnly: '只读',
+  },
 }
diff --git a/packages/client/src/router/index.ts b/packages/client/src/router/index.ts
index f72f74e1..9381f72b 100644
--- a/packages/client/src/router/index.ts
+++ b/packages/client/src/router/index.ts
@@ -11,34 +11,62 @@ const router = createRouter({
       meta: { public: true },
     },
     {
-      path: '/hermes/chat',
-      name: 'hermes.chat',
-      component: () => import('@/views/hermes/ChatView.vue'),
-    },
-    {
-      path: '/hermes/session/:sessionId',
-      name: 'hermes.session',
-      component: () => import('@/views/hermes/ChatView.vue'),
-    },
-    {
-      path: '/hermes/history',
-      name: 'hermes.history',
-      component: () => import('@/views/hermes/HistoryView.vue'),
-    },
-    {
-      path: '/hermes/history/session/:sessionId',
-      name: 'hermes.historySession',
-      component: () => import('@/views/hermes/HistoryView.vue'),
-    },
-    {
-      path: '/hermes/global-agent',
-      name: 'hermes.globalAgent',
-      component: () => import('@/views/hermes/GlobalAgentView.vue'),
-    },
-    {
-      path: '/hermes/global-agent/session/:sessionId',
-      name: 'hermes.globalAgentSession',
-      component: () => import('@/views/hermes/GlobalAgentView.vue'),
+      path: '/hermes/cockpit',
+      name: 'hermes.cockpit',
+      component: () => import('@/custom/cockpit/views/CockpitView.vue'),
+      meta: { fullscreen: true },
+      children: [
+        {
+          path: 'chat',
+          name: 'hermes.chat',
+          component: () => import('@/views/hermes/ChatView.vue'),
+        },
+        {
+          path: 'session/:sessionId',
+          name: 'hermes.session',
+          component: () => import('@/views/hermes/ChatView.vue'),
+        },
+        {
+          path: 'history',
+          name: 'hermes.history',
+          component: () => import('@/views/hermes/HistoryView.vue'),
+        },
+        {
+          path: 'history/session/:sessionId',
+          name: 'hermes.historySession',
+          component: () => import('@/views/hermes/HistoryView.vue'),
+        },
+        {
+          path: 'global-agent',
+          name: 'hermes.globalAgent',
+          component: () => import('@/views/hermes/GlobalAgentView.vue'),
+        },
+        {
+          path: 'global-agent/session/:sessionId',
+          name: 'hermes.globalAgentSession',
+          component: () => import('@/views/hermes/GlobalAgentView.vue'),
+        },
+        {
+          path: 'swarm-kanban',
+          name: 'hermes.swarmKanban',
+          component: () => import('@/custom/kanban/components/KanbanPanel.vue'),
+        },
+        {
+          path: 'workflow',
+          name: 'hermes.workflow',
+          component: () => import('@/views/hermes/WorkflowView.vue'),
+        },
+        {
+          path: 'group-chat',
+          name: 'hermes.groupChat',
+          component: () => import('@/views/hermes/GroupChatView.vue'),
+        },
+        {
+          path: 'group-chat/room/:roomId',
+          name: 'hermes.groupChatRoom',
+          component: () => import('@/views/hermes/GroupChatView.vue'),
+        },
+      ],
     },
     {
       path: '/hermes/jobs',
@@ -50,11 +78,6 @@ const router = createRouter({
       name: 'hermes.kanban',
       component: () => import('@/views/hermes/KanbanView.vue'),
     },
-    {
-      path: '/hermes/workflow',
-      name: 'hermes.workflow',
-      component: () => import('@/views/hermes/WorkflowView.vue'),
-    },
     {
       path: '/hermes/models',
       name: 'hermes.models',
@@ -123,16 +146,6 @@ const router = createRouter({
       name: 'hermes.devices',
       component: () => import('@/views/hermes/DevicesView.vue'),
     },
-    {
-      path: '/hermes/group-chat',
-      name: 'hermes.groupChat',
-      component: () => import('@/views/hermes/GroupChatView.vue'),
-    },
-    {
-      path: '/hermes/group-chat/room/:roomId',
-      name: 'hermes.groupChatRoom',
-      component: () => import('@/views/hermes/GroupChatView.vue'),
-    },
     {
       path: '/hermes/files',
       name: 'hermes.files',
@@ -163,7 +176,7 @@ router.beforeEach((to, _from, next) => {
   if (to.meta.public) {
     // Already has key, skip login
     if (to.name === 'login' && hasApiKey()) {
-      next({ path: '/hermes/chat' })
+      next({ name: 'hermes.cockpit' })
       return
     }
     next()
@@ -177,7 +190,7 @@ router.beforeEach((to, _from, next) => {
   }
 
   if (to.meta.requiresSuperAdmin && !isStoredSuperAdmin()) {
-    next({ name: 'hermes.chat' })
+    next({ name: 'hermes.cockpit' })
     return
   }
 
diff --git a/packages/client/src/stores/hermes/auth.ts b/packages/client/src/stores/hermes/auth.ts
new file mode 100644
index 00000000..3640c773
--- /dev/null
+++ b/packages/client/src/stores/hermes/auth.ts
@@ -0,0 +1,132 @@
+import { defineStore } from 'pinia'
+import { ref, computed } from 'vue'
+import { getApiKey, setApiKey, clearApiKey, getMatrixCredentials, clearMatrixCredentials, storeMatrixCredentials } from '@/api/client'
+import { matrixSdkLogin, hermesMatrixLogin, loginWithPassword, fetchCurrentUser } from '@/api/auth'
+import type { CurrentUser } from '@/api/auth'
+
+export interface UnifiedUser {
+  id: number
+  username: string
+  matrixUserId: string | null
+  displayName: string
+  avatarUrl: string | null
+  role: 'super_admin' | 'admin'
+  authSource: 'local' | 'matrix'
+  homeserverUrl: string | null
+  profiles: string[]
+  defaultProfile: string | null
+  status: string
+  requiresCredentialChange: boolean
+}
+
+export const useAuthStore = defineStore('hermes-auth', () => {
+  const user = ref<UnifiedUser | null>(null)
+  const hermesToken = ref<string | null>(getApiKey())
+  const matrixToken = ref<string | null>(getMatrixCredentials()?.accessToken || null)
+  const isLoading = ref(false)
+  const error = ref<string | null>(null)
+
+  const isLoggedIn = computed(() => !!hermesToken.value && !!user.value)
+  const isMatrixLoggedIn = computed(() => !!matrixToken.value)
+  const isSuperAdmin = computed(() => user.value?.role === 'super_admin')
+
+  async function matrixLogin(homeserverUrl: string, username: string, password: string) {
+    isLoading.value = true
+    error.value = null
+    try {
+      // 1. Login to Matrix homeserver
+      const matrixCreds = await matrixSdkLogin(homeserverUrl, username, password)
+      matrixToken.value = matrixCreds.accessToken
+
+      // 2. Login to Hermes backend with Matrix credentials
+      const hermesRes = await hermesMatrixLogin(
+        matrixCreds.accessToken,
+        matrixCreds.userId,
+        matrixCreds.deviceId,
+        matrixCreds.homeserverUrl
+      )
+      hermesToken.value = hermesRes.token
+      setApiKey(hermesRes.token)
+
+      // Persist Matrix credentials so Matrix Chat can access them
+      storeMatrixCredentials(matrixCreds)
+
+      // 3. Build unified user
+      user.value = buildUnifiedUser(hermesRes.user)
+    } catch (err: any) {
+      error.value = err.message || 'Login failed'
+      throw err
+    } finally {
+      isLoading.value = false
+    }
+  }
+
+  async function localLogin(username: string, password: string) {
+    isLoading.value = true
+    error.value = null
+    try {
+      const token = await loginWithPassword(username, password)
+      hermesToken.value = token
+      setApiKey(token)
+
+      const current = await fetchCurrentUser()
+      user.value = buildUnifiedUser(current)
+      matrixToken.value = null
+    } catch (err: any) {
+      error.value = err.message || 'Login failed'
+      throw err
+    } finally {
+      isLoading.value = false
+    }
+  }
+
+  async function fetchUser() {
+    if (!hermesToken.value) return
+    try {
+      const current = await fetchCurrentUser()
+      user.value = buildUnifiedUser(current)
+    } catch {
+      user.value = null
+    }
+  }
+
+  function logout() {
+    user.value = null
+    hermesToken.value = null
+    matrixToken.value = null
+    clearApiKey()
+    clearMatrixCredentials()
+  }
+
+  function buildUnifiedUser(apiUser: CurrentUser): UnifiedUser {
+    return {
+      id: apiUser.id,
+      username: apiUser.username,
+      matrixUserId: apiUser.matrix_user_id || null,
+      displayName: apiUser.matrix_display_name || apiUser.username,
+      avatarUrl: apiUser.matrix_avatar_url || null,
+      role: apiUser.role,
+      authSource: apiUser.auth_source || 'local',
+      homeserverUrl: apiUser.matrix_homeserver_url || null,
+      profiles: [], // Fetched separately
+      defaultProfile: null,
+      status: apiUser.status,
+      requiresCredentialChange: apiUser.requiresCredentialChange || false,
+    }
+  }
+
+  return {
+    user,
+    hermesToken,
+    matrixToken,
+    isLoading,
+    error,
+    isLoggedIn,
+    isMatrixLoggedIn,
+    isSuperAdmin,
+    matrixLogin,
+    localLogin,
+    fetchUser,
+    logout,
+  }
+})
\ No newline at end of file
diff --git a/packages/client/src/stores/hermes/chat.ts b/packages/client/src/stores/hermes/chat.ts
index fe9b53c4..f81aa333 100644
--- a/packages/client/src/stores/hermes/chat.ts
+++ b/packages/client/src/stores/hermes/chat.ts
@@ -1,9 +1,10 @@
 import { startRunViaSocket, resumeSession, registerSessionHandlers, unregisterSessionHandlers, getChatRunSocket, respondToolApproval, onPeerUserMessage, onSessionCommand, onSessionTitleUpdated, respondClarify, type ChatRunTransport, type RunEvent, type ResumeSessionPayload, type StartRunRequest, type ContentBlock as ContentBlockImport } from '@/api/hermes/chat'
 import { deleteSession as deleteSessionApi, fetchSessionMessagesPage, fetchSessions, setSessionModel, type HermesMessage, type SessionSummary } from '@/api/hermes/sessions'
-import { getActiveProfileName } from '@/api/client'
+import { getActiveProfileName, hasApiKey } from '@/api/client'
 import { inferCodingAgentApiMode, normalizeCodingAgentApiMode } from '@/api/coding-agents'
 import { getDownloadUrl } from '@/api/hermes/download'
 import type { ProviderApiMode } from '@/api/hermes/system'
+import { tagGatewayNotice } from '@/custom/chat/gateway-notice'
 import { defineStore } from 'pinia'
 import { ref, computed, watch } from 'vue'
 import { useAppStore } from './app'
@@ -50,7 +51,7 @@ export interface Message {
   // 不含 <think> 包裹标签；内容自身可以为多段纯文本。
   reasoning?: string
   queued?: boolean
-  systemType?: 'command' | 'error' | 'fork-divider'
+  systemType?: 'command' | 'error' | 'fork-divider' | 'gateway'
   commandAction?: string
   commandData?: Record<string, unknown>
   finishReason?: string | null
@@ -432,7 +433,7 @@ function mapHermesMessages(msgs: HermesMessage[]): Message[] {
       content: displayContent || '',
       timestamp: Math.round(msg.timestamp * 1000),
       reasoning: msg.reasoning ? msg.reasoning : undefined,
-      systemType: displayRole === 'command' ? 'command' : undefined,
+      systemType: tagGatewayNotice(displayContent, displayRole === 'command' ? 'command' : undefined),
       finishReason: readFinishReason(msg),
       runMarker: readRunMarker(msg),
     })
@@ -630,6 +631,13 @@ export const useChatStore = defineStore('chat', () => {
   const pendingForkCommands = ref<Set<string>>(new Set())
   /** Sessions that completed while the user was viewing another session. */
   const completedUnreadSessions = ref<Set<string>>(new Set())
+  // HERMES_CUSTOM[ChatUnread] BEGIN: real unread-message tracking (in-memory, resets on refresh)
+  // Parallel to completedUnreadSessions (a binary "run completed" flag). This tracks actual
+  // unread messages per session for the cockpit notification panel: count + last message
+  // preview/role/timestamp. Seeded by socket events for non-active sessions (requires the
+  // server-side auto-join of all user sessions — patch 088).
+  const unreadMessages = ref<Map<string, { count: number; lastPreview: string; lastRole: string; lastTs: number }>>(new Map())
+  // HERMES_CUSTOM[ChatUnread] END
   const sessionProfileFilter = ref<string | null>(null)
   /** sessionId → queued message count */
   const queueLengths = ref<Map<string, number>>(new Map())
@@ -670,6 +678,7 @@ export const useChatStore = defineStore('chat', () => {
   const isRunActive = computed(() => isStreaming.value)
 
   async function fetchRuntimeSessions(profile?: string | null): Promise<SessionSummary[]> {
+    if (!hasApiKey()) return []
     const scopedProfile = profile || undefined
     if (runtimeMode.value === 'global_agent') return fetchSessions('global_agent', undefined, scopedProfile)
 
@@ -694,6 +703,8 @@ export const useChatStore = defineStore('chat', () => {
     runtimeMode.value = mode
     sessions.value = []
     completedUnreadSessions.value = new Set()
+    // HERMES_CUSTOM[ChatUnread]
+    unreadMessages.value = new Map()
     queueLengths.value = new Map()
     queuedUserMessages.value = new Map()
     pendingApprovals.value = new Map()
@@ -768,8 +779,50 @@ export const useChatStore = defineStore('chat', () => {
   function pruneCompletedUnreadSessions(existingIds: Set<string>) {
     const next = new Set([...completedUnreadSessions.value].filter(id => existingIds.has(id)))
     if (next.size !== completedUnreadSessions.value.size) completedUnreadSessions.value = next
+    // HERMES_CUSTOM[ChatUnread] BEGIN: prune unread map in lockstep
+    const unreadNext = new Map<string, { count: number; lastPreview: string; lastRole: string; lastTs: number }>()
+    for (const [id, info] of unreadMessages.value) {
+      if (existingIds.has(id)) unreadNext.set(id, info)
+    }
+    if (unreadNext.size !== unreadMessages.value.size) unreadMessages.value = unreadNext
+    // HERMES_CUSTOM[ChatUnread] END
+  }
+
+  // HERMES_CUSTOM[ChatUnread] BEGIN: accumulate / query / clear real unread messages
+  function accumulateUnread(sessionId: string, preview: string, role: string, ts?: number) {
+    if (activeSessionId.value === sessionId) return
+    const trimmed = typeof preview === 'string' ? preview.replace(/\s+/g, ' ').trim().slice(0, 120) : ''
+    const existing = unreadMessages.value.get(sessionId)
+    const next = new Map(unreadMessages.value)
+    next.set(sessionId, {
+      count: (existing?.count ?? 0) + 1,
+      lastPreview: trimmed || existing?.lastPreview || '',
+      lastRole: role || existing?.lastRole || '',
+      lastTs: typeof ts === 'number' && Number.isFinite(ts) ? (ts < 1e12 ? ts * 1000 : ts) : Date.now(),
+    })
+    unreadMessages.value = next
+  }
+
+  function isSessionUnread(sessionId: string): boolean {
+    return unreadMessages.value.has(sessionId)
   }
 
+  function getSessionUnreadCount(sessionId: string): number {
+    return unreadMessages.value.get(sessionId)?.count ?? 0
+  }
+
+  function getSessionUnreadInfo(sessionId: string): { count: number; lastPreview: string; lastRole: string; lastTs: number } | null {
+    return unreadMessages.value.get(sessionId) ?? null
+  }
+
+  function clearSessionUnread(sessionId: string) {
+    if (!unreadMessages.value.has(sessionId)) return
+    const next = new Map(unreadMessages.value)
+    next.delete(sessionId)
+    unreadMessages.value = next
+  }
+  // HERMES_CUSTOM[ChatUnread] END
+
   function clearActiveSession() {
     const sid = activeSessionId.value
     activeSessionId.value = null
@@ -1004,6 +1057,8 @@ export const useChatStore = defineStore('chat', () => {
     if (legacyActiveKey) removeItem(legacyActiveKey)
     activeSession.value = sessions.value.find(s => s.id === sessionId) || null
     clearSessionCompletedUnread(sessionId)
+    // HERMES_CUSTOM[ChatUnread]
+    clearSessionUnread(sessionId)
 
     if (!activeSession.value) return
 
@@ -1285,6 +1340,12 @@ export const useChatStore = defineStore('chat', () => {
   }
 
   function addMessage(sessionId: string, msg: Message) {
+    // 兜底：未显式指定 systemType 的消息，若是网关关闭/重启告警则打标，
+    // 供渲染层折叠为紧凑系统提示（两入口之一；历史回放入口在 mapHermesMessages）。
+    if (msg.systemType === undefined) {
+      const tagged = tagGatewayNotice(msg.content, msg.systemType)
+      if (tagged) msg.systemType = tagged as NonNullable<typeof msg.systemType>
+    }
     const s = sessions.value.find(s => s.id === sessionId)
     if (s) s.messages.push(msg)
   }
@@ -1886,7 +1947,7 @@ export const useChatStore = defineStore('chat', () => {
     if (codingAgentId === 'claude-code') {
       return { icon: '/coding-agents/claude-code.svg' }
     }
-    return { icon: '/coding-agents/hermes.png' }
+    return { icon: '' }
   }
 
   function completionNotificationBody(session: Session, message?: Message): string {
@@ -2428,6 +2489,8 @@ export const useChatStore = defineStore('chat', () => {
                   isStreaming: true,
                 })
                 activeAssistantMessageId = newId
+                // HERMES_CUSTOM[ChatUnread]: count each new agent reply (mirrors group-chat message counting)
+                accumulateUnread(sid, nextContent, 'assistant', Date.now())
               }
 
               break
@@ -3002,6 +3065,8 @@ export const useChatStore = defineStore('chat', () => {
               isStreaming: true,
             })
             activeAssistantMessageId = newId
+            // HERMES_CUSTOM[ChatUnread]: count each new agent reply (mirrors group-chat message counting)
+            accumulateUnread(sid, nextContent, 'assistant', Date.now())
           }
 
           break
@@ -3307,6 +3372,8 @@ export const useChatStore = defineStore('chat', () => {
 
   function handlePeerUserMessage(evt: RunEvent) {
     const sid = evt.session_id
+    // Note: peer user messages are NOT counted as unread — only agent (assistant)
+    // replies generate notifications (see accumulateUnread calls at run completion).
     if (!sid || activeSessionId.value !== sid || !activeSession.value) return
 
     const peer = evt.message
@@ -3440,6 +3507,8 @@ export const useChatStore = defineStore('chat', () => {
     window.setInterval(() => {
       if (typeof document !== 'undefined' && document.visibilityState !== 'visible') return
       if (isStreaming.value) return
+      if (!hasApiKey()) return
+      if (!hasApiKey()) return
       void refreshSessionListOnly()
     }, 12_000)
   }
@@ -3559,6 +3628,13 @@ export const useChatStore = defineStore('chat', () => {
     isSessionLive,
     isSessionCompletedUnread,
     clearSessionCompletedUnread,
+    // HERMES_CUSTOM[ChatUnread]
+    unreadMessages,
+    accumulateUnread,
+    isSessionUnread,
+    getSessionUnreadCount,
+    getSessionUnreadInfo,
+    clearSessionUnread,
     sessionProfileFilter,
     compressionState,
     abortState,
diff --git a/packages/client/src/stores/hermes/files.ts b/packages/client/src/stores/hermes/files.ts
index b3a7f8d6..9d2e43e9 100644
--- a/packages/client/src/stores/hermes/files.ts
+++ b/packages/client/src/stores/hermes/files.ts
@@ -117,6 +117,7 @@ export const useFilesStore = defineStore('files', () => {
   const currentPath = ref('')
   const entries = ref<FileEntry[]>([])
   const loading = ref(false)
+  const workspaceRoot = ref<string | undefined>(undefined)
   const sortBy = ref<'name' | 'size' | 'modTime'>('name')
   const sortOrder = ref<'asc' | 'desc'>('asc')
 
@@ -164,7 +165,7 @@ export const useFilesStore = defineStore('files', () => {
     if (path !== undefined) currentPath.value = path
     loading.value = true
     try {
-      const result = await filesApi.listFiles(currentPath.value)
+      const result = await filesApi.listFiles(currentPath.value, workspaceRoot.value)
       entries.value = result.entries
     } catch (err) {
       console.error('Failed to fetch files:', err)
@@ -182,7 +183,7 @@ export const useFilesStore = defineStore('files', () => {
   }
 
   async function openEditor(filePath: string) {
-    const result = await filesApi.readFile(filePath)
+    const result = await filesApi.readFile(filePath, workspaceRoot.value)
     editingFile.value = {
       path: filePath,
       content: result.content,
@@ -193,7 +194,7 @@ export const useFilesStore = defineStore('files', () => {
 
   async function saveEditor() {
     if (!editingFile.value) return
-    await filesApi.writeFile(editingFile.value.path, editingFile.value.content)
+    await filesApi.writeFile(editingFile.value.path, editingFile.value.content, workspaceRoot.value)
     editingFile.value.originalContent = editingFile.value.content
   }
 
@@ -203,10 +204,10 @@ export const useFilesStore = defineStore('files', () => {
     if (isImageFile(entry.name)) {
       previewFile.value = { path: entry.path, type: 'image' }
     } else if (isMarkdownFile(entry.name)) {
-      const result = await filesApi.readFile(entry.path)
+      const result = await filesApi.readFile(entry.path, workspaceRoot.value)
       previewFile.value = { path: entry.path, type: 'markdown', content: result.content }
     } else if (isTextFile(entry.name)) {
-      const result = await filesApi.readFile(entry.path)
+      const result = await filesApi.readFile(entry.path, workspaceRoot.value)
       previewFile.value = {
         path: entry.path,
         type: 'text',
@@ -220,18 +221,18 @@ export const useFilesStore = defineStore('files', () => {
 
   async function createDir(name: string, targetPath = currentPath.value) {
     const path = targetPath ? `${targetPath}/${name}` : name
-    await filesApi.mkDir(path)
+    await filesApi.mkDir(path, workspaceRoot.value)
     await fetchEntries()
   }
 
   async function createFile(name: string) {
     const path = currentPath.value ? `${currentPath.value}/${name}` : name
-    await filesApi.writeFile(path, '')
+    await filesApi.writeFile(path, '', workspaceRoot.value)
     await fetchEntries()
   }
 
   async function deleteEntry(entry: FileEntry) {
-    await filesApi.deleteFile(entry.path, entry.isDir)
+    await filesApi.deleteFile(entry.path, entry.isDir, workspaceRoot.value)
     if (previewFile.value && isAffected(previewFile.value.path, entry.path, entry.isDir)) {
       previewFile.value = null
     }
@@ -244,7 +245,7 @@ export const useFilesStore = defineStore('files', () => {
   async function renameEntry(entry: FileEntry, newName: string) {
     const parentPath = entry.path.includes('/') ? entry.path.slice(0, entry.path.lastIndexOf('/')) : ''
     const newPath = parentPath ? `${parentPath}/${newName}` : newName
-    await filesApi.renameFile(entry.path, newPath)
+    await filesApi.renameFile(entry.path, newPath, workspaceRoot.value)
     if (previewFile.value && isAffected(previewFile.value.path, entry.path, entry.isDir)) {
       previewFile.value = null
     }
@@ -255,12 +256,12 @@ export const useFilesStore = defineStore('files', () => {
   }
 
   async function copyEntry(entry: FileEntry, destPath: string) {
-    await filesApi.copyFile(entry.path, destPath)
+    await filesApi.copyFile(entry.path, destPath, workspaceRoot.value)
     await fetchEntries()
   }
 
   async function uploadFiles(files: File[]) {
-    await filesApi.uploadFiles(currentPath.value, files)
+    await filesApi.uploadFiles(currentPath.value, files, workspaceRoot.value)
     await fetchEntries()
   }
 
@@ -279,7 +280,7 @@ export const useFilesStore = defineStore('files', () => {
   })
 
   return {
-    currentPath, entries, loading, sortBy, sortOrder,
+    currentPath, entries, loading, workspaceRoot, sortBy, sortOrder,
     editingFile, previewFile,
     pathSegments, sortedEntries, hasUnsavedChanges,
     fetchEntries, navigateTo, navigateUp,
diff --git a/packages/client/src/stores/hermes/group-chat.ts b/packages/client/src/stores/hermes/group-chat.ts
index df411c94..e35db326 100644
--- a/packages/client/src/stores/hermes/group-chat.ts
+++ b/packages/client/src/stores/hermes/group-chat.ts
@@ -131,6 +131,12 @@ export interface GroupPendingApproval {
 export const useGroupChatStore = defineStore('groupChat', () => {
     // ─── State ─────────────────────────────────────────────
     const connected = ref(false)
+    // ─── Unread tracking (in-memory, resets on refresh) ──────────────
+    // Tracks per-room unread message count + last message preview for
+    // rooms that are NOT currently active. Cleared on joinRoom.
+    const unreadCounts = ref<Record<string, number>>({})
+    const lastMessageMap = ref<Record<string, { content: string; senderName: string; ts: number }>>({})
+
     const currentRoomId = ref<string | null>(null)
     const rooms = ref<RoomInfo[]>([])
     const messages = ref<ChatMessage[]>([])
@@ -345,7 +351,33 @@ const currentUserAvatar = ref('')
             connected.value = false
         })
 
+        // Server pushes initial per-room state (last message preview) on connect
+        // after auto-joining the user's visible rooms. Unread count starts at 0
+        // (in-memory; resets on refresh — accepted). Subsequent live `message`
+        // events accumulate unread for non-active rooms.
+        socket.on('unread_sync', (sync: { roomId: string; unreadCount: number; lastMessage: { content: string; senderName: string; ts: number } | null }[]) => {
+            if (!Array.isArray(sync)) return
+            const nextLast = { ...lastMessageMap.value }
+            for (const item of sync) {
+                if (!item?.roomId) continue
+                if (item.lastMessage) nextLast[item.roomId] = item.lastMessage
+            }
+            lastMessageMap.value = nextLast
+        })
+
         socket.on('message', (msg: ChatMessage) => {
+            // Unread tracking: only count agent (assistant) replies for non-active rooms.
+            // User-sent messages do not generate notifications.
+            if (msg.roomId !== currentRoomId.value && msg.role === 'assistant') {
+                unreadCounts.value = {
+                    ...unreadCounts.value,
+                    [msg.roomId]: (unreadCounts.value[msg.roomId] ?? 0) + 1,
+                }
+                lastMessageMap.value = {
+                    ...lastMessageMap.value,
+                    [msg.roomId]: { content: msg.content ?? '', senderName: msg.senderName ?? '', ts: Date.now() },
+                }
+            }
             if (msg.roomId === currentRoomId.value) {
                 const idx = messages.value.findIndex(m => m.id === msg.id)
                 const existing = idx >= 0 ? messages.value[idx] : null
@@ -568,6 +600,12 @@ const currentUserAvatar = ref('')
     async function joinRoom(roomId: string) {
         isJoining.value = true
         error.value = null
+        // Clear unread for the room being entered
+        if (unreadCounts.value[roomId]) {
+            const next = { ...unreadCounts.value }
+            delete next[roomId]
+            unreadCounts.value = next
+        }
 
         try {
             const res = await getRoomDetail(roomId)
@@ -810,6 +848,19 @@ const currentUserAvatar = ref('')
         pendingApprovals.value = new Map(pendingApprovals.value)
     }
 
+    function clearRoomUnread(roomId: string) {
+        if (!unreadCounts.value[roomId]) return
+        const next = { ...unreadCounts.value }
+        delete next[roomId]
+        unreadCounts.value = next
+    }
+    function clearAllUnread() {
+        unreadCounts.value = {}
+    }
+    function getRoomUnread(roomId: string): number {
+        return unreadCounts.value[roomId] ?? 0
+    }
+
     return {
         // State
         connected,
@@ -857,6 +908,12 @@ const currentUserAvatar = ref('')
         deleteRoom,
         cloneRoom,
         clearCurrentRoomContext,
+        // Unread tracking state (exported for cockpit notify aggregation)
+        unreadCounts,
+        lastMessageMap,
+        clearRoomUnread,
+        clearAllUnread,
+        getRoomUnread,
         loadAgents,
         addAgentToRoom,
         removeAgentFromRoom,
diff --git a/packages/client/src/stores/hermes/kanban.ts b/packages/client/src/stores/hermes/kanban.ts
index d3358560..b0f49c3f 100644
--- a/packages/client/src/stores/hermes/kanban.ts
+++ b/packages/client/src/stores/hermes/kanban.ts
@@ -1,7 +1,8 @@
 import { defineStore } from 'pinia'
 import { computed, ref } from 'vue'
 import * as kanbanApi from '@/api/hermes/kanban'
-import type { KanbanTask, KanbanStats, KanbanAssignee, KanbanBoard, KanbanCapabilities, KanbanDiagnosticsOptions, KanbanDispatchOptions, KanbanBulkUpdateRequest, KanbanCreateRequest } from '@/api/hermes/kanban'
+import { probeAuthViaRest } from '@/api/client'
+import type { KanbanTask, KanbanTaskStatus, KanbanStats, KanbanAssignee, KanbanBoard, KanbanCapabilities, KanbanDiagnosticsOptions, KanbanDispatchOptions, KanbanBulkUpdateRequest, KanbanCreateRequest, KanbanDiagnostic, KanbanOrchestration, KanbanAttachment } from '@/api/hermes/kanban'
 
 export const KANBAN_SELECTED_BOARD_STORAGE_KEY = 'hermes.kanban.selectedBoard'
 export const DEFAULT_KANBAN_BOARD = 'default'
@@ -42,10 +43,17 @@ export const useKanbanStore = defineStore('kanban', () => {
   const boardsLoading = ref(false)
   const boardWarning = ref<string | null>(null)
 
+  // Advanced features state
+  const diagnostics = ref<KanbanDiagnostic[]>([])
+  const orchestration = ref<KanbanOrchestration | null>(null)
+  const attachments = ref<Record<string, KanbanAttachment[]>>({})
+
   const selectedBoard = ref(normalizeBoardSlug(safeStorageGet(KANBAN_SELECTED_BOARD_STORAGE_KEY)))
 
   const filterStatus = ref<string | null>(null)
   const filterAssignee = ref<string | null>(null)
+  const selectedIds = ref<Set<string>>(new Set())
+  const searchQuery = ref('')
 
   let boardGeneration = 0
   let boardsRequestSeq = 0
@@ -58,6 +66,13 @@ export const useKanbanStore = defineStore('kanban', () => {
   let eventReconnectTimer: ReturnType<typeof setTimeout> | null = null
   let eventRefreshTimer: ReturnType<typeof setTimeout> | null = null
   let eventStreamEnabled = false
+  let eventConsecutiveFailures = 0
+  let eventMaxBackoffMs = 30_000
+  // Failures beyond this threshold trigger an authoritative REST auth probe.
+  // The WebSocket close code (1006) cannot distinguish auth rejection from
+  // transient network/upgrade issues, so we defer to REST for the verdict.
+  const eventAuthProbeThreshold = 3
+  let eventAuthProbeInFlight = false
 
   const activeBoards = computed(() => {
     const visible = boards.value.filter(board => !board.archived)
@@ -140,6 +155,8 @@ export const useKanbanStore = defineStore('kanban', () => {
   function stopEventStream() {
     eventStreamEnabled = false
     eventStreamSeq++
+    eventConsecutiveFailures = 0
+    eventAuthProbeInFlight = false
     clearEventTimers()
     closeEventSocket()
   }
@@ -154,10 +171,11 @@ export const useKanbanStore = defineStore('kanban', () => {
 
   function scheduleEventReconnect(board: string, generation: number, seq: number) {
     if (eventReconnectTimer) clearTimeout(eventReconnectTimer)
+    const delayMs = Math.min(3000 * Math.pow(2, eventConsecutiveFailures), eventMaxBackoffMs)
     eventReconnectTimer = setTimeout(() => {
       if (!eventStreamEnabled || seq !== eventStreamSeq || generation !== boardGeneration || board !== selectedBoard.value) return
       connectEventStream(board, generation, seq)
-    }, 3000)
+    }, delayMs)
   }
 
   function connectEventStream(board: string, generation: number, seq: number) {
@@ -167,6 +185,7 @@ export const useKanbanStore = defineStore('kanban', () => {
       socket = kanbanApi.openKanbanEventStream({ board })
     } catch (err) {
       console.error('Failed to open kanban event stream:', err)
+      eventConsecutiveFailures++
       scheduleEventReconnect(board, generation, seq)
       return
     }
@@ -183,11 +202,44 @@ export const useKanbanStore = defineStore('kanban', () => {
     socket.onerror = () => {
       if (eventSocket === socket) console.error('Kanban event stream error')
     }
-    socket.onclose = () => {
-      if (eventSocket === socket) {
-        eventSocket = null
-        scheduleEventReconnect(board, generation, seq)
+    socket.onclose = (ev: CloseEvent) => {
+      if (eventSocket !== socket) return
+      eventSocket = null
+      eventConsecutiveFailures++
+
+      // 1006 = abnormal close (no close frame — server rejected the upgrade).
+      // After sustained failures, ask REST for an authoritative verdict: the
+      // WebSocket close code alone cannot tell auth rejection apart from
+      // transient network issues. The probe returns false only on a confirmed
+      // 401 (auth expired); 200 or inconclusive errors keep us reconnecting.
+      if (ev.code === 1006 && eventConsecutiveFailures >= eventAuthProbeThreshold && !eventAuthProbeInFlight) {
+        eventAuthProbeInFlight = true
+        console.warn(
+          `Kanban event stream closed abnormally (code=${ev.code}) after ${eventConsecutiveFailures} ` +
+          `consecutive failures. Probing REST to determine whether auth has expired.`,
+        )
+        void probeAuthViaRest().then((ok) => {
+          eventAuthProbeInFlight = false
+          if (!ok) {
+            // REST returned 401 — auth has expired. The probe already cleared
+            // the token and redirected to login. Stop the WS reconnect loop.
+            console.warn('Kanban event stream: REST probe confirmed auth failure; stopping reconnect.')
+            return
+          }
+          // REST is healthy (or the probe itself was inconclusive, e.g. 5xx /
+          // network error). Treat as a WS-only channel problem and resume
+          // reconnecting, but only if the stream is still meant to be active.
+          if (eventStreamEnabled) scheduleEventReconnect(board, generation, seq)
+        })
+        return
       }
+
+      scheduleEventReconnect(board, generation, seq)
+    }
+    // Reset failure counter once the WebSocket is open — a successful handshake
+    // proves the credentials and connection are good.
+    socket.onopen = () => {
+      eventConsecutiveFailures = 0
     }
   }
 
@@ -253,7 +305,7 @@ export const useKanbanStore = defineStore('kanban', () => {
     try {
       const nextBoards = await kanbanApi.listBoards({ includeArchived })
       if (seq !== boardsRequestSeq) return
-      boards.value = nextBoards
+      boards.value = nextBoards || []
       const resolved = resolveAvailableBoard(selectedBoard.value)
       if (resolved !== selectedBoard.value) recoverSelectedBoard(selectedBoard.value)
     } catch (err) {
@@ -271,6 +323,10 @@ export const useKanbanStore = defineStore('kanban', () => {
     }
   }
 
+  async function refreshAll() {
+    await Promise.all([fetchBoards(), fetchTasks(), fetchStats(), fetchAssignees()])
+  }
+
   async function createBoard(data: { slug: string; name?: string; description?: string; icon?: string; color?: string; switchCurrent?: boolean }) {
     const board = await kanbanApi.createBoard(data)
     await fetchBoards()
@@ -299,7 +355,7 @@ export const useKanbanStore = defineStore('kanban', () => {
         assignee: filterAssignee.value || undefined,
         includeArchived: true,
       })
-      if (isCurrentRequest(seq, generation, board, tasksRequestSeq)) tasks.value = nextTasks
+      if (isCurrentRequest(seq, generation, board, tasksRequestSeq)) tasks.value = nextTasks || []
     } catch (err) {
       if (isCurrentRequest(seq, generation, board, tasksRequestSeq)) console.error('Failed to fetch kanban tasks:', err)
     } finally {
@@ -311,7 +367,7 @@ export const useKanbanStore = defineStore('kanban', () => {
     const { seq, generation, board } = nextRequestContext(() => ++statsRequestSeq)
     try {
       const nextStats = await kanbanApi.getStats({ board })
-      if (isCurrentRequest(seq, generation, board, statsRequestSeq)) stats.value = nextStats
+      if (isCurrentRequest(seq, generation, board, statsRequestSeq)) stats.value = nextStats || null
     } catch (err) {
       if (isCurrentRequest(seq, generation, board, statsRequestSeq)) console.error('Failed to fetch kanban stats:', err)
     }
@@ -321,7 +377,7 @@ export const useKanbanStore = defineStore('kanban', () => {
     const { seq, generation, board } = nextRequestContext(() => ++assigneesRequestSeq)
     try {
       const nextAssignees = await kanbanApi.getAssignees({ board })
-      if (isCurrentRequest(seq, generation, board, assigneesRequestSeq)) assignees.value = nextAssignees
+      if (isCurrentRequest(seq, generation, board, assigneesRequestSeq)) assignees.value = nextAssignees || []
     } catch (err) {
       if (isCurrentRequest(seq, generation, board, assigneesRequestSeq)) console.error('Failed to fetch kanban assignees:', err)
     }
@@ -461,8 +517,209 @@ export const useKanbanStore = defineStore('kanban', () => {
     else filterAssignee.value = value
   }
 
-  async function refreshAll() {
-    await Promise.all([fetchBoards(), fetchTasks(), fetchStats(), fetchAssignees()])
+  function setBoard(board?: string | null) {
+    return setSelectedBoard(board)
+  }
+
+  function setStatusFilter(status?: string) {
+    filterStatus.value = status || null
+    fetchTasks()
+  }
+
+  function setAssigneeFilter(assignee?: string) {
+    filterAssignee.value = assignee || null
+    fetchTasks()
+  }
+
+  function setSearchQuery(query: string) {
+    searchQuery.value = query
+  }
+
+  function clearSelection() {
+    selectedIds.value.clear()
+  }
+
+  function toggleSelection(taskId: string) {
+    if (selectedIds.value.has(taskId)) {
+      selectedIds.value.delete(taskId)
+    } else {
+      selectedIds.value.add(taskId)
+    }
+  }
+
+  async function moveTask(taskId: string, status: string) {
+    const board = selectedBoard.value
+    switch (status) {
+      case 'done':
+        await kanbanApi.completeTasks([taskId], undefined, { board })
+        break
+      case 'blocked':
+        await kanbanApi.blockTask(taskId, '', { board })
+        break
+      case 'ready':
+        // ready is the canonical "unblock" target
+        await kanbanApi.unblockTasks([taskId], { board })
+        break
+      case 'todo':
+      case 'scheduled':
+      case 'triage':
+      case 'archived':
+        // These statuses need an explicit PATCH via bulkUpdate, NOT unblock
+        // (unblock always sets status=ready server-side, which would desync)
+        await kanbanApi.bulkUpdateTasks({ ids: [taskId], status: status as any }, { board })
+        break
+      default:
+        await kanbanApi.bulkUpdateTasks({ ids: [taskId], status: status as any }, { board })
+    }
+    if (board === selectedBoard.value) {
+      const task = tasks.value.find(t => t.id === taskId)
+      if (task) task.status = status as KanbanTaskStatus
+      await Promise.all([fetchStats(), fetchBoards()])
+    }
+  }
+
+  async function bulkComplete(taskIds: string[]) {
+    return completeTasks(taskIds)
+  }
+
+  async function bulkBlock(taskIds: string[]) {
+    const board = selectedBoard.value
+    for (const id of taskIds) {
+      await kanbanApi.blockTask(id, '', { board })
+    }
+    if (board === selectedBoard.value) {
+      for (const id of taskIds) {
+        const task = tasks.value.find(t => t.id === id)
+        if (task) task.status = 'blocked'
+      }
+      await Promise.all([fetchStats(), fetchBoards()])
+    }
+  }
+
+  async function bulkUnblock(taskIds: string[]) {
+    return unblockTasks(taskIds)
+  }
+
+  async function bulkArchive(taskIds: string[]) {
+    const board = selectedBoard.value
+    await kanbanApi.bulkUpdateTasks({ ids: taskIds, archive: true }, { board })
+    if (board === selectedBoard.value) {
+      for (const id of taskIds) {
+        const task = tasks.value.find(t => t.id === id)
+        if (task) task.status = 'archived'
+      }
+      await Promise.all([fetchStats(), fetchBoards()])
+    }
+  }
+
+  async function deleteTask(taskId: string) {
+    const board = selectedBoard.value
+    await kanbanApi.deleteTask(taskId, { board })
+    if (board === selectedBoard.value) {
+      tasks.value = tasks.value.filter(t => t.id !== taskId)
+      selectedIds.value.delete(taskId)
+      await Promise.all([fetchStats(), fetchBoards(), fetchAssignees()])
+    }
+  }
+
+  async function patchTask(taskId: string, patch: kanbanApi.KanbanTaskPatch) {
+    const board = selectedBoard.value
+    const updated = await kanbanApi.patchTask(taskId, patch, { board })
+    if (board === selectedBoard.value) {
+      const idx = tasks.value.findIndex(t => t.id === taskId)
+      if (idx >= 0) tasks.value[idx] = updated
+      await Promise.all([fetchStats(), fetchBoards(), fetchAssignees()])
+    }
+    return updated
+  }
+
+  async function decomposeTask(taskId: string, author?: string) {
+    const board = selectedBoard.value
+    const result = await kanbanApi.decomposeTask(taskId, { board, author })
+    if (board === selectedBoard.value) {
+      await Promise.all([fetchTasks(true), fetchStats(), fetchBoards()])
+    }
+    return result
+  }
+
+  async function fetchDiagnostics(opts: Omit<KanbanDiagnosticsOptions, 'board'> = {}) {
+    assertCapability('diagnostics')
+    const board = selectedBoard.value
+    try {
+      const result = await kanbanApi.getDiagnostics({ ...opts, board })
+      if (board === selectedBoard.value) diagnostics.value = result
+    } catch (err) {
+      console.error('Failed to fetch kanban diagnostics:', err)
+    }
+  }
+
+  async function fetchOrchestration() {
+    const board = selectedBoard.value
+    try {
+      const result = await kanbanApi.getOrchestration({ board })
+      if (board === selectedBoard.value) orchestration.value = result
+    } catch (err) {
+      console.error('Failed to fetch kanban orchestration:', err)
+    }
+  }
+
+  async function saveOrchestrationSettings(patch: kanbanApi.KanbanOrchestrationPatch) {
+    const board = selectedBoard.value
+    const result = await kanbanApi.saveOrchestration(patch, { board })
+    if (board === selectedBoard.value) orchestration.value = result
+    return result
+  }
+
+  async function fetchAttachments(taskId: string) {
+    const board = selectedBoard.value
+    try {
+      const result = await kanbanApi.listAttachments(taskId, { board })
+      if (board === selectedBoard.value) attachments.value[taskId] = result
+    } catch (err) {
+      console.error('Failed to fetch attachments:', err)
+    }
+  }
+
+  async function uploadTaskAttachment(taskId: string, file: File) {
+    const board = selectedBoard.value
+    const result = await kanbanApi.uploadAttachment(taskId, file, { board, uploadedBy: 'dashboard' })
+    if (board === selectedBoard.value) {
+      const list = attachments.value[taskId] || []
+      list.push(result)
+      attachments.value[taskId] = list
+    }
+    return result
+  }
+
+  async function deleteTaskAttachment(taskId: string, attachmentId: number) {
+    const board = selectedBoard.value
+    await kanbanApi.deleteAttachment(attachmentId, { board })
+    if (board === selectedBoard.value) {
+      const list = attachments.value[taskId] || []
+      attachments.value[taskId] = list.filter(a => a.id !== attachmentId)
+    }
+  }
+
+  function connectEvents() {
+    return startEventStream()
+  }
+
+  async function downloadTaskAttachment(attachmentId: number): Promise<Blob> {
+    return kanbanApi.downloadAttachment(attachmentId, { board: selectedBoard.value })
+  }
+
+  async function saveProfileDescription(name: string, description: string) {
+    const board = selectedBoard.value
+    const result = await kanbanApi.saveProfileDescription(name, { description }, { board })
+    if (board === selectedBoard.value) await fetchOrchestration()
+    return result
+  }
+
+  async function autoGenerateProfileDescription(name: string, overwrite = false) {
+    const board = selectedBoard.value
+    const result = await kanbanApi.autoGenerateProfileDescription(name, { board, overwrite })
+    if (board === selectedBoard.value) await fetchOrchestration()
+    return result
   }
 
   return {
@@ -479,6 +736,12 @@ export const useKanbanStore = defineStore('kanban', () => {
     selectedBoard,
     filterStatus,
     filterAssignee,
+    selectedIds,
+    searchQuery,
+    // Advanced features
+    diagnostics,
+    orchestration,
+    attachments,
     fetchBoards,
     fetchCapabilities,
     fetchTasks,
@@ -503,11 +766,36 @@ export const useKanbanStore = defineStore('kanban', () => {
     dispatch,
     setFilter,
     setSelectedBoard,
+    setBoard,
+    setStatusFilter,
+    setAssigneeFilter,
+    setSearchQuery,
+    clearSelection,
+    toggleSelection,
+    moveTask,
+    bulkComplete,
+    bulkBlock,
+    bulkUnblock,
+    bulkArchive,
+    connectEvents,
+    deleteTask,
+    patchTask,
+    decomposeTask,
     startEventStream,
     stopEventStream,
     recoverSelectedBoard,
     resolveAvailableBoard,
     clearBoardScopedState,
     refreshAll,
+    // Advanced methods
+    fetchDiagnostics,
+    fetchOrchestration,
+    saveOrchestrationSettings,
+    downloadTaskAttachment,
+    saveProfileDescription,
+    autoGenerateProfileDescription,
+    fetchAttachments,
+    uploadTaskAttachment,
+    deleteTaskAttachment,
   }
 })
diff --git a/packages/client/src/styles/variables.scss b/packages/client/src/styles/variables.scss
index b38d08ee..ccbc436c 100644
--- a/packages/client/src/styles/variables.scss
+++ b/packages/client/src/styles/variables.scss
@@ -45,7 +45,7 @@
   --text-on-overlay: #ffffff;
   --accent-info: #4a90d9;
 
-  // RGB components (for rgba() usage)
+  // HERMES_CUSTOM[Branding] BEGIN: RGB component variables for rgba() usage
   --accent-primary-rgb: 51, 51, 51;
   --accent-hover-rgb: 26, 26, 26;
   --text-primary-rgb: 26, 26, 26;
@@ -54,6 +54,7 @@
   --error-rgb: 198, 40, 40;
   --warning-rgb: 245, 127, 23;
   --accent-info-rgb: 74, 144, 217;
+  // HERMES_CUSTOM[Branding] END
 }
 
 .dark {
@@ -97,7 +98,7 @@
   --text-on-overlay: #ffffff;
   --accent-info: #6ba3d6;
 
-  // RGB components
+  // HERMES_CUSTOM[Branding] BEGIN: RGB component variables for dark theme
   --accent-primary-rgb: 240, 240, 240;
   --accent-hover-rgb: 245, 245, 245;
   --text-primary-rgb: 240, 240, 240;
@@ -106,6 +107,7 @@
   --error-rgb: 239, 83, 80;
   --warning-rgb: 255, 183, 77;
   --accent-info-rgb: 107, 163, 214;
+  // HERMES_CUSTOM[Branding] END
 }
 
 .comic {
@@ -185,3 +187,19 @@ $radius-lg: 14px;
 // Transition
 $transition-fast: 0.15s ease;
 $transition-normal: 0.25s ease;
+
+// Utility
+$text-on-accent: var(--text-on-accent);
+$text-on-overlay: var(--text-on-overlay);
+$accent-info: var(--accent-info);
+
+// HERMES_CUSTOM[Branding] BEGIN: RGB SCSS variables (delegate to CSS custom properties)
+$accent-primary-rgb: var(--accent-primary-rgb);
+$accent-hover-rgb: var(--accent-hover-rgb);
+$text-primary-rgb: var(--text-primary-rgb);
+$text-muted-rgb: var(--text-muted-rgb);
+$success-rgb: var(--success-rgb);
+$error-rgb: var(--error-rgb);
+$warning-rgb: var(--warning-rgb);
+$accent-info-rgb: var(--accent-info-rgb);
+// HERMES_CUSTOM[Branding] END
diff --git a/packages/client/src/views/LoginView.vue b/packages/client/src/views/LoginView.vue
index 9f893c48..0087d6cd 100644
--- a/packages/client/src/views/LoginView.vue
+++ b/packages/client/src/views/LoginView.vue
@@ -2,56 +2,158 @@
 import { ref, onMounted } from "vue";
 import { useRouter } from "vue-router";
 import { useI18n } from "vue-i18n";
-import { setApiKey, hasApiKey } from "@/api/client";
-import { fetchAuthStatus, loginWithPassword } from "@/api/auth";
+import { useAuthStore } from "@/stores/hermes/auth";
 
 const { t } = useI18n();
 const router = useRouter();
+const authStore = useAuthStore();
+
+const activeTab = ref<'matrix' | 'local'>('matrix');
+
+// Matrix login fields
+const homeserverUrl = ref("http://localhost:8008");
+const matrixUsername = ref("testuser1:matrix.test");
+const matrixPassword = ref("");
+
+// Local login fields
+const localUsername = ref("");
+const localPassword = ref("");
+
+const matrixRememberMe = ref(false);
+const localRememberMe = ref(false);
 
-const username = ref("");
-const password = ref("");
 const loading = ref(false);
 const errorMsg = ref("");
-const showLockResetHint = ref(false);
 
-// If already has a key, try to go to main page
-if (hasApiKey()) {
-  router.replace("/hermes/chat");
+// Remember-me localStorage keys
+const STORAGE_KEYS = {
+  matrix: { user: 'matrix_remembered_username', pass: 'matrix_remembered_password' },
+  local: { user: 'local_remembered_username', pass: 'local_remembered_password' },
+} as const;
+
+function encodePassword(pw: string): string {
+  try { return btoa(pw); } catch { return ''; }
 }
 
-onMounted(async () => {
-  try {
-    await fetchAuthStatus();
-  } catch {
-    // Login remains available; the submit request will surface connection errors.
+function decodePassword(encoded: string): string {
+  try { return atob(encoded); } catch { return ''; }
+}
+
+function loadSavedCredentials(form: 'matrix' | 'local'): boolean {
+  const keys = STORAGE_KEYS[form];
+  const savedUser = localStorage.getItem(keys.user);
+  const savedPass = localStorage.getItem(keys.pass);
+  if (savedUser !== null && savedPass !== null) {
+    if (form === 'matrix') {
+      matrixUsername.value = savedUser;
+      matrixPassword.value = decodePassword(savedPass);
+      matrixRememberMe.value = true;
+    } else {
+      localUsername.value = savedUser;
+      localPassword.value = decodePassword(savedPass);
+      localRememberMe.value = true;
+    }
+    return true;
   }
+  return false;
+}
+
+function saveCredentials(form: 'matrix' | 'local'): void {
+  const keys = STORAGE_KEYS[form];
+  const username = form === 'matrix' ? matrixUsername.value.trim() : localUsername.value.trim();
+  const password = form === 'matrix' ? matrixPassword.value : localPassword.value;
+  if (username && password) {
+    localStorage.setItem(keys.user, username);
+    localStorage.setItem(keys.pass, encodePassword(password));
+  }
+}
+
+function clearSavedCredentials(form: 'matrix' | 'local'): void {
+  const keys = STORAGE_KEYS[form];
+  localStorage.removeItem(keys.user);
+  localStorage.removeItem(keys.pass);
+}
+
+onMounted(() => {
+  const savedHs = localStorage.getItem('matrix_homeserver_url');
+  if (savedHs) homeserverUrl.value = savedHs;
+  loadSavedCredentials('matrix');
+  loadSavedCredentials('local');
 });
 
-async function handleLogin() {
-  await handlePasswordLogin();
+function classifyLoginError(err: any): string {
+  const errcode = err?.errcode || err?.data?.errcode;
+  if (errcode === 'M_FORBIDDEN' || errcode === 'M_UNKNOWN' || err?.httpStatus === 403) {
+    return t("login.invalidCredentials");
+  }
+  const name = err?.name || err?.constructor?.name;
+  const msg = String(err?.message || '');
+  if (name === 'ConnectionError') {
+    return t("login.homeserverConnectionFailed");
+  }
+  if (/Failed to fetch|NetworkError|ERR_CONNECTION|ERR_NETWORK|Load failed/i.test(msg)) {
+    return t("login.backendConnectionFailed");
+  }
+  return msg || t("login.invalidCredentials");
 }
 
-async function handlePasswordLogin() {
-  if (!username.value.trim() || !password.value) {
+async function handleMatrixLogin() {
+  if (!homeserverUrl.value.trim()) {
+    errorMsg.value = t("login.homeserverRequired");
+    return;
+  }
+  if (!matrixUsername.value.trim() || !matrixPassword.value) {
     errorMsg.value = t("login.credentialsRequired");
     return;
   }
 
   loading.value = true;
   errorMsg.value = "";
-  showLockResetHint.value = false;
 
   try {
-    const sessionToken = await loginWithPassword(username.value.trim(), password.value);
-    setApiKey(sessionToken);
-    router.replace("/hermes/chat");
+    await authStore.matrixLogin(
+      homeserverUrl.value.trim(),
+      matrixUsername.value.trim(),
+      matrixPassword.value
+    );
+    localStorage.setItem('matrix_homeserver_url', homeserverUrl.value.trim());
+    if (matrixRememberMe.value) {
+      saveCredentials('matrix');
+    } else {
+      clearSavedCredentials('matrix');
+    }
+    router.replace("/hermes/cockpit");
   } catch (err: any) {
-    if (err.status === 429 || err.status === 503) {
-      errorMsg.value = t("login.tooManyAttempts");
-      showLockResetHint.value = true;
+    errorMsg.value = classifyLoginError(err);
+    clearSavedCredentials('matrix');
+    if (errorMsg.value === t("login.backendConnectionFailed")) {
+      activeTab.value = 'local';
+    }
+  } finally {
+    loading.value = false;
+  }
+}
+
+async function handleLocalLogin() {
+  if (!localUsername.value.trim() || !localPassword.value) {
+    errorMsg.value = t("login.credentialsRequired");
+    return;
+  }
+
+  loading.value = true;
+  errorMsg.value = "";
+
+  try {
+    await authStore.localLogin(localUsername.value.trim(), localPassword.value);
+    if (localRememberMe.value) {
+      saveCredentials('local');
     } else {
-      errorMsg.value = err.message || t("login.invalidCredentials");
+      clearSavedCredentials('local');
     }
+    router.replace("/hermes/cockpit");
+  } catch (err: any) {
+    errorMsg.value = err?.message || t("login.invalidCredentials");
+    clearSavedCredentials('local');
   } finally {
     loading.value = false;
   }
@@ -61,39 +163,70 @@ async function handlePasswordLogin() {
 <template>
   <div class="login-view">
     <div class="login-card">
-      <div class="login-logo">
-        <img src="/logo.png" alt="Hermes" width="80" height="80" />
-      </div>
       <h1 class="login-title">{{ t("login.title") }}</h1>
-      <p class="login-desc">{{ t("login.description") }}</p>
-      <p class="login-default-hint">{{ t("login.defaultCredentialsHint") }}</p>
 
-      <form class="login-form" @submit.prevent="handleLogin">
+      <!-- Matrix Login Form (default) -->
+      <form v-if="activeTab === 'matrix'" class="login-form" @submit.prevent="handleMatrixLogin">
+        <p class="login-desc">{{ t("login.matrixDescription") }}</p>
         <input
-          v-model="username"
+          v-model="homeserverUrl"
+          type="url"
+          class="login-input"
+          :placeholder="t('login.homeserverPlaceholder')"
+          autofocus
+        />
+        <input
+          v-model="matrixUsername"
+          type="text"
+          class="login-input"
+          :placeholder="t('login.matrixUsernamePlaceholder')"
+        />
+        <input
+          v-model="matrixPassword"
+          type="password"
+          class="login-input"
+          :placeholder="t('login.passwordPlaceholder')"
+          @keyup.enter="handleMatrixLogin"
+        />
+        <div v-if="errorMsg" class="login-error">{{ errorMsg }}</div>
+        <label class="login-remember">
+          <input v-model="matrixRememberMe" type="checkbox" />
+          {{ t("login.rememberMe") }}
+        </label>
+        <button type="submit" class="login-btn" :disabled="loading">
+          {{ loading ? "..." : t("login.submit") }}
+        </button>
+      </form>
+
+      <!-- Local Login Form (fallback) -->
+      <form v-else-if="activeTab === 'local'" class="login-form" @submit.prevent="handleLocalLogin">
+        <p class="login-fallback-hint">{{ t("login.fallbackToLocalHint") }}</p>
+        <p class="login-desc">{{ t("login.localDescription") }}</p>
+        <input
+          v-model="localUsername"
           type="text"
           class="login-input"
           :placeholder="t('login.usernamePlaceholder')"
           autofocus
         />
         <input
-          v-model="password"
+          v-model="localPassword"
           type="password"
           class="login-input"
           :placeholder="t('login.passwordPlaceholder')"
-          @keyup.enter="handleLogin"
+          @keyup.enter="handleLocalLogin"
         />
-
         <div v-if="errorMsg" class="login-error">{{ errorMsg }}</div>
-        <div v-if="showLockResetHint" class="login-lock-hint">
-          <span>{{ t("login.lockResetHint") }}</span>
-          <code>hermes-web-ui clear-login-locks --restart</code>
-          <span>{{ t("login.defaultLoginResetHint") }}</span>
-          <code>hermes-web-ui reset-default-login</code>
-        </div>
+        <label class="login-remember">
+          <input v-model="localRememberMe" type="checkbox" />
+          {{ t("login.rememberMe") }}
+        </label>
         <button type="submit" class="login-btn" :disabled="loading">
           {{ loading ? "..." : t("login.submit") }}
         </button>
+        <button type="button" class="login-fallback-link" @click="activeTab = 'matrix'">
+          {{ t("login.useMatrixLogin") }}
+        </button>
       </form>
     </div>
   </div>
@@ -124,10 +257,6 @@ async function handlePasswordLogin() {
   }
 }
 
-.login-logo {
-  margin-bottom: 24px;
-}
-
 .login-title {
   font-size: 26px;
   font-weight: 600;
@@ -142,13 +271,6 @@ async function handlePasswordLogin() {
   line-height: 1.6;
 }
 
-.login-default-hint {
-  margin: 0 0 28px;
-  font-family: $font-code;
-  font-size: 13px;
-  color: $text-secondary;
-}
-
 .login-form {
   display: flex;
   flex-direction: column;
@@ -183,25 +305,6 @@ async function handlePasswordLogin() {
   text-align: left;
 }
 
-.login-lock-hint {
-  padding: 10px 12px;
-  border: 1px solid rgba(var(--warning-rgb), 0.35);
-  border-radius: $radius-sm;
-  background: rgba(var(--warning-rgb), 0.08);
-  color: $text-secondary;
-  font-size: 12px;
-  line-height: 1.5;
-  text-align: left;
-
-  code {
-    display: block;
-    margin-top: 4px;
-    color: $text-primary;
-    font-family: $font-code;
-    word-break: break-all;
-  }
-}
-
 .login-btn {
   width: 100%;
   padding: 14px;
@@ -223,4 +326,45 @@ async function handlePasswordLogin() {
     cursor: not-allowed;
   }
 }
+
+.login-fallback-hint {
+  margin: 0 0 12px;
+  padding: 10px 12px;
+  border: 1px solid rgba(var(--warning-rgb), 0.35);
+  border-radius: $radius-sm;
+  background: rgba(var(--warning-rgb), 0.08);
+  color: $text-secondary;
+  font-size: 13px;
+  line-height: 1.5;
+  text-align: left;
+}
+
+.login-fallback-link {
+  margin-top: 4px;
+  padding: 6px;
+  border: none;
+  background: transparent;
+  color: $text-muted;
+  font-size: 13px;
+  cursor: pointer;
+  text-decoration: underline;
+  transition: color $transition-fast;
+
+  &:hover {
+    color: $text-primary;
+  }
+}
+
+.login-remember {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  font-size: 13px;
+  color: $text-secondary;
+  cursor: pointer;
+
+  input[type="checkbox"] {
+    accent-color: $accent-primary;
+  }
+}
 </style>
diff --git a/packages/client/src/views/hermes/ChatView.vue b/packages/client/src/views/hermes/ChatView.vue
index 029708a6..0490594c 100644
--- a/packages/client/src/views/hermes/ChatView.vue
+++ b/packages/client/src/views/hermes/ChatView.vue
@@ -24,7 +24,7 @@ const routeProfile = computed(() => {
   return typeof value === 'string' && value.trim() ? value : null
 })
 
-const productTitle = 'Hermes Studio'
+const productTitle = 'Swarm Studio'
 const tabTitle = computed(() => {
   if (route.name !== 'hermes.session') return productTitle
   return chatStore.activeSession?.title?.trim() || productTitle
diff --git a/packages/client/src/views/hermes/GroupChatView.vue b/packages/client/src/views/hermes/GroupChatView.vue
index dbe540a7..9e33cd79 100644
--- a/packages/client/src/views/hermes/GroupChatView.vue
+++ b/packages/client/src/views/hermes/GroupChatView.vue
@@ -33,7 +33,7 @@ async function syncRouteRoom() {
 }
 
 onMounted(async () => {
-    store.connect()
+    await store.connect()
     await store.loadRooms()
     await syncRouteRoom()
 })
diff --git a/packages/client/src/views/hermes/SettingsView.vue b/packages/client/src/views/hermes/SettingsView.vue
index b4891bd9..4291e7b2 100644
--- a/packages/client/src/views/hermes/SettingsView.vue
+++ b/packages/client/src/views/hermes/SettingsView.vue
@@ -19,13 +19,14 @@ import ModelSettings from "@/components/hermes/settings/ModelSettings.vue";
 import AccountSettings from "@/components/hermes/settings/AccountSettings.vue";
 import UserManagementSettings from "@/components/hermes/settings/UserManagementSettings.vue";
 import VoiceSettings from "@/components/hermes/settings/VoiceSettings.vue";
-import { isStoredSuperAdmin } from "@/api/client";
+import { useAuthStore } from "@/stores/hermes/auth";
 import { useProfilesStore } from "@/stores/hermes/profiles";
 
 const settingsStore = useSettingsStore();
 const profilesStore = useProfilesStore();
+const authStore = useAuthStore();
 const { t } = useI18n();
-const canManageUsers = isStoredSuperAdmin();
+const canManageUsers = computed(() => authStore.isSuperAdmin);
 const route = useRoute();
 const router = useRouter();
 const activeTab = ref("account");
diff --git a/packages/client/src/views/hermes/WorkflowView.vue b/packages/client/src/views/hermes/WorkflowView.vue
index 5a93fd1a..5a3de64b 100644
--- a/packages/client/src/views/hermes/WorkflowView.vue
+++ b/packages/client/src/views/hermes/WorkflowView.vue
@@ -17,7 +17,7 @@ import FolderPicker from '@/components/hermes/chat/FolderPicker.vue'
 import HistoryMessageList from '@/components/hermes/chat/HistoryMessageList.vue'
 import ProfileAvatar from '@/components/hermes/profiles/ProfileAvatar.vue'
 import PageSidebarNav from '@/components/layout/PageSidebarNav.vue'
-import PageSidebarFooter from '@/components/layout/PageSidebarFooter.vue'
+import { useAuthStore } from '@/stores/hermes/auth'
 import { useAppStore } from '@/stores/hermes/app'
 import { useProfilesStore } from '@/stores/hermes/profiles'
 import { uploadRuntimeFiles } from '@/api/hermes/files'
@@ -64,6 +64,7 @@ import '@vue-flow/minimap/dist/style.css'
 const { t } = useI18n()
 const appStore = useAppStore()
 const profilesStore = useProfilesStore()
+const authStore = useAuthStore()
 const message = useMessage()
 const { screenToFlowCoordinate, getViewport, setViewport } = useVueFlow('hermes-workflow')
 const defaultViewport: WorkflowViewport = { x: 80, y: 80, zoom: 0.75 }
@@ -1651,7 +1652,16 @@ function nodeColor(node: { data: WorkflowAgentNodeData }) {
           </NPopconfirm>
         </button>
       </div>
-      <PageSidebarFooter v-if="showWorkflowSidebar" />
+      <div v-if="showWorkflowSidebar" class="page-sidebar-bottom">
+        <button class="page-sidebar-menu-btn" type="button" @click="authStore.logout()">
+          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
+            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
+            <polyline points="16 17 21 12 16 7" />
+            <line x1="21" y1="12" x2="9" y2="12" />
+          </svg>
+          <span>{{ t('sidebar.logout') }}</span>
+        </button>
+      </div>
     </aside>
 
     <main class="workflow-main">
@@ -2549,4 +2559,46 @@ function nodeColor(node: { data: WorkflowAgentNodeData }) {
     display: none;
   }
 }
+
+.page-sidebar-bottom {
+  flex-shrink: 0;
+  padding: 10px 12px;
+  display: flex;
+  align-items: center;
+  gap: 8px;
+}
+
+.page-sidebar-menu-btn {
+  flex: 1 1 auto;
+  width: auto;
+  min-width: 0;
+  height: 36px;
+  border: none;
+  border-radius: $radius-sm;
+  background: transparent;
+  color: $text-secondary;
+  display: inline-flex;
+  align-items: center;
+  justify-content: flex-start;
+  gap: 8px;
+  padding: 8px 10px;
+  cursor: pointer;
+  transition:
+    background-color $transition-fast,
+    color $transition-fast;
+
+  &:hover {
+    background: rgba(var(--accent-primary-rgb), 0.06);
+    color: $text-primary;
+  }
+
+  span {
+    min-width: 0;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+    font-size: 13px;
+    line-height: 18px;
+  }
+}
 </style>
diff --git a/packages/desktop/build/installer.nsh b/packages/desktop/build/installer.nsh
index b8d10b07..2c43af24 100644
--- a/packages/desktop/build/installer.nsh
+++ b/packages/desktop/build/installer.nsh
@@ -1,7 +1,7 @@
 !macro customInit
-  IfFileExists "$INSTDIR\Hermes Studio.exe" 0 hermesStudioStopDone
-    DetailPrint "Stopping Hermes Studio..."
-    nsExec::ExecToLog '"$INSTDIR\Hermes Studio.exe" --quit'
+  IfFileExists "$INSTDIR\SwarmStudio.exe" 0 hermesStudioStopDone
+    DetailPrint "Stopping SwarmStudio..."
+    nsExec::ExecToLog '"$INSTDIR\SwarmStudio.exe" --quit'
     Sleep 2000
 
     InitPluginsDir
@@ -9,7 +9,7 @@
     FileWrite $0 "$$ErrorActionPreference = 'SilentlyContinue'$\r$\n"
     FileWrite $0 "$$target = [System.IO.Path]::GetFullPath($$env:HERMES_STUDIO_EXE)$\r$\n"
     FileWrite $0 "function Get-HermesStudioProcess {$\r$\n"
-    FileWrite $0 "  Get-CimInstance Win32_Process -Filter $\"Name = 'Hermes Studio.exe'$\" | Where-Object {$\r$\n"
+    FileWrite $0 "  Get-CimInstance Win32_Process -Filter $\"Name = 'SwarmStudio.exe'$\" | Where-Object {$\r$\n"
     FileWrite $0 "    try { [System.IO.Path]::GetFullPath($$_.ExecutablePath) -ieq $$target } catch { $$false }$\r$\n"
     FileWrite $0 "  }$\r$\n"
     FileWrite $0 "}$\r$\n"
@@ -31,9 +31,9 @@
     FileWrite $0 "exit 1$\r$\n"
     FileClose $0
 
-    System::Call 'kernel32::SetEnvironmentVariable(t "HERMES_STUDIO_EXE", t "$INSTDIR\Hermes Studio.exe") i .r0'
+    System::Call 'kernel32::SetEnvironmentVariable(t "HERMES_STUDIO_EXE", t "$INSTDIR\SwarmStudio.exe") i .r0'
     nsExec::ExecToLog '"$SYSDIR\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -ExecutionPolicy Bypass -File "$PLUGINSDIR\stop-hermes-studio.ps1"'
     System::Call 'kernel32::SetEnvironmentVariable(t "HERMES_STUDIO_EXE", t "") i .r0'
-    nsExec::ExecToLog 'taskkill.exe /IM "Hermes Studio.exe" /T /F'
+    nsExec::ExecToLog 'taskkill.exe /IM "SwarmStudio.exe" /T /F'
   hermesStudioStopDone:
 !macroend
diff --git a/packages/desktop/build/runtime-release.json b/packages/desktop/build/runtime-release.json
index c5b3b64a..b2834082 100644
--- a/packages/desktop/build/runtime-release.json
+++ b/packages/desktop/build/runtime-release.json
@@ -1,3 +1,3 @@
 {
-  "tag": "hermes-0.15.2-runtime"
+  "tag": "hermes-0.17.0-runtime"
 }
diff --git a/packages/desktop/electron-builder.yml b/packages/desktop/electron-builder.yml
index 0e4fb0d1..1577dd79 100644
--- a/packages/desktop/electron-builder.yml
+++ b/packages/desktop/electron-builder.yml
@@ -1,5 +1,5 @@
-appId: com.hermeswebui.studio
-productName: Hermes Studio
+appId: com.swarmstudio.app
+productName: SwarmStudio
 copyright: Copyright © 2026
 
 directories:
@@ -41,12 +41,59 @@ extraResources:
       - "bin/**"
       - "dist/**"
       - "node_modules/**"
-      # Drop other-platform node-pty prebuilds (saves ~45MB)
-      - "!node_modules/node-pty/prebuilds/!(${platform}-${arch})/**"
+      # 排除 Vite 缓存 (860MB+ 的 deps_temp_* 缓存,绝不能进生产包)
+      - "!node_modules/.vite/**"
+      - "!node_modules/.tmp/**"
+      # Drop other-platform node-pty prebuilds (saves ~58MB)
+      - "!node_modules/node-pty/prebuilds/win32-*/**"
+      - "!node_modules/node-pty/prebuilds/linux-*/**"
+      - "!node_modules/node-pty/prebuilds/darwin-x64/**"
       - "!node_modules/node-pty/build/**"
+      - "!node_modules/node-pty/third_party/**"
+      - "!node_modules/node-pty/deps/**"
+      - "!node_modules/node-pty/src/**"
       - "!packages/desktop/**"
       - "!**/{.git,.github,docs,tests,playwright.config.ts,README*,scripts,*.map}"
       - "!node_modules/**/*.md"
+      # 瘦身:排除构建工具/开发依赖(运行时不需要,client 已 bundle,server 只 external 少数包)
+      - "!node_modules/{typescript,vue-tsc,@tsoa,@rolldown,esbuild,@esbuild,playwright-core,playwright,rollup,vite,@vitejs}/**"
+      - "!node_modules/{@types,.bin}/**"
+      # 瘦身:client 库源码已 bundle 进 dist/client,运行时 node_modules 只需 server external 包
+      # (node-pty/iconv-lite/keygrip)。排除大型 client 库源码。
+      - "!node_modules/{monaco-editor,naive-ui,mermaid,date-fns,js-tiktoken,es-toolkit,@vue,cytoscape-fcose,highlight.js}/**"
+      - "!node_modules/{katex,matrix-widget-api,linkifyjs,markdown-it,markdown-it-katex}/**"
+      - "!node_modules/{@vueuse,vue,vue-router,pinia,vue-i18n,@intlify}/**"
+      - "!node_modules/{lightningcss,lightningcss-darwin-arm64,lightningcss-darwin-x64,lightningcss-linux-x64-gnu,lightningcss-win32-x64-msvc}/**"
+      # 瘦身:测试框架 + 已 bundle 进 client 的库源码
+      - "!node_modules/{vitest,@vitest,jsdom,sass,sass-embedded,@vscode}/**"
+      - "!node_modules/{cytoscape,cytoscape-fcose,cytoscape-cose-bilkent,cytoscape-cxttap,cytoscape-klay,cytoscape-popper,cytoscape-dom-node}/**"
+      - "!node_modules/{rxjs,@mermaid-js,@babel,@rollup}/**"
+      # 瘦身第二轮:排除已 bundle 的 client 库源码(server esbuild 打包后运行时不需要)
+      - "!node_modules/{matrix-js-sdk,@matrix-org,lodash,lodash-es}/**"
+      - "!node_modules/{handlebars,@multiavatar,dayjs,axios,d3}/**"
+      - "!node_modules/{css-tree,cssstyle,css-render,csstype,@asamuzakjp}/**"
+      # 排除开发工具
+      - "!node_modules/{vite-node,uglify-js,js-beautify,nodemon,concurrently,pino-pretty}/**"
+      # 排除 matrix-js-sdk 传递依赖(已 bundle 进 client vendor chunk)
+      - "!node_modules/{@hapi,tldts,pngjs,validator,yaml}/**"
+      # 排除 *.d.ts 和测试/文档目录(通用瘦身)
+      - "!node_modules/**/*.d.ts"
+      - "!node_modules/**/{test,tests,__tests__,docs,doc,example,examples,.github,coverage}/**"
+      # 排除 server source map(12MB,生产环境不需要)
+      - "!dist/server/*.map"
+      # 瘦身第三轮:排除 d3 全家桶 + dagre + khroma (mermaid 依赖,已 bundle 进 client)
+      - "!node_modules/d3-*/**"
+      - "!node_modules/{dagre,dagre-d3-es,khroma,layout-base,graphlib}/**"
+      # 排除更多已 bundle 的 client 依赖
+      - "!node_modules/{mdn-data,neo-async,parse5,parse5-parser,immutable,qrcode,oidc-client-ts,dompurify,js-yaml}/**"
+      - "!node_modules/{ts-node,chai,acorn,source-map,magicast,yargs,body-parser}/**"
+      - "!node_modules/{socket.io-client,engine.io-client}/**"
+      - "!node_modules/{@acemir,@exodus,@parcel,@volar,@upsetjs,@csstools,@css-render,@cspotcode,@juggle,@audio,@one-ini,@jridgewell,@iconify}/**"
+      # 排除 node_modules 中的 source map / flow / coffee 文件
+      - "!node_modules/**/*.map"
+      - "!node_modules/**/*.flow"
+      # 排除 .pnpm 元数据(如有)
+      - "!node_modules/.pnpm/**"
 
 asarUnpack:
   - "**/*.node"
@@ -54,20 +101,24 @@ asarUnpack:
 mac:
   target:
     - target: dmg
-      arch: [arm64, x64]
+      arch: [arm64]
     - target: zip
-      arch: [arm64, x64]
+      arch: [arm64]
   category: public.app-category.developer-tools
   hardenedRuntime: true
   gatekeeperAssess: false
-  notarize: true
-  artifactName: "Hermes.Studio-${version}-${arch}.${ext}"
+  # notarize 需 Apple Developer 凭据;本地/CI 无凭据构建时关闭以免 build 失败。
+  # 正式发布构建设回 true 并通过 CSC_LINK/CSC_KEY_PASSWORD/APPLE_ID 等环境变量提供凭据。
+  notarize: false
+  artifactName: "SwarmStudio-${version}-${arch}.${ext}"
 
 win:
   target:
+    - target: zip
+      arch: [x64]
     - target: nsis
       arch: [x64]
-  artifactName: "Hermes.Studio-${version}-${arch}.${ext}"
+  artifactName: "SwarmStudio-${version}-${arch}.${ext}"
 
 linux:
   icon: build/icons
diff --git a/packages/desktop/package.json b/packages/desktop/package.json
index 91923401..1da0af7f 100644
--- a/packages/desktop/package.json
+++ b/packages/desktop/package.json
@@ -1,11 +1,11 @@
 {
-  "name": "hermes-studio",
+  "name": "swarmstudio",
   "version": "0.6.20",
-  "description": "Hermes Studio desktop distribution with bundled Python runtime and hermes-agent",
+  "description": "SwarmStudio desktop distribution with bundled Python runtime and hermes-agent",
   "homepage": "https://hermes-studio.ai",
   "author": {
-    "name": "Hermes Studio Contributors",
-    "email": "noreply@hermes-studio.local"
+    "name": "SwarmStudio Contributors",
+    "email": "noreply@swarmstudio.local"
   },
   "license": "BSL-1.1",
   "private": true,
diff --git a/packages/desktop/src/main/cli-shim.ts b/packages/desktop/src/main/cli-shim.ts
index 052302df..9a1f60c5 100644
--- a/packages/desktop/src/main/cli-shim.ts
+++ b/packages/desktop/src/main/cli-shim.ts
@@ -17,8 +17,8 @@ const execFileAsync = promisify(execFile)
 
 const SHIM_MARKER = 'HERMES_STUDIO_CLI_SHIM'
 const MCP_SHIM_MARKER = 'HERMES_STUDIO_MCP_SHIM'
-const PATH_MARKER_START = '# >>> Hermes Studio CLI shim >>>'
-const PATH_MARKER_END = '# <<< Hermes Studio CLI shim <<<'
+const PATH_MARKER_START = '# >>> SwarmStudio CLI shim >>>'
+const PATH_MARKER_END = '# <<< SwarmStudio CLI shim <<<'
 const WINDOWS_USER_PATH_ENV_B64 = 'HERMES_STUDIO_WINDOWS_USER_PATH_B64'
 
 type ShimInstallStatus = 'installed' | 'updated' | 'unchanged' | 'skipped'
@@ -90,7 +90,7 @@ export function createShimContent(
   executablePath: string,
   platform: NodeJS.Platform = process.platform,
   archName: string = process.arch,
-  runtimeVersion = '0.15.2',
+  runtimeVersion = '0.17.0',
   nodePath = process.execPath,
   webUiScriptPath = resolve(process.cwd(), 'bin', 'hermes-web-ui.mjs'),
 ): string {
@@ -110,7 +110,7 @@ export function createShimContent(
       'if /I "%~1"=="--help" goto help',
       'if /I "%~1"=="cli" goto runCli',
       'if /I "%~1"=="web" goto runWeb',
-      'echo Unknown Hermes Studio command: %~1 1>&2',
+      'echo Unknown SwarmStudio command: %~1 1>&2',
       'echo Run hermes-studio --help for usage. 1>&2',
       'exit /b 2',
       ':resolveRuntime',
@@ -127,21 +127,21 @@ export function createShimContent(
       ':runCli',
       'call :resolveRuntime',
       'if not exist "%PYTHON%" (',
-      '  echo Hermes Studio Python runtime not found at "%PYTHON%" 1>&2',
-      '  echo Open Hermes Studio once to finish runtime setup, then retry hermes-studio cli. 1>&2',
+      '  echo SwarmStudio Python runtime not found at "%PYTHON%" 1>&2',
+      '  echo Open SwarmStudio once to finish runtime setup, then retry hermes-studio cli. 1>&2',
       '  exit /b 127',
       ')',
       'if not exist "%NODE%" (',
-      '  echo Hermes Studio Node runtime not found at "%NODE%" 1>&2',
-      '  echo Open Hermes Studio once to finish runtime setup, then retry hermes-studio cli. 1>&2',
+      '  echo SwarmStudio Node runtime not found at "%NODE%" 1>&2',
+      '  echo Open SwarmStudio once to finish runtime setup, then retry hermes-studio cli. 1>&2',
       '  exit /b 127',
       ')',
       `"%NODE%" -e "${cliForwarder}" %*`,
       'exit /b %ERRORLEVEL%',
       ':runWeb',
       'if not exist "%NODE%" (',
-      '  echo Hermes Studio Node runtime not found at "%NODE%" 1>&2',
-      '  echo Open Hermes Studio once to finish runtime setup, then retry hermes-studio web. 1>&2',
+      '  echo SwarmStudio Node runtime not found at "%NODE%" 1>&2',
+      '  echo Open SwarmStudio once to finish runtime setup, then retry hermes-studio web. 1>&2',
       '  exit /b 127',
       ')',
       'if not exist "%WEBUI_SCRIPT%" (',
@@ -157,8 +157,8 @@ export function createShimContent(
       'echo Usage: hermes-studio [command] [options]',
       'echo.',
       'echo Commands:',
-      'echo   ^(no command^)       Open Hermes Studio desktop app',
-      'echo   cli [args...]       Run bundled Hermes Agent CLI',
+      'echo   ^(no command^)       Open SwarmStudio desktop app',
+      'echo   cli [args...]       Run bundled Swarm Agent CLI',
       'echo   web [args...]       Run bundled hermes-web-ui command',
       'echo   help, -h, --help    Show this help message',
       'exit /b 0',
@@ -177,14 +177,14 @@ export function createShimContent(
     'Usage: hermes-studio [command] [options]',
     '',
     'Commands:',
-    '  (no command)       Open Hermes Studio desktop app',
-    '  cli [args...]      Run bundled Hermes Agent CLI',
+    '  (no command)       Open SwarmStudio desktop app',
+    '  cli [args...]      Run bundled Swarm Agent CLI',
     '  web [args...]      Run bundled hermes-web-ui command',
     '  help, -h, --help   Show this help message',
     'EOF',
     '}',
     'if [ ! -x "$APP" ]; then',
-    '  echo "Hermes Studio executable not found at $APP" >&2',
+    '  echo "SwarmStudio executable not found at $APP" >&2',
     '  exit 127',
     'fi',
     'unset ELECTRON_RUN_AS_NODE',
@@ -199,8 +199,8 @@ export function createShimContent(
     '  web)',
     '    shift',
     '    if [ ! -x "$NODE" ]; then',
-    '      echo "Hermes Studio Node runtime not found at $NODE" >&2',
-    '      echo "Open Hermes Studio once to finish runtime setup, then retry hermes-studio web." >&2',
+    '      echo "SwarmStudio Node runtime not found at $NODE" >&2',
+    '      echo "Open SwarmStudio once to finish runtime setup, then retry hermes-studio web." >&2',
     '      exit 127',
     '    fi',
     '    if [ ! -f "$WEBUI_SCRIPT" ]; then',
@@ -214,7 +214,7 @@ export function createShimContent(
     '    exit 0',
     '    ;;',
     '  *)',
-    '    echo "Unknown Hermes Studio command: $1" >&2',
+    '    echo "Unknown SwarmStudio command: $1" >&2',
     '    echo "Run hermes-studio --help for usage." >&2',
     '    exit 2',
     '    ;;',
@@ -236,12 +236,12 @@ export function createMcpShimContent(
       `set "NODE=${nodePath}"`,
       `set "SCRIPT=${scriptPath}"`,
       'if not exist "%NODE%" (',
-      '  echo Hermes Studio Node runtime not found at "%NODE%" 1>&2',
-      '  echo Open Hermes Studio once to finish runtime setup, then retry hermes-studio-mcp. 1>&2',
+      '  echo SwarmStudio Node runtime not found at "%NODE%" 1>&2',
+      '  echo Open SwarmStudio once to finish runtime setup, then retry hermes-studio-mcp. 1>&2',
       '  exit /b 127',
       ')',
       'if not exist "%SCRIPT%" (',
-      '  echo Hermes Studio MCP script not found at "%SCRIPT%" 1>&2',
+      '  echo SwarmStudio MCP script not found at "%SCRIPT%" 1>&2',
       '  exit /b 127',
       ')',
       'if "%HERMES_WEB_UI_URL%"=="" (',
@@ -264,12 +264,12 @@ export function createMcpShimContent(
     `NODE=${shellQuote(nodePath)}`,
     `SCRIPT=${shellQuote(scriptPath)}`,
     'if [ ! -x "$NODE" ]; then',
-    '  echo "Hermes Studio Node runtime not found at $NODE" >&2',
-    '  echo "Open Hermes Studio once to finish runtime setup, then retry hermes-studio-mcp." >&2',
+    '  echo "SwarmStudio Node runtime not found at $NODE" >&2',
+    '  echo "Open SwarmStudio once to finish runtime setup, then retry hermes-studio-mcp." >&2',
     '  exit 127',
     'fi',
     'if [ ! -f "$SCRIPT" ]; then',
-    '  echo "Hermes Studio MCP script not found at $SCRIPT" >&2',
+    '  echo "SwarmStudio MCP script not found at $SCRIPT" >&2',
     '  exit 127',
     'fi',
     'if [ -z "${HERMES_WEB_UI_URL:-}" ]; then',
@@ -446,7 +446,7 @@ export async function installHermesStudioCliShim(options: CliShimInstallOptions
     shimPath,
     status,
     pathUpdated,
-    reason: status === 'skipped' ? 'existing hermes-studio shim is not managed by Hermes Studio' : undefined,
+    reason: status === 'skipped' ? 'existing hermes-studio shim is not managed by SwarmStudio' : undefined,
   }
 }
 
@@ -471,6 +471,6 @@ export async function installHermesStudioMcpShim(options: McpShimInstallOptions
     shimPath,
     status,
     pathUpdated,
-    reason: status === 'skipped' ? 'existing hermes-studio-mcp shim is not managed by Hermes Studio' : undefined,
+    reason: status === 'skipped' ? 'existing hermes-studio-mcp shim is not managed by SwarmStudio' : undefined,
   }
 }
diff --git a/packages/desktop/src/main/desktop-i18n.ts b/packages/desktop/src/main/desktop-i18n.ts
index 8f2fe1d6..3a21b469 100644
--- a/packages/desktop/src/main/desktop-i18n.ts
+++ b/packages/desktop/src/main/desktop-i18n.ts
@@ -48,34 +48,34 @@ const supportedLocales: DesktopLocale[] = ['en', 'zh', 'zh-TW', 'ja', 'ko', 'fr'
 
 const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
   en: {
-    'tray.show': 'Show Hermes Studio',
-    'tray.hide': 'Hide Hermes Studio',
+    'tray.show': 'Show SwarmStudio',
+    'tray.hide': 'Hide SwarmStudio',
     'tray.checkForUpdates': 'Check for Updates',
     'tray.openAtLogin': 'Open at Login',
-    'tray.quit': 'Quit Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio is up to date.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'Quit SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio is up to date.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'Checking for updates...',
     'update.currentVersion': 'Current version: {version}',
     'update.availableTitle': 'Update available',
-    'update.availableMessage': 'Hermes Studio {version} is available.',
+    'update.availableMessage': 'SwarmStudio {version} is available.',
     'update.downloading': 'The update is downloading in the background.',
     'update.readyTitle': 'Update ready',
-    'update.readyMessage': 'Hermes Studio {version} is ready to install.',
+    'update.readyMessage': 'SwarmStudio {version} is ready to install.',
     'update.readyDetail': 'Restart now to apply the update, or it will be installed on next quit.',
     'update.restartNow': 'Restart now',
     'update.download': 'Download',
     'update.later': 'Later',
     'update.failedTitle': 'Update check failed',
-    'update.failedMessage': 'Could not check for Hermes Studio updates.',
+    'update.failedMessage': 'Could not check for SwarmStudio updates.',
     'update.noUpdateInfoMessage': 'Update information is not available for this platform yet.',
     'update.packagedOnlyMessage': 'Automatic updates are only available in the packaged desktop app.',
     'desktop.startingLocalServices': 'Starting local services...',
     'desktop.selectRuntimeSource': 'Select a runtime download source to start local services.',
     'desktop.downloadFailed': 'Download failed',
     'desktop.downloadCloudflareTitle': 'Download from Cloudflare',
-    'desktop.downloadCloudflareDetail': 'Use the Hermes Studio download proxy.',
+    'desktop.downloadCloudflareDetail': 'Use the SwarmStudio download proxy.',
     'desktop.downloadGithubTitle': 'Download from GitHub',
     'desktop.downloadGithubDetail': 'Use the release asset directly from GitHub.',
     'desktop.failedPrepareRuntime': 'Failed to prepare Hermes runtime',
@@ -90,34 +90,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': 'OK',
   },
   zh: {
-    'tray.show': '显示 Hermes Studio',
-    'tray.hide': '隐藏 Hermes Studio',
+    'tray.show': '显示 SwarmStudio',
+    'tray.hide': '隐藏 SwarmStudio',
     'tray.checkForUpdates': '检查更新',
     'tray.openAtLogin': '开机启动',
-    'tray.quit': '退出 Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio 已是最新版本。',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': '退出 SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio 已是最新版本。',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': '正在检查更新...',
     'update.currentVersion': '当前版本：{version}',
     'update.availableTitle': '发现新版本',
-    'update.availableMessage': 'Hermes Studio {version} 可用。',
+    'update.availableMessage': 'SwarmStudio {version} 可用。',
     'update.downloading': '更新正在后台下载。',
     'update.readyTitle': '更新已就绪',
-    'update.readyMessage': 'Hermes Studio {version} 已准备好安装。',
+    'update.readyMessage': 'SwarmStudio {version} 已准备好安装。',
     'update.readyDetail': '立即重启以应用更新，或下次退出时自动安装。',
     'update.restartNow': '立即重启',
     'update.download': '下载',
     'update.later': '稍后',
     'update.failedTitle': '检查更新失败',
-    'update.failedMessage': '无法检查 Hermes Studio 更新。',
+    'update.failedMessage': '无法检查 SwarmStudio 更新。',
     'update.noUpdateInfoMessage': '当前平台的更新信息暂不可用。',
     'update.packagedOnlyMessage': '自动更新仅在打包后的桌面应用中可用。',
     'desktop.startingLocalServices': '正在启动本地服务...',
     'desktop.selectRuntimeSource': '请选择运行时下载源以启动本地服务。',
     'desktop.downloadFailed': '下载失败',
     'desktop.downloadCloudflareTitle': '从 Cloudflare 下载',
-    'desktop.downloadCloudflareDetail': '使用 Hermes Studio 下载代理。',
+    'desktop.downloadCloudflareDetail': '使用 SwarmStudio 下载代理。',
     'desktop.downloadGithubTitle': '从 GitHub 下载',
     'desktop.downloadGithubDetail': '直接使用 GitHub Release 资源。',
     'desktop.failedPrepareRuntime': '准备 Hermes 运行时失败',
@@ -132,34 +132,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': '确定',
   },
   'zh-TW': {
-    'tray.show': '顯示 Hermes Studio',
-    'tray.hide': '隱藏 Hermes Studio',
+    'tray.show': '顯示 SwarmStudio',
+    'tray.hide': '隱藏 SwarmStudio',
     'tray.checkForUpdates': '檢查更新',
     'tray.openAtLogin': '開機啟動',
-    'tray.quit': '結束 Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio 已是最新版本。',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': '結束 SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio 已是最新版本。',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': '正在檢查更新...',
     'update.currentVersion': '目前版本：{version}',
     'update.availableTitle': '發現新版本',
-    'update.availableMessage': 'Hermes Studio {version} 可用。',
+    'update.availableMessage': 'SwarmStudio {version} 可用。',
     'update.downloading': '更新正在背景下載。',
     'update.readyTitle': '更新已就緒',
-    'update.readyMessage': 'Hermes Studio {version} 已準備好安裝。',
+    'update.readyMessage': 'SwarmStudio {version} 已準備好安裝。',
     'update.readyDetail': '立即重新啟動以套用更新，或下次結束時自動安裝。',
     'update.restartNow': '立即重新啟動',
     'update.download': '下載',
     'update.later': '稍後',
     'update.failedTitle': '檢查更新失敗',
-    'update.failedMessage': '無法檢查 Hermes Studio 更新。',
+    'update.failedMessage': '無法檢查 SwarmStudio 更新。',
     'update.noUpdateInfoMessage': '目前平台的更新資訊暫不可用。',
     'update.packagedOnlyMessage': '自動更新僅可在打包後的桌面應用中使用。',
     'desktop.startingLocalServices': '正在啟動本地服務...',
     'desktop.selectRuntimeSource': '請選擇運行時下載來源以啟動本地服務。',
     'desktop.downloadFailed': '下載失敗',
     'desktop.downloadCloudflareTitle': '從 Cloudflare 下載',
-    'desktop.downloadCloudflareDetail': '使用 Hermes Studio 下載代理。',
+    'desktop.downloadCloudflareDetail': '使用 SwarmStudio 下載代理。',
     'desktop.downloadGithubTitle': '從 GitHub 下載',
     'desktop.downloadGithubDetail': '直接使用 GitHub Release 資源。',
     'desktop.failedPrepareRuntime': '準備 Hermes 運行時失敗',
@@ -174,34 +174,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': '確定',
   },
   ja: {
-    'tray.show': 'Hermes Studio を表示',
-    'tray.hide': 'Hermes Studio を隠す',
+    'tray.show': 'SwarmStudio を表示',
+    'tray.hide': 'SwarmStudio を隠す',
     'tray.checkForUpdates': 'アップデートを確認',
     'tray.openAtLogin': 'ログイン時に開く',
-    'tray.quit': 'Hermes Studio を終了',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio は最新です。',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'SwarmStudio を終了',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio は最新です。',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'アップデートを確認しています...',
     'update.currentVersion': '現在のバージョン: {version}',
     'update.availableTitle': 'アップデートがあります',
-    'update.availableMessage': 'Hermes Studio {version} が利用できます。',
+    'update.availableMessage': 'SwarmStudio {version} が利用できます。',
     'update.downloading': 'アップデートをバックグラウンドでダウンロードしています。',
     'update.readyTitle': 'アップデートの準備ができました',
-    'update.readyMessage': 'Hermes Studio {version} をインストールできます。',
+    'update.readyMessage': 'SwarmStudio {version} をインストールできます。',
     'update.readyDetail': '今すぐ再起動して適用するか、次回終了時にインストールされます。',
     'update.restartNow': '今すぐ再起動',
     'update.download': 'ダウンロード',
     'update.later': '後で',
     'update.failedTitle': 'アップデート確認に失敗しました',
-    'update.failedMessage': 'Hermes Studio のアップデートを確認できませんでした。',
+    'update.failedMessage': 'SwarmStudio のアップデートを確認できませんでした。',
     'update.noUpdateInfoMessage': 'このプラットフォームのアップデート情報はまだ利用できません。',
     'update.packagedOnlyMessage': '自動アップデートはパッケージ版デスクトップアプリでのみ利用できます。',
     'desktop.startingLocalServices': 'ローカルサービスを起動しています...',
     'desktop.selectRuntimeSource': 'ローカルサービスを開始するためのランタイムダウンロード元を選択してください。',
     'desktop.downloadFailed': 'ダウンロードに失敗しました',
     'desktop.downloadCloudflareTitle': 'Cloudflare からダウンロード',
-    'desktop.downloadCloudflareDetail': 'Hermes Studio のダウンロードプロキシを使用します。',
+    'desktop.downloadCloudflareDetail': 'SwarmStudio のダウンロードプロキシを使用します。',
     'desktop.downloadGithubTitle': 'GitHub からダウンロード',
     'desktop.downloadGithubDetail': 'GitHub のリリースアセットを直接使用します。',
     'desktop.failedPrepareRuntime': 'Hermes ランタイムの準備に失敗しました',
@@ -216,34 +216,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': 'OK',
   },
   ko: {
-    'tray.show': 'Hermes Studio 표시',
-    'tray.hide': 'Hermes Studio 숨기기',
+    'tray.show': 'SwarmStudio 표시',
+    'tray.hide': 'SwarmStudio 숨기기',
     'tray.checkForUpdates': '업데이트 확인',
     'tray.openAtLogin': '로그인 시 열기',
-    'tray.quit': 'Hermes Studio 종료',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio가 최신 버전입니다.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'SwarmStudio 종료',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio가 최신 버전입니다.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': '업데이트를 확인하는 중...',
     'update.currentVersion': '현재 버전: {version}',
     'update.availableTitle': '업데이트 사용 가능',
-    'update.availableMessage': 'Hermes Studio {version}을 사용할 수 있습니다.',
+    'update.availableMessage': 'SwarmStudio {version}을 사용할 수 있습니다.',
     'update.downloading': '업데이트를 백그라운드에서 다운로드하고 있습니다.',
     'update.readyTitle': '업데이트 준비 완료',
-    'update.readyMessage': 'Hermes Studio {version}을 설치할 준비가 되었습니다.',
+    'update.readyMessage': 'SwarmStudio {version}을 설치할 준비가 되었습니다.',
     'update.readyDetail': '지금 다시 시작해 업데이트를 적용하거나 다음 종료 시 설치합니다.',
     'update.restartNow': '지금 다시 시작',
     'update.download': '다운로드',
     'update.later': '나중에',
     'update.failedTitle': '업데이트 확인 실패',
-    'update.failedMessage': 'Hermes Studio 업데이트를 확인할 수 없습니다.',
+    'update.failedMessage': 'SwarmStudio 업데이트를 확인할 수 없습니다.',
     'update.noUpdateInfoMessage': '이 플랫폼의 업데이트 정보를 아직 사용할 수 없습니다.',
     'update.packagedOnlyMessage': '자동 업데이트는 패키징된 데스크톱 앱에서만 사용할 수 있습니다.',
     'desktop.startingLocalServices': '로컬 서비스를 시작하는 중...',
     'desktop.selectRuntimeSource': '로컬 서비스를 시작할 런타임 다운로드 소스를 선택하세요.',
     'desktop.downloadFailed': '다운로드 실패',
     'desktop.downloadCloudflareTitle': 'Cloudflare에서 다운로드',
-    'desktop.downloadCloudflareDetail': 'Hermes Studio 다운로드 프록시를 사용합니다.',
+    'desktop.downloadCloudflareDetail': 'SwarmStudio 다운로드 프록시를 사용합니다.',
     'desktop.downloadGithubTitle': 'GitHub에서 다운로드',
     'desktop.downloadGithubDetail': 'GitHub 릴리스 자산을 직접 사용합니다.',
     'desktop.failedPrepareRuntime': 'Hermes 런타임 준비 실패',
@@ -258,34 +258,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': '확인',
   },
   fr: {
-    'tray.show': 'Afficher Hermes Studio',
-    'tray.hide': 'Masquer Hermes Studio',
+    'tray.show': 'Afficher SwarmStudio',
+    'tray.hide': 'Masquer SwarmStudio',
     'tray.checkForUpdates': 'Rechercher les mises a jour',
     'tray.openAtLogin': 'Ouvrir a la connexion',
-    'tray.quit': 'Quitter Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio est a jour.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'Quitter SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio est a jour.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'Recherche de mises a jour...',
     'update.currentVersion': 'Version actuelle : {version}',
     'update.availableTitle': 'Mise a jour disponible',
-    'update.availableMessage': 'Hermes Studio {version} est disponible.',
+    'update.availableMessage': 'SwarmStudio {version} est disponible.',
     'update.downloading': 'La mise a jour se telecharge en arriere-plan.',
     'update.readyTitle': 'Mise a jour prete',
-    'update.readyMessage': 'Hermes Studio {version} est pret a etre installe.',
+    'update.readyMessage': 'SwarmStudio {version} est pret a etre installe.',
     'update.readyDetail': 'Redemarrez maintenant pour appliquer la mise a jour, ou elle sera installee a la prochaine fermeture.',
     'update.restartNow': 'Redemarrer maintenant',
     'update.download': 'Telecharger',
     'update.later': 'Plus tard',
     'update.failedTitle': 'Echec de la recherche de mise a jour',
-    'update.failedMessage': 'Impossible de rechercher les mises a jour de Hermes Studio.',
+    'update.failedMessage': 'Impossible de rechercher les mises a jour de SwarmStudio.',
     'update.noUpdateInfoMessage': 'Les informations de mise a jour ne sont pas encore disponibles pour cette plateforme.',
     'update.packagedOnlyMessage': 'Les mises a jour automatiques ne sont disponibles que dans l application de bureau packagee.',
     'desktop.startingLocalServices': 'Demarrage des services locaux...',
     'desktop.selectRuntimeSource': 'Selectionnez une source de telechargement du runtime pour demarrer les services locaux.',
     'desktop.downloadFailed': 'Echec du telechargement',
     'desktop.downloadCloudflareTitle': 'Telecharger depuis Cloudflare',
-    'desktop.downloadCloudflareDetail': 'Utilise le proxy de telechargement Hermes Studio.',
+    'desktop.downloadCloudflareDetail': 'Utilise le proxy de telechargement SwarmStudio.',
     'desktop.downloadGithubTitle': 'Telecharger depuis GitHub',
     'desktop.downloadGithubDetail': 'Utilise directement l asset de release GitHub.',
     'desktop.failedPrepareRuntime': 'Echec de la preparation du runtime Hermes',
@@ -300,34 +300,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': 'OK',
   },
   es: {
-    'tray.show': 'Mostrar Hermes Studio',
-    'tray.hide': 'Ocultar Hermes Studio',
+    'tray.show': 'Mostrar SwarmStudio',
+    'tray.hide': 'Ocultar SwarmStudio',
     'tray.checkForUpdates': 'Buscar actualizaciones',
     'tray.openAtLogin': 'Abrir al iniciar sesion',
-    'tray.quit': 'Salir de Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio esta actualizado.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'Salir de SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio esta actualizado.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'Buscando actualizaciones...',
     'update.currentVersion': 'Version actual: {version}',
     'update.availableTitle': 'Actualizacion disponible',
-    'update.availableMessage': 'Hermes Studio {version} esta disponible.',
+    'update.availableMessage': 'SwarmStudio {version} esta disponible.',
     'update.downloading': 'La actualizacion se esta descargando en segundo plano.',
     'update.readyTitle': 'Actualizacion lista',
-    'update.readyMessage': 'Hermes Studio {version} esta listo para instalarse.',
+    'update.readyMessage': 'SwarmStudio {version} esta listo para instalarse.',
     'update.readyDetail': 'Reinicia ahora para aplicar la actualizacion, o se instalara al salir.',
     'update.restartNow': 'Reiniciar ahora',
     'update.download': 'Descargar',
     'update.later': 'Mas tarde',
     'update.failedTitle': 'Error al buscar actualizaciones',
-    'update.failedMessage': 'No se pudieron buscar actualizaciones de Hermes Studio.',
+    'update.failedMessage': 'No se pudieron buscar actualizaciones de SwarmStudio.',
     'update.noUpdateInfoMessage': 'La informacion de actualizacion aun no esta disponible para esta plataforma.',
     'update.packagedOnlyMessage': 'Las actualizaciones automaticas solo estan disponibles en la app de escritorio empaquetada.',
     'desktop.startingLocalServices': 'Iniciando servicios locales...',
     'desktop.selectRuntimeSource': 'Selecciona una fuente de descarga del runtime para iniciar los servicios locales.',
     'desktop.downloadFailed': 'Error de descarga',
     'desktop.downloadCloudflareTitle': 'Descargar desde Cloudflare',
-    'desktop.downloadCloudflareDetail': 'Usa el proxy de descarga de Hermes Studio.',
+    'desktop.downloadCloudflareDetail': 'Usa el proxy de descarga de SwarmStudio.',
     'desktop.downloadGithubTitle': 'Descargar desde GitHub',
     'desktop.downloadGithubDetail': 'Usa directamente el recurso de release de GitHub.',
     'desktop.failedPrepareRuntime': 'No se pudo preparar el runtime de Hermes',
@@ -342,34 +342,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': 'Aceptar',
   },
   de: {
-    'tray.show': 'Hermes Studio anzeigen',
-    'tray.hide': 'Hermes Studio ausblenden',
+    'tray.show': 'SwarmStudio anzeigen',
+    'tray.hide': 'SwarmStudio ausblenden',
     'tray.checkForUpdates': 'Nach Updates suchen',
     'tray.openAtLogin': 'Beim Anmelden offnen',
-    'tray.quit': 'Hermes Studio beenden',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio ist auf dem neuesten Stand.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'SwarmStudio beenden',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio ist auf dem neuesten Stand.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'Suche nach Updates...',
     'update.currentVersion': 'Aktuelle Version: {version}',
     'update.availableTitle': 'Update verfugbar',
-    'update.availableMessage': 'Hermes Studio {version} ist verfugbar.',
+    'update.availableMessage': 'SwarmStudio {version} ist verfugbar.',
     'update.downloading': 'Das Update wird im Hintergrund heruntergeladen.',
     'update.readyTitle': 'Update bereit',
-    'update.readyMessage': 'Hermes Studio {version} ist zur Installation bereit.',
+    'update.readyMessage': 'SwarmStudio {version} ist zur Installation bereit.',
     'update.readyDetail': 'Jetzt neu starten, um das Update anzuwenden, oder es wird beim nachsten Beenden installiert.',
     'update.restartNow': 'Jetzt neu starten',
     'update.download': 'Herunterladen',
     'update.later': 'Spater',
     'update.failedTitle': 'Update-Prufung fehlgeschlagen',
-    'update.failedMessage': 'Updates fur Hermes Studio konnten nicht gepruft werden.',
+    'update.failedMessage': 'Updates fur SwarmStudio konnten nicht gepruft werden.',
     'update.noUpdateInfoMessage': 'Update-Informationen sind fur diese Plattform noch nicht verfugbar.',
     'update.packagedOnlyMessage': 'Automatische Updates sind nur in der paketierten Desktop-App verfugbar.',
     'desktop.startingLocalServices': 'Lokale Dienste werden gestartet...',
     'desktop.selectRuntimeSource': 'Wahlen Sie eine Runtime-Downloadquelle aus, um lokale Dienste zu starten.',
     'desktop.downloadFailed': 'Download fehlgeschlagen',
     'desktop.downloadCloudflareTitle': 'Von Cloudflare herunterladen',
-    'desktop.downloadCloudflareDetail': 'Verwendet den Hermes Studio Download-Proxy.',
+    'desktop.downloadCloudflareDetail': 'Verwendet den SwarmStudio Download-Proxy.',
     'desktop.downloadGithubTitle': 'Von GitHub herunterladen',
     'desktop.downloadGithubDetail': 'Verwendet das Release-Asset direkt von GitHub.',
     'desktop.failedPrepareRuntime': 'Hermes Runtime konnte nicht vorbereitet werden',
@@ -384,34 +384,34 @@ const translations: Record<DesktopLocale, Record<TranslationKey, string>> = {
     'common.ok': 'OK',
   },
   pt: {
-    'tray.show': 'Mostrar Hermes Studio',
-    'tray.hide': 'Ocultar Hermes Studio',
+    'tray.show': 'Mostrar SwarmStudio',
+    'tray.hide': 'Ocultar SwarmStudio',
     'tray.checkForUpdates': 'Verificar atualizacoes',
     'tray.openAtLogin': 'Abrir ao iniciar sessao',
-    'tray.quit': 'Sair do Hermes Studio',
-    'update.upToDateTitle': 'Hermes Studio',
-    'update.upToDateMessage': 'Hermes Studio esta atualizado.',
-    'update.checkingTitle': 'Hermes Studio',
+    'tray.quit': 'Sair do SwarmStudio',
+    'update.upToDateTitle': 'SwarmStudio',
+    'update.upToDateMessage': 'SwarmStudio esta atualizado.',
+    'update.checkingTitle': 'SwarmStudio',
     'update.checkingMessage': 'Verificando atualizacoes...',
     'update.currentVersion': 'Versao atual: {version}',
     'update.availableTitle': 'Atualizacao disponivel',
-    'update.availableMessage': 'Hermes Studio {version} esta disponivel.',
+    'update.availableMessage': 'SwarmStudio {version} esta disponivel.',
     'update.downloading': 'A atualizacao esta sendo baixada em segundo plano.',
     'update.readyTitle': 'Atualizacao pronta',
-    'update.readyMessage': 'Hermes Studio {version} esta pronto para instalar.',
+    'update.readyMessage': 'SwarmStudio {version} esta pronto para instalar.',
     'update.readyDetail': 'Reinicie agora para aplicar a atualizacao, ou ela sera instalada ao sair.',
     'update.restartNow': 'Reiniciar agora',
     'update.download': 'Baixar',
     'update.later': 'Depois',
     'update.failedTitle': 'Falha ao verificar atualizacoes',
-    'update.failedMessage': 'Nao foi possivel verificar atualizacoes do Hermes Studio.',
+    'update.failedMessage': 'Nao foi possivel verificar atualizacoes do SwarmStudio.',
     'update.noUpdateInfoMessage': 'As informacoes de atualizacao ainda nao estao disponiveis para esta plataforma.',
     'update.packagedOnlyMessage': 'Atualizacoes automaticas estao disponiveis apenas no app desktop empacotado.',
     'desktop.startingLocalServices': 'Iniciando servicos locais...',
     'desktop.selectRuntimeSource': 'Selecione uma fonte de download do runtime para iniciar os servicos locais.',
     'desktop.downloadFailed': 'Falha no download',
     'desktop.downloadCloudflareTitle': 'Baixar pelo Cloudflare',
-    'desktop.downloadCloudflareDetail': 'Usa o proxy de download do Hermes Studio.',
+    'desktop.downloadCloudflareDetail': 'Usa o proxy de download do SwarmStudio.',
     'desktop.downloadGithubTitle': 'Baixar pelo GitHub',
     'desktop.downloadGithubDetail': 'Usa diretamente o asset de release do GitHub.',
     'desktop.failedPrepareRuntime': 'Falha ao preparar o runtime Hermes',
diff --git a/packages/desktop/src/main/index.ts b/packages/desktop/src/main/index.ts
index 09490ae7..bf1c86d2 100644
--- a/packages/desktop/src/main/index.ts
+++ b/packages/desktop/src/main/index.ts
@@ -185,7 +185,7 @@ function createTray() {
     icon.setTemplateImage(true)
   }
   tray = new Tray(icon)
-  tray.setToolTip('Hermes Studio')
+  tray.setToolTip('SwarmStudio')
   tray.on('click', () => {
     showMainWindow()
     updateTrayMenu()
@@ -199,7 +199,7 @@ function createWindow() {
     height: 820,
     minWidth: 960,
     minHeight: 600,
-    title: 'Hermes Studio',
+    title: 'SwarmStudio',
     backgroundColor: '#1a1a1a',
     autoHideMenuBar: true,
     show: false,
@@ -259,7 +259,7 @@ function createWindow() {
 
 function splashHtml(label = t('desktop.startingLocalServices')): string {
   const startingLabel = escapeHtml(label)
-  const html = `<!doctype html><html><head><meta charset="utf-8"><title>Hermes Studio</title>
+  const html = `<!doctype html><html><head><meta charset="utf-8"><title>SwarmStudio</title>
 <style>
   html,body{margin:0;height:100%;background:#1a1a1a;color:#e5e5e5;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Helvetica,Arial,sans-serif;-webkit-app-region:drag;}
   .wrap{display:flex;flex-direction:column;align-items:center;justify-content:center;height:100%;gap:20px}
@@ -273,7 +273,7 @@ function splashHtml(label = t('desktop.startingLocalServices')): string {
   .bar{width:0;height:100%;background:#d8d8d8;transition:width .18s ease}
   h1{font-weight:500;margin:0;font-size:18px}
 </style></head><body><div class="wrap">
-<h1>Hermes Studio</h1>
+<h1>SwarmStudio</h1>
 <div class="row"><div class="dot"></div><div class="dot"></div><div class="dot"></div></div>
 <div id="label" class="label">${startingLabel}</div>
 <div class="progress"><div id="bar" class="bar"></div></div>
@@ -308,9 +308,6 @@ function escapeHtml(value: string): string {
 
 function resolveRuntimeSourceLogo(): string {
   const candidates = [
-    join(webuiDir(), 'dist', 'client', 'logo.png'),
-    join(webuiDir(), 'packages', 'client', 'public', 'logo.png'),
-    join(webuiDir(), 'logo.png'),
     desktopIcon(),
   ]
   return candidates.find(candidate => existsSync(candidate)) || desktopIcon()
@@ -336,7 +333,7 @@ function runtimeSourceHtml(errorMessage?: string): string {
         <pre>${safeError}</pre>
        </section>`
     : ''
-  const html = `<!doctype html><html><head><meta charset="utf-8"><title>Hermes Studio</title>
+  const html = `<!doctype html><html><head><meta charset="utf-8"><title>SwarmStudio</title>
 <style>
   :root{color-scheme:dark}
   *{box-sizing:border-box}
@@ -363,7 +360,7 @@ function runtimeSourceHtml(errorMessage?: string): string {
     button{min-height:78px}
   }
 </style></head><body><main class="wrap">
-<div class="brand">${logoUrl ? `<img class="mark" src="${logoUrl}" alt="Hermes Studio">` : ''}<h1>Hermes Studio</h1></div>
+<div class="brand">${logoUrl ? `<img class="mark" src="${logoUrl}" alt="SwarmStudio">` : ''}<h1>SwarmStudio</h1></div>
 <p class="label">${escapeHtml(t('desktop.selectRuntimeSource'))}</p>
 ${errorBlock}
 <div class="actions">
@@ -516,7 +513,7 @@ ipcMain.handle('hermes-desktop:notify-completion', (_event, payload?: { title?:
 
   const title = typeof payload?.title === 'string' && payload.title.trim()
     ? payload.title.trim()
-    : 'Hermes Studio'
+    : 'SwarmStudio'
   const body = typeof payload?.body === 'string' ? payload.body.trim().slice(0, 240) : ''
   const icon = resolveNotificationIcon(payload?.icon)
   const notification = new Notification({
diff --git a/packages/desktop/src/main/paths.ts b/packages/desktop/src/main/paths.ts
index 343a6233..fdc99d40 100644
--- a/packages/desktop/src/main/paths.ts
+++ b/packages/desktop/src/main/paths.ts
@@ -10,7 +10,7 @@ import {
 import { compareHermesAgentVersions, hermesAgentVersionFromRuntimeTag } from './runtime-version'
 
 const isWin = platform() === 'win32'
-const DEFAULT_HERMES_AGENT_VERSION = '0.15.2'
+const DEFAULT_HERMES_AGENT_VERSION = '0.17.0'
 const MIN_COMPATIBLE_WEB_UI_VERSION = '0.6.14'
 const PACKAGED_RUNTIME_RELEASE_NAME = 'runtime-release.json'
 const ACTIVE_RUNTIME_VERSION_NAME = 'active-version.json'
diff --git a/packages/desktop/src/main/updater-helpers.ts b/packages/desktop/src/main/updater-helpers.ts
index bb979588..854f67bc 100644
--- a/packages/desktop/src/main/updater-helpers.ts
+++ b/packages/desktop/src/main/updater-helpers.ts
@@ -35,6 +35,6 @@ export function pendingUpdateDirectories(options: {
   if (localAppData) bases.add(localAppData)
   if (appDataPath) bases.add(appDataPath)
   if (!bases.size) return []
-  return [...bases].flatMap(base => updateCacheNames(options.appName || 'Hermes Studio')
+  return [...bases].flatMap(base => updateCacheNames(options.appName || 'Swarm Studio')
     .map(name => join(base, name, 'pending')))
 }
diff --git a/packages/desktop/src/main/webui-server.ts b/packages/desktop/src/main/webui-server.ts
index e9bb9e52..1df19e87 100644
--- a/packages/desktop/src/main/webui-server.ts
+++ b/packages/desktop/src/main/webui-server.ts
@@ -390,7 +390,7 @@ export async function startWebUiServer(port = DEFAULT_PORT): Promise<string> {
     // still override by setting GATEWAY_ALLOW_ALL_USERS=false in their
     // HERMES_HOME/.env or by configuring per-platform allowlists.
     GATEWAY_ALLOW_ALL_USERS: process.env.GATEWAY_ALLOW_ALL_USERS ?? 'true',
-    // Keep the bundled Hermes Agent, bridge, gateway, and Web UI path helpers
+    // Keep the bundled Swarm Agent, bridge, gateway, and Web UI path helpers
     // on the same data directory. Native Windows uses an existing
     // %LOCALAPPDATA%\hermes or %APPDATA%\hermes; otherwise all platforms keep
     // the standard ~/.hermes layout.
diff --git a/packages/server/src/config.ts b/packages/server/src/config.ts
index fcfc5dfc..9ea96779 100644
--- a/packages/server/src/config.ts
+++ b/packages/server/src/config.ts
@@ -24,6 +24,9 @@ import { homedir } from 'os'
  * - HERMES_GATEWAY_URL / GATEWAY_URL: Explicit Hermes gateway upstream URL for proxy routes.
  * - GATEWAY_HOST: Default Hermes gateway upstream host. Default: 127.0.0.1.
  * - GATEWAY_PORT: Default Hermes gateway upstream port. Default: 8642.
+ * - HERMES_DASHBOARD_URL: Base URL of the Hermes agent dashboard serving the kanban plugin page (embedded via iframe at /hermes/kanban). Overrides HERMES_DASHBOARD_HOST/PORT.
+ * - HERMES_DASHBOARD_HOST: Dashboard host. Default: 127.0.0.1.
+ * - HERMES_DASHBOARD_PORT: Dashboard port. Default: 9119.
  * - HERMES_WEB_UI_DISABLE_GATEWAY_AUTOSTART: Disable Web UI gateway autostart checks and config-driven gateway start/stop reconciliation.
  * - HERMES_WEB_UI_MANAGED_GATEWAY: Web UI-managed Hermes gateway handling. Enabled by default; set 0/false/off to use CLI start.
  * - HERMES_WEB_UI_STOP_GATEWAYS_ON_SHUTDOWN: Whether Web UI shutdown also stops managed gateways.
@@ -59,6 +62,21 @@ export function getCorsOrigins(env: Record<string, string | undefined> = process
   return env.CORS_ORIGINS?.trim() || ''
 }
 
+/**
+ * Base URL of the Hermes agent dashboard that serves the kanban plugin page.
+ * The /hermes/kanban view embeds `${dashboardUrl}/kanban` in an iframe; the
+ * dashboard injects its own session token into the served HTML on loopback, so
+ * the embedded page authenticates itself without any token handling here.
+ * Default assumes the dashboard is running on its default loopback port.
+ */
+export function getDashboardUrl(env: Record<string, string | undefined> = process.env): string {
+  const url = env.HERMES_DASHBOARD_URL?.trim()
+  if (url) return url.replace(/\/+$/, '')
+  const host = env.HERMES_DASHBOARD_HOST?.trim() || '127.0.0.1'
+  const port = parseInt(env.HERMES_DASHBOARD_PORT || '9119', 10)
+  return `http://${host}:${port}`
+}
+
 const appHome = getWebUiHome()
 
 export const config = {
diff --git a/packages/server/src/controllers/auth.ts b/packages/server/src/controllers/auth.ts
index 155822dd..f5adac67 100644
--- a/packages/server/src/controllers/auth.ts
+++ b/packages/server/src/controllers/auth.ts
@@ -6,14 +6,19 @@ import {
   bootstrapDefaultSuperAdmin,
   countActiveSuperAdmins,
   countUsers,
+  createFromMatrix,
   createUser,
   deleteUser,
+  findByMatrixUserId,
   findUserById,
   findUserByUsername,
   getUserAvatar,
   listUserProfiles,
   listUsers,
+  replaceUserProfiles,
   setUserAvatar,
+  syncMatrixMetadata,
+  updateMatrixUserMetadata,
   updateUser,
   updateUsername,
   updateUserPassword,
@@ -25,6 +30,17 @@ import {
 import { issueUserJwt } from '../middleware/user-auth'
 import { listProfileNamesFromDisk } from '../services/hermes/hermes-profile'
 import { startOutboundRelayClient, stopOutboundRelayClient } from '../services/global-agent/outbound-relay-client'
+// HERMES_CUSTOM[MatrixAuth] BEGIN: Matrix 管理员服务集成
+import {
+  validateMatrixToken,
+  getMatrixUserInfo,
+  listMatrixUsers as listMatrixUsersFromServer,
+  createMatrixUser as createMatrixUserOnServer,
+  resetMatrixUserPassword as resetMatrixUserPasswordOnServer,
+  setMatrixUserActive,
+  deleteMatrixUser as deleteMatrixUserOnServer,
+} from '../custom/matrix/admin-service'
+// HERMES_CUSTOM[MatrixAuth] END
 
 /**
  * GET /api/auth/status
@@ -43,6 +59,9 @@ export async function authStatus(ctx: Context) {
  */
 export async function currentUser(ctx: Context) {
   const userId = ctx.state.user?.id
+  const username = ctx.state.user?.username
+  const role = ctx.state.user?.role
+
   const user = userId ? findUserById(userId) : null
   if (!user) {
     ctx.status = 404
@@ -62,6 +81,11 @@ export async function currentUser(ctx: Context) {
       requiresCredentialChange: process.env.HERMES_DESKTOP === 'true'
         ? false
         : user.username === DEFAULT_USERNAME && verifyPassword(DEFAULT_PASSWORD, user.password_hash),
+      matrix_user_id: user.matrix_user_id,
+      matrix_display_name: user.matrix_display_name,
+      matrix_avatar_url: user.matrix_avatar_url,
+      matrix_homeserver_url: user.matrix_homeserver_url,
+      auth_source: user.auth_source,
     },
   }
 }
@@ -93,7 +117,7 @@ function isValidAvatarPayload(value: unknown): { ok: true; json: string } | { ok
  */
 export async function getMyAvatar(ctx: Context) {
   const userId = ctx.state.user?.id
-  if (!userId) {
+  if (userId == null) {
     ctx.status = 401
     ctx.body = { error: 'Unauthorized' }
     return
@@ -405,6 +429,386 @@ function validateProfiles(profiles: string[]): string | null {
   return missing || null
 }
 
+// Helper: get admin token from config or current user
+function getMatrixAdminToken(ctx: Context): string | null {
+  // For now, use the current user's Matrix accessToken if they are admin
+  // In production, this should come from server config
+  // TODO: Read from Hermes config system
+  return null
+}
+
+/**
+ * POST /api/auth/matrix-login
+ * Authenticate with Matrix accessToken, find/create Hermes user, return JWT.
+ */
+export async function matrixLogin(ctx: Context) {
+  const { matrixAccessToken, matrixUserId, deviceId, homeserverUrl } = ctx.request.body as {
+    matrixAccessToken?: string
+    matrixUserId?: string
+    deviceId?: string
+    homeserverUrl?: string
+  }
+
+  if (!matrixAccessToken || !matrixUserId || !homeserverUrl) {
+    ctx.status = 400
+    ctx.body = { error: 'Matrix accessToken, userId, and homeserverUrl are required' }
+    return
+  }
+
+  // 1. Validate the Matrix token
+  const validation = await validateMatrixToken(matrixAccessToken, homeserverUrl)
+  if (!validation || validation.userId !== matrixUserId) {
+    ctx.status = 401
+    ctx.body = { error: 'Invalid Matrix access token' }
+    return
+  }
+
+  // 2. Get user info from Matrix (including admin status)
+  // Use the user's own token to check their profile; admin status requires admin token
+  const matrixInfo = await getMatrixUserInfo(matrixUserId, matrixAccessToken, homeserverUrl)
+
+  // 3. Find or create Hermes user
+  let user = findByMatrixUserId(matrixUserId)
+  const now = Date.now()
+
+  if (!user) {
+    const localpart = matrixUserId.replace(/^@/, '').split(':')[0]
+    const role = 'super_admin'
+    user = createFromMatrix({
+      matrixUserId,
+      username: localpart,
+      displayName: matrixInfo?.displayName || localpart,
+      avatarUrl: matrixInfo?.avatarUrl || '',
+      homeserverUrl,
+      role,
+    })
+    if (user) {
+      // Ensure matrix user has access to all profiles (same permission as local admin)
+      const allProfiles = listProfileNamesFromDisk()
+      replaceUserProfiles(user.id, allProfiles, allProfiles[0] || 'default')
+      user = findByMatrixUserId(matrixUserId)
+    }
+  } else {
+    // Update cached metadata
+    syncMatrixMetadata(user.id, {
+      displayName: matrixInfo?.displayName || undefined,
+      avatarUrl: matrixInfo?.avatarUrl || undefined,
+      lastLoginAt: now,
+    })
+    // Ensure matrix user has access to all profiles (same permission as local admin)
+    const allProfiles = listProfileNamesFromDisk()
+    replaceUserProfiles(user.id, allProfiles, allProfiles[0] || 'default')
+    user = findByMatrixUserId(matrixUserId)
+  }
+
+  if (!user) {
+    ctx.status = 500
+    ctx.body = { error: 'Failed to create or update user record' }
+    return
+  }
+
+  // 4. Issue Hermes JWT
+  try {
+    const token = await issueUserJwt(user)
+    ctx.body = { token, user: {
+      id: user.id,
+      username: user.username,
+      role: user.role,
+      status: user.status,
+      matrix_user_id: user.matrix_user_id,
+      matrix_display_name: user.matrix_display_name,
+      matrix_avatar_url: user.matrix_avatar_url,
+      matrix_homeserver_url: user.matrix_homeserver_url,
+      auth_source: user.auth_source,
+      created_at: user.created_at,
+      updated_at: user.updated_at,
+      last_login_at: user.last_login_at,
+    }}
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err?.message || 'Failed to issue login token' }
+  }
+}
+
+/**
+ * GET /api/auth/matrix-users
+ * List Matrix users merged with Hermes metadata.
+ */
+export async function listMatrixUsers(ctx: Context) {
+  const adminToken = getMatrixAdminToken(ctx)
+  const homeserverUrl = 'http://localhost:8008' // TODO: Get from config or user
+
+  if (!adminToken) {
+    // Fallback: return only Hermes users with matrix_user_id
+    const hermesUsers = listUsers().filter(u => u.matrix_user_id)
+    ctx.body = {
+      users: hermesUsers.map(u => ({
+        matrix_user_id: u.matrix_user_id,
+        display_name: u.matrix_display_name,
+        avatar_url: u.matrix_avatar_url,
+        role: u.role,
+        status: u.status,
+        profiles: u.profiles,
+        last_login_at: u.last_login_at,
+        is_admin: false,
+        source: 'hermes',
+      })),
+      total: hermesUsers.length,
+    }
+    return
+  }
+
+  const matrixResult = await listMatrixUsersFromServer(adminToken, homeserverUrl)
+  if (!matrixResult) {
+    ctx.status = 503
+    ctx.body = { error: 'Failed to fetch users from Matrix homeserver' }
+    return
+  }
+
+  // Merge with Hermes metadata
+  const mergedUsers = matrixResult.users.map((matrixUser: { name: string; displayname: string | null; avatar_url: string | null; is_admin: boolean; deactivated: boolean }) => {
+    const hermesUser = findByMatrixUserId(matrixUser.name)
+    return {
+      matrix_user_id: matrixUser.name,
+      display_name: matrixUser.displayname || matrixUser.name.replace(/^@/, '').split(':')[0],
+      avatar_url: matrixUser.avatar_url,
+      role: hermesUser?.role || (matrixUser.is_admin ? 'super_admin' : 'admin'),
+      status: hermesUser?.status || (matrixUser.deactivated ? 'disabled' : 'active'),
+      profiles: hermesUser ? listUserProfiles(hermesUser.id).map(p => p.profile_name) : [],
+      last_login_at: hermesUser?.last_login_at || null,
+      is_admin: matrixUser.is_admin,
+      source: 'matrix',
+    }
+  })
+
+  ctx.body = { users: mergedUsers, total: matrixResult.total }
+}
+
+/**
+ * POST /api/auth/matrix-users
+ * Create a Matrix user and Hermes metadata.
+ */
+export async function createMatrixUser(ctx: Context) {
+  const body = ctx.request.body as {
+    userId?: string
+    password?: string
+    displayName?: string
+    role?: unknown
+    profiles?: unknown
+  }
+  const userId = String(body.userId || '').trim()
+  const password = String(body.password || '')
+  const role = normalizeRole(body.role || 'admin')
+  const profiles = normalizeProfiles(body.profiles)
+
+  if (!userId || !password || password.length < 6 || !role) {
+    ctx.status = 400
+    ctx.body = { error: 'Invalid userId, password (min 6 chars), or role' }
+    return
+  }
+
+  const adminToken = getMatrixAdminToken(ctx)
+  const homeserverUrl = 'http://localhost:8008' // TODO: Config
+
+  if (!adminToken) {
+    ctx.status = 503
+    ctx.body = { error: 'Matrix admin API not configured' }
+    return
+  }
+
+  const created = await createMatrixUserOnServer(userId, password, adminToken, homeserverUrl)
+  if (!created) {
+    ctx.status = 500
+    ctx.body = { error: 'Failed to create Matrix user' }
+    return
+  }
+
+  // Create Hermes record
+  const localpart = userId.replace(/^@/, '').split(':')[0]
+  const hermesUser = createFromMatrix({
+    matrixUserId: userId,
+    username: localpart,
+    displayName: body.displayName || localpart,
+    avatarUrl: '',
+    homeserverUrl,
+    role,
+  })
+
+  if (profiles.length > 0 && hermesUser) {
+    replaceUserProfiles(hermesUser.id, profiles, profiles[0])
+  }
+
+  ctx.status = 201
+  ctx.body = { user: hermesUser }
+}
+
+/**
+ * PUT /api/auth/matrix-users/:id
+ * Update Matrix user (Hermes metadata + Matrix profile).
+ */
+export async function updateMatrixUser(ctx: Context) {
+  const matrixUserId = decodeURIComponent(ctx.params.id)
+  const body = ctx.request.body as {
+    displayName?: string
+    role?: unknown
+    status?: unknown
+    profiles?: unknown
+  }
+
+  const role = body.role == null ? undefined : normalizeRole(body.role)
+  const status = body.status == null ? undefined : normalizeStatus(body.status)
+  const profiles = body.profiles == null ? undefined : normalizeProfiles(body.profiles)
+
+  const hermesUser = findByMatrixUserId(matrixUserId)
+  if (!hermesUser) {
+    ctx.status = 404
+    ctx.body = { error: 'User not found' }
+    return
+  }
+
+  // Update Hermes metadata
+  if (role !== undefined || status !== undefined) {
+    updateMatrixUserMetadata(matrixUserId, { role: role || undefined, status: status || undefined })
+  }
+
+  if (profiles !== undefined) {
+    replaceUserProfiles(hermesUser.id, profiles, profiles[0] || null)
+  }
+
+  // Update Matrix display name if provided
+  if (body.displayName) {
+    const adminToken = getMatrixAdminToken(ctx)
+    const homeserverUrl = hermesUser.matrix_homeserver_url || 'http://localhost:8008'
+    if (adminToken) {
+      await fetch(`${homeserverUrl}/_matrix/client/v3/profile/${encodeURIComponent(matrixUserId)}/displayname`, {
+        method: 'PUT',
+        headers: {
+          Authorization: `Bearer ${adminToken}`,
+          'Content-Type': 'application/json',
+        },
+        body: JSON.stringify({ displayname: body.displayName }),
+      })
+    }
+  }
+
+  ctx.body = { user: findByMatrixUserId(matrixUserId) }
+}
+
+/**
+ * DELETE /api/auth/matrix-users/:id
+ * Delete a Matrix user and Hermes record.
+ */
+export async function deleteMatrixUser(ctx: Context) {
+  const matrixUserId = decodeURIComponent(ctx.params.id)
+  const hermesUser = findByMatrixUserId(matrixUserId)
+
+  if (!hermesUser) {
+    ctx.status = 404
+    ctx.body = { error: 'User not found' }
+    return
+  }
+
+  if (ctx.state.user?.id === hermesUser.id) {
+    ctx.status = 400
+    ctx.body = { error: 'You cannot delete your own account' }
+    return
+  }
+
+  if (hermesUser.role === 'super_admin' && hermesUser.status === 'active' && countActiveSuperAdmins(hermesUser.id) === 0) {
+    ctx.status = 400
+    ctx.body = { error: 'At least one active super administrator is required' }
+    return
+  }
+
+  const adminToken = getMatrixAdminToken(ctx)
+  const homeserverUrl = hermesUser.matrix_homeserver_url || 'http://localhost:8008'
+
+  if (adminToken) {
+    await deleteMatrixUserOnServer(matrixUserId, adminToken, homeserverUrl)
+  }
+
+  deleteUser(hermesUser.id)
+  ctx.body = { success: true }
+}
+
+/**
+ * POST /api/auth/matrix-users/:id/deactivate
+ * Activate or deactivate a Matrix user.
+ */
+export async function deactivateMatrixUser(ctx: Context) {
+  const matrixUserId = decodeURIComponent(ctx.params.id)
+  const { active } = ctx.request.body as { active?: boolean }
+
+  if (active === undefined) {
+    ctx.status = 400
+    ctx.body = { error: 'active field is required' }
+    return
+  }
+
+  const hermesUser = findByMatrixUserId(matrixUserId)
+  if (!hermesUser) {
+    ctx.status = 404
+    ctx.body = { error: 'User not found' }
+    return
+  }
+
+  if (ctx.state.user?.id === hermesUser.id && !active) {
+    ctx.status = 400
+    ctx.body = { error: 'You cannot disable your own account' }
+    return
+  }
+
+  const adminToken = getMatrixAdminToken(ctx)
+  const homeserverUrl = hermesUser.matrix_homeserver_url || 'http://localhost:8008'
+
+  if (adminToken) {
+    await setMatrixUserActive(matrixUserId, active, adminToken, homeserverUrl)
+  }
+
+  updateMatrixUserMetadata(matrixUserId, { status: active ? 'active' : 'disabled' })
+  ctx.body = { success: true }
+}
+
+/**
+ * POST /api/auth/matrix-users/:id/reset-password
+ * Reset a Matrix user's password.
+ */
+export async function resetMatrixUserPassword(ctx: Context) {
+  const matrixUserId = decodeURIComponent(ctx.params.id)
+  const { password } = ctx.request.body as { password?: string }
+
+  if (!password || password.length < 6) {
+    ctx.status = 400
+    ctx.body = { error: 'Password must be at least 6 characters' }
+    return
+  }
+
+  const hermesUser = findByMatrixUserId(matrixUserId)
+  if (!hermesUser) {
+    ctx.status = 404
+    ctx.body = { error: 'User not found' }
+    return
+  }
+
+  const adminToken = getMatrixAdminToken(ctx)
+  const homeserverUrl = hermesUser.matrix_homeserver_url || 'http://localhost:8008'
+
+  if (!adminToken) {
+    ctx.status = 503
+    ctx.body = { error: 'Matrix admin API not configured' }
+    return
+  }
+
+  const ok = await resetMatrixUserPasswordOnServer(matrixUserId, password, adminToken, homeserverUrl)
+  if (!ok) {
+    ctx.status = 500
+    ctx.body = { error: 'Failed to reset password' }
+    return
+  }
+
+  ctx.body = { success: true }
+}
+
 /**
  * GET /api/auth/users
  * Super admin user management list.
diff --git a/packages/server/src/controllers/health.ts b/packages/server/src/controllers/health.ts
index 34289689..832cff6e 100644
--- a/packages/server/src/controllers/health.ts
+++ b/packages/server/src/controllers/health.ts
@@ -189,7 +189,7 @@ async function refreshAgentBridgeHealth(): Promise<AgentBridgeHealthPayload> {
 
 export async function healthCheck(ctx: any) {
   const raw = await hermesCli.getVersion()
-  const hermesVersion = raw.split('\n')[0].replace('Hermes Agent ', '') || ''
+  const hermesVersion = raw.split('\n')[0].replace('Swarm Agent ', '') || ''
   const agentBridge = await getAgentBridgeHealth()
   ctx.body = {
     status: 'ok',
diff --git a/packages/server/src/controllers/hermes/kanban.ts b/packages/server/src/controllers/hermes/kanban.ts
index 31545577..759a6423 100644
--- a/packages/server/src/controllers/hermes/kanban.ts
+++ b/packages/server/src/controllers/hermes/kanban.ts
@@ -1,10 +1,11 @@
 import type { Context } from 'koa'
-import { readFile } from 'fs/promises'
+import { readFile, stat, copyFile } from 'fs/promises'
 import { resolve, normalize } from 'path'
 import { homedir } from 'os'
 import * as kanbanCli from '../../services/hermes/hermes-kanban'
 import { isPathWithin } from '../../services/hermes/hermes-path'
-import { listProfileNamesFromDisk } from '../../services/hermes/hermes-profile'
+import { listProfileNamesFromDisk, getActiveProfileName, getProfileDir } from '../../services/hermes/hermes-profile'
+import { safeFileStore } from '../../services/safe-file-store'
 import {
   searchSessionSummariesWithProfile,
   getSessionDetailFromDbWithProfile,
@@ -398,6 +399,7 @@ export async function create(ctx: Context) {
   const assignee = optionalString(payload.assignee, 'assignee')
   const priority = optionalInteger(payload.priority, 'priority')
   const tenant = optionalString(payload.tenant, 'tenant')
+  const project = optionalString(payload.project, 'project')
   const workspace = optionalString(payload.workspace, 'workspace')
   const branch = optionalString(payload.branch, 'branch')
   const triage = optionalBoolean(payload.triage, 'triage')
@@ -406,7 +408,7 @@ export async function create(ctx: Context) {
   const maxRetries = optionalPositiveInteger(payload.maxRetries, 'maxRetries', 100)
   const goalMode = optionalBoolean(payload.goalMode, 'goalMode')
   const goalMaxTurns = optionalPositiveInteger(payload.goalMaxTurns, 'goalMaxTurns', 100)
-  if (rejectBadRequest(ctx, title.error || body.error || assignee.error || priority.error || tenant.error || workspace.error || branch.error || triage.error || skills.error || maxRuntime.error || maxRetries.error || goalMode.error || goalMaxTurns.error)) return
+  if (rejectBadRequest(ctx, title.error || body.error || assignee.error || priority.error || tenant.error || project.error || workspace.error || branch.error || triage.error || skills.error || maxRuntime.error || maxRetries.error || goalMode.error || goalMaxTurns.error)) return
   const targetAssignee = assignee.value || requestedProfile(ctx) || undefined
   if (targetAssignee && denyProfileAccess(ctx, targetAssignee)) return
   const board = requestBoard(ctx)
@@ -418,6 +420,7 @@ export async function create(ctx: Context) {
       assignee: targetAssignee,
       priority: priority.value,
       tenant: tenant.value,
+      project: project.value,
       workspace: workspace.value,
       branch: branch.value,
       triage: triage.value,
@@ -755,6 +758,30 @@ export async function readArtifact(ctx: Context) {
   }
 }
 
+interface WorkspaceFileEntry { name: string; isDir: boolean; size: number; modified: number; children?: WorkspaceFileEntry[] }
+async function readDirTree(dir: string, depth: number): Promise<WorkspaceFileEntry[]> {
+  if (depth <= 0) return []; let names: string[]; try { names = await readdir(dir) } catch { return [] }
+  return Promise.all(names.map(async (name) => {
+    const full = join(dir, name); let st: Awaited<ReturnType<typeof stat>>; try { st = await stat(full) } catch { return { name, isDir: false, size: 0, modified: 0 } }
+    const entry: WorkspaceFileEntry = { name, isDir: st.isDirectory(), size: st.size, modified: st.mtimeMs }
+    if (entry.isDir && depth > 1) entry.children = await readDirTree(full, depth - 1); return entry
+  }))
+}
+export async function listWorkspaceFiles(ctx: Context) {
+  const taskId = ctx.query.task_id as string | undefined; const board = (ctx.query.board as string | undefined) || 'default'; const sub = (ctx.query.path as string | undefined) ?? ''; const depthRaw = parseInt(ctx.query.depth as string, 10); const depth = Number.isFinite(depthRaw) ? Math.min(Math.max(depthRaw, 1), 4) : 2
+  if (!taskId) { ctx.status = 400; ctx.body = { error: 'task_id is required' }; return }
+  const detail = await kanbanCli.getTask(taskId, { board }); const wsPath = detail?.task?.workspace_path; if (!wsPath) { ctx.body = { path: '', entries: [] }; return }
+  const kanbanHome = resolve(homedir(), '.hermes', 'kanban'); const workspacesRoot = board === 'default' ? resolve(kanbanHome, 'workspaces') : resolve(kanbanHome, 'boards', board, 'workspaces')
+  const root = resolve(workspacesRoot, normalize(wsPath)); if (!isPathWithin(root, workspacesRoot)) { ctx.status = 403; ctx.body = { error: 'workspace path out of sandbox' }; return }
+  const target = sub ? resolve(root, normalize(sub)) : root; if (!isPathWithin(target, root)) { ctx.status = 403; ctx.body = { error: 'sub path out of task workspace' }; return }
+  try { const entries = await readDirTree(target, depth); ctx.body = { path: sub, entries } } catch (err: any) { ctx.status = 500; ctx.body = { error: err.message } }
+}
+export async function listTimeline(ctx: Context) {
+  const limitRaw = parseInt(ctx.query.limit as string, 10); const limit = Number.isFinite(limitRaw) ? Math.min(Math.max(limitRaw, 1), 500) : 100; const since = parseInt(ctx.query.since as string, 10); const sinceTs = Number.isFinite(since) ? since : 0
+  const allTasks = await kanbanCli.listTasks({}); const items: Array<Record<string, unknown>> = []
+  await Promise.all(allTasks.map(async (t) => { const detail = await kanbanCli.getTask(t.id); if (!detail) return; const archived = detail.task.status === 'archived'; for (const e of detail.events ?? []) items.push({ source: 'event', id: 'evt-' + e.id, taskId: t.id, taskTitle: t.title, taskArchived: archived, ts: e.created_at, kind: e.kind, payload: e.payload }); for (const c of detail.comments ?? []) items.push({ source: 'comment', id: 'cmt-' + c.id, taskId: t.id, taskTitle: t.title, taskArchived: archived, ts: c.created_at, author: c.author, body: c.body }) }))
+  const filtered = items.filter(x => (x.ts as number) >= sinceTs).sort((a, b) => (b.ts as number) - (a.ts as number)).slice(0, limit); ctx.body = { items: filtered, total: filtered.length }
+}
 export async function searchSessions(ctx: Context) {
   const { task_id, profile, q } = ctx.query as {
     task_id?: string
@@ -812,3 +839,554 @@ export async function searchSessions(ctx: Context) {
     ctx.body = { error: err.message }
   }
 }
+
+export async function deleteTask(ctx: Context) {
+  try {
+    const taskId = ctx.params.id
+    const board = ctx.query.board as string | undefined
+    await kanbanCli.deleteTask(taskId, { board })
+    ctx.body = { ok: true }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function patchTask(ctx: Context) {
+  try {
+    const taskId = ctx.params.id
+    const board = ctx.query.board as string | undefined
+    const body = ctx.request.body as any
+    const task = await kanbanCli.patchTask(taskId, { ...body, board })
+    ctx.body = { task }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function decomposeTask(ctx: Context) {
+  try {
+    const taskId = ctx.params.id
+    const board = ctx.query.board as string | undefined
+    const body = (ctx.request.body as any) || {}
+    const result = await kanbanCli.decomposeTask(taskId, { board, author: body.author })
+    ctx.body = { result }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+// ─── Orchestration ──────────────────────────────────────────────
+
+import { readConfigYaml, writeConfigYaml } from '../../services/config-helpers'
+
+export async function getOrchestration(ctx: Context) {
+  try {
+    const config = await readConfigYaml()
+    const kanban = config.kanban || {}
+    const explicitOrch = kanban.orchestrator_profile || null
+    const explicitDefault = kanban.default_assignee || null
+    const autoDecompose = kanban.auto_decompose !== false // default true
+    const autoPromoteChildren = kanban.auto_promote_children === true // default false
+
+    // Resolve fallbacks
+    const profiles = listProfileNamesFromDisk()
+    const activeProfile = config.profile?.default || 'default'
+    const resolvedOrch = explicitOrch || activeProfile
+    const resolvedDefault = explicitDefault || activeProfile
+
+    ctx.body = {
+      orchestrator_profile: explicitOrch,
+      default_assignee: explicitDefault,
+      auto_decompose: autoDecompose,
+      auto_promote_children: autoPromoteChildren,
+      resolved_orchestrator_profile: resolvedOrch,
+      resolved_default_assignee: resolvedDefault,
+      active_profile: activeProfile,
+      profiles,
+    }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function setOrchestration(ctx: Context) {
+  const bodyResult = requestBody(ctx)
+  if (rejectBadRequest(ctx, bodyResult.error)) return
+  const body = bodyResult.body
+
+  try {
+    const config = await readConfigYaml()
+    const kanban = config.kanban || {}
+
+    // Validate profile names if provided
+    const profiles = new Set(listProfileNamesFromDisk())
+    if (hasOwn(body, 'orchestrator_profile')) {
+      const orch = body.orchestrator_profile
+      if (orch !== null && orch !== undefined && !profiles.has(orch as string)) {
+        ctx.status = 400
+        ctx.body = { error: `Profile "${orch}" does not exist` }
+        return
+      }
+      if (orch === null || orch === undefined) {
+        delete kanban.orchestrator_profile
+      } else {
+        kanban.orchestrator_profile = orch as string
+      }
+    }
+    if (hasOwn(body, 'default_assignee')) {
+      const def = body.default_assignee
+      if (def !== null && def !== undefined && !profiles.has(def as string)) {
+        ctx.status = 400
+        ctx.body = { error: `Profile "${def}" does not exist` }
+        return
+      }
+      if (def === null || def === undefined) {
+        delete kanban.default_assignee
+      } else {
+        kanban.default_assignee = def as string
+      }
+    }
+    if (hasOwn(body, 'auto_decompose')) {
+      kanban.auto_decompose = !!body.auto_decompose
+    }
+    if (hasOwn(body, 'auto_promote_children')) {
+      kanban.auto_promote_children = !!body.auto_promote_children
+    }
+
+    config.kanban = kanban
+    await writeConfigYaml(config)
+
+    // Return updated settings
+    await getOrchestration(ctx)
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+// ─── Attachments ──────────────────────────────────────────────
+
+import { mkdir, writeFile, unlink, readdir } from 'fs/promises'
+import { join, basename, dirname } from 'path'
+import { createHash } from 'crypto'
+
+async function syncAttachmentToWorkspace(taskId: string, board: string | undefined, filename: string, srcPath: string): Promise<void> {
+  try {
+    const detail = await kanbanCli.getTask(taskId, { board: board || 'default' })
+    const wsPath = detail?.task?.workspace_path; if (!wsPath) return
+    const kanbanHome = resolve(homedir(), '.hermes', 'kanban')
+    const workspacesRoot = (!board || board === 'default') ? resolve(kanbanHome, 'workspaces') : resolve(kanbanHome, 'boards', board, 'workspaces')
+    const root = resolve(workspacesRoot, normalize(wsPath)); if (!isPathWithin(root, workspacesRoot)) return
+    await mkdir(root, { recursive: true }); await copyFile(srcPath, join(root, filename)).catch(() => {})
+  } catch { }
+}
+async function removeAttachmentFromWorkspace(taskId: string, board: string | undefined, filename: string): Promise<void> {
+  try {
+    const detail = await kanbanCli.getTask(taskId, { board: board || 'default' })
+    const wsPath = detail?.task?.workspace_path; if (!wsPath) return
+    const kanbanHome = resolve(homedir(), '.hermes', 'kanban')
+    const workspacesRoot = (!board || board === 'default') ? resolve(kanbanHome, 'workspaces') : resolve(kanbanHome, 'boards', board, 'workspaces')
+    const root = resolve(workspacesRoot, normalize(wsPath)); if (!isPathWithin(root, workspacesRoot)) return
+    await unlink(join(root, filename)).catch(() => {})
+  } catch { }
+}
+const MAX_ATTACHMENT_BYTES = 25 * 1024 * 1024 // 25 MB
+
+function attachmentsRoot(board?: string): string {
+  const override = process.env.HERMES_KANBAN_ATTACHMENTS_ROOT?.trim()
+  if (override) return resolve(override)
+  const base = resolve(homedir(), '.hermes', 'kanban', 'attachments')
+  if (!board || board === 'default') return base
+  return join(base, board)
+}
+
+function taskAttachmentsDir(taskId: string, board?: string): string {
+  return join(attachmentsRoot(board), taskId)
+}
+
+function safeAttachmentName(filename: string): string {
+  const name = basename(filename).trim()
+  if (!name || name.startsWith('.')) return 'unnamed'
+  // Strip control characters and path separators
+  return name.replace(/[\x00-\x1f\\/]/g, '_').slice(0, 255)
+}
+
+interface AttachmentRecord {
+  id: number
+  task_id: string
+  filename: string
+  stored_path: string
+  content_type: string | null
+  size: number
+  uploaded_by: string | null
+  created_at: number
+}
+
+// In-memory attachment store (simple JSON file per board)
+async function readAttachmentsMeta(board?: string): Promise<AttachmentRecord[]> {
+  const metaPath = join(attachmentsRoot(board), '.attachments.json')
+  try {
+    const raw = await readFile(metaPath, 'utf-8')
+    return JSON.parse(raw)
+  } catch {
+    return []
+  }
+}
+
+async function writeAttachmentsMeta(records: AttachmentRecord[], board?: string): Promise<void> {
+  const metaPath = join(attachmentsRoot(board), '.attachments.json')
+  await mkdir(dirname(metaPath), { recursive: true })
+  await writeFile(metaPath, JSON.stringify(records, null, 2) + '\n')
+}
+
+async function addAttachment(record: Omit<AttachmentRecord, 'id'>, board?: string): Promise<AttachmentRecord> {
+  const records = await readAttachmentsMeta(board)
+  const id = records.length > 0 ? Math.max(...records.map(r => r.id)) + 1 : 1
+  const newRecord = { ...record, id }
+  records.push(newRecord)
+  await writeAttachmentsMeta(records, board)
+  return newRecord
+}
+
+async function getAttachment(id: number, board?: string): Promise<AttachmentRecord | null> {
+  const records = await readAttachmentsMeta(board)
+  return records.find(r => r.id === id) || null
+}
+
+async function deleteAttachment(id: number, board?: string): Promise<AttachmentRecord | null> {
+  const records = await readAttachmentsMeta(board)
+  const idx = records.findIndex(r => r.id === id)
+  if (idx === -1) return null
+  const [removed] = records.splice(idx, 1)
+  await writeAttachmentsMeta(records, board)
+  // Best-effort file deletion
+  try { await unlink(removed.stored_path) } catch { /* ignore */ }
+  return removed
+}
+
+export async function listAttachments(ctx: Context) {
+  const taskId = ctx.params.id
+  const board = ctx.query.board as string | undefined
+  try {
+    const records = await readAttachmentsMeta(board)
+    const attachments = records.filter(r => r.task_id === taskId)
+    ctx.body = { attachments }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function uploadAttachment(ctx: Context) {
+  const taskId = ctx.params.id
+  const board = ctx.query.board as string | undefined
+
+  try {
+    // Parse multipart form data manually (project uses custom parser)
+    const contentType = ctx.get('content-type') || ''
+    if (!contentType.startsWith('multipart/form-data')) {
+      ctx.status = 400
+      ctx.body = { error: 'Expected multipart/form-data' }
+      return
+    }
+
+    const { parseMultipartBoundary, parseMultipartFilename, splitMultipart } = await import('../../lib/multipart')
+    const boundaryBuf = parseMultipartBoundary(contentType)
+    if (!boundaryBuf) {
+      ctx.status = 400
+      ctx.body = { error: 'Missing boundary' }
+      return
+    }
+
+    const chunks: Buffer[] = []
+    let totalSize = 0
+    for await (const chunk of (ctx as any).req) {
+      totalSize += chunk.length
+      if (totalSize > MAX_ATTACHMENT_BYTES) {
+        ctx.status = 413
+        ctx.body = { error: `Attachment too large (max ${MAX_ATTACHMENT_BYTES} bytes)` }
+        return
+      }
+      chunks.push(chunk)
+    }
+    const raw = Buffer.concat(chunks)
+    const parts = splitMultipart(raw, boundaryBuf)
+
+    let uploadedFile: { filename: string; data: Buffer; size: number } | null = null
+
+    for (const part of parts) {
+      const headerEnd = part.indexOf(Buffer.from('\r\n\r\n'))
+      if (headerEnd === -1) continue
+      const headerBuf = part.subarray(0, headerEnd)
+      const header = headerBuf.toString('utf-8')
+      const data = part.subarray(headerEnd + 4, part.length - 2)
+      const filename = parseMultipartFilename(header)
+      if (!filename) continue
+      uploadedFile = { filename, data, size: data.length }
+      break // Only process first file
+    }
+
+    if (!uploadedFile) {
+      ctx.status = 400
+      ctx.body = { error: 'No file uploaded' }
+      return
+    }
+
+    const safeName = safeAttachmentName(uploadedFile.filename)
+    const destDir = taskAttachmentsDir(taskId, board)
+    await mkdir(destDir, { recursive: true })
+
+    // Collision resolution
+    let candidate = safeName
+    let destPath = join(destDir, candidate)
+    let counter = 1
+    while (true) {
+      try {
+        await readFile(destPath)
+        // File exists, try next name
+        const extIndex = safeName.lastIndexOf('.')
+        const baseName = extIndex > 0 ? safeName.slice(0, extIndex) : safeName
+        const ext = extIndex > 0 ? safeName.slice(extIndex) : ''
+        candidate = `${baseName} (${counter})${ext}`
+        destPath = join(destDir, candidate)
+        counter++
+      } catch {
+        // File doesn't exist, we can use this path
+        break
+      }
+    }
+
+    await writeFile(destPath, uploadedFile.data)
+    await syncAttachmentToWorkspace(taskId, board, candidate, destPath)
+
+    const uploadedBy = ctx.state.user?.username || 'dashboard'
+    const record = await addAttachment({
+      task_id: taskId,
+      filename: candidate,
+      stored_path: destPath,
+      content_type: null,
+      size: uploadedFile.size,
+      uploaded_by: uploadedBy,
+      created_at: Date.now(),
+    }, board)
+
+    ctx.body = { attachment: record }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function downloadAttachment(ctx: Context) {
+  const attachmentId = parseInt(ctx.params.id, 10)
+  const board = ctx.query.board as string | undefined
+
+  if (!Number.isFinite(attachmentId)) {
+    ctx.status = 400
+    ctx.body = { error: 'Invalid attachment ID' }
+    return
+  }
+
+  try {
+    const record = await getAttachment(attachmentId, board)
+    if (!record) {
+      ctx.status = 404
+      ctx.body = { error: 'Attachment not found' }
+      return
+    }
+
+    // Security check: ensure file is within attachments root
+    const root = resolve(attachmentsRoot(board))
+    const stored = resolve(record.stored_path)
+    if (!stored.startsWith(root + '/') && stored !== root) {
+      ctx.status = 403
+      ctx.body = { error: 'Invalid attachment path' }
+      return
+    }
+
+    const content = await readFile(stored)
+    ctx.set('Content-Disposition', `attachment; filename="${encodeURIComponent(record.filename)}"`)
+    ctx.set('Content-Type', record.content_type || 'application/octet-stream')
+    ctx.body = content
+  } catch (err: any) {
+    if (err.code === 'ENOENT') {
+      ctx.status = 404
+      ctx.body = { error: 'Attachment file not found' }
+      return
+    }
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function removeAttachment(ctx: Context) {
+  const attachmentId = parseInt(ctx.params.id, 10)
+  const board = ctx.query.board as string | undefined
+
+  if (!Number.isFinite(attachmentId)) {
+    ctx.status = 400
+    ctx.body = { error: 'Invalid attachment ID' }
+    return
+  }
+
+  try {
+    const record = await deleteAttachment(attachmentId, board)
+    if (!record) {
+      ctx.status = 404
+      ctx.body = { error: 'Attachment not found' }
+      return
+    }
+    ctx.body = { ok: true, id: attachmentId }
+    await removeAttachmentFromWorkspace(record.task_id, board, record.filename)
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+// ─── Home channel notification subscriptions ──────────────────────
+
+export async function homeChannels(ctx: Context) {
+  try {
+    const taskId = ctx.query.task as string | undefined || null
+    const board = requestBoard(ctx)
+    if (board === null) return
+
+    const channels = await kanbanCli.getHomeChannels(taskId, { board })
+    ctx.body = { home_channels: channels }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function debugHomeChannels(ctx: Context) {
+  try {
+    const profile = getActiveProfileName() || 'default'
+    const configPath = join(getProfileDir(profile), 'config.yaml')
+    
+    let config: Record<string, any> = {}
+    try {
+      config = await safeFileStore.readYaml(configPath)
+    } catch (err: any) {
+      ctx.body = { error: `Failed to read config: ${err.message}`, configPath }
+      return
+    }
+    
+    const gatewayPlatforms = config.gateway?.platforms || {}
+    const topLevelPlatforms = config.platforms || {}
+    const platforms = { ...gatewayPlatforms, ...topLevelPlatforms }
+    
+    const homes: any[] = []
+    for (const [platformName, platformCfg] of Object.entries(platforms)) {
+      if (!platformCfg || typeof platformCfg !== 'object') continue
+      const hc = (platformCfg as Record<string, any>).home_channel
+      
+      // Check env vars using the same mapping as listConfiguredHomeChannels
+      const upper = platformName.toUpperCase().replace(/-/g, '_')
+      const isMatrix = platformName === 'matrix'
+      const isEmail = platformName === 'email'
+      const envChatIdKey = isMatrix ? 'MATRIX_HOME_ROOM' : isEmail ? 'EMAIL_HOME_ADDRESS' : `${upper}_HOME_CHANNEL`
+      const envNameKey = isMatrix ? 'MATRIX_HOME_ROOM_NAME' : isEmail ? 'EMAIL_HOME_ADDRESS_NAME' : `${upper}_HOME_CHANNEL_NAME`
+      const envThreadIdKey = isMatrix ? 'MATRIX_HOME_ROOM_THREAD_ID' : isEmail ? 'EMAIL_HOME_ADDRESS_THREAD_ID' : `${upper}_HOME_CHANNEL_THREAD_ID`
+      
+      const envChatId = process.env[envChatIdKey]
+      const envName = process.env[envNameKey]
+      const envThreadId = process.env[envThreadIdKey]
+      
+      const chatId = envChatId || hc?.chat_id || ''
+      if (chatId) {
+        homes.push({
+          platform: platformName,
+          chat_id: chatId,
+          thread_id: envThreadId || hc?.thread_id || '',
+          name: envName || hc?.name || 'Home',
+          source: envChatId ? 'env' : 'yaml',
+          envKeys: { chatId: envChatIdKey, name: envNameKey, threadId: envThreadIdKey },
+        })
+      }
+    }
+    
+    ctx.body = {
+      configPath,
+      profile,
+      configKeys: Object.keys(config),
+      gatewayPlatformsKeys: Object.keys(gatewayPlatforms),
+      topLevelPlatformsKeys: Object.keys(topLevelPlatforms),
+      homesFound: homes.length,
+      homes,
+    }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function subscribeHome(ctx: Context) {
+  const taskId = ctx.params.id
+  const platform = ctx.params.platform
+  if (!taskId || !platform) {
+    ctx.status = 400
+    ctx.body = { error: 'Missing task_id or platform' }
+    return
+  }
+
+  try {
+    // Fetch configured home channels to find the chat_id for this platform
+    const homes = await kanbanCli.getHomeChannels(null)
+    const home = homes.find(h => h.platform === platform)
+    if (!home) {
+      ctx.status = 404
+      ctx.body = { error: `No home channel configured for platform "${platform}"` }
+      return
+    }
+
+    const board = requestBoard(ctx)
+    if (board === null) return
+
+    await kanbanCli.subscribeHomeChannel(taskId, platform, home.chat_id, {
+      board,
+      threadId: home.thread_id || undefined,
+    })
+    ctx.body = { ok: true, task_id: taskId, home_channel: { platform, chat_id: home.chat_id, thread_id: home.thread_id, name: home.name } }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
+
+export async function unsubscribeHome(ctx: Context) {
+  const taskId = ctx.params.id
+  const platform = ctx.params.platform
+  if (!taskId || !platform) {
+    ctx.status = 400
+    ctx.body = { error: 'Missing task_id or platform' }
+    return
+  }
+
+  try {
+    const homes = await kanbanCli.getHomeChannels(null)
+    const home = homes.find(h => h.platform === platform)
+    if (!home) {
+      ctx.status = 404
+      ctx.body = { error: `No home channel configured for platform "${platform}"` }
+      return
+    }
+
+    const board = requestBoard(ctx)
+    if (board === null) return
+
+    await kanbanCli.unsubscribeHomeChannel(taskId, platform, home.chat_id, {
+      board,
+      threadId: home.thread_id || undefined,
+    })
+    ctx.body = { ok: true, task_id: taskId }
+  } catch (err: any) {
+    ctx.status = 500
+    ctx.body = { error: err.message }
+  }
+}
diff --git a/packages/server/src/controllers/hermes/profiles.ts b/packages/server/src/controllers/hermes/profiles.ts
index f461fa74..f71b80f2 100644
--- a/packages/server/src/controllers/hermes/profiles.ts
+++ b/packages/server/src/controllers/hermes/profiles.ts
@@ -747,13 +747,13 @@ export async function switchProfile(ctx: any) {
 
       const profileConfig = join(detail.path, 'config.yaml')
       if (!existsSync(profileConfig)) {
-        writeFileSync(profileConfig, '# Hermes Agent Configuration\n', 'utf-8')
+        writeFileSync(profileConfig, '# Swarm Agent Configuration\n', 'utf-8')
         logger.info('Created config.yaml for: %s', detail.path)
       }
 
       const profileEnv = join(detail.path, '.env')
       if (!existsSync(profileEnv)) {
-        writeFileSync(profileEnv, '# Hermes Agent Environment Configuration\n', 'utf-8')
+        writeFileSync(profileEnv, '# Swarm Agent Environment Configuration\n', 'utf-8')
         logger.info('Created .env for: %s', detail.path)
       }
     } catch (err: any) {
diff --git a/packages/server/src/custom b/packages/server/src/custom
new file mode 120000
index 00000000..3c02b52a
--- /dev/null
+++ b/packages/server/src/custom
@@ -0,0 +1 @@
+/Volumes/nvme2230/lab/ncwk/overlay/custom/server
\ No newline at end of file
diff --git a/packages/server/src/db/hermes/schemas.ts b/packages/server/src/db/hermes/schemas.ts
index ddbf9f1d..47734a43 100644
--- a/packages/server/src/db/hermes/schemas.ts
+++ b/packages/server/src/db/hermes/schemas.ts
@@ -204,6 +204,11 @@ export const USERS_SCHEMA: Record<string, string> = {
   updated_at: 'INTEGER NOT NULL',
   last_login_at: 'INTEGER',
   avatar: "TEXT NOT NULL DEFAULT ''",
+  matrix_user_id: 'TEXT UNIQUE',
+  matrix_display_name: 'TEXT',
+  matrix_avatar_url: 'TEXT',
+  matrix_homeserver_url: 'TEXT',
+  auth_source: "TEXT NOT NULL DEFAULT 'local'",
 }
 
 export const USER_PROFILES_TABLE = 'user_profiles'
@@ -515,11 +520,22 @@ function addMissingSafeColumns(
 
   for (const [columnName, columnDef] of Object.entries(schema)) {
     if (existingColumns.has(columnName)) continue
+    // SQLite's ALTER TABLE ADD COLUMN cannot carry a UNIQUE constraint, so
+    // previously UNIQUE columns (e.g. users.matrix_user_id) were skipped on
+    // pre-existing tables, leaving login queries to fail with
+    // "no such column". Add the column without the constraint and enforce
+    // uniqueness via a separate unique index instead. Multiple NULLs are
+    // allowed under a SQLite unique index, so existing rows are unaffected.
     if (!canAddColumnToExistingTable(columnDef)) {
       console.warn(`[Schema] ${tableName}.${columnName} cannot be added safely to existing table; skipping`)
       continue
     }
-    db.exec(`ALTER TABLE ${quoteIdentifier(tableName)} ADD COLUMN ${quoteIdentifier(columnName)} ${columnDef}`)
+    const bareDef = columnDef.replace(/\s+UNIQUE\b/i, '')
+    db.exec(`ALTER TABLE ${quoteIdentifier(tableName)} ADD COLUMN ${quoteIdentifier(columnName)} ${bareDef}`)
+    if (/\bUNIQUE\b/i.test(columnDef)) {
+      const indexName = `idx_${tableName}_${columnName}_unique`
+      db.exec(`CREATE UNIQUE INDEX IF NOT EXISTS ${quoteIdentifier(indexName)} ON ${quoteIdentifier(tableName)} (${quoteIdentifier(columnName)})`)
+    }
   }
 }
 
diff --git a/packages/server/src/db/hermes/session-store.ts b/packages/server/src/db/hermes/session-store.ts
index f3315e6c..e0f82ee5 100644
--- a/packages/server/src/db/hermes/session-store.ts
+++ b/packages/server/src/db/hermes/session-store.ts
@@ -439,6 +439,20 @@ export function listSessions(profile?: string, source?: string, limit = 2000): H
   return rows.map(mapSessionRow)
 }
 
+// HERMES_CUSTOM[ChatRunAutoJoin] BEGIN: list session ids owned by a user, for socket auto-join
+// Enables cockpit unread notifications: the chat-run socket auto-joins all of a user's
+// session rooms on connect so it receives run.completed / peer_user_message events for
+// sessions the user is not currently viewing. Unread is in-memory on the client.
+export function listSessionIdsByUserId(userId: string, limit = 2000): string[] {
+  if (!isSqliteAvailable()) return []
+  const db = getDb()!
+  const rows = db.prepare(
+    `SELECT id FROM ${SESSIONS_TABLE} WHERE user_id = ? ORDER BY last_active DESC LIMIT ?`,
+  ).all(String(userId), limit) as Record<string, unknown>[]
+  return rows.map(row => String(row.id || '')).filter(Boolean)
+}
+// HERMES_CUSTOM[ChatRunAutoJoin] END
+
 export function searchSessions(profile: string | null | undefined, query: string, limit = 20): HermesSessionSearchRow[] {
   if (!isSqliteAvailable()) return []
   const profileFilter = profile?.trim()
diff --git a/packages/server/src/db/hermes/sessions-db.ts b/packages/server/src/db/hermes/sessions-db.ts
index 67470d4e..9f88d26a 100644
--- a/packages/server/src/db/hermes/sessions-db.ts
+++ b/packages/server/src/db/hermes/sessions-db.ts
@@ -210,7 +210,7 @@ function normalizeTitleLikeQuery(query: string): string {
 function shouldUseLiteralContentSearch(query: string): boolean {
   const trimmed = query.trim()
   if (!trimmed) return false
-  if (/[^\p{L}\p{N}\s"*.-]/u.test(trimmed)) return true
+  if (new RegExp('[^\\p{L}\\p{N}\\s"*.-]', 'u').test(trimmed)) return true
 
   const tokens = trimmed.match(/"[^"]*"\*?|\S+/g)
   if (!tokens) return true
@@ -224,17 +224,17 @@ function shouldUseLiteralContentSearch(query: string): boolean {
     if (raw.startsWith('"') && raw.endsWith('"')) {
       const inner = raw.slice(1, -1)
       if (!inner.trim()) return true
-      if (!/^[\p{L}\p{N}\s.-]+$/u.test(inner)) return true
-      if ((inner.includes('.') || inner.includes('-')) && !/^[\p{L}\p{N}]+(?:[.-][\p{L}\p{N}]+)*(?:\s+[\p{L}\p{N}]+(?:[.-][\p{L}\p{N}]+)*)*$/u.test(inner)) return true
+      if (!new RegExp('^[\\p{L}\\p{N}\\s.-]+$', 'u').test(inner)) return true
+      if ((inner.includes('.') || inner.includes('-')) && !new RegExp('^[\\p{L}\\p{N}]+(?:[.-][\\p{L}\\p{N}]+)*(?:\\s+[\\p{L}\\p{N}]+(?:[.-][\\p{L}\\p{N}]+)*)*$', 'u').test(inner)) return true
       continue
     }
 
     if (raw.includes('.') || raw.includes('-')) {
-      if (!/^[\p{L}\p{N}]+(?:[.-][\p{L}\p{N}]+)*$/u.test(raw)) return true
+      if (!new RegExp('^[\\p{L}\\p{N}]+(?:[.-][\\p{L}\\p{N}]+)*$', 'u').test(raw)) return true
       continue
     }
 
-    if (!/^[\p{L}\p{N}]+$/u.test(raw)) return true
+    if (!new RegExp('^[\\p{L}\\p{N}]+$', 'u').test(raw)) return true
   }
 
   return false
@@ -290,7 +290,7 @@ function sanitizeFtsQuery(query: string): string {
   sanitized = sanitized.replace(/(^|\s)\*/g, '$1')
   sanitized = sanitized.trim().replace(/^(AND|OR|NOT)\b\s*/i, '')
   sanitized = sanitized.trim().replace(/\s+(AND|OR|NOT)\s*$/i, '')
-  sanitized = sanitized.replace(/\b([\p{L}\p{N}]+(?:[.-][\p{L}\p{N}]+)+)\b/gu, '"$1"')
+  sanitized = sanitized.replace(new RegExp('\b([\\p{L}\\p{N}]+(?:[.-][\\p{L}\\p{N}]+)+)\b', 'gu'), '"$1"')
 
   for (let i = 0; i < quotedParts.length; i += 1) {
     sanitized = sanitized.replace(`\u0000Q${i}\u0000`, quotedParts[i])
diff --git a/packages/server/src/db/hermes/users-store.ts b/packages/server/src/db/hermes/users-store.ts
index 0cc66828..158cae0f 100644
--- a/packages/server/src/db/hermes/users-store.ts
+++ b/packages/server/src/db/hermes/users-store.ts
@@ -16,6 +16,11 @@ export interface UserRecord {
   updated_at: number
   last_login_at: number | null
   avatar: string
+  matrix_user_id: string | null
+  matrix_display_name: string | null
+  matrix_avatar_url: string | null
+  matrix_homeserver_url: string | null
+  auth_source: 'local' | 'matrix'
 }
 
 export interface UserProfileRecord {
@@ -35,6 +40,9 @@ export interface UserSummary {
   created_at: number
   updated_at: number
   last_login_at: number | null
+  matrix_user_id: string | null
+  matrix_display_name: string | null
+  matrix_avatar_url: string | null
 }
 
 export const DEFAULT_USERNAME = 'admin'
@@ -82,6 +90,13 @@ export function findUserByUsername(username: string): UserRecord | null {
   return row || null
 }
 
+export function findByMatrixUserId(matrixUserId: string): UserRecord | null {
+  const db = getDb()
+  if (!db) return null
+  const row = db.prepare(`SELECT * FROM ${USERS_TABLE} WHERE matrix_user_id = ?`).get(matrixUserId) as UserRecord | undefined
+  return row || null
+}
+
 export function findFirstUser(): UserRecord | null {
   const db = getDb()
   if (!db) return null
@@ -93,7 +108,7 @@ export function listUsers(): UserSummary[] {
   const db = getDb()
   if (!db) return []
   const users = db.prepare(
-    `SELECT id, username, role, status, created_at, updated_at, last_login_at FROM ${USERS_TABLE} ORDER BY id ASC`
+    `SELECT id, username, role, status, created_at, updated_at, last_login_at, matrix_user_id, matrix_display_name, matrix_avatar_url FROM ${USERS_TABLE} ORDER BY id ASC`
   ).all() as Array<Omit<UserSummary, 'profiles' | 'default_profile'>>
   return users.map(user => {
     const profiles = listUserProfiles(user.id)
@@ -162,6 +177,67 @@ export function touchUserLogin(userId: UserId, at = Date.now()): void {
   db.prepare(`UPDATE ${USERS_TABLE} SET last_login_at = ?, updated_at = ? WHERE id = ?`).run(at, at, id)
 }
 
+export function syncMatrixMetadata(
+  userId: UserId,
+  data: {
+    displayName?: string
+    avatarUrl?: string
+    lastLoginAt?: number
+  }
+): boolean {
+  const db = getDb()
+  if (!db) return false
+  const id = normalizeUserId(userId)
+  if (!id) return false
+  const sets: string[] = []
+  const values: (string | number)[] = []
+  if (data.displayName !== undefined) {
+    sets.push('matrix_display_name = ?')
+    values.push(data.displayName)
+  }
+  if (data.avatarUrl !== undefined) {
+    sets.push('matrix_avatar_url = ?')
+    values.push(data.avatarUrl)
+  }
+  if (data.lastLoginAt !== undefined) {
+    sets.push('last_login_at = ?')
+    values.push(data.lastLoginAt)
+  }
+  if (sets.length === 0) return false
+  sets.push('updated_at = ?')
+  values.push(Date.now())
+  values.push(id)
+  const result = db.prepare(`UPDATE ${USERS_TABLE} SET ${sets.join(', ')} WHERE id = ?`).run(...values)
+  return result.changes > 0
+}
+
+export function updateMatrixUserMetadata(
+  matrixUserId: string,
+  data: {
+    role?: UserRole
+    status?: UserStatus
+  }
+): boolean {
+  const db = getDb()
+  if (!db) return false
+  const sets: string[] = []
+  const values: (string | number)[] = []
+  if (data.role !== undefined) {
+    sets.push('role = ?')
+    values.push(data.role)
+  }
+  if (data.status !== undefined) {
+    sets.push('status = ?')
+    values.push(data.status)
+  }
+  if (sets.length === 0) return false
+  sets.push('updated_at = ?')
+  values.push(Date.now())
+  values.push(matrixUserId)
+  const result = db.prepare(`UPDATE ${USERS_TABLE} SET ${sets.join(', ')} WHERE matrix_user_id = ?`).run(...values)
+  return result.changes > 0
+}
+
 export function updateUserPassword(userId: UserId, password: string): boolean {
   const db = getDb()
   if (!db) return false
@@ -224,6 +300,38 @@ export function createUser(input: {
   return user
 }
 
+export function createFromMatrix(data: {
+  matrixUserId: string
+  username: string
+  displayName: string
+  avatarUrl: string
+  homeserverUrl: string
+  role: UserRole
+}): UserRecord | null {
+  const db = getDb()
+  if (!db) return null
+  const now = Date.now()
+  db.prepare(
+    `INSERT INTO ${USERS_TABLE} (username, password_hash, role, status, created_at, updated_at, matrix_user_id, matrix_display_name, matrix_avatar_url, matrix_homeserver_url, auth_source)
+     VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
+  ).run(
+    data.username,
+    '', // No local password for Matrix users
+    data.role,
+    'active',
+    now,
+    now,
+    data.matrixUserId,
+    data.displayName,
+    data.avatarUrl,
+    data.homeserverUrl,
+    'matrix'
+  )
+  const user = findByMatrixUserId(data.matrixUserId)
+  if (user) replaceUserProfiles(user.id, [DEFAULT_PROFILE_NAME], DEFAULT_PROFILE_NAME)
+  return user
+}
+
 export function updateUser(input: {
   userId: UserId
   username?: string
diff --git a/packages/server/src/index.ts b/packages/server/src/index.ts
index 3415547f..10aadf6d 100644
--- a/packages/server/src/index.ts
+++ b/packages/server/src/index.ts
@@ -252,14 +252,18 @@ export async function bootstrap() {
       const skillInjector = new HermesSkillInjector()
       const injectionResult = await skillInjector.injectMissingSkills()
       if (injectionResult.injected.length > 0) {
+        const injectedSet = new Set(injectionResult.injected)
+        const injectedArray = Array.from(injectedSet)
         logger.info({
-          injected: [...new Set(injectionResult.injected)],
+          injected: injectedArray,
           targetCount: injectionResult.targets.length,
         }, '[bootstrap] bundled skills injected')
       }
       if (injectionResult.updated.length > 0) {
+        const updatedSet = new Set(injectionResult.updated)
+        const updatedArray = Array.from(updatedSet)
         logger.info({
-          updated: [...new Set(injectionResult.updated)],
+          updated: updatedArray,
           targetCount: injectionResult.targets.length,
         }, '[bootstrap] bundled skills updated')
       }
@@ -308,9 +312,66 @@ export async function bootstrap() {
   // SPA fallback
   const distDir = resolve(__dirname, '..', 'client')
   app.use(createStaticCompressionMiddleware())
+
+  // Serve element-web static files at /element-web/*
+  const elementWebDir = resolve(__dirname, '..', 'element-web')
+  app.use(async (ctx, next) => {
+    if (ctx.path.startsWith('/element-web/')) {
+      const relativePath = ctx.path.replace(/^\/element-web\//, '') || 'index.html'
+      try {
+        await send(ctx, relativePath, { root: elementWebDir })
+        return
+      } catch {
+        // Fall through to SPA fallback for index.html
+        await send(ctx, 'index.html', { root: elementWebDir })
+        return
+      }
+    }
+    // Dynamic element-web config.json — inject homeserver from query params or default
+    if (ctx.path === '/element-web/config.json') {
+      const homeserverUrl = (ctx.query.hs as string) || 'https://matrix.org'
+      ctx.type = 'application/json'
+      ctx.body = JSON.stringify({
+        'default_server_config': {
+          'm.homeserver': {
+            'base_url': homeserverUrl,
+            'server_name': new URL(homeserverUrl).hostname,
+          },
+        },
+        'disable_custom_urls': true,
+        'disable_guests': true,
+        'brand': 'Hermes Matrix Chat',
+      })
+      return
+    }
+    await next()
+  })
+
+  // Proxy /agent-health/* → hermes-agent health endpoint (for CockpitTopBar)
+  const agentHealthTarget = process.env.HERMES_AGENT_HEALTH_URL || 'http://127.0.0.1:8650'
+  app.use(async (ctx, next) => {
+    if (ctx.path.startsWith('/agent-health/')) {
+      const targetPath = ctx.path.replace('/agent-health', '/health')
+      const qs = ctx.querystring ? `?${ctx.querystring}` : ''
+      try {
+        const res = await fetch(`${agentHealthTarget}${targetPath}${qs}`)
+        ctx.status = res.status
+        const contentType = res.headers.get('content-type') || 'application/json'
+        ctx.type = contentType
+        ctx.body = await res.text()
+      } catch {
+        ctx.status = 502
+        ctx.body = JSON.stringify({ error: 'Agent health endpoint unreachable' })
+      }
+      return
+    }
+    await next()
+  })
+
   app.use(serve(distDir))
   app.use(async (ctx) => {
     if (!ctx.path.startsWith('/api') &&
+      !ctx.path.startsWith('/element-web/') &&
       ctx.path !== '/health' &&
       ctx.path !== '/upload' &&
       ctx.path !== '/webhook') {
diff --git a/packages/server/src/middleware/user-auth.ts b/packages/server/src/middleware/user-auth.ts
index c45a093c..55ba8112 100644
--- a/packages/server/src/middleware/user-auth.ts
+++ b/packages/server/src/middleware/user-auth.ts
@@ -15,6 +15,7 @@ export interface AuthenticatedUser {
   username: string
   role: UserRole
   profiles?: string[]
+  matrix_user_id?: string
 }
 
 export interface RequestProfile {
@@ -109,16 +110,17 @@ function isProtectedHttpPath(path: string): boolean {
 }
 
 export function signUserJwt(
-  user: Pick<UserRecord, 'id' | 'username' | 'role'>,
+  user: Pick<UserRecord, 'id' | 'username' | 'role' | 'matrix_user_id'>,
   secret: string,
   now = Date.now(),
   expiresSeconds = DEFAULT_EXPIRES_SECONDS,
 ): string {
   const iat = Math.floor(now / 1000)
-  const payload: JwtPayload = {
+  const payload = {
     sub: String(user.id),
     username: user.username,
     role: user.role,
+    matrix_user_id: user.matrix_user_id || null,
     type: 'access',
     aud: JWT_AUDIENCE,
     iat,
@@ -149,21 +151,22 @@ export function verifyUserJwt(token: string, secret: string, now = Date.now()):
   }
 }
 
-export async function issueUserJwt(user: Pick<UserRecord, 'id' | 'username' | 'role'>): Promise<string> {
+export async function issueUserJwt(user: Pick<UserRecord, 'id' | 'username' | 'role' | 'matrix_user_id'>): Promise<string> {
   const secret = await getJwtSecret()
   return signUserJwt(user, secret)
 }
 
-export async function issueModelRunJwt(user: Pick<UserRecord, 'id' | 'username' | 'role'>): Promise<string> {
+export async function issueModelRunJwt(user: Pick<UserRecord, 'id' | 'username' | 'role' | 'matrix_user_id'>): Promise<string> {
   const secret = await getJwtSecret()
   return signUserJwt(user, secret, Date.now(), MODEL_RUN_EXPIRES_SECONDS)
 }
 
-export function toAuthenticatedUser(user: Pick<UserRecord, 'id' | 'username' | 'role'>): AuthenticatedUser {
+export function toAuthenticatedUser(user: Pick<UserRecord, 'id' | 'username' | 'role' | 'matrix_user_id'>): AuthenticatedUser {
   const authenticated: AuthenticatedUser = {
     id: user.id,
     username: user.username,
     role: user.role,
+    matrix_user_id: user.matrix_user_id || undefined,
   }
   if (user.role !== 'super_admin') {
     authenticated.profiles = listUserProfiles(user.id).map(profile => profile.profile_name)
diff --git a/packages/server/src/routes/auth.ts b/packages/server/src/routes/auth.ts
index 897d064b..89ce33a3 100644
--- a/packages/server/src/routes/auth.ts
+++ b/packages/server/src/routes/auth.ts
@@ -7,6 +7,7 @@ export const authPublicRoutes = new Router()
 authPublicRoutes.get('/api/auth/status', ctrl.authStatus)
 authPublicRoutes.post('/api/auth/login', ctrl.login)
 authPublicRoutes.post('/api/auth/mcu-login', ctrl.microcontrollerLogin)
+authPublicRoutes.post('/api/auth/matrix-login', ctrl.matrixLogin) // NEW
 
 // Protected routes (auth required)
 export const authProtectedRoutes = new Router()
@@ -17,9 +18,20 @@ authProtectedRoutes.post('/api/auth/change-username', ctrl.changeUsername)
 authProtectedRoutes.get('/api/auth/avatar', ctrl.getMyAvatar)
 authProtectedRoutes.put('/api/auth/avatar', ctrl.updateMyAvatar)
 authProtectedRoutes.delete('/api/auth/password', ctrl.removePassword)
+
+// Existing local user management (preserved)
 authProtectedRoutes.get('/api/auth/users', requireSuperAdmin, ctrl.listManagedUsers)
 authProtectedRoutes.post('/api/auth/users', requireSuperAdmin, ctrl.createManagedUser)
 authProtectedRoutes.put('/api/auth/users/:id', requireSuperAdmin, ctrl.updateManagedUser)
 authProtectedRoutes.delete('/api/auth/users/:id', requireSuperAdmin, ctrl.deleteManagedUser)
+
+// NEW: Matrix user management
+authProtectedRoutes.get('/api/auth/matrix-users', requireSuperAdmin, ctrl.listMatrixUsers)
+authProtectedRoutes.post('/api/auth/matrix-users', requireSuperAdmin, ctrl.createMatrixUser)
+authProtectedRoutes.put('/api/auth/matrix-users/:id', requireSuperAdmin, ctrl.updateMatrixUser)
+authProtectedRoutes.delete('/api/auth/matrix-users/:id', requireSuperAdmin, ctrl.deleteMatrixUser)
+authProtectedRoutes.post('/api/auth/matrix-users/:id/deactivate', requireSuperAdmin, ctrl.deactivateMatrixUser)
+authProtectedRoutes.post('/api/auth/matrix-users/:id/reset-password', requireSuperAdmin, ctrl.resetMatrixUserPassword)
+
 authProtectedRoutes.get('/api/auth/locked-ips', ctrl.listLockedIps)
 authProtectedRoutes.delete('/api/auth/locked-ips', ctrl.unlockIpHandler)
diff --git a/packages/server/src/routes/hermes/download.ts b/packages/server/src/routes/hermes/download.ts
index d67ca71a..fea0ec34 100644
--- a/packages/server/src/routes/hermes/download.ts
+++ b/packages/server/src/routes/hermes/download.ts
@@ -1,5 +1,5 @@
 import Router from '@koa/router'
-import { basename, extname, isAbsolute } from 'path'
+import { basename, extname, isAbsolute, join, resolve } from 'path'
 import {
   createFileProvider,
   localProvider,
@@ -70,6 +70,7 @@ function requestedProfile(ctx: any): string {
 downloadRoutes.get('/api/hermes/download', async (ctx) => {
   const filePath = ctx.query.path as string | undefined
   const fileName = ctx.query.name as string | undefined
+  const root = (ctx.query.root as string | undefined)?.trim() || undefined
 
   if (!filePath) {
     ctx.status = 400
@@ -80,8 +81,13 @@ downloadRoutes.get('/api/hermes/download', async (ctx) => {
   try {
     const profile = requestedProfile(ctx)
     // Validate the path first
-    // Support both absolute and relative paths
-    const validPath = isAbsolute(filePath) ? validatePath(filePath) : resolveHermesPath(filePath, profile)
+    // Support both absolute and relative paths; when a root is provided,
+    // resolve the relative path against it (same semantics as files/list).
+    const validPath = isAbsolute(filePath)
+      ? validatePath(filePath)
+      : root
+        ? resolve(root, filePath)  // root acts as the base for relative paths
+        : resolveHermesPath(filePath, profile)
 
     // Choose provider: always use local for upload directory files
     let data: Buffer
diff --git a/packages/server/src/routes/hermes/files.ts b/packages/server/src/routes/hermes/files.ts
index f01227b3..75ce6333 100644
--- a/packages/server/src/routes/hermes/files.ts
+++ b/packages/server/src/routes/hermes/files.ts
@@ -1,10 +1,12 @@
 import Router from '@koa/router'
+import { join, resolve } from 'path'
 import {
   createFileProvider,
   resolveHermesPath,
   isSensitivePath,
   MAX_EDIT_SIZE,
 } from '../../services/hermes/file-provider'
+import { relativePathFromBase } from '../../services/hermes/hermes-path'
 import { requireSuperAdmin } from '../../middleware/user-auth'
 import { MultipartParseError, parseMultipartBoundary, parseMultipartFilename, splitMultipart } from '../../lib/multipart'
 
@@ -12,7 +14,14 @@ function requestedProfile(ctx: any): string | undefined {
   return ctx.state?.profile?.name
 }
 
+function requestedRoot(ctx: any): string | undefined {
+  const root = ctx.query.root as string | undefined
+  return root?.trim() || undefined
+}
+
 function resolveRequestPath(ctx: any, relativePath: string): string {
+  const root = requestedRoot(ctx)
+  if (root) return relativePath ? join(root, relativePath) : root
   return resolveHermesPath(relativePath, requestedProfile(ctx))
 }
 
@@ -20,7 +29,21 @@ async function createRequestFileProvider(ctx: any) {
   return createFileProvider(requestedProfile(ctx))
 }
 
+// When a `root` is in effect, the FileProvider still computes entry.path relative
+// to the profile home (or falls back to a bare filename for paths outside home).
+// The `?root=` client model assumes entry.path is relative to `root`, so that
+// navigation like fetchEntries(entry.path) resolves correctly via join(root, path).
+// Re-base each entry's path against `root` when present; keep the provider's value otherwise.
+function rebaseEntryPath<T extends { name: string; path: string }>(entry: T, dirAbsPath: string, root: string | undefined): T {
+  if (!root) return entry
+  const absEntry = resolve(dirAbsPath, entry.name)
+  const rel = relativePathFromBase(absEntry, root)
+  return { ...entry, path: rel ?? entry.name }
+}
+
 function withAbsolutePath<T extends { path: string }>(ctx: any, entry: T): T & { absolutePath: string } {
+  const root = requestedRoot(ctx)
+  if (root) return { ...entry, absolutePath: join(root, entry.path) }
   return { ...entry, absolutePath: resolveRequestPath(ctx, entry.path) }
 }
 
@@ -49,6 +72,7 @@ function handleError(ctx: any, err: any) {
 // GET /api/hermes/files/list?path=
 fileRoutes.get('/api/hermes/files/list', async (ctx) => {
   const relativePath = (ctx.query.path as string) || ''
+  const root = requestedRoot(ctx)
   try {
     const absPath = resolveRequestPath(ctx, relativePath)
     const provider = await createRequestFileProvider(ctx)
@@ -57,7 +81,8 @@ fileRoutes.get('/api/hermes/files/list', async (ctx) => {
       if (a.isDir !== b.isDir) return a.isDir ? -1 : 1
       return a.name.localeCompare(b.name)
     })
-    ctx.body = { entries: entries.map(entry => withAbsolutePath(ctx, entry)), path: relativePath, absolutePath: absPath }
+    const rebased = root ? entries.map(entry => rebaseEntryPath(entry, absPath, root)) : entries
+    ctx.body = { entries: rebased.map(entry => withAbsolutePath(ctx, entry)), path: relativePath, absolutePath: absPath }
   } catch (err: any) {
     handleError(ctx, err)
   }
diff --git a/packages/server/src/routes/hermes/kanban.ts b/packages/server/src/routes/hermes/kanban.ts
index 254b117c..49253444 100644
--- a/packages/server/src/routes/hermes/kanban.ts
+++ b/packages/server/src/routes/hermes/kanban.ts
@@ -1,5 +1,13 @@
 import Router from '@koa/router'
 import * as ctrl from '../../controllers/hermes/kanban'
+import { getDashboardUrl } from '../../config'
+
+// Separate router for the dashboard-url endpoint so it is not shadowed by
+// the /:id catch-all in the main kanban router.
+export const kanbanDashboardUrlRoutes = new Router()
+kanbanDashboardUrlRoutes.get('/api/hermes/kanban/dashboard-url', (ctx) => {
+  ctx.body = { url: getDashboardUrl() }
+})
 
 export const kanbanRoutes = new Router()
 
@@ -10,12 +18,24 @@ kanbanRoutes.get('/api/hermes/kanban/capabilities', ctrl.capabilities)
 kanbanRoutes.get('/api/hermes/kanban/stats', ctrl.stats)
 kanbanRoutes.get('/api/hermes/kanban/assignees', ctrl.assignees)
 kanbanRoutes.get('/api/hermes/kanban/diagnostics', ctrl.diagnostics)
+kanbanRoutes.get('/api/hermes/kanban/orchestration', ctrl.getOrchestration)
+kanbanRoutes.put('/api/hermes/kanban/orchestration', ctrl.setOrchestration)
 kanbanRoutes.post('/api/hermes/kanban/dispatch', ctrl.dispatch)
 kanbanRoutes.get('/api/hermes/kanban/artifact', ctrl.readArtifact)
+kanbanRoutes.get('/api/hermes/kanban/workspace-files', ctrl.listWorkspaceFiles)
+kanbanRoutes.get('/api/hermes/kanban/timeline', ctrl.listTimeline)
 kanbanRoutes.get('/api/hermes/kanban/search-sessions', ctrl.searchSessions)
 kanbanRoutes.post('/api/hermes/kanban/links', ctrl.linkTasks)
 kanbanRoutes.delete('/api/hermes/kanban/links', ctrl.unlinkTasks)
 kanbanRoutes.post('/api/hermes/kanban/tasks/bulk', ctrl.bulkUpdateTasks)
+kanbanRoutes.get('/api/hermes/kanban/home-channels', ctrl.homeChannels)
+kanbanRoutes.get('/api/hermes/kanban/debug-home-channels', ctrl.debugHomeChannels)
+kanbanRoutes.post('/api/hermes/kanban/:id/home-subscribe/:platform', ctrl.subscribeHome)
+kanbanRoutes.delete('/api/hermes/kanban/:id/home-subscribe/:platform', ctrl.unsubscribeHome)
+kanbanRoutes.get('/api/hermes/kanban/:id/attachments', ctrl.listAttachments)
+kanbanRoutes.post('/api/hermes/kanban/:id/attachments', ctrl.uploadAttachment)
+kanbanRoutes.get('/api/hermes/kanban/attachments/:id', ctrl.downloadAttachment)
+kanbanRoutes.delete('/api/hermes/kanban/attachments/:id', ctrl.removeAttachment)
 kanbanRoutes.get('/api/hermes/kanban', ctrl.list)
 kanbanRoutes.get('/api/hermes/kanban/:id', ctrl.get)
 kanbanRoutes.post('/api/hermes/kanban', ctrl.create)
@@ -28,3 +48,6 @@ kanbanRoutes.get('/api/hermes/kanban/:id/log', ctrl.taskLog)
 kanbanRoutes.post('/api/hermes/kanban/:id/reclaim', ctrl.reclaim)
 kanbanRoutes.post('/api/hermes/kanban/:id/reassign', ctrl.reassign)
 kanbanRoutes.post('/api/hermes/kanban/:id/specify', ctrl.specify)
+kanbanRoutes.post('/api/hermes/kanban/:id/decompose', ctrl.decomposeTask)
+kanbanRoutes.patch('/api/hermes/kanban/:id', ctrl.patchTask)
+kanbanRoutes.delete('/api/hermes/kanban/:id', ctrl.deleteTask)
diff --git a/packages/server/src/routes/index.ts b/packages/server/src/routes/index.ts
index e9402724..b74a5eea 100644
--- a/packages/server/src/routes/index.ts
+++ b/packages/server/src/routes/index.ts
@@ -33,7 +33,7 @@ import { fileRoutes } from './hermes/files'
 import { downloadRoutes } from './hermes/download'
 import { jobRoutes } from './hermes/jobs'
 import { cronHistoryRoutes } from './hermes/cron-history'
-import { kanbanRoutes } from './hermes/kanban'
+import { kanbanDashboardUrlRoutes, kanbanRoutes } from './hermes/kanban'
 import { workflowRoutes } from './hermes/workflows'
 import { ttsRoutes, ttsProtectedRoutes } from './hermes/tts'
 import { sttProtectedRoutes } from './hermes/stt'
@@ -93,6 +93,7 @@ export function registerRoutes(app: any, authMiddleware: Array<(ctx: Context, ne
   app.use(downloadRoutes.routes())
   app.use(jobRoutes.routes())
   app.use(cronHistoryRoutes.routes())
+  app.use(kanbanDashboardUrlRoutes.routes())
   app.use(kanbanRoutes.routes())
   app.use(workflowRoutes.routes())
   app.use(ttsProtectedRoutes.routes())
@@ -102,5 +103,5 @@ export function registerRoutes(app: any, authMiddleware: Array<(ctx: Context, ne
   app.use(performanceMonitorRoutes.routes())
   app.use(mcpRoutes.routes())                   // MCP management
   app.use(runtimeVersionRoutes.routes())         // Runtime and version management
-  app.use(writeGateRoutes.routes())              // Hermes Agent write approval review
+  app.use(writeGateRoutes.routes())              // Swarm Agent write approval review
 }
diff --git a/packages/server/src/services/app-config.ts b/packages/server/src/services/app-config.ts
index 2bebe954..da65b16e 100644
--- a/packages/server/src/services/app-config.ts
+++ b/packages/server/src/services/app-config.ts
@@ -14,7 +14,7 @@ export interface GatewayAutoStartConfig {
   enabled?: boolean
   include?: string[]
   exclude?: string[]
-  // Derived from Hermes Agent default config.yaml when returned by the config
+  // Derived from Swarm Agent default config.yaml when returned by the config
   // controller. It is not persisted in the Web UI app config.
   management?: 'auto' | 'per_profile' | 'unified'
 }
@@ -58,7 +58,7 @@ export interface AppConfig {
 
   // Web UI-only manually entered model IDs. Keys are provider -> model IDs.
   // This lets users persist provider-supported models that are absent from a
-  // provider catalog response without changing Hermes Agent config.yaml.
+  // provider catalog response without changing Swarm Agent config.yaml.
   customModels?: Record<string, string[]>
 
   // Web UI-only model picker visibility. This filters what the WUI exposes in
diff --git a/packages/server/src/services/hermes/agent-bridge/manager.ts b/packages/server/src/services/hermes/agent-bridge/manager.ts
index a8c93816..cb7ffe2a 100644
--- a/packages/server/src/services/hermes/agent-bridge/manager.ts
+++ b/packages/server/src/services/hermes/agent-bridge/manager.ts
@@ -13,7 +13,7 @@ const DEFAULT_AGENT_BRIDGE_RECOVERY_EXIT_TIMEOUT_MS = 5000
 const DEFAULT_AGENT_BRIDGE_RECOVERY_SIGKILL_WAIT_MS = 250
 const OPENROUTER_WEB_UI_ATTRIBUTION_ENV = {
   HERMES_OPENROUTER_APP_REFERER: 'https://hermes-studio.ai',
-  HERMES_OPENROUTER_APP_TITLE: 'Hermes Studio',
+  HERMES_OPENROUTER_APP_TITLE: 'Swarm Studio',
   HERMES_OPENROUTER_APP_CATEGORIES: 'cli-agent,personal-agent',
 } as const
 
diff --git a/packages/server/src/services/hermes/custom-providers-compat.ts b/packages/server/src/services/hermes/custom-providers-compat.ts
index 4299df30..5cf6c51c 100644
--- a/packages/server/src/services/hermes/custom-providers-compat.ts
+++ b/packages/server/src/services/hermes/custom-providers-compat.ts
@@ -1,7 +1,7 @@
 /**
- * Compatibility layer for the two `config.yaml` provider schemas used by Hermes Agent.
+ * Compatibility layer for the two `config.yaml` provider schemas used by Swarm Agent.
  *
- * Hermes Agent's v12 migration converts `custom_providers:` (a list of provider
+ * Swarm Agent's v12 migration converts `custom_providers:` (a list of provider
  * entries) into `providers:` (a dict keyed by provider name) and deletes the list.
  * Agent itself ships a compatibility helper, `get_compatible_custom_providers()`,
  * so every consumer in core sees a unified list view across both schemas:
@@ -195,7 +195,7 @@ export function normalizeCustomProviderEntry(
 
 /**
  * Return the full set of custom providers visible to Studio, drawn from both
- * schemas with name/base_url/model deduplication. Mirrors Hermes Agent's
+ * schemas with name/base_url/model deduplication. Mirrors Swarm Agent's
  * `get_compatible_custom_providers()`.
  *
  * Order: legacy list entries first, then v12+ dict entries — matching Agent's
diff --git a/packages/server/src/services/hermes/group-chat/agent-clients.ts b/packages/server/src/services/hermes/group-chat/agent-clients.ts
index 795064f0..da9770d6 100644
--- a/packages/server/src/services/hermes/group-chat/agent-clients.ts
+++ b/packages/server/src/services/hermes/group-chat/agent-clients.ts
@@ -416,7 +416,7 @@ class AgentClient {
         }
     }
 
-    // ─── Hermes Agent Bridge Integration ───────────────────────
+    // ─── Swarm Agent Bridge Integration ───────────────────────
 
     /**
      * Handle an @mention from the server side.
diff --git a/packages/server/src/services/hermes/group-chat/index.ts b/packages/server/src/services/hermes/group-chat/index.ts
index 395022fc..b8c9aa95 100644
--- a/packages/server/src/services/hermes/group-chat/index.ts
+++ b/packages/server/src/services/hermes/group-chat/index.ts
@@ -409,6 +409,16 @@ class ChatStorage {
         return this.mapStoredMessageRow(row)
     }
 
+    // HERMES_CUSTOM[GroupChatAutoJoin] BEGIN: last message per room for unread preview sync
+    getLastMessage(roomId: string): ChatMessage | null {
+        const rows = (this.db()?.prepare(
+            'SELECT id, roomId, senderId, senderName, content, timestamp, role, tool_call_id, tool_calls, tool_name, finish_reason, reasoning, reasoning_details, reasoning_content FROM gc_messages WHERE roomId = ? ORDER BY timestamp DESC, id DESC LIMIT 1'
+        ).all(roomId) || []) as any[]
+        if (!rows.length) return null
+        return this.mapStoredMessageRow(rows[0])
+    }
+    // HERMES_CUSTOM[GroupChatAutoJoin] END
+
     addMessage(msg: ChatMessage): void {
         this.upsertMessage(msg)
     }
@@ -900,6 +910,42 @@ export class GroupChatServer {
 
         logger.debug(`[GroupChat] Connected: ${userName} (socket=${socket.id}, user=${userId})`)
 
+        // HERMES_CUSTOM[GroupChatAutoJoin] BEGIN: auto-join user's visible rooms on connect
+        // so the socket receives live `message` events for unread tracking while the user
+        // is in the cockpit (not actively viewing any group room). Unread is in-memory on
+        // the client (resets on refresh — accepted). We emit `unread_sync` with the last
+        // message per room to seed the preview; unread count starts at 0.
+        if (requestedSource === 'human' && authenticatedUser) {
+            try {
+                const visibleRooms = authenticatedUser.role === 'super_admin'
+                    ? this.storage.getAllRooms()
+                    : this.storage.getRoomsForProfiles(authenticatedUser.profiles || [])
+                const sync: { roomId: string; unreadCount: number; lastMessage: { content: string; senderName: string; ts: number } | null }[] = []
+                for (const r of visibleRooms) {
+                    let room = this.rooms.get(r.id)
+                    if (!room) {
+                        room = new ChatRoom(r.id, r.name)
+                        this.rooms.set(r.id, room)
+                    }
+                    room.addOrUpdateMember(socket.id, userId, userName, description, 'human', '')
+                    if (typeof authUserId === 'number') {
+                        this.storage.addRoomMember(r.id, userId, userName, description, '', authUserId)
+                    }
+                    socket.join(r.id)
+                    const last = this.storage.getLastMessage(r.id)
+                    sync.push({
+                        roomId: r.id,
+                        unreadCount: 0,
+                        lastMessage: last ? { content: last.content ?? '', senderName: last.senderName ?? '', ts: typeof last.timestamp === 'number' ? last.timestamp : Date.now() } : null,
+                    })
+                }
+                socket.emit('unread_sync', sync)
+            } catch (err) {
+                logger.info(`[GroupChat] auto-join failed for ${userName}: ${(err as Error).message}`)
+            }
+        }
+        // HERMES_CUSTOM[GroupChatAutoJoin] END
+
         socket.on('join', (data: { roomId?: string; name?: string }, ack?: (response?: unknown) => void) => this.handleJoin(socket, data, ack))
         socket.on('message', (data: Partial<ChatMessage> & { roomId?: string; content: string | Array<Record<string, unknown>>; id?: string; mentionDepth?: number }, ack?: (response?: unknown) => void) => this.handleMessage(socket, data, ack))
         socket.on('message_stream_start', (data: { roomId?: string; id?: string; senderId?: string; senderName?: string; timestamp?: number }) => this.handleMessageStreamStart(socket, data))
diff --git a/packages/server/src/services/hermes/hermes-kanban.ts b/packages/server/src/services/hermes/hermes-kanban.ts
index e20dc99b..4edaf020 100644
--- a/packages/server/src/services/hermes/hermes-kanban.ts
+++ b/packages/server/src/services/hermes/hermes-kanban.ts
@@ -1,6 +1,9 @@
 import type { ChildProcess } from 'child_process'
 import { logger } from '../logger'
 import { execHermes, spawnHermes } from './hermes-process'
+import { safeFileStore } from '../safe-file-store'
+import { getActiveProfileName, getProfileDir } from './hermes-profile'
+import { join } from 'path'
 
 const execOpts = { windowsHide: true }
 const BOARD_SLUG_RE = /^[a-z0-9][a-z0-9_-]{0,63}$/
@@ -41,6 +44,7 @@ export interface KanbanTask {
   workspace_kind: string
   workspace_path: string | null
   tenant: string | null
+  project_id: string | null
   result: string | null
   skills: string[] | null
 }
@@ -153,6 +157,7 @@ export interface KanbanBulkTaskUpdateOptions extends KanbanBoardOptions {
   ids: string[]
   status?: KanbanTaskStatus
   assignee?: string | null
+  priority?: number
   archive?: boolean
   summary?: string
   reason?: string
@@ -251,7 +256,7 @@ export async function getCapabilities(): Promise<KanbanCapabilities> {
     { key: 'links', status: 'supported', canonicalRoute: '/links', canonicalCommand: 'link/unlink', requiresBoard: true },
     { key: 'bulk', status: 'partial', reason: 'WUI applies supported bulk-equivalent CLI transitions per id and returns per-task outcomes; direct priority/status patch parity remains deferred', canonicalRoute: '/tasks/bulk', canonicalCommand: 'bulk-equivalent via complete/block/unblock/archive/assign', requiresBoard: true },
     { key: 'events', status: 'partial', reason: 'WUI exposes a board-scoped WebSocket bridge backed by the canonical `kanban watch` stream; payload is currently a refresh invalidation signal, not a typed event model', canonicalRoute: '/events', canonicalCommand: 'watch', requiresBoard: true },
-    { key: 'homeSubscriptions', status: 'missing', reason: 'Deferred from current WUI parity batch', canonicalRoute: '/home-channels and subscription routes', canonicalCommand: 'notify-*', requiresBoard: true },
+    { key: 'homeSubscriptions', status: 'supported', canonicalRoute: '/home-channels and subscription routes', canonicalCommand: 'notify-subscribe/notify-list/notify-unsubscribe', requiresBoard: true },
   ]
   const supports = Object.fromEntries(capabilities.map(capability => [capability.key, capability.status === 'supported'])) as Record<string, boolean>
   const missing = capabilities
@@ -516,6 +521,7 @@ export async function createTask(
     assignee?: string
     priority?: number
     tenant?: string
+    project?: string
     workspace?: string
     branch?: string
     triage?: boolean
@@ -531,6 +537,7 @@ export async function createTask(
   if (opts?.assignee) args.push('--assignee', opts.assignee)
   if (opts?.priority !== undefined) args.push('--priority', String(opts.priority))
   if (opts?.tenant) args.push('--tenant', opts.tenant)
+  if (opts?.project) args.push('--project', opts.project)
   if (opts?.workspace) args.push('--workspace', opts.workspace)
   if (opts?.branch) args.push('--branch', opts.branch)
   if (opts?.triage) args.push('--triage')
@@ -660,3 +667,261 @@ export async function getAssignees(opts?: KanbanBoardOptions): Promise<KanbanAss
     throw new Error(`Failed to get kanban assignees: ${err.message}`)
   }
 }
+
+export async function deleteTask(taskId: string, opts?: KanbanBoardOptions): Promise<boolean> {
+  const args = [...boardArgs(opts?.board), 'rm', taskId, '--json']
+  try {
+    await execHermes(args, { maxBuffer: 50 * 1024 * 1024, timeout: 30000, ...execOpts })
+    return true
+  } catch (err: any) {
+    logger.error(err, 'Hermes CLI: kanban delete task failed')
+    throw new Error(`Failed to delete kanban task: ${err.message}`)
+  }
+}
+
+export async function decomposeTask(taskId: string, opts?: KanbanBoardOptions & { author?: string }): Promise<unknown> {
+  const args = [...boardArgs(opts?.board), 'decompose', taskId, '--json']
+  pushOptional(args, '--author', opts?.author)
+  try {
+    const { stdout } = await execHermes(args, {
+      maxBuffer: 50 * 1024 * 1024,
+      timeout: 120000,
+      ...execOpts,
+    })
+    return parseJsonPayload(stdout)
+  } catch (err: any) {
+    logger.error(err, 'Hermes CLI: kanban decompose failed')
+    throw new Error(`Failed to decompose kanban task: ${err.message}`)
+  }
+}
+
+export interface KanbanTaskPatchOptions extends KanbanBoardOptions {
+  status?: string
+  assignee?: string | null
+  priority?: number
+  title?: string
+  body?: string
+  result?: string
+  block_reason?: string
+  summary?: string
+}
+
+export async function patchTask(taskId: string, patch: KanbanTaskPatchOptions): Promise<KanbanTask | null> {
+  const board = patch.board
+  // Map PATCH fields to existing CLI verbs (same approach as agent plugin_api.py)
+  if (patch.status) {
+    const s = patch.status
+    if (s === 'done') {
+      await completeTasks([taskId], patch.summary, { board })
+    } else if (s === 'blocked') {
+      await blockTask(taskId, patch.block_reason || '', { board })
+    } else if (s === 'ready') {
+      const task = await getTask(taskId, { board })
+      if (task?.task.status === 'blocked' || task?.task.status === 'scheduled') {
+        await unblockTasks([taskId], { board })
+      } else {
+        // Direct status set for todo->ready etc. via unblock (CLI treats as reopen)
+        await unblockTasks([taskId], { board })
+      }
+    } else if (s === 'archived') {
+      await archiveTasks([taskId], { board })
+    } else if (s === 'todo' || s === 'triage' || s === 'scheduled') {
+      // Use bulk update for direct status writes
+      await bulkUpdateTasks({ ids: [taskId], status: s as KanbanTaskStatus, board })
+    }
+  }
+  if (patch.assignee !== undefined) {
+    await assignTask(taskId, patch.assignee || 'none', { board })
+  }
+  if (patch.priority !== undefined) {
+    await bulkUpdateTasks({ ids: [taskId], priority: patch.priority, board })
+  }
+  // For title/body edits, we don't have a direct CLI verb — use create+replace pattern
+  // is not available, so title/body edits fall back to re-fetching.
+  return (await getTask(taskId, { board }))?.task || null
+}
+
+// ─── Home channel notification subscriptions ──────────────────────
+
+export interface HomeChannel {
+  platform: string
+  chat_id: string
+  thread_id: string
+  name: string
+  subscribed: boolean
+}
+
+export async function getHomeChannels(taskId: string | null, opts?: KanbanBoardOptions): Promise<HomeChannel[]> {
+  // Step 1: Build the list of configured home channels from config.yaml + env vars
+  const homes = await listConfiguredHomeChannels()
+
+  if (homes.length === 0) return []
+
+  // Step 2: If a taskId is given, fetch existing subscriptions and mark matches
+  let subs: Array<{ platform: string; chat_id: string; thread_id: string }> = []
+  if (taskId) {
+    try {
+      const args = [...boardArgs(opts?.board), 'notify-list', taskId, '--json']
+      const { stdout } = await execHermes(args, { maxBuffer: 1024 * 1024, ...execOpts })
+      const parsed = JSON.parse(stdout.trim())
+      subs = Array.isArray(parsed) ? parsed : []
+    } catch {
+      subs = []
+    }
+  }
+
+  return homes.map(h => ({
+    platform: h.platform,
+    chat_id: h.chat_id,
+    thread_id: h.thread_id,
+    name: h.name,
+    subscribed: subs.some(s =>
+      s.platform === h.platform && String(s.chat_id) === String(h.chat_id) && String(s.thread_id || '') === String(h.thread_id || ''),
+    ),
+  }))
+}
+
+export async function subscribeHomeChannel(
+  taskId: string,
+  platform: string,
+  chatId: string,
+  opts?: KanbanBoardOptions & { threadId?: string; userId?: string; notifierProfile?: string },
+): Promise<{ ok: boolean }> {
+  const args = [...boardArgs(opts?.board), 'notify-subscribe', taskId, '--platform', platform, '--chat-id', chatId]
+  pushOptional(args, '--thread-id', opts?.threadId)
+  pushOptional(args, '--user-id', opts?.userId)
+  pushOptional(args, '--notifier-profile', opts?.notifierProfile)
+  await execKanbanMutation(args, 'Hermes CLI: kanban notify-subscribe failed', 'Failed to subscribe home channel')
+  return { ok: true }
+}
+
+export async function unsubscribeHomeChannel(
+  taskId: string,
+  platform: string,
+  chatId: string,
+  opts?: KanbanBoardOptions & { threadId?: string },
+): Promise<{ ok: boolean }> {
+  const args = [...boardArgs(opts?.board), 'notify-unsubscribe', taskId, '--platform', platform, '--chat-id', chatId]
+  pushOptional(args, '--thread-id', opts?.threadId)
+  await execKanbanMutation(args, 'Hermes CLI: kanban notify-unsubscribe failed', 'Failed to unsubscribe home channel')
+  return { ok: true }
+}
+
+// ─── Internal: read configured home channels from profile config + env vars ──
+
+interface ConfiguredHomeChannel {
+  platform: string
+  chat_id: string
+  thread_id: string
+  name: string
+}
+
+/**
+ * Per-platform env var name overrides for the home-channel chat_id.
+ *
+ * Most platforms follow the `<PLATFORM>_HOME_CHANNEL` convention, but a few
+ * use platform-specific names that must match the Python gateway config
+ * (see `load_gateway_config` in gateway/config.py):
+ *   - Matrix  → MATRIX_HOME_ROOM / MATRIX_HOME_ROOM_NAME / MATRIX_HOME_ROOM_THREAD_ID
+ *   - Email   → EMAIL_HOME_ADDRESS / EMAIL_HOME_ADDRESS_NAME / EMAIL_HOME_ADDRESS_THREAD_ID
+ */
+const HOME_ENV_OVERRIDES: Record<string, { chatId: string; name: string; threadId: string }> = {
+  matrix: { chatId: 'MATRIX_HOME_ROOM', name: 'MATRIX_HOME_ROOM_NAME', threadId: 'MATRIX_HOME_ROOM_THREAD_ID' },
+  email: { chatId: 'EMAIL_HOME_ADDRESS', name: 'EMAIL_HOME_ADDRESS_NAME', threadId: 'EMAIL_HOME_ADDRESS_THREAD_ID' },
+}
+
+function homeEnvNames(platform: string): { chatId: string; name: string; threadId: string } {
+  if (HOME_ENV_OVERRIDES[platform]) return HOME_ENV_OVERRIDES[platform]
+  const upper = platform.toUpperCase().replace(/-/g, '_')
+  return {
+    chatId: `${upper}_HOME_CHANNEL`,
+    name: `${upper}_HOME_CHANNEL_NAME`,
+    threadId: `${upper}_HOME_CHANNEL_THREAD_ID`,
+  }
+}
+
+/**
+ * Read configured home channels from the active profile's config.yaml + .env.
+ * Mirrors the Python `_configured_home_channels()` logic in plugin_api.py.
+ */
+async function listConfiguredHomeChannels(): Promise<ConfiguredHomeChannel[]> {
+  const { readFile } = await import('fs/promises')
+  const profile = getActiveProfileName() || 'default'
+  const profileDir = getProfileDir(profile)
+  const configPath = join(profileDir, 'config.yaml')
+  const envPath = join(profileDir, '.env')
+
+  let config: Record<string, any> = {}
+  try {
+    config = await safeFileStore.readYaml(configPath)
+  } catch (err: any) {
+    logger.warn(`Failed to read config.yaml at ${configPath}: ${err?.message}`)
+    return []
+  }
+
+  // Read .env file for env-var overrides (Python gateway loads these via dotenv)
+  let envVars: Record<string, string> = {}
+  try {
+    const envRaw = await readFile(envPath, 'utf-8')
+    envVars = parseDotEnv(envRaw)
+  } catch { /* no .env is fine */ }
+
+  // Merge both `gateway.platforms` and top-level `platforms` (same as Python)
+  const gatewayPlatforms = config.gateway?.platforms || {}
+  const topLevelPlatforms = config.platforms || {}
+  const platforms = { ...gatewayPlatforms, ...topLevelPlatforms }
+
+  const result: ConfiguredHomeChannel[] = []
+
+  for (const [platformName, platformCfg] of Object.entries(platforms)) {
+    if (!platformCfg || typeof platformCfg !== 'object') continue
+
+    const hc = (platformCfg as Record<string, any>).home_channel
+    const envNames = homeEnvNames(platformName)
+
+    // Env var overrides: process.env first, then .env file (same as Python)
+    const envChatId = process.env[envNames.chatId] || envVars[envNames.chatId]
+    const envName = process.env[envNames.name] || envVars[envNames.name]
+    const envThreadId = process.env[envNames.threadId] || envVars[envNames.threadId]
+
+    // Determine final home channel: env override > yaml config
+    const chatId = envChatId || (hc?.chat_id as string) || ''
+    if (!chatId) continue
+
+    const threadId = envThreadId || (hc?.thread_id as string) || ''
+    const name = envName || (hc?.name as string) || 'Home'
+
+    result.push({
+      platform: platformName,
+      chat_id: chatId,
+      thread_id: threadId || '',
+      name,
+    })
+  }
+
+  // Stable alphabetical order (matching Python)
+  result.sort((a, b) => a.platform.localeCompare(b.platform))
+  return result
+}
+
+/**
+ * Parse a .env file into a key→value map. Handles quoted values, comments,
+ * and blank lines. Does NOT expand variable references.
+ */
+function parseDotEnv(raw: string): Record<string, string> {
+  const env: Record<string, string> = {}
+  for (const line of raw.split('\n')) {
+    const trimmed = line.trim()
+    if (!trimmed || trimmed.startsWith('#')) continue
+    const eqIdx = trimmed.indexOf('=')
+    if (eqIdx === -1) continue
+    const key = trimmed.slice(0, eqIdx).trim()
+    let val = trimmed.slice(eqIdx + 1).trim()
+    // Strip surrounding quotes
+    if ((val.startsWith('"') && val.endsWith('"')) || (val.startsWith("'") && val.endsWith("'"))) {
+      val = val.slice(1, -1)
+    }
+    if (key && val) env[key] = val
+  }
+  return env
+}
diff --git a/packages/server/src/services/hermes/plugins.ts b/packages/server/src/services/hermes/plugins.ts
index 46710f23..b065a0b9 100644
--- a/packages/server/src/services/hermes/plugins.ts
+++ b/packages/server/src/services/hermes/plugins.ts
@@ -67,7 +67,7 @@ try:
     from hermes_constants import get_hermes_home
 except Exception as exc:
     print(json.dumps({
-        "error": "Failed to import Hermes Agent plugin modules",
+        "error": "Failed to import Swarm Agent plugin modules",
         "detail": str(exc),
         "traceback": traceback.format_exc(),
     }))
diff --git a/packages/server/src/services/hermes/run-chat/abort.ts b/packages/server/src/services/hermes/run-chat/abort.ts
index 735e380b..58023da1 100644
--- a/packages/server/src/services/hermes/run-chat/abort.ts
+++ b/packages/server/src/services/hermes/run-chat/abort.ts
@@ -12,7 +12,7 @@ import { replaceState } from './compression'
 import { calcAndUpdateUsage } from './usage'
 import type { QueuedRun, SessionState } from './types'
 
-const ABORT_BRIDGE_SYNC_TIMEOUT_MESSAGE = 'Hermes Agent did not confirm stop before timeout. Local run state was released so you can continue.'
+const ABORT_BRIDGE_SYNC_TIMEOUT_MESSAGE = 'Swarm Agent did not confirm stop before timeout. Local run state was released so you can continue.'
 
 function isBridgeRunSource(source?: string): boolean {
   return source === 'cli' || source === 'global_agent' || source === 'workflow'
diff --git a/packages/server/src/services/hermes/run-chat/index.ts b/packages/server/src/services/hermes/run-chat/index.ts
index 1932a05b..1d9591ab 100644
--- a/packages/server/src/services/hermes/run-chat/index.ts
+++ b/packages/server/src/services/hermes/run-chat/index.ts
@@ -10,7 +10,7 @@
 import type { Server, Socket } from 'socket.io'
 import { logger } from '../../logger'
 import { getSystemPrompt } from '../../../lib/llm-prompt'
-import { clearSessionMessages, getSession, getSessionDetail  } from '../../../db/hermes/session-store'
+import { clearSessionMessages, getSession, getSessionDetail, listSessionIdsByUserId } from '../../../db/hermes/session-store'
 import { getActiveProfileName, getProfileDir, listProfileNamesFromDisk } from '../hermes-profile'
 import { AgentBridgeClient } from '../agent-bridge'
 import { getAgentBridgeManager } from '../agent-bridge/manager'
@@ -142,6 +142,22 @@ export class ChatRunSocket {
   private onConnection(socket: Socket) {
     const socketUser = socket.data.user as AuthenticatedUser | undefined
     const socketProfile = (socket.handshake.query?.profile as string) || 'default'
+
+    // HERMES_CUSTOM[ChatRunAutoJoin] BEGIN: auto-join all of the user's session rooms on connect
+    // so the socket receives run.completed / run.peer_user_message events for sessions the
+    // user is not currently viewing. The client accumulates real unread messages from these.
+    // Joining a room only subscribes to broadcasts; it does NOT resume/load messages.
+    if (socketUser) {
+      try {
+        for (const sid of listSessionIdsByUserId(String(socketUser.id))) {
+          socket.join(`session:${sid}`)
+        }
+      } catch (err) {
+        logger.info(`[chat-run-socket] auto-join sessions failed for user ${socketUser.id}: ${(err as Error).message}`)
+      }
+    }
+    // HERMES_CUSTOM[ChatRunAutoJoin] END
+
     const currentProfile = () => socketProfile || getActiveProfileName() || 'default'
     const profileExists = (profile: string) => {
       if (!profile || profile === 'default') return true
diff --git a/packages/server/src/services/hermes/run-chat/model-run-prompt.ts b/packages/server/src/services/hermes/run-chat/model-run-prompt.ts
index f383b98d..2fd55b5f 100644
--- a/packages/server/src/services/hermes/run-chat/model-run-prompt.ts
+++ b/packages/server/src/services/hermes/run-chat/model-run-prompt.ts
@@ -21,7 +21,12 @@ export function modelRunProfileTokenPath(profile: string): string {
 
 export async function writeModelRunProfileToken(user: AuthenticatedUser | undefined, profile: string): Promise<void> {
   if (!user) return
-  const token = await issueModelRunJwt(user)
+  const token = await issueModelRunJwt({
+    id: user.id,
+    username: user.username,
+    role: user.role,
+    matrix_user_id: user.matrix_user_id || null,
+  })
   const tokenPath = modelRunProfileTokenPath(profile)
   const mkdirOptions: any = { recursive: true }
   const writeOptions: any = {}
diff --git a/packages/server/src/services/hermes/write-gate.ts b/packages/server/src/services/hermes/write-gate.ts
index b32b9f16..26644ee5 100644
--- a/packages/server/src/services/hermes/write-gate.ts
+++ b/packages/server/src/services/hermes/write-gate.ts
@@ -250,7 +250,7 @@ async function listPendingFor(profile: string, subsystem: WriteGateSubsystem): P
       const record = normalizeRecord(raw, subsystem, fallbackId)
       if (record) records.push(record)
     } catch {
-      // Skip unreadable records, matching Hermes Agent's pending list behavior.
+      // Skip unreadable records, matching Swarm Agent's pending list behavior.
     }
   }
   return records.sort((a, b) => (a.created_at || 0) - (b.created_at || 0))
@@ -361,7 +361,7 @@ function tryResolveAgentRoot(): string {
 function resolveAgentRoot(): string {
   const agentRoot = tryResolveAgentRoot()
   if (agentRoot) return agentRoot
-  throw new Error('Hermes Agent source not found. Set HERMES_AGENT_ROOT to enable write approval actions.')
+  throw new Error('Swarm Agent source not found. Set HERMES_AGENT_ROOT to enable write approval actions.')
 }
 
 function resolveHermesPython(agentRoot?: string): string {
@@ -414,7 +414,7 @@ async function runPythonAction(
   assertSubsystem(subsystem)
   assertPendingId(pendingId)
   if (!isWriteGateSupported()) {
-    throw new Error('Hermes Agent write approval is not supported by the installed Hermes Agent version.')
+    throw new Error('Swarm Agent write approval is not supported by the installed Swarm Agent version.')
   }
   const agentRoot = tryResolveAgentRoot()
   const python = resolveHermesPython(agentRoot)
diff --git a/packages/server/src/services/safe-file-store.ts b/packages/server/src/services/safe-file-store.ts
index 4ff74ad6..98f6160b 100644
--- a/packages/server/src/services/safe-file-store.ts
+++ b/packages/server/src/services/safe-file-store.ts
@@ -17,11 +17,11 @@ export interface SafeYamlOptions extends SafeWriteOptions {
 }
 
 function isTextUpdateResult<T>(value: unknown): value is { content: string; result: T } {
-  return !!value && typeof value === 'object' && Object.hasOwn(value as Record<string, unknown>, 'content')
+  return !!value && typeof value === 'object' && Object.prototype.hasOwnProperty.call(value as Record<string, unknown>, 'content')
 }
 
 function isYamlUpdateResult<T>(value: unknown): value is YamlUpdateResult<T> {
-  return !!value && typeof value === 'object' && Object.hasOwn(value as Record<string, unknown>, 'data')
+  return !!value && typeof value === 'object' && Object.prototype.hasOwnProperty.call(value as Record<string, unknown>, 'data')
 }
 
 function shouldUseFallbackBackup(err: any): boolean {
diff --git a/packages/server/src/services/system-info.ts b/packages/server/src/services/system-info.ts
index 23546401..0bf34d52 100644
--- a/packages/server/src/services/system-info.ts
+++ b/packages/server/src/services/system-info.ts
@@ -67,7 +67,7 @@ export function getHermesWebUiVersion(): string {
 }
 
 export function normalizeHermesAgentVersion(raw: string): string {
-  return raw.split('\n')[0]?.replace(/^Hermes Agent\s+/, '').trim() || ''
+  return raw.split('\n')[0]?.replace(/^Swarm Agent\s+/, '').trim() || ''
 }
 
 function isValidDeviceIdentity(value: any): value is DeviceIdentity {
diff --git a/packages/website/src/components/landing/InstallSection.vue b/packages/website/src/components/landing/InstallSection.vue
index 040b83c4..daeba9ec 100644
--- a/packages/website/src/components/landing/InstallSection.vue
+++ b/packages/website/src/components/landing/InstallSection.vue
@@ -48,7 +48,7 @@ function copyText(text: string) {
         <p class="panel-desc reveal">{{ t('install.desc') }}</p>
       </div>
       <div class="install-mark reveal reveal-delay-1" aria-hidden="true">
-        <img src="/logo.png" alt="" />
+        <img src= alt="" />
       </div>
     </div>
 
diff --git a/packages/website/src/components/layout/SiteFooter.vue b/packages/website/src/components/layout/SiteFooter.vue
index 1945833e..af0b1af6 100644
--- a/packages/website/src/components/layout/SiteFooter.vue
+++ b/packages/website/src/components/layout/SiteFooter.vue
@@ -9,7 +9,7 @@ const { t } = useI18n()
     <div class="footer-inner">
       <div class="footer-left">
         <div class="footer-brand">
-          <img src="/logo.png" :alt="t('brand.logoAlt')" class="footer-logo" />
+          <img src= :alt="t('brand.logoAlt')" class="footer-logo" />
           <span>{{ t('brand.name') }}</span>
         </div>
         <p class="footer-desc">{{ t('footer.description') }}</p>
diff --git a/packages/website/src/components/layout/SiteHeader.vue b/packages/website/src/components/layout/SiteHeader.vue
index fdf3af97..723ff9a7 100644
--- a/packages/website/src/components/layout/SiteHeader.vue
+++ b/packages/website/src/components/layout/SiteHeader.vue
@@ -28,7 +28,7 @@ function goHome() {
   <header class="site-header">
     <div class="header-inner">
       <div class="header-left" @click="goHome">
-        <img src="/logo.png" :alt="t('brand.logoAlt')" class="logo-icon" />
+        <img src= :alt="t('brand.logoAlt')" class="logo-icon" />
         <span class="logo-text">{{ t('brand.name') }}</span>
       </div>
 
diff --git a/packages/website/src/i18n/en.ts b/packages/website/src/i18n/en.ts
index 4c920e9b..c54dea5f 100644
--- a/packages/website/src/i18n/en.ts
+++ b/packages/website/src/i18n/en.ts
@@ -1,6 +1,6 @@
 export default {
   brand: {
-    name: 'Hermes Studio',
+    name: 'Swarm Studio',
     logoAlt: 'Hermes',
   },
   ui: {
@@ -29,8 +29,8 @@ export default {
       deploy: 'Deploy',
     },
     badge: 'Local-first AI Agent Workspace',
-    title: 'Hermes Studio',
-    subtitle: 'A desktop app and local control plane for Hermes Agent. Chat, manage profiles, run coding agents, inspect files, automate jobs, and keep your runtime under your control.',
+    title: 'Swarm Studio',
+    subtitle: 'A desktop app and local control plane for Swarm Agent. Chat, manage profiles, run coding agents, inspect files, automate jobs, and keep your runtime under your control.',
     cta: 'Download Desktop',
     docsCta: 'Read Docs',
     viewGithub: 'View on GitHub',
@@ -40,14 +40,14 @@ export default {
     docsTitle: 'Documentation',
     docsLibrary: 'Library',
     install: 'npm install -g hermes-web-ui',
-    previewAlt: 'Hermes Studio dashboard preview',
+    previewAlt: 'Swarm Studio dashboard preview',
   },
   features: {
-    title: 'One Local Console for Hermes Agent',
-    desc: 'Hermes Studio brings chat, runtime management, automation, files, coding agents, and release-ready desktop distribution into one workspace.',
+    title: 'One Local Console for Swarm Agent',
+    desc: 'Swarm Studio brings chat, runtime management, automation, files, coding agents, and release-ready desktop distribution into one workspace.',
     streaming: {
       title: 'Agent Chat',
-      desc: 'Streaming Hermes Agent conversations with sessions, Markdown, tool traces, uploads, downloads, model badges, and context usage.',
+      desc: 'Streaming Swarm Agent conversations with sessions, Markdown, tool traces, uploads, downloads, model badges, and context usage.',
     },
     platforms: {
       title: 'Platform Channels',
@@ -83,7 +83,7 @@ export default {
     },
     quickInstall: {
       title: 'Desktop Runtime',
-      desc: 'Windows, macOS, and Linux desktop builds bundle the Web UI runtime and manage Hermes Studio state separately from Hermes Agent data.',
+      desc: 'Windows, macOS, and Linux desktop builds bundle the Web UI runtime and manage Swarm Studio state separately from Swarm Agent data.',
     },
     i18n: {
       title: 'Coding Agents & MCP',
@@ -115,22 +115,22 @@ export default {
     items: [
       {
         src: '/image.gif',
-        alt: 'Hermes Studio product walkthrough',
-        title: 'Hermes Studio Workflow',
+        alt: 'Swarm Studio product walkthrough',
+        title: 'Swarm Studio Workflow',
         desc: 'Chat with coding agents, preview versions, manage runtime assets, and browse your workspace in one app.',
       },
     ],
   },
   install: {
     title: 'Quick Start',
-    desc: 'Download the desktop app or run Hermes Studio yourself.',
+    desc: 'Download the desktop app or run Swarm Studio yourself.',
     desktop: {
       title: 'Desktop',
       download: 'Download',
       githubDownload: 'GitHub Download',
       cloudflareDownload: 'Cloudflare Download',
       allDownloads: 'View all release assets',
-      prereq: 'Desktop builds bundle the Hermes Studio runtime.',
+      prereq: 'Desktop builds bundle the Swarm Studio runtime.',
       downloads: [
         {
           title: 'macOS Apple Silicon',
@@ -189,7 +189,7 @@ export default {
     chartAlt: 'Star History',
   },
   footer: {
-    description: 'Self-hosted AI chat dashboard for Hermes Agent.',
+    description: 'Self-hosted AI chat dashboard for Swarm Agent.',
     license: 'BSL-1.1 License',
     madeWith: 'Built with Vue 3, Naive UI, and TypeScript.',
     github: 'Open GitHub',
@@ -209,14 +209,14 @@ export default {
     },
     gettingStarted: {
       title: 'Getting Started',
-      intro: 'Hermes Studio is a self-hosted web dashboard for managing AI conversations, platform channels, scheduled jobs, and more. It wraps the Hermes Agent CLI and provides a beautiful web interface.',
+      intro: 'Swarm Studio is a self-hosted web dashboard for managing AI conversations, platform channels, scheduled jobs, and more. It wraps the Swarm Agent CLI and provides a beautiful web interface.',
       install: {
         title: 'Installation',
         content: 'Install globally via npm. Node.js 23 or higher is required.',
       },
       firstRun: {
         title: 'First Run',
-        content: 'On first start, Hermes Studio will automatically generate an auth token, initialize local data, start the Hermes agent bridge, and open the dashboard in your browser.',
+        content: 'On first start, Swarm Studio will automatically generate an auth token, initialize local data, start the Hermes agent bridge, and open the dashboard in your browser.',
       },
       login: {
         title: 'Login',
@@ -225,13 +225,13 @@ export default {
     },
     configuration: {
       title: 'Configuration',
-      intro: 'Hermes Studio can be configured via environment variables.',
+      intro: 'Swarm Studio can be configured via environment variables.',
       envVars: {
         title: 'Environment Variables',
         rows: [
           ['PORT', 'Server listen port (default: 8648)'],
           ['BIND_HOST', 'Server bind host (default: 0.0.0.0). Set :: explicitly to enable IPv6 listening.'],
-          ['HERMES_WEB_UI_HOME', 'Hermes Studio data home for auth token, credentials, logs, DB, and default uploads'],
+          ['HERMES_WEB_UI_HOME', 'Swarm Studio data home for auth token, credentials, logs, DB, and default uploads'],
           ['HERMES_WEBUI_STATE_DIR', 'Compatibility alias for HERMES_WEB_UI_HOME'],
           ['HERMES_WEB_UI_DISABLE_MCP_AUTOINJECT', 'Disable startup injection of the managed hermes-studio MCP server into Hermes profile configs'],
           ['HERMES_WEB_UI_ALLOW_TRANSIENT_MCP_AUTOINJECT', 'Allow managed MCP injection when HERMES_WEB_UI_HOME is under a temporary directory, such as Version Preview runtimes'],
@@ -247,7 +247,7 @@ export default {
           ['WORKSPACE_BASE', 'Base directory for workspace browsing'],
           ['HERMES_HOME', 'Hermes data home'],
           ['HERMES_BIN', 'Custom Hermes CLI binary path'],
-          ['HERMES_AGENT_ROOT', 'Hermes Agent source checkout containing run_agent.py'],
+          ['HERMES_AGENT_ROOT', 'Swarm Agent source checkout containing run_agent.py'],
           ['HERMES_AGENT_BRIDGE_PYTHON', 'Python interpreter used to launch the agent bridge'],
           ['HERMES_AGENT_BRIDGE_UV', 'uv executable used to launch the agent bridge when available'],
           ['UV', 'Fallback uv executable path'],
@@ -258,20 +258,20 @@ export default {
           ['HERMES_AGENT_BRIDGE_STARTUP_TIMEOUT_MS', 'Timeout while waiting for the Python bridge to become ready'],
           ['HERMES_AGENT_BRIDGE_AUTO_RESTART', 'Auto-restart the bridge broker after unexpected exit; set 0/false/no/off to disable'],
           ['HERMES_AGENT_BRIDGE_RESTART_DELAY_MS', 'Base delay for bridge auto-restart backoff'],
-          ['HERMES_AGENT_BRIDGE_PLATFORM', 'Platform identity passed to Hermes Agent'],
+          ['HERMES_AGENT_BRIDGE_PLATFORM', 'Platform identity passed to Swarm Agent'],
           ['HERMES_AGENT_BRIDGE_WORKER_TRANSPORT', 'Profile worker endpoint transport. Set tcp for loopback TCP, or ipc/unix for Unix domain sockets; defaults to Windows TCP and macOS/Linux IPC'],
           ['HERMES_AGENT_BRIDGE_WORKER_PORT_BASE', 'Base port for TCP worker endpoints (default: 18780). Version Preview uses an isolated 19650 port range'],
           ['HERMES_BRIDGE_PROVIDER', 'Provider override for bridge runs'],
           ['HERMES_BRIDGE_TOOLSETS', 'Toolset override for bridge runs'],
           ['HERMES_BRIDGE_MAX_TURNS', 'Maximum turn override for bridge runs'],
-          ['HERMES_BRIDGE_SUPPRESS_PLATFORM_HINT', 'Controls bridge platform hint suppression passed to Hermes Agent'],
+          ['HERMES_BRIDGE_SUPPRESS_PLATFORM_HINT', 'Controls bridge platform hint suppression passed to Swarm Agent'],
           ['HERMES_OPENROUTER_APP_REFERER', 'OpenRouter attribution referer sent by bridge runs'],
           ['HERMES_OPENROUTER_APP_TITLE', 'OpenRouter attribution title sent by bridge runs'],
           ['HERMES_OPENROUTER_APP_CATEGORIES', 'OpenRouter attribution categories sent by bridge runs'],
           ['HERMES_WEB_UI_MANAGED_GATEWAY', 'Controls Web UI-managed Hermes gateway handling. Enabled by default; set 0/false/off to use hermes gateway start'],
           ['HERMES_WEB_UI_DISABLE_GATEWAY_AUTOSTART', 'Skip startup gateway checks/autostart for dashboard-only deployments where another service owns Hermes gateway lifecycle'],
-          ['HERMES_WEB_UI_DISABLE_SKILL_INJECTION', 'Skip startup bundled skill injection when skills are managed outside Hermes Web UI. Enabled injection only updates Web UI-managed or identical bundled copies; local edits are skipped'],
-          ['HERMES_WEB_UI_STOP_GATEWAYS_ON_SHUTDOWN', 'Controls whether Hermes Studio shutdown also stops managed gateway processes'],
+          ['HERMES_WEB_UI_DISABLE_SKILL_INJECTION', 'Skip startup bundled skill injection when skills are managed outside Swarm Web UI. Enabled injection only updates Web UI-managed or identical bundled copies; local edits are skipped'],
+          ['HERMES_WEB_UI_STOP_GATEWAYS_ON_SHUTDOWN', 'Controls whether Swarm Studio shutdown also stops managed gateway processes'],
           ['HERMES_GATEWAY_URL / GATEWAY_URL', 'Explicit Hermes gateway upstream URL for proxy routes'],
           ['GATEWAY_HOST', 'Default Hermes gateway upstream host for proxy routes'],
           ['GATEWAY_PORT', 'Default Hermes gateway upstream port for proxy routes'],
@@ -284,7 +284,7 @@ export default {
       },
       gateway: {
         title: 'Agent Bridge Runtime',
-        content: 'Chat runs are handled through the Hermes agent bridge, which runs alongside the Hermes Studio server and talks directly to the Hermes Agent runtime. HERMES_AGENT_BRIDGE_ENDPOINT controls the Node-to-broker address, while HERMES_AGENT_BRIDGE_WORKER_TRANSPORT controls the broker-to-profile-worker transport. Switching the frontend Hermes Profile changes later request context only; it does not restart the bridge or clear other running tasks.',
+        content: 'Chat runs are handled through the Hermes agent bridge, which runs alongside the Swarm Studio server and talks directly to the Swarm Agent runtime. HERMES_AGENT_BRIDGE_ENDPOINT controls the Node-to-broker address, while HERMES_AGENT_BRIDGE_WORKER_TRANSPORT controls the broker-to-profile-worker transport. Switching the frontend Hermes Profile changes later request context only; it does not restart the bridge or clear other running tasks.',
       },
       profiles: {
         title: 'Profiles',
@@ -293,10 +293,10 @@ export default {
     },
     features: {
       title: 'Features',
-      intro: 'Explore the core features of Hermes Studio.',
+      intro: 'Explore the core features of Swarm Studio.',
       chat: {
         title: 'AI Chat',
-        content: 'Real-time chat streaming over Socket.IO /chat-run. Supports multi-session management, Markdown rendering with syntax highlighting, tool call inspection, profile-scoped upload, path-based download, and Ctrl+K search across the Hermes Studio local session database.',
+        content: 'Real-time chat streaming over Socket.IO /chat-run. Supports multi-session management, Markdown rendering with syntax highlighting, tool call inspection, profile-scoped upload, path-based download, and Ctrl+K search across the Swarm Studio local session database.',
       },
       kanban: {
         title: 'Kanban Board',
@@ -332,8 +332,8 @@ export default {
       },
     },
     hermesStudioManual: {
-      title: 'Hermes Studio Client Manual',
-      intro: 'This page publishes the complete Hermes Studio 0.6.12 client operations manual. The full manual is currently authored in Chinese and covers day-to-day usage, configuration, operations, acceptance review, internal training, visible navigation, dialogs, status pages, key actions, common workflows, and risk notes.',
+      title: 'Swarm Studio Client Manual',
+      intro: 'This page publishes the complete Swarm Studio 0.6.12 client operations manual. The full manual is currently authored in Chinese and covers day-to-day usage, configuration, operations, acceptance review, internal training, visible navigation, dialogs, status pages, key actions, common workflows, and risk notes.',
       open: {
         title: 'Open the full manual',
         content: 'The uploaded manual preserves the original HTML layout, annotated screenshots, tables, and PDF export. Use the HTML version for online browsing and the PDF version for offline review, archiving, or training distribution.',
@@ -353,7 +353,7 @@ export default {
       scope: {
         title: 'Scope',
         rows: [
-          ['Version', 'Hermes Studio 0.6.12 client operations manual'],
+          ['Version', 'Swarm Studio 0.6.12 client operations manual'],
           ['Language', 'Full manual in Chinese, with bilingual website entry copy'],
           ['Coverage', 'Login, chat, history, memory, skills, plugins, files, terminal, jobs, group chat, Kanban, usage, logs, MCP, platform integrations, devices, settings, backup, security, and troubleshooting'],
           ['Audience', 'Individual users, administrators, support teams, acceptance reviewers, and internal training teams'],
@@ -366,7 +366,7 @@ export default {
     },
     esp32Intro: {
       title: 'Hermes ESP32-C3 Device Intro',
-      intro: 'This page publishes the public introduction for the Hermes ESP32-C3 desktop AI conversation box. The device handles the small display, microphone, speaker, physical buttons, and local network connection, while Hermes Studio handles model access, speech recognition, speech synthesis, and session execution.',
+      intro: 'This page publishes the public introduction for the Hermes ESP32-C3 desktop AI conversation box. The device handles the small display, microphone, speaker, physical buttons, and local network connection, while Swarm Studio handles model access, speech recognition, speech synthesis, and session execution.',
       open: {
         title: 'Open the full intro page',
         content: 'The uploaded intro page preserves the original HTML layout, product visuals, setup flow, specification notes, pre-purchase checklist, and FAQ. It is useful for public presentation, handoff notes, and quick user orientation.',
@@ -374,26 +374,26 @@ export default {
           {
             label: 'Open the Chinese ESP32 intro page',
             href: '/docs/hermes-esp32-intro/index.html',
-            description: 'Includes product visuals, hardware highlights, Wi-Fi provisioning notes, Hermes Studio connection guidance, specifications, and FAQ.',
+            description: 'Includes product visuals, hardware highlights, Wi-Fi provisioning notes, Swarm Studio connection guidance, specifications, and FAQ.',
           },
         ],
       },
       overview: {
         title: 'Device positioning',
-        content: 'This is a desktop hardware entry point for Hermes Studio, not an offline standalone large-model device. The ESP32-C3 provides local interaction and status feedback, while AI capability comes from Hermes Studio running on a computer or server plus the model, STT, and TTS services configured by the user.',
+        content: 'This is a desktop hardware entry point for Swarm Studio, not an offline standalone large-model device. The ESP32-C3 provides local interaction and status feedback, while AI capability comes from Swarm Studio running on a computer or server plus the model, STT, and TTS services configured by the user.',
       },
       requirements: {
         title: 'Requirements',
         rows: [
-          ['Network', 'The ESP32 device and Hermes Studio should communicate on the same local network.'],
+          ['Network', 'The ESP32 device and Swarm Studio should communicate on the same local network.'],
           ['Service address', 'Enter the computer or server LAN IP plus port on the device page; do not use 127.0.0.1.'],
-          ['Voice capability', 'Hermes Studio needs working speech recognition, speech synthesis, and model services.'],
-          ['Best fit', 'Built for Hermes Studio users, ESP32/Arduino/PlatformIO makers, open hardware customization, and desktop AI interaction demos.'],
+          ['Voice capability', 'Swarm Studio needs working speech recognition, speech synthesis, and model services.'],
+          ['Best fit', 'Built for Swarm Studio users, ESP32/Arduino/PlatformIO makers, open hardware customization, and desktop AI interaction demos.'],
         ],
       },
       maintenance: {
         title: 'Maintenance note',
-        content: 'If firmware behavior, provisioning pages, ports, device visuals, or the Hermes Studio device connection flow changes, update the static HTML asset and this page scope together.',
+        content: 'If firmware behavior, provisioning pages, ports, device visuals, or the Swarm Studio device connection flow changes, update the static HTML asset and this page scope together.',
       },
     },
     platforms: {
@@ -434,7 +434,7 @@ export default {
     },
     api: {
       title: 'API Reference',
-      intro: 'Hermes Studio provides a local BFF API for the dashboard and Socket.IO endpoints for streaming chat.',
+      intro: 'Swarm Studio provides a local BFF API for the dashboard and Socket.IO endpoints for streaming chat.',
       local: {
         title: 'Local BFF Endpoints',
         content: 'The Koa server handles session management, profile CRUD, account- and profile-scoped management, config read/write, log access, skill listing, memory operations, and static assets.',
diff --git a/packages/website/src/i18n/zh.ts b/packages/website/src/i18n/zh.ts
index bc33290d..c5412b12 100644
--- a/packages/website/src/i18n/zh.ts
+++ b/packages/website/src/i18n/zh.ts
@@ -1,6 +1,6 @@
 export default {
   brand: {
-    name: 'Hermes Studio',
+    name: 'Swarm Studio',
     logoAlt: 'Hermes',
   },
   ui: {
@@ -29,8 +29,8 @@ export default {
       deploy: '部署',
     },
     badge: '本地优先的 AI Agent 工作区',
-    title: 'Hermes Studio',
-    subtitle: '面向 Hermes Agent 的桌面应用和本地控制台。聊天、管理 Profile、运行 Coding Agent、查看文件、自动化任务，并掌控自己的运行环境。',
+    title: 'Swarm Studio',
+    subtitle: '面向 Swarm Agent 的桌面应用和本地控制台。聊天、管理 Profile、运行 Coding Agent、查看文件、自动化任务，并掌控自己的运行环境。',
     cta: '下载桌面版',
     docsCta: '查看文档',
     viewGithub: '查看 GitHub',
@@ -40,14 +40,14 @@ export default {
     docsTitle: '文档',
     docsLibrary: '资料库',
     install: 'npm install -g hermes-web-ui',
-    previewAlt: 'Hermes Studio 仪表板预览',
+    previewAlt: 'Swarm Studio 仪表板预览',
   },
   features: {
-    title: 'Hermes Agent 的本地控制台',
-    desc: 'Hermes Studio 把聊天、运行时管理、自动化、文件、Coding Agent 和桌面分发整合到一个工作区。',
+    title: 'Swarm Agent 的本地控制台',
+    desc: 'Swarm Studio 把聊天、运行时管理、自动化、文件、Coding Agent 和桌面分发整合到一个工作区。',
     streaming: {
       title: 'Agent 聊天',
-      desc: 'Hermes Agent 流式对话，支持会话、Markdown、工具轨迹、上传下载、模型标签和上下文用量。',
+      desc: 'Swarm Agent 流式对话，支持会话、Markdown、工具轨迹、上传下载、模型标签和上下文用量。',
     },
     platforms: {
       title: '平台渠道',
@@ -83,7 +83,7 @@ export default {
     },
     quickInstall: {
       title: '桌面运行时',
-      desc: 'Windows、macOS 和 Linux 桌面版内置 Web UI 运行时，并把 Hermes Studio 状态与 Hermes Agent 数据分开管理。',
+      desc: 'Windows、macOS 和 Linux 桌面版内置 Web UI 运行时，并把 Swarm Studio 状态与 Swarm Agent 数据分开管理。',
     },
     i18n: {
       title: 'Coding Agent 与 MCP',
@@ -115,22 +115,22 @@ export default {
     items: [
       {
         src: '/image.gif',
-        alt: 'Hermes Studio 产品流程演示',
-        title: 'Hermes Studio 工作流',
+        alt: 'Swarm Studio 产品流程演示',
+        title: 'Swarm Studio 工作流',
         desc: '在一个应用里完成 Agent 对话、版本预览、Runtime 管理和工作区浏览。',
       },
     ],
   },
   install: {
     title: '快速开始',
-    desc: '下载桌面应用，或自行运行 Hermes Studio。',
+    desc: '下载桌面应用，或自行运行 Swarm Studio。',
     desktop: {
       title: '桌面版',
       download: '下载',
       githubDownload: 'GitHub 下载',
       cloudflareDownload: 'Cloudflare 下载',
       allDownloads: '查看全部发布文件',
-      prereq: '桌面版已内置 Hermes Studio 运行时。',
+      prereq: '桌面版已内置 Swarm Studio 运行时。',
       downloads: [
         {
           title: 'macOS Apple Silicon',
@@ -189,7 +189,7 @@ export default {
     chartAlt: 'Star 历史',
   },
   footer: {
-    description: 'Hermes Agent 的自托管 AI 聊天仪表板。',
+    description: 'Swarm Agent 的自托管 AI 聊天仪表板。',
     license: 'BSL-1.1 开源协议',
     madeWith: '使用 Vue 3、Naive UI 和 TypeScript 构建。',
     github: '打开 GitHub',
@@ -209,14 +209,14 @@ export default {
     },
     gettingStarted: {
       title: '快速开始',
-      intro: 'Hermes Studio 是一个自托管的 Web 仪表板，用于管理 AI 对话、平台通道、定时任务等。它封装了 Hermes Agent CLI 并提供美观的 Web 界面。',
+      intro: 'Swarm Studio 是一个自托管的 Web 仪表板，用于管理 AI 对话、平台通道、定时任务等。它封装了 Swarm Agent CLI 并提供美观的 Web 界面。',
       install: {
         title: '安装',
         content: '通过 npm 全局安装。需要 Node.js 23 或更高版本。',
       },
       firstRun: {
         title: '首次运行',
-        content: '首次启动时，Hermes Studio 会自动生成认证令牌、初始化本地数据、启动 Hermes agent bridge 并在浏览器中打开仪表板。',
+        content: '首次启动时，Swarm Studio 会自动生成认证令牌、初始化本地数据、启动 Hermes agent bridge 并在浏览器中打开仪表板。',
       },
       login: {
         title: '登录',
@@ -225,13 +225,13 @@ export default {
     },
     configuration: {
       title: '配置说明',
-      intro: 'Hermes Studio 可通过环境变量进行配置。',
+      intro: 'Swarm Studio 可通过环境变量进行配置。',
       envVars: {
         title: '环境变量',
         rows: [
           ['PORT', '服务器监听端口（默认：8648）'],
           ['BIND_HOST', '服务器绑定地址（默认：0.0.0.0）。如需 IPv6，请显式设置为 ::。'],
-          ['HERMES_WEB_UI_HOME', 'Hermes Studio 数据目录，用于认证 token、登录凭据、日志、数据库和默认上传目录'],
+          ['HERMES_WEB_UI_HOME', 'Swarm Studio 数据目录，用于认证 token、登录凭据、日志、数据库和默认上传目录'],
           ['HERMES_WEBUI_STATE_DIR', 'HERMES_WEB_UI_HOME 的兼容别名'],
           ['HERMES_WEB_UI_DISABLE_MCP_AUTOINJECT', '关闭启动时向 Hermes profile 配置自动注入托管的 hermes-studio MCP server'],
           ['HERMES_WEB_UI_ALLOW_TRANSIENT_MCP_AUTOINJECT', '当 HERMES_WEB_UI_HOME 位于临时目录（例如 Version Preview runtime）时，仍允许托管 MCP 自动注入'],
@@ -247,7 +247,7 @@ export default {
           ['WORKSPACE_BASE', 'Workspace 浏览根目录'],
           ['HERMES_HOME', 'Hermes 数据目录'],
           ['HERMES_BIN', '自定义 Hermes CLI 二进制路径'],
-          ['HERMES_AGENT_ROOT', '包含 run_agent.py 的 Hermes Agent 源码目录'],
+          ['HERMES_AGENT_ROOT', '包含 run_agent.py 的 Swarm Agent 源码目录'],
           ['HERMES_AGENT_BRIDGE_PYTHON', '用于启动 agent bridge 的 Python 解释器'],
           ['HERMES_AGENT_BRIDGE_UV', '可用时用于启动 agent bridge 的 uv 可执行文件'],
           ['UV', 'uv 可执行文件 fallback'],
@@ -258,20 +258,20 @@ export default {
           ['HERMES_AGENT_BRIDGE_STARTUP_TIMEOUT_MS', '等待 Python bridge ready 的超时'],
           ['HERMES_AGENT_BRIDGE_AUTO_RESTART', 'bridge broker 意外退出后是否自动重启；设为 0/false/no/off 可关闭'],
           ['HERMES_AGENT_BRIDGE_RESTART_DELAY_MS', 'bridge 自动重启退避的基础延迟'],
-          ['HERMES_AGENT_BRIDGE_PLATFORM', '传给 Hermes Agent 的 platform 标识'],
+          ['HERMES_AGENT_BRIDGE_PLATFORM', '传给 Swarm Agent 的 platform 标识'],
           ['HERMES_AGENT_BRIDGE_WORKER_TRANSPORT', 'profile worker endpoint transport。设为 tcp 使用 loopback TCP；设为 ipc/unix 使用 Unix domain socket；默认 Windows TCP、macOS/Linux IPC'],
           ['HERMES_AGENT_BRIDGE_WORKER_PORT_BASE', 'TCP worker endpoint 起始端口（默认：18780）。Version Preview 会使用独立端口段 19650'],
           ['HERMES_BRIDGE_PROVIDER', 'bridge 运行时的 provider 覆盖'],
           ['HERMES_BRIDGE_TOOLSETS', 'bridge 运行时的 toolset 覆盖'],
           ['HERMES_BRIDGE_MAX_TURNS', 'bridge 运行时的最大轮数覆盖'],
-          ['HERMES_BRIDGE_SUPPRESS_PLATFORM_HINT', '控制传给 Hermes Agent 的 bridge platform hint suppression'],
+          ['HERMES_BRIDGE_SUPPRESS_PLATFORM_HINT', '控制传给 Swarm Agent 的 bridge platform hint suppression'],
           ['HERMES_OPENROUTER_APP_REFERER', 'bridge 运行发送给 OpenRouter 的 attribution referer'],
           ['HERMES_OPENROUTER_APP_TITLE', 'bridge 运行发送给 OpenRouter 的 attribution title'],
           ['HERMES_OPENROUTER_APP_CATEGORIES', 'bridge 运行发送给 OpenRouter 的 attribution categories'],
           ['HERMES_WEB_UI_MANAGED_GATEWAY', '控制 Web UI 托管 Hermes gateway。默认开启；设为 0/false/off 时改用 hermes gateway start'],
           ['HERMES_WEB_UI_DISABLE_GATEWAY_AUTOSTART', '跳过启动时的 gateway 检查/自动启动；适用于由其它服务管理 Hermes gateway 的 dashboard-only 部署'],
-          ['HERMES_WEB_UI_DISABLE_SKILL_INJECTION', '跳过启动时的内置 skill 注入；适用于由 Hermes Web UI 外部管理 skills 的部署。启用注入时只更新 Web UI 管理或完全相同的内置副本，本地修改会跳过'],
-          ['HERMES_WEB_UI_STOP_GATEWAYS_ON_SHUTDOWN', 'Hermes Studio 关闭时是否同时停止托管的 gateway 进程'],
+          ['HERMES_WEB_UI_DISABLE_SKILL_INJECTION', '跳过启动时的内置 skill 注入；适用于由 Swarm Web UI 外部管理 skills 的部署。启用注入时只更新 Web UI 管理或完全相同的内置副本，本地修改会跳过'],
+          ['HERMES_WEB_UI_STOP_GATEWAYS_ON_SHUTDOWN', 'Swarm Studio 关闭时是否同时停止托管的 gateway 进程'],
           ['HERMES_GATEWAY_URL / GATEWAY_URL', 'proxy 路由使用的 Hermes gateway upstream URL'],
           ['GATEWAY_HOST', 'proxy 路由使用的默认 Hermes gateway upstream host'],
           ['GATEWAY_PORT', 'proxy 路由使用的默认 Hermes gateway upstream port'],
@@ -284,7 +284,7 @@ export default {
       },
       gateway: {
         title: 'Agent Bridge 运行时',
-        content: '聊天运行通过 Hermes agent bridge 处理。它随 Hermes Studio 服务一起运行，并直接连接 Hermes Agent runtime。HERMES_AGENT_BRIDGE_ENDPOINT 控制 Node 与 bridge broker 的连接地址；HERMES_AGENT_BRIDGE_WORKER_TRANSPORT 控制 broker 与各 Profile worker 的连接方式。前端切换 Hermes Profile 只影响后续请求上下文，不会重启 bridge 或清理其他正在运行的任务。',
+        content: '聊天运行通过 Hermes agent bridge 处理。它随 Swarm Studio 服务一起运行，并直接连接 Swarm Agent runtime。HERMES_AGENT_BRIDGE_ENDPOINT 控制 Node 与 bridge broker 的连接地址；HERMES_AGENT_BRIDGE_WORKER_TRANSPORT 控制 broker 与各 Profile worker 的连接方式。前端切换 Hermes Profile 只影响后续请求上下文，不会重启 bridge 或清理其他正在运行的任务。',
       },
       profiles: {
         title: '配置文件',
@@ -293,10 +293,10 @@ export default {
     },
     features: {
       title: '功能详解',
-      intro: '探索 Hermes Studio 的核心功能。',
+      intro: '探索 Swarm Studio 的核心功能。',
       chat: {
         title: 'AI 聊天',
-        content: '通过 Socket.IO /chat-run 实时流式聊天。支持多会话管理、Markdown 渲染与语法高亮、工具调用检查、按 Profile 上传、按路径下载，以及 Ctrl+K 搜索 Hermes Studio 本地会话库。',
+        content: '通过 Socket.IO /chat-run 实时流式聊天。支持多会话管理、Markdown 渲染与语法高亮、工具调用检查、按 Profile 上传、按路径下载，以及 Ctrl+K 搜索 Swarm Studio 本地会话库。',
       },
       kanban: {
         title: '看板管理',
@@ -332,8 +332,8 @@ export default {
       },
     },
     hermesStudioManual: {
-      title: 'Hermes Studio 客户端完整操作手册',
-      intro: '这份手册面向 Hermes Studio 桌面客户端和 Web 界面的使用、配置、运维、验收与内部培训场景，覆盖当前客户端可见导航、设置、弹窗、状态页、关键按钮、常见流程和风险提示。',
+      title: 'Swarm Studio 客户端完整操作手册',
+      intro: '这份手册面向 Swarm Studio 桌面客户端和 Web 界面的使用、配置、运维、验收与内部培训场景，覆盖当前客户端可见导航、设置、弹窗、状态页、关键按钮、常见流程和风险提示。',
       open: {
         title: '打开完整手册',
         content: '完整手册保留原始 HTML 排版、截图标注和 PDF 版本。HTML 版本适合在线浏览，PDF 版本适合归档、打印和离线分发。',
@@ -353,7 +353,7 @@ export default {
       scope: {
         title: '适用范围',
         rows: [
-          ['适用版本', 'Hermes Studio 0.6.12 客户端操作手册'],
+          ['适用版本', 'Swarm Studio 0.6.12 客户端操作手册'],
           ['文档语言', '中文完整手册；官网提供中英文入口说明'],
           ['覆盖内容', '登录、聊天、历史、记忆、技能、插件、文件、终端、任务、群聊、看板、用量、日志、MCP、平台集成、设备、设置、备份、安全和故障排除'],
           ['推荐读者', '个人用户、管理员、技术支持、交付验收人员和内部培训对象'],
@@ -366,7 +366,7 @@ export default {
     },
     esp32Intro: {
       title: 'Hermes ESP32-C3 设备介绍',
-      intro: '这页发布 Hermes ESP32-C3 桌面 AI 对话小方盒的官网介绍资料。设备侧负责小屏、麦克风、扬声器、实体按键和局域网连接；Hermes Studio 负责模型、语音识别、语音合成和会话运行。',
+      intro: '这页发布 Hermes ESP32-C3 桌面 AI 对话小方盒的官网介绍资料。设备侧负责小屏、麦克风、扬声器、实体按键和局域网连接；Swarm Studio 负责模型、语音识别、语音合成和会话运行。',
       open: {
         title: '打开完整介绍页',
         content: '完整介绍页保留原始 HTML 排版、产品图、使用流程、规格说明、购买前须知和常见问题，适合公开展示、交付说明和用户快速了解设备定位。',
@@ -374,26 +374,26 @@ export default {
           {
             label: '在线打开 ESP32 中文介绍页',
             href: '/docs/hermes-esp32-intro/index.html',
-            description: '包含产品图、硬件亮点、配网教程、连接 Hermes Studio 的说明、规格与常见问题。',
+            description: '包含产品图、硬件亮点、配网教程、连接 Swarm Studio 的说明、规格与常见问题。',
           },
         ],
       },
       overview: {
         title: '设备定位',
-        content: '这是配合 Hermes Studio 使用的桌面硬件入口，不是离线独立大模型设备。ESP32-C3 提供本地交互与状态反馈，AI 能力由电脑端或服务器端 Hermes Studio 以及用户配置的模型、STT、TTS 服务提供。',
+        content: '这是配合 Swarm Studio 使用的桌面硬件入口，不是离线独立大模型设备。ESP32-C3 提供本地交互与状态反馈，AI 能力由电脑端或服务器端 Swarm Studio 以及用户配置的模型、STT、TTS 服务提供。',
       },
       requirements: {
         title: '使用前提',
         rows: [
-          ['网络', 'ESP32 设备和 Hermes Studio 需要在同一局域网内通信。'],
+          ['网络', 'ESP32 设备和 Swarm Studio 需要在同一局域网内通信。'],
           ['服务地址', '设备端应填写电脑或服务器的局域网 IP + 端口，不能填写 127.0.0.1。'],
-          ['语音能力', '需要在 Hermes Studio 中配置可用的语音识别、语音合成和模型服务。'],
-          ['适合用户', '适合 Hermes Studio 用户、ESP32/Arduino/PlatformIO 玩家、开源硬件改造和桌面 AI 交互演示场景。'],
+          ['语音能力', '需要在 Swarm Studio 中配置可用的语音识别、语音合成和模型服务。'],
+          ['适合用户', '适合 Swarm Studio 用户、ESP32/Arduino/PlatformIO 玩家、开源硬件改造和桌面 AI 交互演示场景。'],
         ],
       },
       maintenance: {
         title: '维护说明',
-        content: '后续如固件、配网页、端口、设备外观或 Hermes Studio 设备接入流程变化，请同步更新静态 HTML 资源和本页的适用说明。',
+        content: '后续如固件、配网页、端口、设备外观或 Swarm Studio 设备接入流程变化，请同步更新静态 HTML 资源和本页的适用说明。',
       },
     },
     platforms: {
@@ -434,7 +434,7 @@ export default {
     },
     api: {
       title: 'API 参考',
-      intro: 'Hermes Studio 提供本地 BFF API，并通过 Socket.IO 端点进行聊天流式通信。',
+      intro: 'Swarm Studio 提供本地 BFF API，并通过 Socket.IO 端点进行聊天流式通信。',
       local: {
         title: '本地 BFF 端点',
         content: 'Koa 服务器处理会话管理、Profile CRUD、分账户分 Profile 管理、配置读写、日志访问、技能列表、记忆操作和静态资源。',
diff --git a/tests/client/matrix-message-actions.test.ts b/tests/client/matrix-message-actions.test.ts
new file mode 100644
index 00000000..bf040c06
--- /dev/null
+++ b/tests/client/matrix-message-actions.test.ts
@@ -0,0 +1,208 @@
+// @vitest-environment jsdom
+//
+// Tests for the message-action parity work (react / reply / reply-in-thread)
+// against the element-web reference behaviour:
+//   - ReactionPicker.onChoose toggle semantics (send if absent, redact if present)
+//   - RoomContext power-level gating (canReact / canSendMessages / canSelfRedact)
+//   - EventTileActionBarViewModel.canShowReplyInThreadAction + isThreadReplyAllowed
+//   - MessageContextMenu ReplyInThreadButton branch (open existing vs start new)
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+
+const ROOM_ID = '!room:server'
+const MY_USER = '@me:server'
+const EVENT_ID = '$evt:server'
+const ROOT_ID = '$root:server'
+
+// ── Mocked SDK room state ────────────────────────────────────────────────
+const maySendEvent = vi.fn(() => true)
+const maySendMessage = vi.fn(() => true)
+const maySendRedactionForEvent = vi.fn(() => true)
+const mockRoomState = { maySendEvent, maySendMessage, maySendRedactionForEvent }
+
+// A minimal annotation set used by getEventReactions.
+function makeAnnotationSet(entries: Array<{ key: string; sender: string; id: string; redacted?: boolean }>) {
+  return {
+    getSortedAnnotationsByKey: () => {
+      const byKey = new Map<string, any[]>()
+      for (const e of entries) {
+        if (e.redacted) continue
+        if (!byKey.has(e.key)) byKey.set(e.key, [])
+        byKey.get(e.key)!.push({ getSender: () => e.sender, getId: () => e.id, isRedacted: () => false })
+      }
+      return Array.from(byKey.entries())
+    },
+  }
+}
+
+let currentAnnotations: any = null
+const mockRoom: any = {
+  roomId: ROOM_ID,
+  currentState: mockRoomState,
+  relations: {
+    getChildEventsForEvent: vi.fn(() => currentAnnotations),
+  },
+}
+
+const sendEvent = vi.fn()
+const redactEvent = vi.fn()
+const mockClient: any = {
+  getUserId: () => MY_USER,
+  getRoom: () => mockRoom,
+  sendEvent,
+  redactEvent,
+}
+
+vi.mock('@/custom/matrix-chat/stores/matrix-client', () => ({
+  useMatrixClientStore: () => ({ client: mockClient, userId: MY_USER, error: null }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-room', () => ({
+  useMatrixRoomStore: () => ({
+    activeRoom: mockRoom,
+    activeRoomId: ROOM_ID,
+  }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-events', () => ({
+  matrixEventBus: {
+    onSelectRoom: { value: null },
+    onThreadUpdate: { value: null },
+    onRoomListChange: { value: null },
+    onTimeline: { value: null },
+  },
+}))
+
+import { useMatrixComposerStore } from '@/custom/matrix-chat/stores/matrix-composer'
+
+// Helper to build a fake MatrixEvent with the bits the composer store reads.
+function fakeEvent(overrides: Record<string, any> = {}) {
+  const content = overrides.content ?? { msgtype: 'm.text', body: 'hi' }
+  return {
+    getId: () => overrides.id ?? EVENT_ID,
+    getType: () => overrides.type ?? 'm.room.message',
+    getSender: () => overrides.sender ?? MY_USER,
+    getStatus: () => overrides.status ?? null,
+    getContent: () => content,
+    getOriginalContent: () => content,
+    getRelation: () => overrides.relation ?? undefined,
+    isRedacted: () => overrides.redacted ?? false,
+    isRelation: () => !!overrides.relation,
+    replyEventId: overrides.replyEventId,
+    threadRootId: overrides.threadRootId,
+    isThreadRoot: overrides.isThreadRoot ?? false,
+    replacingEvent: () => overrides.replacingEvent ?? null,
+  } as any
+}
+
+describe('matrix-composer store — reaction toggle (element-web ReactionPicker.onChoose parity)', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+    vi.clearAllMocks()
+    currentAnnotations = null
+  })
+
+  it('sends a reaction when the user has not reacted yet', async () => {
+    const store = useMatrixComposerStore()
+    const result = await store.toggleReaction(EVENT_ID, '👍')
+    expect(result).toBe('sent')
+    expect(sendEvent).toHaveBeenCalledWith(
+      ROOM_ID,
+      'm.reaction',
+      expect.objectContaining({
+        'm.relates_to': expect.objectContaining({ rel_type: 'm.annotation', event_id: EVENT_ID, key: '👍' }),
+      }),
+    )
+    expect(redactEvent).not.toHaveBeenCalled()
+  })
+
+  it('redacts the existing reaction when the user already reacted', async () => {
+    currentAnnotations = makeAnnotationSet([
+      { key: '👍', sender: MY_USER, id: '$my-react:server' },
+      { key: '👍', sender: '@other:server', id: '$other-react:server' },
+    ])
+    const store = useMatrixComposerStore()
+    const result = await store.toggleReaction(EVENT_ID, '👍')
+    expect(result).toBe('redacted')
+    expect(redactEvent).toHaveBeenCalledWith(ROOM_ID, '$my-react:server')
+    expect(sendEvent).not.toHaveBeenCalled()
+  })
+
+  it('getMyReactionKeys returns the keys the current user has sent', () => {
+    currentAnnotations = makeAnnotationSet([
+      { key: '👍', sender: MY_USER, id: '$r1:server' },
+      { key: '🎉', sender: '@other:server', id: '$r2:server' },
+      { key: '❤️', sender: MY_USER, id: '$r3:server' },
+    ])
+    const store = useMatrixComposerStore()
+    const keys = store.getMyReactionKeys(EVENT_ID)
+    // Only the current user's keys, not @other's 🎉 (order-independent).
+    expect(keys.size).toBe(2)
+    expect(keys.has('👍')).toBe(true)
+    expect(keys.has('❤️')).toBe(true)
+    expect(keys.has('🎉')).toBe(false)
+  })
+})
+
+describe('matrix-composer store — power-level gating (element-web RoomContext parity)', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+    vi.clearAllMocks()
+  })
+
+  it('canSendMessages reflects room.currentState.maySendMessage', () => {
+    maySendMessage.mockReturnValue(true)
+    const store = useMatrixComposerStore()
+    expect(store.canSendMessages).toBe(true)
+  })
+
+  it('canSendMessages is false when maySendMessage returns false', () => {
+    maySendMessage.mockReturnValue(false)
+    const store = useMatrixComposerStore()
+    expect(store.canSendMessages).toBe(false)
+  })
+
+  it('canReact reflects room.currentState.maySendEvent(m.reaction)', () => {
+    maySendEvent.mockReturnValue(true)
+    const store = useMatrixComposerStore()
+    expect(store.canReact).toBe(true)
+    expect(maySendEvent).toHaveBeenCalledWith('m.reaction', MY_USER)
+  })
+})
+
+describe('matrix-composer store — reply-in-thread guards (element-web EventTileActionBarViewModel parity)', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+  })
+
+  it('canShowReplyInThreadAction is true for a normal message in the room timeline', () => {
+    const store = useMatrixComposerStore()
+    expect(store.canShowReplyInThreadAction(fakeEvent(), 'room')).toBe(true)
+  })
+
+  it('canShowReplyInThreadAction is false inside a thread timeline', () => {
+    const store = useMatrixComposerStore()
+    expect(store.canShowReplyInThreadAction(fakeEvent(), 'thread')).toBe(false)
+  })
+
+  it('canShowReplyInThreadAction is false for m.key.verification.request', () => {
+    const store = useMatrixComposerStore()
+    const evt = fakeEvent({ content: { msgtype: 'm.key.verification.request', body: '' } })
+    expect(store.canShowReplyInThreadAction(evt, 'room')).toBe(false)
+  })
+
+  it('canShowReplyInThreadAction is false for m.beacon_info', () => {
+    const store = useMatrixComposerStore()
+    expect(store.canShowReplyInThreadAction(fakeEvent({ type: 'm.beacon_info' }), 'room')).toBe(false)
+  })
+
+  it('isThreadReplyAllowed is false when the event has a non-thread relation (e.g. a reply)', () => {
+    const store = useMatrixComposerStore()
+    const reply = fakeEvent({ relation: { rel_type: 'm.annotation', event_id: 'x' } })
+    expect(store.isThreadReplyAllowed(reply)).toBe(false)
+  })
+
+  it('isThreadReplyAllowed is true when the relation is a thread relation', () => {
+    const store = useMatrixComposerStore()
+    const threadReply = fakeEvent({ relation: { rel_type: 'm.thread', event_id: ROOT_ID } })
+    expect(store.isThreadReplyAllowed(threadReply)).toBe(true)
+  })
+})
diff --git a/tests/client/matrix-right-panel.test.ts b/tests/client/matrix-right-panel.test.ts
new file mode 100644
index 00000000..145913bb
--- /dev/null
+++ b/tests/client/matrix-right-panel.test.ts
@@ -0,0 +1,65 @@
+// @vitest-environment jsdom
+import { beforeEach, describe, expect, it } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+import { useMatrixRightPanelStore } from '@/custom/matrix-chat/stores/matrix-right-panel'
+
+describe('matrix-right-panel store (thread phases)', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+  })
+
+  it('openThreadPanel sets phase to ThreadPanel with null root id', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadPanel()
+    expect(store.rightPanelPhase).toBe('ThreadPanel')
+    expect(store.rightPanelThreadRootId).toBe(null)
+  })
+
+  it('openThreadView sets phase to ThreadView with root event id', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadView('$evt1:server')
+    expect(store.rightPanelPhase).toBe('ThreadView')
+    expect(store.rightPanelThreadRootId).toBe('$evt1:server')
+  })
+
+  it('openThreadView records history so rightPanelBack returns to ThreadPanel', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadPanel()
+    store.openThreadView('$evt1:server')
+    store.rightPanelBack()
+    expect(store.rightPanelPhase).toBe('ThreadPanel')
+    expect(store.rightPanelThreadRootId).toBe(null)
+  })
+
+  it('clearThreadView from ThreadView returns to ThreadPanel', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadView('$evt1:server')
+    store.clearThreadView()
+    expect(store.rightPanelPhase).toBe('ThreadPanel')
+    expect(store.rightPanelThreadRootId).toBe(null)
+  })
+
+  it('clearThreadView from ThreadPanel closes the panel', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadPanel()
+    store.clearThreadView()
+    expect(store.rightPanelPhase).toBe(null)
+  })
+
+  it('setThreadFilter updates filter', () => {
+    const store = useMatrixRightPanelStore()
+    expect(store.rightPanelThreadFilter).toBe('all')
+    store.setThreadFilter('my')
+    expect(store.rightPanelThreadFilter).toBe('my')
+  })
+
+  it('closeRightPanel clears thread root id and filter', () => {
+    const store = useMatrixRightPanelStore()
+    store.openThreadView('$evt1:server')
+    store.setThreadFilter('my')
+    store.closeRightPanel()
+    expect(store.rightPanelPhase).toBe(null)
+    expect(store.rightPanelThreadRootId).toBe(null)
+    expect(store.rightPanelThreadFilter).toBe('all')
+  })
+})
diff --git a/tests/client/matrix-room-threads.test.ts b/tests/client/matrix-room-threads.test.ts
new file mode 100644
index 00000000..792e427c
--- /dev/null
+++ b/tests/client/matrix-room-threads.test.ts
@@ -0,0 +1,69 @@
+// @vitest-environment jsdom
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+
+const ROOM_ID = '!room:server'
+const allSet = { filterId: 'all' }
+const mySet = { filterId: 'my' }
+const threadObj = { id: '$root:server' }
+
+const mockRoom = {
+  threadsTimelineSets: [allSet, mySet],
+  getThread: vi.fn((id: string) => (id === '$root:server' ? threadObj : null)),
+  createThreadsTimelineSets: vi.fn().mockResolvedValue(undefined),
+  fetchRoomThreads: vi.fn().mockResolvedValue(undefined),
+}
+
+const mockClient = {
+  getRoom: vi.fn((id: string) => (id === ROOM_ID ? mockRoom : null)),
+}
+
+vi.mock('@/custom/matrix-chat/stores/matrix-client', () => ({
+  useMatrixClientStore: () => ({ client: mockClient, error: null }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-events', () => ({
+  matrixEventBus: {
+    onSelectRoom: { value: null },
+    onThreadUpdate: { value: null },
+    onRoomListChange: { value: null },
+    onTimeline: { value: null },
+  },
+}))
+
+import { useMatrixRoomStore } from '@/custom/matrix-chat/stores/matrix-room'
+
+describe('matrix-room store thread helpers', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+    vi.clearAllMocks()
+  })
+
+  it('getThreadsTimelineSet returns [0] for all, [1] for my', () => {
+    const store = useMatrixRoomStore()
+    store.activeRoomId = ROOM_ID
+    expect(store.getThreadsTimelineSet('all')).toBe(allSet)
+    expect(store.getThreadsTimelineSet('my')).toBe(mySet)
+  })
+
+  it('getThreadsTimelineSet returns undefined when no active room', () => {
+    const store = useMatrixRoomStore()
+    // activeRoomId 未设置 ⇒ activeRoom computed 为 null
+    expect(store.getThreadsTimelineSet('all')).toBe(undefined)
+  })
+
+  it('getThreadById returns the SDK Thread object', () => {
+    const store = useMatrixRoomStore()
+    store.activeRoomId = ROOM_ID
+    expect(store.getThreadById('$root:server')).toBe(threadObj)
+    expect(store.getThreadById('$missing:server')).toBe(null)
+  })
+
+  it('initRoomThreads calls createThreadsTimelineSets + fetchRoomThreads', async () => {
+    const store = useMatrixRoomStore()
+    store.activeRoomId = ROOM_ID
+    await store.initRoomThreads()
+    expect(mockRoom.createThreadsTimelineSets).toHaveBeenCalled()
+    expect(mockRoom.fetchRoomThreads).toHaveBeenCalled()
+  })
+})
+
diff --git a/tests/client/matrix-thread-notifications.test.ts b/tests/client/matrix-thread-notifications.test.ts
new file mode 100644
index 00000000..449b50c4
--- /dev/null
+++ b/tests/client/matrix-thread-notifications.test.ts
@@ -0,0 +1,59 @@
+// @vitest-environment jsdom
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+import { NotificationCountType } from 'matrix-js-sdk'
+
+// mock room store: 提供一个可控的 activeRoom
+const mockRoom = {
+  threadsAggregateNotificationType: 0,
+}
+vi.mock('@/custom/matrix-chat/stores/matrix-room', () => ({
+  useMatrixRoomStore: () => ({
+    get activeRoom() {
+      return mockRoom
+    },
+  }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-client', () => ({
+  useMatrixClientStore: () => ({ client: null, error: null }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-events', () => ({
+  matrixEventBus: {
+    onSelectRoom: { value: null },
+    onThreadUpdate: { value: null },
+  },
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-composer', () => ({
+  useMatrixComposerStore: () => ({
+    composerMode: 'normal',
+    replyToEvent: null,
+    editingEvent: null,
+    stripPlainReply: (s: string) => s,
+  }),
+}))
+
+import { useMatrixThreadStore } from '@/custom/matrix-chat/stores/matrix-thread'
+
+describe('matrix-thread store notification helper', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+  })
+
+  it('returns "highlight" when room has highlight thread notifications', () => {
+    mockRoom.threadsAggregateNotificationType = NotificationCountType.Highlight
+    const store = useMatrixThreadStore()
+    expect(store.getThreadNotificationIndicator()).toBe('highlight')
+  })
+
+  it('returns "unread" when room has total (non-highlight) thread notifications', () => {
+    mockRoom.threadsAggregateNotificationType = NotificationCountType.Total
+    const store = useMatrixThreadStore()
+    expect(store.getThreadNotificationIndicator()).toBe('unread')
+  })
+
+  it('returns "none" when no thread notifications', () => {
+    mockRoom.threadsAggregateNotificationType = 0
+    const store = useMatrixThreadStore()
+    expect(store.getThreadNotificationIndicator()).toBe('none')
+  })
+})
diff --git a/tests/client/matrix-thread-summary.test.ts b/tests/client/matrix-thread-summary.test.ts
new file mode 100644
index 00000000..0423c2be
--- /dev/null
+++ b/tests/client/matrix-thread-summary.test.ts
@@ -0,0 +1,71 @@
+// @vitest-environment jsdom
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+import { mount } from '@vue/test-utils'
+
+vi.mock('@/custom/matrix-chat/stores/matrix-room', () => ({
+  useMatrixRoomStore: () => ({
+    getUserAvatarUrl: () => null,
+  }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-client', () => ({
+  useMatrixClientStore: () => ({ client: null, error: null, userId: '' }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-events', () => ({
+  matrixEventBus: {
+    onSelectRoom: { value: null },
+    onThreadUpdate: { value: null },
+    onRoomListChange: { value: null },
+    onTimeline: { value: null },
+  },
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-composer', () => ({
+  useMatrixComposerStore: () => ({ stripPlainReply: (s: string) => s }),
+}))
+
+import MatrixThreadSummary from '@/custom/matrix-chat/components/MatrixThreadSummary.vue'
+
+function makeThread(length: number, lastReply?: any) {
+  return {
+    length,
+    replyToEvent: lastReply ?? null,
+    id: '$root:server',
+    room: { roomId: '!r:s' },
+  }
+}
+
+describe('MatrixThreadSummary', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+  })
+
+  it('renders reply count label', () => {
+    const thread = makeThread(3)
+    const wrapper = mount(MatrixThreadSummary, {
+      props: { thread, mxEvent: { getId: () => '$root:server' } as any },
+      global: { mocks: { $t: (k: string, p?: any) => (p ? `${k}:${JSON.stringify(p)}` : k) } },
+    })
+    expect(wrapper.text()).toContain('3')
+  })
+
+  it('emits openThread with root event id on click', async () => {
+    const thread = makeThread(1)
+    const wrapper = mount(MatrixThreadSummary, {
+      props: { thread, mxEvent: { getId: () => '$root:server' } as any },
+      global: { mocks: { $t: (k: string) => k } },
+    })
+    await wrapper.find('button').trigger('click')
+    const emitted = wrapper.emitted('openThread')
+    expect(emitted).toBeTruthy()
+    expect(emitted![0]).toEqual(['$root:server'])
+  })
+
+  it('renders empty string count when thread has 0 replies', () => {
+    const thread = makeThread(0)
+    const wrapper = mount(MatrixThreadSummary, {
+      props: { thread, mxEvent: { getId: () => '$root:server' } as any },
+      global: { mocks: { $t: (k: string) => k } },
+    })
+    expect(wrapper.find('.mx_ThreadSummary_repliesAmount').text()).toBe('')
+  })
+})
diff --git a/tests/client/matrix-timeline-panel-params.test.ts b/tests/client/matrix-timeline-panel-params.test.ts
new file mode 100644
index 00000000..9b0a74e3
--- /dev/null
+++ b/tests/client/matrix-timeline-panel-params.test.ts
@@ -0,0 +1,45 @@
+// @vitest-environment jsdom
+import { beforeEach, describe, expect, it, vi } from 'vitest'
+import { createPinia, setActivePinia } from 'pinia'
+
+// 这个测试只验证 props 的默认值与派生逻辑入口,不挂载完整组件(避免触发 onMounted
+// 的 SDK 路径)。组件渲染行为由 Task 5 的 ThreadSummary 测试间接覆盖。
+vi.mock('@/custom/matrix-chat/stores/matrix-client', () => ({
+  useMatrixClientStore: () => ({ client: null, error: null, userId: '' }),
+}))
+vi.mock('@/custom/matrix-chat/stores/matrix-events', () => ({
+  matrixEventBus: {
+    onSelectRoom: { value: null },
+    onThreadUpdate: { value: null },
+    onRoomListChange: { value: null },
+    onTimeline: { value: null },
+  },
+}))
+
+describe('MatrixTimelinePanel params (props contract)', () => {
+  beforeEach(() => {
+    setActivePinia(createPinia())
+  })
+
+  it('exposes optional props with documented defaults via component meta', async () => {
+    const mod = await import('@/custom/matrix-chat/components/MatrixTimelinePanel.vue')
+    const comp: any = mod.default
+    const props = comp.props as Record<string, { default?: any; required?: boolean; type?: any }>
+    // 旧的调用点不传这些 prop → 它们必须有默认值,且不 required
+    expect(props.timelineSet?.required).toBeFalsy()
+    expect(props.timelineSet?.default).toBeUndefined() // undefined ⇒ 走 roomStore 现状分支
+    expect(props.renderingType?.required).toBeFalsy()
+    expect(props.renderingType?.default).toBe('room')
+    expect(props.threadId?.required).toBeFalsy()
+    expect(props.showReadReceipts?.required).toBeFalsy()
+    expect(props.showReadReceipts?.default).toBe(true)
+    expect(props.showReactions?.required).toBeFalsy()
+    expect(props.showReactions?.default).toBe(true)
+    expect(props.hideThreadedMessages?.required).toBeFalsy()
+    expect(props.hideThreadedMessages?.default).toBe(false)
+    expect(props.alwaysShowTimestamps?.required).toBeFalsy()
+    expect(props.alwaysShowTimestamps?.default).toBe(false)
+    expect(props.disableGrouping?.required).toBeFalsy()
+    expect(props.disableGrouping?.default).toBe(false)
+  })
+})
diff --git a/tests/server/hermes-kanban-service.test.ts b/tests/server/hermes-kanban-service.test.ts
index 59b51a6b..4eb0fd81 100644
--- a/tests/server/hermes-kanban-service.test.ts
+++ b/tests/server/hermes-kanban-service.test.ts
@@ -48,7 +48,7 @@ describe('hermes kanban service', () => {
     await expect(service.getCapabilities()).resolves.toMatchObject({
       source: 'hermes-cli',
       supports: { boardsList: true, boardCreate: true, commentsWrite: true, dispatch: true, links: true },
-      missing: expect.arrayContaining(['cliCurrentSwitch', 'bulk', 'homeSubscriptions']),
+      missing: expect.arrayContaining(['cliCurrentSwitch', 'bulk', 'events']),
       capabilities: expect.arrayContaining([
         expect.objectContaining({ key: 'commentsWrite', status: 'supported', canonicalCommand: 'comment', requiresBoard: true }),
         expect.objectContaining({ key: 'links', status: 'supported', canonicalRoute: '/links', canonicalCommand: 'link/unlink', requiresBoard: true }),
diff --git a/tests/server/kanban-controller.test.ts b/tests/server/kanban-controller.test.ts
index 197f2831..0416d3fd 100644
--- a/tests/server/kanban-controller.test.ts
+++ b/tests/server/kanban-controller.test.ts
@@ -36,6 +36,7 @@ vi.mock('fs/promises', () => ({
 
 vi.mock('os', () => ({
   homedir: () => '/Users/tester',
+  tmpdir: () => '/tmp',
 }))
 
 vi.mock('../../packages/server/src/services/hermes/hermes-kanban', () => ({
diff --git a/tests/server/kanban-routes.test.ts b/tests/server/kanban-routes.test.ts
index 8aeadf24..e352e0cd 100644
--- a/tests/server/kanban-routes.test.ts
+++ b/tests/server/kanban-routes.test.ts
@@ -22,6 +22,19 @@ const handlers = {
   addComment: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
   taskLog: vi.fn(async (ctx: any) => { ctx.body = { log: '' } }),
   diagnostics: vi.fn(async (ctx: any) => { ctx.body = { diagnostics: [] } }),
+  getOrchestration: vi.fn(async (ctx: any) => { ctx.body = { orchestration: {} } }),
+  setOrchestration: vi.fn(async (ctx: any) => { ctx.body = { orchestration: {} } }),
+  homeChannels: vi.fn(async (ctx: any) => { ctx.body = { channels: [] } }),
+  debugHomeChannels: vi.fn(async (ctx: any) => { ctx.body = { debug: [] } }),
+  subscribeHome: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
+  unsubscribeHome: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
+  listAttachments: vi.fn(async (ctx: any) => { ctx.body = { attachments: [] } }),
+  uploadAttachment: vi.fn(async (ctx: any) => { ctx.body = { attachment: {} } }),
+  downloadAttachment: vi.fn(async (ctx: any) => { ctx.body = '' }),
+  removeAttachment: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
+  deleteTask: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
+  patchTask: vi.fn(async (ctx: any) => { ctx.body = { task: {} } }),
+  decomposeTask: vi.fn(async (ctx: any) => { ctx.body = { result: {} } }),
   reclaim: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
   reassign: vi.fn(async (ctx: any) => { ctx.body = { ok: true } }),
   specify: vi.fn(async (ctx: any) => { ctx.body = { results: [] } }),
diff --git a/vite.config.ts b/vite.config.ts
index b1d8fddc..d6593c5e 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -5,7 +5,7 @@ import { resolve } from 'path'
 import pkg from './package.json'
 
 const FRONTEND_PORT = Number(process.env.HERMES_WEB_UI_FRONTEND_PORT || 8649)
-const BACKEND_PORT = process.env.HERMES_WEB_UI_BACKEND_PORT || '8648'
+const BACKEND_PORT = process.env.HERMES_WEB_UI_BACKEND_PORT || '8647'
 const BACKEND = `http://127.0.0.1:${BACKEND_PORT}`
 
 function createProxyConfig(): ProxyOptions {
@@ -93,6 +93,9 @@ export default defineConfig({
       'vue-router',
       'pinia',
       'naive-ui',
+      // HERMES_CUSTOM[MatrixChat] BEGIN: pre-bundle matrix-js-sdk
+      'matrix-js-sdk',
+      // HERMES_CUSTOM[MatrixChat] END
     ],
   },
   server: {
diff --git a/vitest.config.ts b/vitest.config.ts
index 0868870e..a8011625 100644
--- a/vitest.config.ts
+++ b/vitest.config.ts
@@ -6,6 +6,8 @@ export default defineConfig({
   plugins: [vue()],
   resolve: {
     alias: {
+      '@/custom': resolve(__dirname, '../../overlay/custom/client'),
+      '@custom': resolve(__dirname, '../../overlay/custom/client'),
       '@': resolve(__dirname, 'packages/client/src'),
       '/logo.png': resolve(__dirname, 'packages/client/public/logo.png'),
     },
