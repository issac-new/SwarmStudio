diff --git a/packages/server/src/modules/studio/infrastructure/database/schemas.ts b/packages/server/src/modules/studio/infrastructure/database/schemas.ts
index b803491..119752f 100644
--- a/packages/server/src/modules/studio/infrastructure/database/schemas.ts
+++ b/packages/server/src/modules/studio/infrastructure/database/schemas.ts
@@ -102,6 +102,10 @@ export const SESSIONS_SCHEMA: Record<string, string> = {
   workspace: 'TEXT',
   category_id: 'INTEGER',
   history_revision: 'INTEGER NOT NULL DEFAULT 0',
+  // R6 执行租约 daemon 形态（routa execution-backend.ts:54 语义）：会话运行归属
+  // 实例 + 租约到期（重启按租约接管，多实例协同最小充分机制）
+  owner_instance_id: 'TEXT NOT NULL DEFAULT \'\'',
+  lease_expires_at: 'INTEGER NOT NULL DEFAULT 0',
 }
 
 export const SESSIONS_INDEXES = {
