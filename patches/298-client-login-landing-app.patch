diff --git a/packages/client/src/views/LoginView.vue b/packages/client/src/views/LoginView.vue
index 5d0a11b5..9cfed10f 100644
--- a/packages/client/src/views/LoginView.vue
+++ b/packages/client/src/views/LoginView.vue
@@ -24,11 +24,11 @@ const localRememberMe = ref(false);
 const loading = ref(false);
 const errorMsg = ref("");
 
-// overlay[ide]: 默认登录后跳 IDE 工作台（/ide）；保留 query.redirect 保真。
-// 2026-09-16 用户裁决：与上游 071+276 守卫落点一致。
+// overlay[nav]: 默认登录后跳驾驶舱总览 /app；保留 query.redirect 保真。
+// 2026-09-18 统一导航裁决：与 router 守卫落点一致。
 function loginRedirectTarget(): string {
   const resolved = resolveLoginRedirect(route.query.redirect);
-  return resolved === '/hermes/chat' ? '/ide' : resolved;
+  return resolved === "/hermes/chat" ? "/app" : resolved;
 }
 
 const STORAGE_KEYS = {
