diff --git a/packages/server/src/modules/studio/controllers/download.ts b/packages/server/src/modules/studio/controllers/download.ts
index ff5c216..a72b2c4 100644
--- a/packages/server/src/modules/studio/controllers/download.ts
+++ b/packages/server/src/modules/studio/controllers/download.ts
@@ -10,6 +10,9 @@ import { getActiveProfileName } from '../public/profile-config'
 // HERMES_CUSTOM[SecFileRootSandbox] BEGIN: root 参数沙箱校验
 import { isPathWithin } from '../../hermes/services/runtime/path'
 import { getHermesBaseDir } from '../../hermes/services/profiles/profile'
+// HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 放行任务 workspace_path 越界请求
+import { isWithinKnownTaskWorkspace, primeTaskWorkspaceCache } from '../../../custom/services/hermes/task-workspace-cache'
+// HERMES_CUSTOM[TaskWorkspaceAllowlist] END
 // HERMES_CUSTOM[SecFileRootSandbox] END
 import { createAppImagePreview } from '../services/files/app-image-preview'
 
@@ -83,6 +86,9 @@ export async function download(ctx: any) {
 
   try {
     const profile = requestedProfile(ctx)
+    // HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 预热任务 workspace 缓存
+    if (root) await primeTaskWorkspaceCache()
+    // HERMES_CUSTOM[TaskWorkspaceAllowlist] END
     // Validate the path first
     // Support both absolute and relative paths; when a root is provided,
     // resolve the relative path against it (same semantics as files/list).
@@ -98,7 +104,9 @@ export async function download(ctx: any) {
       }
       const hermesBase = getHermesBaseDir()
       const candidate = resolve(root, filePath)
-      if (!isPathWithin(candidate, hermesBase)) {
+      // HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 越界时放行已知任务 workspace_path
+      if (!isPathWithin(candidate, hermesBase) && !isWithinKnownTaskWorkspace(candidate)) {
+      // HERMES_CUSTOM[TaskWorkspaceAllowlist] END
         ctx.status = 403
         ctx.body = { error: 'Requested path is outside the hermes base sandbox', code: 'invalid_path' }
         return
diff --git a/packages/server/src/modules/studio/controllers/files.ts b/packages/server/src/modules/studio/controllers/files.ts
index 9b66354..f3ae528 100644
--- a/packages/server/src/modules/studio/controllers/files.ts
+++ b/packages/server/src/modules/studio/controllers/files.ts
@@ -7,6 +7,9 @@ import {
 } from '../services/files/file-provider'
 import { isPathWithin, relativePathFromBase } from '../../hermes/services/runtime/path'
 import { getHermesBaseDir } from '../../hermes/services/profiles/profile'
+// HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 放行任务 workspace_path 越界请求
+import { isWithinKnownTaskWorkspace, primeTaskWorkspaceCache } from '../../../custom/services/hermes/task-workspace-cache'
+// HERMES_CUSTOM[TaskWorkspaceAllowlist] END
 import { MultipartParseError, parseMultipartBoundary, parseMultipartFilename, splitMultipart } from '../public/multipart'
 
 function requestedProfile(ctx: any): string | undefined {
@@ -27,7 +30,9 @@ function resolveRequestPath(ctx: any, relativePath: string): string {
   if (root) {
     const hermesBase = getHermesBaseDir()
     const candidate = relativePath ? join(root, relativePath) : root
-    if (!isPathWithin(candidate, hermesBase)) {
+    // HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 越界时放行已知任务 workspace_path
+    // 否则系统级任务（workspace_path=/Users/<user> 等 ~/.hermes 外目录）无法浏览
+    if (!isPathWithin(candidate, hermesBase) && !isWithinKnownTaskWorkspace(candidate)) {
       throw Object.assign(new Error('Requested path is outside the hermes base sandbox'), { code: 'invalid_path' })
     }
     return candidate
@@ -82,6 +87,9 @@ function handleError(ctx: any, err: any) {
 export async function list(ctx: any) {
   const relativePath = (ctx.query.path as string) || ''
   const root = requestedRoot(ctx)
+  // HERMES_CUSTOM[TaskWorkspaceAllowlist] BEGIN: 预热任务 workspace 缓存
+  if (root) await primeTaskWorkspaceCache()
+  // HERMES_CUSTOM[TaskWorkspaceAllowlist] END
   // HERMES_CUSTOM[SecFileRootSandbox] BEGIN: 使用 root 时要求 super_admin
   if (root && ctx.state.user?.role !== 'super_admin') {
     ctx.status = 403
